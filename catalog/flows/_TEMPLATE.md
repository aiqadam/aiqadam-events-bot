# Flow: <Display Name>

- **Статус**: DRAFT | ENABLED | DISABLED
- **Триггер**: <cron `*/5 * * * *` / webhook / app event> — <детали>
- **Назначение**: <одно предложение>
- **Flow ID (MCP)**: <id из ap_list_flows — для справки>

## Шаги

> Снимай через `ap_flow_structure`. Для CODE-шагов — `ap_read_step_code`.
>
> **Колонки с инпутами здесь нет и не должно быть.** `settings.input` —
> URL, cron, таймзоны, `format`, ссылки `{{step_N['output']...}}` — живёт
> в экспорте `flows/<flow>.json` ([ADR-0018](../../docs/adr/0018-rest-read-for-everyone.md)
> п. 4), и дублировать его пересказом значит завести третье описание системы,
> которое разойдётся первым. В карточке — назначение шага; всё, что не
> очевидно из инпутов (сентинелы, `only_if`, `continueOnFailure` и зачем они),
> идёт в «Заметки».

| Step | Piece / Action | Назначение |
|------|----------------|-----------|
| trigger | @aiqadam/... | <что триггерит> |
| step_1 | @aiqadam/... : <action> | <что делает> |
| step_2 | ... | ... |

## Зависимости

- **Таблицы**: <список или —>
- **Переменные**: <список или —>
- **Connections**: <тип/назначение или —>

## Заметки

- <edge cases, мьютексы, gotchas, почему так сделано>

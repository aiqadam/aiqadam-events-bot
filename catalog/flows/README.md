# Flows: как читать этот каталог

Живое состояние flows проекта `events-dev`. Формат файла — [_TEMPLATE.md](_TEMPLATE.md).
Каталог обновляется после **каждого** изменения flow через MCP
([ADR-0004](../../docs/adr/0004-catalog-instead-of-flow-export.md)).

## Два идентификатора у флоу — как у таблиц

| Где | Какой ключ | Как узнать |
| --- | --- | --- |
| MCP-инструменты (`ap_flow_structure`, `ap_test_flow`, `ap_lock_and_publish`) | **flowId** | `ap_list_flows` |
| `flow` в `subflows / callFlow` (`{"externalId": ...}`) | **externalId** флоу | `ap_resolve_property_options` для `propertyName = flow` |

Оба записаны в каждом файле. **Все id — свои у каждого проекта**: при пересборке
в `events-prod` (W15) сначала создаются subflow'ы, потом по их новым externalId
настраиваются вызовы.

## Проверенные факты про subflow'ы (2026-09-08, пакет W2)

> **Актуальность (W22, 2026-09-13):** subflow-«функций» `fn-*` в проекте больше
> нет — удалены, канон переехал в [snippets/](../snippets/). Единственный
> `callFlow` в проекте — `tg-router` → `registration`. Факты ниже сохранены,
> потому что этот единственный вызов ими и живёт, а `events-prod` (W15) будет
> собираться с нуля и наступит на те же грабли. Пункты 5–13 к subflow'ам
> отношения не имеют вовсе — это факты про MCP и логи прогонов.

1. **Вход subflow'а лежит в `trigger['output'].data`, а не в корне.** `callFlow`
   передаёт `{ data: <payload>, callbackUrl: ... }`. Ссылка `{{trigger['output'].eventId}}`
   молча даёт `undefined` — тест через `ap_test_flow` с плоскими sample data проходит,
   а реальный вызов ломается. Ссылаться только через `.data.`.
2. **Ответ `callFlow` — `{ status: 'success', data: <тело respond> }`.** То есть поле
   ответа читается как `{{step_N['output'].data.field}}`.
3. **Callee обязан быть published + ENABLED**, иначе его нет в dropdown'е `flow`.
   Отсюда порядок сборки: callee публикуется раньше вызывающего.
4. **Вызванный subflow исполняется в PRODUCTION даже когда вызывающий тестируется.**
   Ошибка внутри callee видна не в отчёте теста, а по ссылке на его собственный run.
5. **`ap_validate_flow` считает `{{variables['NAME']}}` битой ссылкой** —
   `"references {{variables...}} which does not exist in the flow"`. Это ложное
   срабатывание: те же шаги на прогоне подставляют реальные значения (проверено
   сравнением HMAC с контрольным значением на пустом ключе). Флоу с этим замечанием
   публикуются штатно, но **само замечание нельзя использовать как признак опечатки
   в имени переменной** — опечатка даёт пустую строку и тоже «валидна».
6. **`ap_test_step` не всегда переигрывает предшественников**: на шаге в середине
   цепочки предыдущие шаги вернули `{}`. Для сквозной проверки — `ap_test_flow`.
7. **MCP не отдаёт inputs PIECE-шагов.** `ap_read_step_code` работает только с CODE,
   `ap_get_run` показывает только outputs. Владельцу пакета остаётся UI; ревьюеру
   разрешён read-only REST-экспорт ([ADR-0006](../../docs/adr/0006-rest-read-only-for-review.md)),
   и именно он ловит расхождения между каталогом и конфигурацией шага.
   **Практический вывод для владельца: утверждение о конфигурации PIECE-шага,
   которое нельзя подтвердить поведением, писать в каталог нельзя.** Проверять
   такие вещи надо прогоном, специально подобранным так, чтобы конфигурация была
   видна в результате (как 51 дубль для проверки снятого лимита).
8. **Одиночное действие можно прогнать без флоу** — `ap_run_action`. Так проверены
   семантика `crypto / hmac-signature` и поведение фильтров `tables-find-records`
   ([Q15](../../docs/OPEN-QUESTIONS.md#q15)): дешевле, чем собирать пробный флоу.
   **Но такой прогон не сохраняется как flow run:** `ap_get_run` на его id отвечает
   «Flow run not found». Значит ссылка «см. прогон X» для `ap_run_action`
   **доказательством не является** — нужно контрольное значение и способ пересчёта,
   а id годится только как отметка «когда делали».
9. **`ap_delete_records` работает в пределах одной таблицы за вызов.** Список id
   из трёх таблиц удалит только строки одной из них, молча.
10. **`ap_update_step` сливает `input` с существующим и не умеет удалять ключи.**
    Передача полного набора без ключа его **не убирает**. Максимум — присвоить
    `null`. Это стоило пакету W2 полутора кругов ревью: `limit: 50` в
    `fn-find-registration / step_2` считался снятым, а лежал на месте, и увидел
    его только REST-экспорт (ADR-0006). Проверять снятие ключа — поведением.
11. **Прогон доказателен только после блокировки версии.** Правка `input` новой
    версии не создаёт, а id версии в логе прогона не хранится — значит прогон,
    сделанный внутри жизни draft'а, доказывает лишь «тогда работало», а не то,
    что стоит в опубликованной конфигурации. Именно эта механика два круга
    скрывала `limit: 50`. Порядок такой: правка → `ap_lock_and_publish` →
    прогон → и только теперь на него можно ссылаться.
12. **`lastUpdatedDate` шага и сам экспорт не доказывают неизменность.** Открытие
    флоу в UI пересохраняет шаги: у `fn-event-card` шесть шагов получили новые
    `lastUpdatedDate` и заполненный `propertySettings` при неизменных
    `settings.input`, коде и `publishedVersionId`. Экспорт отдаёт **текущие**
    записи шагов, а не снимок опубликованной версии, поэтому дифф двух экспортов,
    снятых в разное время, даёт ложные расхождения: сравнивать надо конфигурацию
    (`settings.input` + код), а не файлы.
13. **Слишком большой вывод шага в логе заменяется файлом**: вместо значения
    появляется `{fileId, size, url}` (`FLOW_RUN_LOG_SLICE`, ссылка с подписанным
    токеном). Следующий шаг при этом получает данные целиком — проверено на
    выборке из 51 строки (71 КБ). Для чтения логов это значит, что «в выводе шага
    ничего нет» может означать «вывод в файле», а не «выборка пуста».

Эти же факты сведены в
[ARCHITECTURE](../../docs/ARCHITECTURE.md#subflowы-на-практике--проверено-на-инстансе-2026-09-08-w2)
— там их ищет следующий пакет, идущий по порядку чтения.

## Соглашения, унаследованные сниппетами

Эти правила писались для subflow-«функций» `fn-*`. Флоу удалены (W22), но
правила пережили их: они теперь относятся к CODE-шагам, встраиваемым по
эталонам из [snippets/](../snippets/).

- **Не падаем на мусорном входе.** Возвращается `{ valid|found|allowed: false, error|reason }`.
  Исключение — подпись QR (`hmac-qr`): подписать пустой `eventId` хуже, чем упасть.
- **Сентинелы вместо пустых фильтров.** Пустой `event_id` в `tables-find-records`
  превратился бы в выборку чужих строк, поэтому подставляется `-` (вне алфавита
  slug'а и `telegram_id`); для фильтра `in` — `!no-key` (пустой список валит шаг).
- **Отбор повторяется в коде, а не только в фильтре чтения** (добавлено W22).
  Эталон `find-registration` сам отбрасывает строки с чужими `event_id`/`telegram_id`.
  Это нужно и для латентности (одно широкое чтение кормит несколько потребителей),
  и как страховка от fail-open платформы
  ([#382](https://github.com/aiqadam/qadam-flow/issues/382), [Q25](../../docs/OPEN-QUESTIONS.md#q25)).
- **Шаг не читает больше строк, чем ему нужно** — но и не читает одно и то же
  дважды. Проекция колонок у `tables` **есть** (проп `columns`), и она — способ
  не тащить в лог прогона лишние поля
  ([ADR-0005](../../docs/adr/0005-secrets-visible-in-run-logs.md),
  [Q17](../../docs/OPEN-QUESTIONS.md#q17)). Замер W22: цена чтения от объёма
  выборки почти не зависит (30 строк — 472 мс, 3 строки — 531 мс), поэтому
  «прочитать надмножество один раз» дешевле нескольких узких чтений.
- **Code step — чистая функция.** Ни сети, ни записи в БД. Исключение для HMAC —
  [ADR-0010](../../docs/adr/0010-unsandboxed-code-step-for-crypto.md).
- **`cells` разбираются по `fieldName`, а не по id поля** — id разные в каждом
  проекте, `fieldName` приезжает в той же ячейке.

# W62. Экран контроля (сканер) — приведение к прототипу

- **Статус**: в работе
- **Владелец**: агент
- **Волна**: вне волн (хвост соответствия прототипу)
- **Зависит от**: W49 — ✅ (прототип-эталон), W50 — ✅, W33 — ✅
- **Начат**: 2026-09-21 · **Закрыт**: —

## Цель

Живой экран контроля `#/scan` совпадает с прототипом по видимой части:
иконка вердикта, подпись, экран фатальной ошибки с одной кнопкой выхода,
подсказка в нативном сканере и счётчик «Отмечено X из Y». Рамка видоискателя
не переносится: в продукте камера нативная (`showScanQrPopup`), страница под
попапом не видна — это свойство платформы, а не отставание.

## Что построено

| Артефакт | ID / имя | Каталог |
|----------|----------|---------|
| flow `checkin-counter-api` | `<заполнить после публикации>` | [catalog/flows/checkin-counter-api.md](../../catalog/flows/checkin-counter-api.md) |
| SPA `#/scan` | `miniapp/src/routes/Scan.tsx` | — |

## Чек-лист готовности

- [ ] вердикт: иконка + текст + подпись (как `renderScan` в `prototypes/app.js`)
- [ ] фатальная ошибка: иконка + заголовок + одна кнопка («Закрыть» → `#/events`), повторяемая — «Продолжить»
- [ ] подсказка `scan.hint` отдаётся в нативный попап (`showScanQrPopup({text})`)
- [ ] счётчик и полоса: `registered` берётся один раз при открытии, `checked_in` растёт локально на исходе `ok` (как в прототипе)
- [ ] `checkin-api` не тронут — счётчики отдаёт отдельный гейтнутый `checkin-counter-api`
- [ ] живые проверки: `curl` на `/sync` (негатив мусорным `initData`), позитив — хвост W15
- [ ] `catalog/` совпадает с живым проектом, `flows/*.json` + `migrations` тем же коммитом
- [ ] `check-texts.py`, `check-commands.py`, `check-export-secrets.sh`, `tsc` чисты
- [ ] независимое ревью, вердикт «замечаний нет»

## Как проверено

- <проверка> → <результат>

## Журнал

- **2026-09-21** — пакет заведён по поручению владельца: «привести экран
  контроля к прототипу, что технически возможно». Разбор дельты живого
  `Scan.tsx` против `renderScan` (`prototypes/app.js:430-504`): совпадают
  тона/тексты/луп, расходятся иконки, подписи, экран ошибки, счётчик и
  подсказка. Счётчик требует серверных данных — `checkin-api` отдаёт только
  `{status,text}`, в `tables` агрегата нет, поэтому заводится отдельный
  гейтнутый `checkin-counter-api` (в `checkin-api` горячий путь не трогаем).
  Решение о неверности «овнер всегда staff своих» из W50 учтено: гейт — по
  `event_staff` конкретного события (STF-2).

## Ревью

- **Ревьюер**: — · **Дата**: — · **Вердикт**: —

### Замечания

1. —

## Хвосты и блокеры

- Полная живая проверка позитива (нужен человек с `initData`) — на W15.

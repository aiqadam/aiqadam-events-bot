# W74. Бейдж «Регистрация закрыта»

- **Статус**: на проверке
- **Владелец**: агент
- **Волна**: P1
- **Зависит от**: #118 (тот же `manage-api` — после него)
- **Начат**: 2026-09-23 · **Закрыт**: —

## Цель

Опубликованное событие с прошедшим дедлайном регистрации выглядит как обычное
опубликованное. Показать организатору бейдж «Регистрация закрыта» в списке и
предупреждение в форме, не блокируя сохранение.

## Что построено

| Артефакт | ID / имя | Каталог |
|----------|----------|---------|
| flow `manage-api` (`step_32`, список) | `CcGPwuW4ws5hkcaOPerEG` | [catalog/flows/manage-api.md](../../catalog/flows/manage-api.md) |
| Mini App `Manage.tsx` (список + шаг «Где и когда») | — | — |
| `i18n/ru.json`: `manage.badge.reg_closed`, `manage.warn.deadline_past` | — | — |
| `miniapp/src/index.css`: `.helper.warning`, перенос `.event-card .event-top` | — | — |

`step_32` теперь кладёт в строку списка сырой `reg_deadline_at` (раньше отдавал
`status` без него). Формулу показа («`published` + дедлайн в прошлом + начало в
будущем») считает страница — сервер не решает, что показать: `reg_deadline_at`
уже нужен форме, и одна формула на список и форму не расходится.

## Чек-лист готовности

> Из [issue #126](https://github.com/aiqadam/aiqadam-events-bot/issues/126).

- [x] бейдж в списке и предупреждение в форме работают
- [x] `catalog/flows/manage-api.md`, экспорт, `_manifest.json` обновлены
- [x] `check-texts.py`, `npm run build` зелёные
- [x] `catalog/` совпадает с живым проектом

## Как проверено

**Живой `curl` на опубликованный `/sync`** (`manage-api`, версия
`g0Tjak1LdiTkWI9UN0Pc4`). `initData` подписывал временный флоу
`zz-qa-mint-w74` (создан MCP, удалён после тестов; в манифесте и каталоге его
нет). Ответы:

| Случай | Ответ |
|---|---|
| staff `322876545`, `action=list` | `200`, 2 события; в обеих строках есть `reg_deadline_at` (`строк без ключа: []`) |
| «Кладдадд» `mu9uzchnu2il` (published, дедлайн `2026-09-20`, начало `2026-09-25`) | `starts_at=2026-09-25T13:35:00.000Z`, `reg_deadline_at=2026-09-20T18:35:00.000Z` → бейдж (в момент прогона `2026-09-23`) |
| «AI Qadam Meetup #3» `mu9rqipgetmp` (published, дедлайн `2026-10-12`, начало `2026-10-17`) | `reg_deadline_at=2026-10-12T12:05:00.000Z` → бейджа нет |
| не-staff `532804490`, `action=list` | `403 forbidden` — регресса прав нет |

Дословный вывод прогона (до удаления временного флоу):

```
== A. owner list: reg_deadline_at в строках ==
HTTP 200 count= 2
строк без reg_deadline_at: []
   mu9uzchnu2il | Кладдадд | status= published | starts_at= 2026-09-25T13:35:00.000Z | reg_deadline_at= 2026-09-20T18:35:00.000Z
   mu9rqipgetmp | AI Qadam Meetup #3 | status= published | starts_at= 2026-10-17T08:00:00.000Z | reg_deadline_at= 2026-10-12T12:05:00.000Z
== B. не-staff list: 403 (регресс прав) ==
HTTP 403 error= forbidden
```

**Headless-стенд Mini App** `~/w41/w74-shots.mjs` (статика `dist` + мок
`Telegram.WebApp` и `manage-api`, скриншоты `~/w41/shots-w74`): **14/14 OK,
console-ошибок 0**. Различающие случаи списка: `published` с прошедшим
дедлайном и будущим началом → бейдж есть; дедлайн в будущем, `cancelled`,
`finished`, пустой дедлайн, начавшееся событие → бейджа нет. Форма:
предупреждение показано при дедлайне в прошлом и начале в будущем (цвет
`warning`, не `.error`), при дедлайне в будущем и при начале в прошлом — нет;
«Далее» предупреждением не блокируется. Светлая и тёмная темы — скриншотами.

**Офлайн:** `tools/check-export-secrets.sh` (0 совпадений), `check-texts.py`
(245 пар, 0 расхождений), `check-commands.py`, `check-agents.py` — зелёные;
`cd miniapp && npm run build` (tsc + vite) проходит.

## Журнал

- **2026-09-23** — пакет взят.
- **2026-09-23** — реализация. Точка отката `manage-api` до правки —
  `PLEU2BP6GTKuhHxXr2VMJ`. Правка `step_32` (полный `sourceCode`, гоча 16:
  правки одного флоу — последовательно); `ap_validate_flow` → публикация
  `g0Tjak1LdiTkWI9UN0Pc4` → экспорт снят сразу после неё (гоча 14), до тестов.
- **2026-09-23** — живой curl: `reg_deadline_at` появился в строках списка,
  не-staff по-прежнему `403`. Первый прогон стенда показал вылезающий за край
  бейдж «Вы создали» — добавлен перенос `.event-card .event-top` (W74) в
  `index.css`; повторный стенд 14/14.
- **2026-09-23** — временный флоу `zz-qa-mint-w74` (подпись `initData` через
  `node:crypto`, `BOT_TOKEN` — переменной) создан для curl и удалён; в
  манифесте/каталоге его нет.
- **2026-09-23** — `migrations`: строка `2026-09-23-w74-01` (`flow:manage-api`,
  `publish`, `version_id: g0Tjak1LdiTkWI9UN0Pc4`).

## Ревью

> Заполняет независимый ревьюер.

- **Ревьюер**: — · **Дата**: — · **Вердикт**: —

## Хвосты и блокеры

- **Живой скриншот на устройстве** (реальный Telegram, не стенд) — хвост на
  владельца: бейдж и предупреждение проверены мок-стендом и живым curl, но не
  глазами на телефоне.
- **Дедлайн в прошлом у уже опубликованного события** предупреждение серверу
  не добавляет: `step_7` по-прежнему проверяет «в будущем» только для
  **изменённой** даты (это и есть текущее поведение из issue — форма не
  блокируется). Предупреждение видит только страница.

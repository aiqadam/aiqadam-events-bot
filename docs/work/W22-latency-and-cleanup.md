# W22. Дешёвая латентность после исчерпания inline + уборка

- **Статус**: готов
- **Владелец**: агент W22
- **Волна**: 5
- **Зависит от**: W21, W20, W17
- **Начат**: 2026-09-13 · **Закрыт**: 2026-09-13

## Цель

Снять с горячего пути то, что осталось после [ADR-0012](../adr/0012-end-to-end-flows-instead-of-subflow-functions.md):
повторные чтения таблиц, лишние CODE-шаги, лишние сообщения `/start`.
Удалить `fn-*`-флоу, которых после W21 никто не вызывает. Привести каталог
в соответствие с живым проектом. Продолжение [Q28](../OPEN-QUESTIONS.md#q28).

## Замер «до» (2026-09-13, разложение живых PRODUCTION-прогонов)

Метод — сумма длительностей шагов по `ap_get_run`; после W17 она равна
длительности прогона (пауза ≈0).

| Флоу | Прогон | Итого | CODE | Таблицы/store | Telegram HTTP |
|---|---|---|---|---|---|
| `tg-router` (шаг визарда) | `PNTy26gz6KhzlxCZLiC2r` | 7,9 с (своя работа 2,28 с) | 4 шага, 0,31 с | 4 обращения, 1,96 с | — |
| `registration` `/start` | `vAVCNJuF1SedAdEPBhQzh` | 7,7 с | 11 шагов, 0,75 с | 8 обращений, 3,42 с | 4 отправки, 2,98 с |
| `checkin-api` скан `ok` | `AYf8AII1XYoz5zso1pDHR` | 4,3 с | 17 шагов, 1,25 с | 6 обращений, 2,63 с | ответ 0,38 с |

**Цены операций, замеренные, а не оценённые:**

| Операция | Цена |
|---|---|
| CODE-шаг (любой, включая тривиальный) | 57–101 мс, в среднем ~72 |
| `tables-find` / `update` / `create` | 360–560 мс |
| `store put_if_absent` | 483 мс |
| отправка сообщения через Bot API | 670–860 мс |
| `callFlow` inline hop | ~520 мс (было 1,0–1,2 с в Q28) |

**Два факта, которых в [Q28](../OPEN-QUESTIONS.md#q28) нет:**

1. **Цена чтения `tables` не зависит от объёма выборки.** 30 строк — 472 мс,
   3 строки — 531 мс. Следствие: «прочитать надмножество один раз и
   отфильтровать в CODE» почти всегда выигрывает у нескольких узких чтений.
2. **CODE-шаг не бесплатен в количестве.** Q28 оценил его в 0,06 с и назвал
   дешёвым — поштучно верно, но в `checkin-api` их 17, и это 1,25 с, 29 %
   прогона.

## Что делаем

- [x] `registration`: три чтения `strings` в ветке `start` → одно;
      два чтения `registrations` → одно
- [ ] `registration`: чтение `sessions` убрано — **не сделано**, см. «Хвосты»
- [x] `registration`: `/start` отправляет 2 сообщения вместо 4
      ([ADR-0013](../adr/0013-fewer-messages-on-start.md) + правка OWN-2 в SPEC)
- [ ] `checkin-api`: CODE-цепочки слиты — **не сделано**, см. «Хвосты»
- [ ] `checkin-api`: чтение `users` для языка контролёра убрано — **отвергнуто**,
      см. «Хвосты»
- [ ] `my-qr-api`: CODE-цепочки слиты — **не сделано**, см. «Хвосты»
- [x] `tg-router`: `users` не переписывается без изменений
- [x] эталон `find-registration` отбирает строки по ключам в коде
      (латентность + смягчение [Q25](../OPEN-QUESTIONS.md#q25)); **все пять**
      живых копий шага 3 обновлены одним текстом
- [x] девять `fn-*` удалены; логика `fn-resolve-segment` сохранена сниппетом
- [x] замеры «после» записаны сюда и в [ADR-0009](../adr/0009-hot-path-latency-budget-and-order.md)
- [x] `catalog/` совпадает с живым проектом
- [x] независимое ревью, вердикт «замечаний нет» (круг 2, 2026-09-13)

**Пакет закрывается не полностью, и это названо прямо.** Взяты те пункты, что
не требуют переопределения эталонов ADR-0012 и не размывают инварианты.
Оставшееся перечислено в «Хвостах» с причиной по каждому — и продублировано
в ADR-0009, чтобы следующий пакет нашёл его по порядку чтения, а не в журнале.

## Как проверено

Юнит-тестов нет, поэтому каждое утверждение — прогоном, и там, где изменение
могло «работать по совпадению», прогоном **различающим**.

### `registration`, ветка `/start` → `new`

Прогон `alppXYnzFLbjo8iI140Tr` (TESTING), **4,7 с против 7,7 с** до правки.
Вход подобран так, чтобы не трогать боевые данные: `telegramId = 999999999`
(регистрации нет → ветка `new`), `chatId` — владельца (сообщения видны глазами).

**Различающее наблюдение, а не просто «зелёный прогон»:** широкое чтение
`step_22` вернуло строку **чужого** участника (`telegram_id 322876545`), а
`step_79` отдал `found: false`. То есть новый отбор по ключам в коде реально
отсекает чужие строки — если бы он не работал, флоу принял бы чужую
регистрацию за свою и ушёл в ветку `existing`.

Отправлено ровно два сообщения (`step_20` карточка, `step_32` согласие),
`sendVenue` и подсказки нет. Созданная тестовая сессия удалена
(`t2zvO2c7O1FSIK9qCMmuf`).

### `tg-router`, пропуск записи в `users` — три прогона

| Прогон | Вход | Ожидание | Факт |
|---|---|---|---|
| `rVKYNeuRrYBwPNIvMFNgL` | пользователя нет | завести строку | ветка «заводим», `step_11` отработал |
| `5cxzKm1Q1FRxdw380UScy` | тот же апдейт ещё раз | **не писать** | `needsUpdate: false`, ветка пропуска, `step_10` не выполнялся, 1,9 с против 2,3 с |
| `UiJARuiA4j2XSU9zuYrK6` | **изменился `username`** | записать | `needsUpdate: true`, `step_10` отработал, в строке `w22_probe_renamed` |

Третий прогон — тот, ради которого пропуск нельзя принимать на веру: без него
«записи нет» неотличимо от «оптимизация сломала запись вообще».

Попутно подтверждена семантика ROUTER'а: в третьем прогоне **обе** ветки
дали `evaluation: true`, исполнилась первая — то есть порядок веток
(изменилось → нечего менять → завести) и есть работающая логика, переставлять
их нельзя. Тестовая строка `users` удалена (`296xILwIaZGE0Jrv8t1ah`).

### Сквозной прогон после удаления `fn-*`

`xM2kywPt13JN2GzTtAMja` — `/start edemo` от владельца через `tg-router`:
дедуп → `users` (ветка пропуска) → `sessions` → разбор deep link → `callFlow`
в `registration` (ветка `existing`, 4,8 с) → сообщения отправлены. 7,0 с.
Единственный оставшийся `callFlow` в проекте работает, удаление девяти флоу
ничего не оторвало.

### Боевой прогон владельца — весь путь на проде (2026-09-13)

Владелец прошёл путь вручную: deep link → согласие на ПД → рассылка → телефон
→ QR → скан контролёром. Все прогоны `SUCCEEDED`. Это **снимает главную
оговорку метода**: «до» и «после» теперь оба в `PRODUCTION`, один сценарий,
один человек, один ивент.

| Шаг | `tg-router` до → после | `registration` до → после |
|---|---|---|
| `/start edemo` | 10,5 → **6,7 с** | 7,7 → **4,5 с** |
| согласие на ПД | 9,0 → 8,7 с | 6,6 → 6,3 с |
| ответ про рассылку | 8,2 → 7,2 с | 5,6 → 4,9 с |
| телефон | 7,9 → 6,7 с | 5,1 → 4,3 с |
| **весь визард** | **35,6 → 29,3 с** | |

`/start` — единственный шаг, который правился по существу: **−36 %**. Три
остальных получили только пропуск записи в `users`: −0,3 / −1,0 / −1,2 с при
ожидаемых −0,56, то есть эффект в нужную сторону, но по величине неотличим
от разброса инстанса — числом его называть нельзя.

**Контроль:** `my-qr-api` не правился и показал 1,6 с до и 1,6 с после
(`Qglq1MrtBNlZjZXUO5U9e` → `ZNKza1Ad05K4KsjXpP14e`) — инстанс сам по себе
за эти часы быстрее не стал.

**Пробел проверки, который был закрыт этим прогоном.** До него правка эталона
`find-registration` в `checkin-api` и `my-qr-api` держалась на рассуждении
(«фильтры узкие, значит постфильтр отбрасывает ноль строк»). Боевой скан
`GDAtnQYQ3OybvG22E8kaJ` проверил её на живых данных: `step_42` и `step_29`
отдали `found: true`, `checked_in_at` записан (`20:35:46.753Z`), контролёр
увидел `Binali Rustamov — отмечен` — то есть работают и постфильтр, и
резолвинг имени.

**Чего этот прогон не доказывает.** Он прошёл только happy path. Ветки
`already`, `invalid`, `403`, `401` после правки эталона прогоном не
проверялись — постфильтр в них тоже участвует (`already` — напрямую).
Это остаётся ревьюеру.

### `checkin-api` — сравнивать 4,3 → 4,8 с нельзя

Скан «после» 4,8 с против 4,3 с «до» выглядит как замедление, но графы разные:
в текущей версии за записью идёт перечитывание после CAS
(`step_19`/`step_29`/`step_30`/`step_31`, +0,64 с) — это W20/W21, не W22.
В `checkin-api` W22 не менял ничего, кроме постфильтра, и ускорения не ждал.

### Побайтовая сверка копий эталона — не сделана механически

Все пять копий записаны одним и тем же литеральным текстом в одной сессии, и
одна из них (`checkin-api/step_29`) прочитана обратно и совпала с
отправленной. Полный `diff` всех копий с
`catalog/snippets/find-registration.md` — блок 2a
[чек-листа ревьюера](REVIEW-CHECKLIST.md), и в этом пакете он особенно
нужен: копий пять.

## Журнал

- **2026-09-13** — пакет взят. Замер «до» снят разложением трёх живых
  PRODUCTION-прогонов, а не по каталогу.
- **2026-09-13** — **инстанс весь день отдавал `502` пачками** по 2–3 запроса
  подряд, потом отвечал. На работу не повлияло (все вызовы идемпотентны и
  повторялись), но заложите на это время.
- **2026-09-13** — [#411](https://github.com/aiqadam/qadam-flow/issues/411)
  **подтверждён заново**: `ap_update_step` на шаге `tables` падает с
  `qadam_metadata_not_found qadamName=@aiqadam/qadam-tables`, хотя
  `ap_get_piece_props` и `ap_research_pieces` тот же qadam находят, и `auth`
  ничего не меняет. Лечится только пересозданием. Полезное наблюдение:
  **платформа переиспользует освободившееся имя шага** — удалили `step_21`
  (ROUTER venue) и `step_22` (`sendVenue`), а следующие два добавленных шага
  получили ровно эти имена. Ссылки при этом не «чинятся» сами: `step_21`
  теперь **другой шаг**. При чтении каталога и старых журналов это ловушка.
- **2026-09-13** — **у `tables` появился `tables-upsert-records`**, которого не
  было в списке возможностей ([AGENTS.md](../../AGENTS.md) утверждает «upsert'а
  нет»). В W22 не использован: апсерт `users` в `tg-router` всё равно требует
  предварительного чтения (нужны язык и согласия для классификации), а при
  наличии чтения условный пропуск записи выгоднее апсерта — он не пишет вовсе.
  Но факт стоит внести в описание платформы отдельной правкой.
- **2026-09-13** — **отвергнут кандидат «язык контролёра из `initData`»**
  (−0,4 с в `checkin-api`). `initData` подписана, брать из неё
  `user.language_code` технически безопасно, но это перетёрло бы явный выбор
  языка пользователя локалью клиента — ровно то, что [I18N.md](../I18N.md)
  запрещает и что комментарий в `tg-router/step_8` защищает отдельной строкой.
  Обмен «инвариант за 0,4 с» плохой; кандидат закрыт, а не отложен.
- **2026-09-13** — **правка эталона `find-registration` оказалась не только про
  латентность.** Чтобы одно широкое чтение кормило двух потребителей, шаг
  обязан сам отбирать свои строки. Ровно этот же постфильтр — недостающая
  страховка от fail-open [#382](https://github.com/aiqadam/qadam-flow/issues/382):
  выпавший фильтр чтения больше не отдаёт чужую регистрацию. Это частично
  смягчает [Q25](../OPEN-QUESTIONS.md#q25), но **не закрывает** его: самое
  опасное место — `checkin-api/step_6` (`isStaff = records.length > 0`) —
  постфильтра по-прежнему не имеет, и это остаётся за [W18](../BACKLOG.md).
- **2026-09-13** — при удалении `fn-resolve-segment` выяснилось, что его логика
  (сегменты рассылки, окно `no_show`, исключение заблокировавших) **нигде не
  встроена**: W14 не собран. Флоу удалён только после переноса кода в
  [`catalog/snippets/resolve-segment.md`](../../catalog/snippets/resolve-segment.md).
  Остальные восемь `fn-*` имели живые копии-эталоны и удалены без переноса.
- **2026-09-13** — по просьбе владельца по ходу пакета удалена единственная
  строка `registrations` (`demo` / `322876545`) — чтобы он прошёл путь
  регистрации с нуля вручную. Ручной проход **оказался лучшей проверкой, чем
  всё, что я успел собрать сам**: он дал before/after в одном окружении и
  закрыл единственный пункт, который до него держался на рассуждении
  (правка эталона в `checkin-api`). Синтетические прогоны выше оставлены —
  они покрывают то, чего ручной проход не даёт: различающие ветки пропуска
  записи в `users`.

## Ревью

- **Ревьюер**: независимый агент, чистый контекст · **Дата**: 2026-09-13 ·
  **Вердикт круга 1**: есть замечания (блокеров нет)
- **Круг 2**: 2026-09-13, вердикт — **замечаний нет**.

Круг 2 перепроверил `sha256` того, что я переносил руками
(`catalog/snippets/event-card.md` блок `extract` против живого
`registration/step_96`): `0996953a62e236db…`, 3005 байт, `difflib` — ноль строк
расхождения. Транскрипционной ошибки нет. Остальные шесть исправлений сверены
с живым проектом, не по дифу.

**Про Q31 ревьюер отдельно сказал не переигрывать:** правка `columns` у
`step_22` — изменение живого флоу, которому нужен свой прогон и своя сверка
каталога, то есть полноценный круг; протаскивать её под видом реакции на
замечание хуже, чем оставить открытый вопрос со сроком. Место этой правки —
W23.

**Необязательное наблюдение круга 2, принято и внесено в Q31:** список колонок
в варианте 1 верен для `step_22`, но эталон `find-registration` читает из
строки ещё `checked_in_by`, `cancelled_at`, `source`, `id`. Перенос того же
списка на **узкие** чтения (`checkin-api/step_41`, `step_19`,
`my-qr-api/step_22`) молча обнулил бы поля, участвующие в IDM-2. Список в Q31
теперь явно привязан к `step_22` с предупреждением.

Ревьюер сделал то, что владелец оставил незакрытым, и это стоит записать
отдельно: **побайтовая сверка по `sha256`** всех живых копий эталонов против
`catalog/snippets/` — шаг 3 `find-registration` пять копий, шаг 1 четыре,
плюс `fmt-time` (4), `hmac-init-data` (2), `hmac-qr` (3), `i18n-resolve` (8),
`parse-start` (1). Все совпали. Он же проверил регистрозависимость `eq` на
живой таблице (`event_id eq "DEMO"` → 0 записей, `eq "demo"` → 1) — именно
это делает новый постфильтр гарантированно нулевым для узких чтений, то есть
закрыл разбором ветки `checkin-api`, которые я не прогнал.

### Замечания

1. **важно** — **В BACKLOG.md не было секции W22** — `docs/BACKLOG.md`.
   Пакет взяли, не оформив его чек-листа в источнике правды: объём определил
   сам себе журнал, а «хвосты» лежали только в ADR-0009, откуда пакеты не берут.
   - *Исправлено*: заведены секции **W22** (с честной пометкой, что она
     написана задним числом) и **W23** — все хвосты стали оформленной работой
     со своим чек-листом готовности. Строка W23 добавлена в STATUS.md.
     (2026-09-13)

2. **важно** — **У эталона `event-card` не осталось побайтового источника** —
   `catalog/snippets/event-card.md`. До W22 файл отсылал за эталоном к живому
   `fn-event-card/step_3`; W22 удалил этот флоу и переписал фразу на
   «сравнивайте с текстом ниже», **не приведя текст к живому виду** (`return`
   отличался форматированием). Блок 2a по этому эталону перестал проходить
   против чего-либо — то есть единственная защита от тихого дрейфа исчезла,
   и исчезла именно из-за моей правки.
   - *Исправлено*: блок `extract` заменён дословным кодом
     `registration/step_96`; пометка о расхождении заменена на датированную
     запись о приведении. У блока `assemble` явно сказано, что живого
     не-конвертного носителя у него нет и побайтово он не сверяется.
     **Прошу круг 2 перепроверить это `sha256`** — текст переносился руками.
     (2026-09-13)

3. **на будущее** — Дубль строки `registration / step_31` в таблице
   «Встраивают» — `catalog/snippets/i18n-resolve.md`.
   - *Исправлено*: устаревшая строка от 2026-09-12 удалена. (2026-09-13)

4. **на будущее** — Недатированное «Все девять копий сверены ревьюером по
   sha256» — `catalog/snippets/find-registration.md`. Утверждение осталось от
   W21, а код после него менялся: до сверки круга 1 оно было просроченным.
   - *Исправлено*: заменено на датированную запись со списком копий и прямым
     предупреждением «правя код эталона, эту строку надо переписать или
     удалить». (2026-09-13)

5. **на будущее** — Расхождение в журнале: «четыре живые копии» против
   «копий пять».
   - *Исправлено*: в чек-листе теперь «все пять живых копий шага 3».
     (2026-09-13)

6. **на будущее** — Три устаревших утверждения о текущем состоянии, которые
   уборка не поймала при заявленном «каталог совпадает с живым проектом»:
   `checkin-api.md` («обе **callFlow**-проверки» — `callFlow` там нет с W21),
   `registration.md` (`custom_api_call` в списке connections — их не осталось
   ни одного после удаления `sendVenue`), `registration.md` («`fn-t` вернёт
   сам ключ»).
   - *Исправлено*: все три переписаны на имена живых шагов. (2026-09-13)

7. **на будущее** — Цена широкого чтения названа в каталоге, но не в ADR-0009,
   который читают как руководство. `registration/step_22` без `limit` и без
   `columns` кладёт в лог `telegram_id` всех участников ивента на **каждый**
   `/start`, а `/start` дёргается публичным deep link'ом кем угодно.
   - *Исправлено*: правило «читать надмножество один раз» в ADR-0009 получило
     оговорку о цене, и заведён [Q31](../OPEN-QUESTIONS.md#q31) — с порогом,
     вариантами (первый и самый дешёвый — задать `columns`) и сроком «до W15».
     (2026-09-13)

## Хвосты и блокеры

Всё перечисленное продублировано в
[ADR-0009](../adr/0009-hot-path-latency-budget-and-order.md#что-осталось-по-убыванию-выигрыша) —
там его найдёт следующий пакет.

1. **`checkin-api`: 17 CODE-шагов → ~8** (−0,7 с) и **`my-qr-api`: 11 → ~4**
   (−0,5 с). Не сделано **сознательно**: слияние соседних CODE-шагов ломает
   побайтовую сверку эталонов [ADR-0012](../adr/0012-end-to-end-flows-instead-of-subflow-functions.md).
   Делать это надо не «по месту», а переопределив сами эталоны в слитой форме
   (`hmac-init-data` + разбор + проверка = один шаг) и обновив все копии
   одновременно. Это отдельный пакет, а не хвост оптимизации.
2. **`checkin-api`: два чтения `users`** (0,81 с). Слить нельзя без
   переупорядочивания флоу: `telegram_id` участника известен только после
   разбора QR, а язык контролёра нужен раньше — в ветке `403`. Вариант «взять
   язык из `initData`» отвергнут (см. журнал).
3. **`registration`: по два чтения `strings`** в ветках `existing`,
   `mkt_answer`, `phone_answer` — по −0,46 с на ветку. Механически то же, что
   уже сделано в `start`; не сделано из-за объёма пересоздания tables-шагов
   (#411) в одном пакете.
4. **`registration/step_26`** (чтение `sessions`, 0,36 с) — роутер его уже
   прочитал. Требует правки payload'а `callFlow`, форму которого MCP не
   показывает ([#102](https://github.com/aiqadam/qadam-flow/issues/102)).
   Переписывать вслепую ради 0,36 с — плохой размен.
5. **Ключ `reg.venue.hint` в `i18n/*.json` осиротел** после ADR-0013. Оставлен
   намеренно: удаление строк i18n — отдельная уборка, и `i18n-sync` от лишнего
   ключа не страдает.
6. [Q29](../OPEN-QUESTIONS.md#q29) (`registration/step_38` получает пустой
   вход) в объём не входит — чинит владелец W5. **Внимание:** после W22
   `step_38` — это по-прежнему шаг ветки `declined` (формат дедлайна), а
   пустой вход был у шага поиска регистрации в `pdn_yes`; номера шагов в Q29
   стоит перепроверить по живому флоу, платформа переиспользует имена.

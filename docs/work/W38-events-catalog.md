# W38. Четвёртая страница Mini App — каталог ивентов `#/events`

- **Статус**: на проверке
- **Владелец**: агент
- **Волна**: v0.1 (первый пакет пары W38 → W43, [ROADMAP](../ROADMAP.md#v01--порядок-работ-один-агент-последовательно))
- **Зависит от**: W33 (✅ SPA и роутер), ADR-0017, W30/ADR-0019 (`EventCard`), ADR-0023
- **Начат**: 2026-09-16 · **Закрыт**: —

## Цель

PAR-3 переезжает из чат-списка `events-list` на экран `#/events`: две вкладки
(будущие/прошедшие), карточки `EventCard`, тап — в уже построенный `reg-start`
по deep link. Чат-флоу `events-list` и его ветки в `tg-router` выводятся
из эксплуатации. Подробности — [BACKLOG W38](../BACKLOG.md#w38-четвёртая-страница-mini-app--каталог-ивентов-events),
дублировать не надо.

## Что построено

| Артефакт | ID / имя | Каталог |
|----------|----------|---------|
| flow `events-api` (новый) | `wEdKdE4RBGKIzWkl4MJHG`, версия `ytVOqCYzA5fLsIufaAQd6` | [catalog/flows/events-api.md](../../catalog/flows/events-api.md) |
| flow `menu` (правка) | `1DORFhP9F3W00KpKz5wDw`, версия `JYOyGHCtsFPFu8mMRoj4u` | [catalog/flows/menu.md](../../catalog/flows/menu.md) |
| flow `tg-router` (правка) | `nyaBzgKGG8TTTsryjc9tW`, версия `F7pxsKGZXUAqxBl29KuGq` | [catalog/flows/tg-router.md](../../catalog/flows/tg-router.md) |
| flow `events-list` (удалён) | был `UxuFOI7GvXaboDQZHHKSI` | — |
| роут SPA `#/events` | `miniapp/src/routes/Events.tsx` (ленивый чанк 3,7 КиБ / 1,4 gzip) | [catalog/overview.md](../../catalog/overview.md) |

## Чек-лист готовности

> Скопирован из [BACKLOG.md](../BACKLOG.md#w38-четвёртая-страница-mini-app--каталог-ивентов-events).

- [x] каталог открывается из меню одной кнопкой, будущие и прошедшие раздельны
      (PAR-3 не потерян — воспроизведены оба среза `events-list`);
- [x] тап по ивенту открывает `reg-start` этого ивента (проверено вживую:
      переход → карточка регистрации);
- [x] `register_link` строит сервер; в SPA нет литерального `BOT_USERNAME`
      (grep), имя бота приходит с API;
- [x] `events-api` не отдаёт персональных данных и неопубликованных ивентов;
      проверено различающим прогоном (черновик не виден);
- [x] `events-list` удалён; каталог и `flows/` без него, `tg-router` без его
      веток и команды `/events`, `migrations` дописаны;
- [x] каталог на брендовых токенах и `EventCard`, своих цветов/шрифтов нет
      ([ADR-0019](../adr/0019-design-system-from-brand-repo.md)); каталог — ленивый
      чанк, веб-шрифты не грузятся ([ADR-0020](../adr/0020-no-web-fonts-on-venue-pages.md));
- [x] тексты из `i18n/ru.json`, `check-texts.py` чист;
- [x] `catalog/flows/events-api.md`, раздел Mini App в `catalog/overview.md`,
      экспорт `flows/` тем же коммитом, `check-export-secrets.sh` чист;
- [ ] независимое ревью, вердикт «замечаний нет».

Плюс обязательный пункт: `catalog/` совпадает с живым проектом.

**Граница доказательства по «тапу».** Живого Telegram-клиента у агента нет:
доказаны обе половины цепочки по отдельности — серверная (`registerLink`
с `start=e<id>` в ответе) и клиентская (`href` кнопки в DOM, `openTelegramLink`
в WebView), плюс то, что `/start e<id>` у бота ведёт в `reg-start` (построено
и принято в W5/W28). Тап живьём и переход «меню → каталог» на боевом Pages
закрываются после мержа — записано в «Хвостах».

## Как проверено

> Чем именно, а не «протестировано». Фикстуры, отрицательные сценарии, что видел на экране.

- **`events-api`, различающий прогон на черновике (PRODUCTION, `curl`).**
  Создана временная строка `events` со `status = draft` (`id = w38drafttest`,
  запись `TSjUJnJ4V9TsgXm1kbeJt`, `starts_at 2026-10-20T14:00Z`; прогон
  `ap_run_action` `ZxNZ9byPmG3JtNNM0BKJX`), `POST .../wEdKdE4RBGKIzWkl4MJHG/sync`
  → `200 {"ok":true,"upcoming":[4],"past":[3]}`; черновик **отсутствует** и в
  `upcoming`, и в `past`; в выдаче только поля карточки (`id`, `title`,
  `address`, `startsAt`, `endsAt`, `status`, `registerLink`) —
  `staff_id`/`chapter_id`/`telegram_id` не читаются вовсе.
  Фикстура удалена (`tables-delete-record`, прогон
  `qgPw52VPRtxJTmy4WTisd` → `{"success":true}`).
  **Вывод сохранён здесь намеренно:** прогоны `ap_run_action` через
  `ap_get_run` не читаются (`Flow run not found`) — единственный след
  доказательства, как и требует [README](README.md#доказательство-переживает-то-что-его-породило).
- **`registerLink` строит сервер**: в ответе
  `https://t.me/aiqadam_events_dev_bot?start=e<id>` — `BOT_USERNAME` подставлен;
  `grep` по `miniapp/src/` литералов `BOT_USERNAME`/`aiqadam_events` не находит.
- **`events-api`**: `ap_validate_flow` — valid; TESTING-прогон
  `p8xt8jlJQMBqvfaDQtT7J` (деление и `registerLink` на боевых строках);
  живой `curl` — `200 {ok, upcoming[4], past[3]}`.
- **`menu`**: прогон `M62dDF6YDjxjpM012GwRS` — в `reply_markup` кнопка
  «Ивенты» стала `web_app` с `https://miniapp.events.aiqadam.org/#/events`;
  живая доставка — прогон `tg-router` `5qUrqNcfiR2Np1sg1zqTC` (`/start` →
  `route=menu`, очередь) → прогон `menu` `ikWQHcyte3onrflvuLaig` SUCCEEDED,
  карточка ушла владельцу. Побочный эффект теста: `tg-router` пишет `users`
  с именем из тестового апдейта («Тест»); имя вернётся при следующем
  живом сообщении владельца.
- **`tg-router`**: `ap_validate_flow` — valid (19 шагов); полный TESTING-прогон
  `0Yth6r4tELdDo6neNgXY5` — старый колбэк `ev:list:upcoming` даёт `route=none`
  и уходит в `Otherwise` (step_16, тишина); полный прогон `5qUrqNcfiR2Np1sg1zqTC`
  — `/start` даёт `route=menu`, ветка `menu` единственная совпавшая.
- **SPA `#/events`** (headless Playwright, 390×844, deviceScaleFactor 2,
  локальная сборка `miniapp/dist` + `i18n/`, API — живой `events-api`):
  будущие — 4 карточки с датой-платой, статусом «Опубликован», временем
  Asia/Tashkent (OWN-3) и кнопкой «Зарегистрироваться»; прошедшие — 3 карточки
  со статусом «Завершён» и **без** кнопки; вкладки переключаются, `?tab=past`
  открывается диплинком; пустой срез (мок ответа `{upcoming:[],past:[]}`) —
  `EmptyState` строкой «Пока ничего не запланировано.», а не молчание;
  светлая и тёмная темы. Скриншоты — `~/w41/shots-w38/`,
  скрипт `~/w41/w38.mjs`.
- **Тексты**: `tools/check-texts.py` — 17 флоу, 114 пар, 0 расхождений;
  новые ключи SPA (`events.loading`, `events.err.network`, `events.err.server`)
  только в `ru.json` (ADR-0014); мёртвый `events.list.item` снят.
- **Экспорт и проверки**: `tools/check-export-secrets.sh` — чист;
  `tools/check-commands.py i18n/ru.json flows/*.json` — 0 нарушений;
  `miniapp` `npm run build` — tsc + vite без ошибок; веб-шрифтов нет
  (в вендоренном CSS только data-URI).

## Журнал

- **2026-09-16** — взятие пакета. Границы: `miniapp/src/App.tsx` (роут
  `#/events` ленивым чанком), новый `events-api` (sync-вебхук, чтение `events`
  со `status = published`, деление будущие/прошедшие в CODE, `register_link`
  собирает сервер из `BOT_USERNAME`), `menu` (кнопка «Афиша» → `web_app`
  на `{{MINIAPP_URL}}#/events` вместо колбэка `ev:list:upcoming`), удаление
  `events-list` и его веток/команды `/events` в `tg-router`, `i18n/ru.json`,
  каталог, `flows/`, `migrations`. `reg-start` не трогается — регистрацию
  и согласия переносит в каталог W43, идущий следом. Порядок работ — BACKLOG
  W38 п. 1–5; вебхук собирается первым, SPA — поверх его контракта ответа.
- **2026-09-16 — сборка и прогоны.** `events-api` собран одним `ap_build_flow`
  (4 шага), `ap_validate_flow`, TESTING-прогон, публикация. `menu/step_4`:
  одна строка кода (кнопка «Ивенты» — `callback_data` → `web_app`), тексты
  не тронуты — `ap_update_step` принимает только `sourceCode` и не задевает
  `input.texts` (гоча 12). `tg-router`: из `step_10` убрана ветка `events_list`,
  шаг `step_23` (`callFlow events-list`) удалён, затем пустая ветка 3 из
  `step_11` — `ap_validate_flow` до удаления ветки ругался на empty branch,
  порядок «шаг → ветка» сработал. `events-list` удалён после снятия ссылок.
- **2026-09-16 — Q56 (буква ADR-0023 против W12).** Обнаружено при выборе
  фильтра: ADR-0023 п. 3 говорит «только published», но W12 переводит
  прошедшие в `finished`, и буквальный фильтр выбросил бы их из «Прошедших».
  Решение — `in (published, finished)` + постфильтр в CODE; запись
  в [OPEN-QUESTIONS Q56](../OPEN-QUESTIONS.md#q56) (ADR задним числом
  не правится).
- **2026-09-16 — экспорт после тестовых прогонов дал DRAFT.** `ap_export_flow`
  по `tg-router` вернул `state: DRAFT`: прогоны `ap_test_step`/`ap_test_flow`
  после публикации пишут в черновик sample-данные (в экспорт их срезает
  нормализация, но версия перестаёт быть LOCKED). Лечение — повторная
  `ap_lock_and_publish` (контент не меняется) и экспорт сразу после неё;
  в `migrations` пишется одна финальная `publish`-строка. **Для следующих
  пакетов: экспорт снимать до тестовых прогонов** (или републиковать).
- **2026-09-16 — Gotcha тестового инструмента: `ap_test_step` по callableFlow
  без обёртки `{"data": {...}}` даёт пустые поля.** Плоский `triggerTestData`
  ложится в output триггера как есть, а входы шагов читают
  `{{trigger['output'].data.X}}` — прогон `vMi9FI5Pjbv8c0f1CCD49` получил
  пустой `firstName` и анонимное приветствие там, где ожидалось именное;
  тот же тест с обёрткой (`M62dDF6YDjxjpM012GwRS`) — правильный. В реальном
  рантайме `payload` разворачивается платформой в `.data` сам. Дописано
  гочей 13 в [CLAUDE.md](../../CLAUDE.md).
- **2026-09-16 — MCP-снимок экспорта беднее REST** (ожидаемо, Q44/ADR-0021):
  в шагах нет `auth`, у `menu` исчез пустой `ownerRecords: ""`, у `tg-router`
  — пустые `utm`/`callbackData` в `exampleData`. Для ре-импорта это значит
  перенастройку connection'ов; источник правды — репозиторий, расхождение
  фиксировано `source: "mcp"` в `_manifest.json`.
- **2026-09-16 — `events.list.item` снят из `ru.json`** как мёртвый после
  удаления чат-листа; остальные `events.list.*` переиспользованы SPA
  (табы, заголовки, пустые срезы) — новых текстов, кроме трёх `events.*`,
  не заводилось.
- **2026-09-16 — `migrations` и ветка.** Коммит `6c9d63c` (ветка
  `w38-events-catalog`), строки `migrations`: `2026-09-16-w38-01` (create
  `events-api`), `-02` (publish `ytVOqCYzA5fLsIufaAQd6`), `-03` (publish
  `menu` `JYOyGHCtsFPFu8mMRoj4u`), `-04` (publish `tg-router`
  `F7pxsKGZXUAqxBl29KuGq`), `-05` (delete `events-list`). Републикация
  `tg-router` после тестовых прогонов (перед экспортом) — в `-04` одна
  финальная строка, промежуточная не записана (контент не менялся).
  Сверка `tools/check-migrations.py` требует ключа платформы — прогон
  за ревьюером.
- **2026-09-16 — круг 1 ревью: 3 «важно» + 3 «на будущее», исправлено.**
  Каталог переменных (читатели `BOT_USERNAME`/`MINIAPP_URL`, строка
  `YANDEX_GEOCODER_API_KEY` и счётчик 5 — хвост W42, подобран W38);
  `utcToWhen` приведён к эталону + раздел «Сверка с прототипом-эталоном»;
  битые `../../docs/…` в `catalog/overview.md`; вывод фикстурного прогона
  перенесён в журнал до его исчезновения; шапка статуса и опечатка Q56.
  Все — по замечаниям под «Ревью»; ждём повторного круга.

## Сверка с прототипом-эталоном (ADR-0027)

Прототип W41 — целевой эталон ([ADR-0027](../adr/0027-prototype-as-target-reference.md));
код прототипа в продукт не переносится, сверяется форма. Экран каталога
в эталоне — `prototypes/app.js` (`renderEvents`, `eventCardEl`).

| Что у эталона | В W38 | Почему |
|---|---|---|
| Мета карточки «сб, 26 сентября · 18:30» | То же — `utcToWhen` приведён к форме эталона (запятая после дня недели) | Мелочь, но правило пакета — расхождений без причины не оставлять |
| Три таба: «Мои билеты», «Будущие», «Прошедшие» | Два: «Будущие», «Прошедшие» | «Мои билеты» — регистрации, их создаёт [W43](../BACKLOG.md#w43-регистрация-и-мои-билеты-в-каталоге-events) (регистрация на месте); до W43 у каталога нет своих билетов |
| Регистрация — шитом внутри Mini App (PAR-1/PAR-2 на месте) | Кнопка ведёт по deep link в `reg-start` (согласия — в чате) | [ADR-0023](../adr/0023-fourth-miniapp-page-event-catalog.md) п. 2 и 6: в W38 инвариант PAR не трогается, перенос — W43 |
| Каталог — домашний экран (`#/events?tab=mine` при открытии) | Вход — кнопка «Ивенты» в меню; `#/events` не роут по умолчанию | До W43 домашний таб пустой (билетов нет); меню остаётся хабом (W34), W43 заведёт `?tab=mine` |
| В карточке — счётчик мест и «был на ивенте» | Нет | Требует регистраций (W43) и статистики участников (W13); в W38 данных для честного счётчика нет |
| Фото афиши в карточке | Нет | OWN-1 без фото ([Q46](../OPEN-QUESTIONS.md#q46)); возврат — [W39](../BACKLOG.md#w39-возврат-афиши-в-mini-app-форма-и-доставка) |
| Действие «Показать QR», если уже зарегистрирован | Нет — на всех будущих кнопка «Зарегистрироваться» | Каталог не знает регистраций вызывающего без `initData`; появится в W43 |
| Пустой срез — `EmptyState`; дата-плата, статус, заголовок, мета, брендовые токены, своя тема | То же | Сверено скриншотами `~/w41/shots-w38/` (светлая/тёмная, пустой срез) |

## Ревью

> Заполняет **независимый ревьюер** по [REVIEW-CHECKLIST.md](REVIEW-CHECKLIST.md).
> Владелец пакета сюда не пишет — только отвечает под замечаниями, что исправлено.

- **Ревьюер**: независимый агент (чистый контекст; MCP, curl, офлайн-проверки,
  сборка SPA) · **Дата**: 2026-09-16 · **Вердикт**: **есть замечания**
  (3 важно, 3 на будущее)

### Замечания

1. **важно** — каталог не обновлён по переменным. `events-api` читает
   `BOT_USERNAME` (`step_2`, вход `botUsername`), а в таблице «Кто их читает»
   (`catalog/variables.md`) этого читателя нет; `menu` собирает
   `MINIAPP_URL` ещё и в `#/events`, а у `MINIAPP_URL` в той же таблице —
   только `#/manage` и `#/scan?event_id=`. Отдельно, не от W38: живой проект
   содержит **5** переменных — `YANDEX_GEOCODER_API_KEY` (W42) нет ни в
   `variables.md`, ни в `catalog/overview.md` («Variables | 4 — …»), хотя
   `catalog/flows/manage-api.md` её уже описывает. Почему важно: каталог —
   источник правды, и журнал сам заявляет «catalog/ совпадает с живым
   проектом»; по переменным это не так, следующий агент по каталогу
   не увидит зависимостей флоу. Чинить одним коммитом: читатели
   `events-api`/`menu` + строка и счётчик `YANDEX_GEOCODER_API_KEY`.
   - *Исправлено*: `catalog/variables.md` — добавлен читатель `BOT_USERNAME`
     у `events-api`, у `MINIAPP_URL` в строке `menu` назван и `#/events`,
     добавлена строка `YANDEX_GEOCODER_API_KEY` (читает `manage-api`,
     ветка `geo_link`) и её читатель; `catalog/overview.md` — `Variables`
     4 → 5 с именем переменной (2026-09-16).
2. **важно** — сверка с прототипом-эталоном (ADR-0027, чек-лист п. 3a)
   в журнале не отражена. Конкретное расхождение: мета даты карточки —
   прототип рисует «сб, 26 сентября · 18:30» (`prototypes/data.js`, поле
   `when`), SPA — «сб 26 сентября · 18:30» (`miniapp/src/lib/dates.ts`,
   `utcToWhen`: `[weekday, day, month].join(' ')`; комментарий там же
   обещает форму с запятой). Остальные отличия от эталона объяснены вне
   журнала: три таба у прототипа против двух (W43), шит регистрации против
   deep link в чат (ADR-0023 п. 2), каталог — домашний экран
   (`#/events?tab=mine` первым, W43); в журнале W38 они не названы. Почему
   важно: v0.1 закрывается соответствием прототипу, и правило пакета —
   каждое расхождение назвать с причиной до сдачи. Чинить: запятую
   привести к эталону (или назвать), зону W43 — строкой в журнале со
   ссылками на ADR-0023/W43.
   - *Исправлено*: `utcToWhen` приведён к форме эталона («сб, 26 сентября ·
     18:30»), SPA пересобрана, headless-прогон и скриншоты обновлены
     (запятая видна в `~/w41/shots-w38/01-…`); добавлен раздел
     «Сверка с прототипом-эталоном» со всеми отличиями и причинами
     (2026-09-16).
3. **важно** — битые относительные ссылки в `catalog/overview.md`, внесены
   этим коммитом: строки 63, 69, 70 и 77 ведут `../../docs/...`, а файл
   лежит в `catalog/`, поэтому `../../` уводит за пределы репозитория
   (19 остальных ссылок файла — корректные `../docs/...`; форма
   `../../docs/...` верна для `catalog/flows/*.md`, но не для корня
   каталога). Почему важно: карта проекта перестала вести к
   ADR-0017/0022/0028 и BACKLOG W43 — документам, по которым принимаются
   решения.
   - *Исправлено*: пять ссылок `../../docs/…` → `../docs/…` в
     `catalog/overview.md` (2026-09-16).
4. **на будущее** — доказательство различающего прогона на черновике не
   сохранилось. `ap_get_run qgPw52VPRtxJTmy4WTisd` (журнал: «фикстура
   удалена») отвечает «Flow run not found»; записи о создании
   `w38drafttest` нет; в прогонах `events-api` черновика нет и быть не
   может — фильтр `status in (published,finished)` срезает его до вывода,
   поэтому «черновик не виден» само по себе не отличает работающий фильтр
   от случайно пустой выдачи. Состояние сейчас чистое: в `events` 7
   записей, все `published`, `w38drafttest` нет — уборка сделана.
   Собственный прогон ревьюер выполнить не смог: в тулчейне сессии нет
   `ap_run_action`, REST-запись запрещена. Код защищён двухслойно (фильтр
   шага + `visible()` в CODE) и проверен чтением; на будущее — вывод
   проверки копировать в журнал **до** удаления фикстуры
   (`docs/work/README.md`, «Доказательство переживает то, что его
   породило»).
   - *Исправлено*: выводы обоих прогонов (создание `ZxNZ9byPmG3JtNNM0BKJX`,
     удаление `qgPw52VPRtxJTmy4WTisd`) и результат `curl` дословно
     перенесены в «Как проверено» — прогоны `ap_run_action` через
     `ap_get_run` не читаются, журнал остаётся единственным следом
     (2026-09-16).
5. **на будущее** — шапка журнала говорит `- **Статус**: в работе`, тогда
   как `docs/STATUS.md` — `на проверке`. Синхронизировать при закрытии,
   чтобы индекс и журнал не расходились.
   - *Исправлено*: шапка — «на проверке» (2026-09-16).
6. **на будущее** — опечатка в Q56 (`docs/OPEN-QUESTIONS.md`):
   «**Письмо** ADR-0023 п. 3 — неточность формулировки» — по смыслу
   «Буква».
   - *Исправлено*: «Буква» (2026-09-16).

### Что проверено и чем (ревьюер)

- **Живой проект (MCP).** `ap_list_flows` — 17 флоу, `events-api`
  (`wEdKdE4RBGKIzWkl4MJHG`) ENABLED/published, `events-list` отсутствует.
  `ap_flow_structure` + `ap_read_step_code`: `events-api` — 4 шага
  (catch_webhook → tables-find-records → CODE → return_response), все
  configured; проекция `step_1` — ровно 6 полей карточки (externalId'ы
  сверены с `catalog/tables/events.md`), фильтр `status in
  (published,finished)`, `limit 50`, таблица `R4aSQpLZvw7d3u6DVOSjH`;
  `step_2` побайтово совпал с `flows/events-api.json`; `step_3` —
  `{ok, upcoming, past}`, status 200. `menu/step_4` — кнопка «Ивенты» стала
  `web_app` на `https://miniapp.events.aiqadam.org/#/events` (PRODUCTION-
  прогон `ikWQHcyte3onrflvuLaig`: Bot API 200, message_id 2198). `tg-router`
  — 19 шагов, в `step_11` 7 веток без `events_list`, `step_23` нет,
  в `step_10` нет `ev:list:*`. `ap_validate_flow`: 4/4, 6/6, 19/19 — valid.
- **Версии и `migrations`.** `ap_export_flow`: `events-api` →
  `flows[0].id ytVOqCYzA5fLsIufaAQd6`, `menu` → `JYOyGHCtsFPFu8mMRoj4u`,
  `tg-router` → `F7pxsKGZXUAqxBl29KuGq`, все `state: LOCKED` — совпали с
  `flows/_manifest.json`. `migrations` (13 строк по четырём объектам):
  `2026-09-16-w38-01` create `events-api`, `-02` publish `ytVOq…`, `-03`
  `JYOy…`, `-04` `F7px…`, `-05` delete `events-list` (`UxuFOI7GvXaboDQZHHKSI`);
  publish-строк после W38 нет, `events-list` на инстансе отсутствует.
- **Прогоны.** `vMi9FI5Pjbv8c0f1CCD49` (плоский вход — анонимное «Привет!»)
  и `M62dDF6YDjxjpM012GwRS` (обёртка `{"data":{…}}` — «Привет, Тест!») —
  гоча 13 подтверждена; `0Yth6r4tELdDo6neNgXY5` — старый колбэк
  `ev:list:upcoming` даёт `route=none` → Otherwise (`step_16`, тишина);
  `5qUrqNcfiR2Np1sg1zqTC` — `/start` → `route=menu`, ветка `menu`
  единственная совпавшая; `p8xt8jlJQMBqvfaDQtT7J` (TESTING) — деление и
  `registerLink` на боевых строках.
- **Живой `events-api` (curl, мой прогон `CCBgJXvFvedPDzg1tuwJy`).**
  `200 {"ok":true,"upcoming":[…4],"past":[…3]}`; персональных полей нет
  (только `id`, `title`, `address`, `startsAt`, `endsAt`, `status`,
  `registerLink`); `registerLink = https://t.me/aiqadam_events_dev_bot?start=e<id>`
  (BOT_USERNAME подставляет сервер, тело запроса не читается); обе вкладки
  отсортированы по `startsAt` по возрастанию; деление по
  `endsAt \|\| startsAt`.
- **Офлайн.** `check-export-secrets.sh` — 0 (21 `auth`-ссылка, значений
  нет); `check-texts.py` — 17 флоу / 114 пар / 0; `check-commands.py` —
  0 нарушений; `npm run build` — tsc+vite чисто, `Events` отдельным
  ленивым чанком 3,69 КиБ (1,41 gzip); grep по `miniapp/src`: литералов
  `BOT_USERNAME`/`aiqadam_events` нет, `innerHTML`/`dangerouslySetInnerHTML`
  нет, литералов цвета/шрифта в `Events.tsx`/`App.tsx` нет; `parseHash`
  разбирает `#/events?tab=past`; `events-list` не остался ни в `flows/`,
  ни в `catalog/`, ни в `ru.json`. Q56: `finished` продуктовыми путями
  достижим только из `published` (`manage-api` его формой не выставляет),
  `draft`/`cancelled` не проходят ни фильтр, ни постфильтр — уточнение
  ADR-0023 дыры не создаёт, постфильтр `visible()` на месте.
- **Гоча 14** — проверена косвенно: все три экспорта сейчас `LOCKED`,
  версии совпали с манифестом; сам эпизод «экспорт отдал DRAFT до
  републикации» не воспроизводился (потребовал бы тестового прогона и
  черновик-версии).

### Не удалось воспроизвести

- `tools/check-migrations.py`: exit 2 — ключа `QADAM_API_KEY` в сессии нет
  (fail-closed, как и ожидалось); сверка манифест ↔ инстанс ↔ `migrations`
  сделана вручную через MCP (выше), расхождений нет.
- Собственный различающий прогон с черновиком — см. замечание 4
  (`ap_run_action` в тулчейне нет; REST-запись запрещена).
- Headless-браузером SPA не перегонял: тулчейн в `~/w41` этой сессии
  недоступен по правам окружения; журнальные скриншоты (`~/w41/shots-w38`)
  не смотрел по той же причине. Проверены сборка, код, разметка и живой
  ответ API.
- Живого `finished`-ивента нет (W12 не построен, в таблице 0 `finished`) —
  ветка Q56 проверена чтением фильтра/постфильтра, не прогоном.

## Хвосты и блокеры

- **После мержа — живая проверка цепочки** «меню → кнопка „Ивенты“ → каталог
  на Pages» и «карточка → тап → карточка регистрации в `reg-start`»: агент
  не может нажать кнопку в реальном клиенте, обе половины доказаны
  по отдельности (см. «Как проверено» и «Границу доказательства» выше).
  Новая карточка меню уже лежит в чате владельца (прогон
  `ikWQHcyte3onrflvuLaig`) — до деплоя Pages её кнопка ведёт на старую сборку
  без `#/events`.
- **`events-api` в v0.1 отдаёт лимит 50** (Q15) — на боевом масштабе
  срез режется по `starts_at`; курсор/пагинация каталога не строились
  осознанно (не входит в W38).
- **Имя владельца в `users` перезаписано тестовым** («Тест») прогонами
  `tg-router`; вернётся при следующем живом сообщении бота.

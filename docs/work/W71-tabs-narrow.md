# W71. Phase 2: вкладки в узком окне без горизонтальной прокрутки

- **Статус**: готов
- **Владелец**: агент
- **Волна**: P2 ([#123](https://github.com/aiqadam/aiqadam-events-bot/issues/123))
- **Зависит от**: —
- **Начат**: 2026-09-24 · **Закрыт**: 2026-09-24

## Цель

В узком окне Telegram Desktop (~380px) последняя вкладка обрезается, табы едут
вбок. Уместить их без горизонтальной прокрутки (ширину окна Mini App задать
нельзя). Раскладка — `index.css` + эталон `prototypes/proto.css`; при нужде —
короткая подпись в `i18n/ru.json`.

## Что построено

| Артефакт | ID / имя | Каталог |
|----------|----------|---------|
| `miniapp/src/index.css` | — | — |
| `prototypes/proto.css` | — | — |

## Чек-лист готовности

- [x] При 360px все табы каталога и правки видны без горизонтальной прокрутки
- [x] Эталон `prototypes/proto.css` обновлён первым (ADR-0027)
- [x] `overflow-x: auto` оставлен фолбэком для очень узких окон
- [x] `check-texts`/`check-commands`/`prototypes/check.mjs` зелёные
- [x] `catalog/` совпадает с живым проектом (флоу не меняются)

## Как проверено

- **Headless-стенд** `~/w41/w71-shots.mjs` (статика `dist` + мок Telegram.WebApp
  и вебхуков), ширины 360 и 380px: **14/14** — ни у каталога (`#events-tabs`),
  ни у правки (`#manage-tabs`) нет переполнения страницы, контейнер без
  горизонтальной прокрутки, все 4 таба видны целиком; **фильтр участников
  `#parts-filter` не накладывается** (остаётся со скроллом, как до W71).
  Скриншоты `~/w41/shots-w71/{events,manage,parts}-{360,380}.png`.
- Короткая подпись («Билеты») не понадобилась: «Мои билеты» и «Контролёры»
  влезают при 360px — `i18n/ru.json` не менялся.
- `npm run build` (tsc + vite) — зелёный; `prototypes/check.mjs` OK; офлайн:
  секреты, тексты 254/0, команды 0, агенты 0.
- Флоу не менялись → публикаций/`migrations` нет.

## Журнал

- **2026-09-24** — взятие пакета, ветка `w71-tabs-narrow`.
- **2026-09-24** — сначала правил эталон `prototypes/proto.css` (ADR-0027), затем
  `miniapp/src/index.css`; блок `@media (max-width: 400px)` сжимает табы
  (`flex: 1 1 0; min-width: 0; padding: 0 6px`), для админских снимает
  `min-width: max-content` у `.tabs-scroll .tabs`. `overflow-x: auto` из W51/W53
  оставлен фолбэком для очень узких окон — это пересмотр решения W51/W53
  («однострочный скролл»), что и требовал issue.
- **2026-09-24** — проверил 360px: «Мои билеты» (каталог) и «Контролёры»
  (правка) влезают, ellipsis/перекрытия нет — шаг 3 (короткая подпись в
  `ru.json`) не понадобился.
- **2026-09-24** — **ревью круг 1: одно «важно»** — универсальный `.tabs .tab`
  задел **фильтр участников `#parts-filter`** (в продукте это `.tabs` со
  счётчиками `.count`; в эталоне фильтр — `.segmented`): на 360/380px подписи
  наложились. Сузил селектор до `.tabs:not(#parts-filter) .tab` в обоих файлах
  (в эталоне `:not` — no-op, там фильтра-`.tabs` нет); фильтр вернулся к скроллу,
  как до W71. Стенд расширен проверкой `#parts-filter` на наложения — 14/14.
  «На будущее»: статус журнала приведён к «на проверке»; шаг 2 issue (ellipsis)
  закрыт выбором (а) — целевым табам ellipsis не нужен.
- **2026-09-24** — **ревью круг 2: «замечаний нет».** Наблюдения ревьюера учтены:
  стенд усилен — в проверке `#parts-filter` добавлено утверждение «текст не сжат»
  (`scrollWidth <= clientWidth` у каждого `.tab`), это реальный страж регресса
  круга 1 (проверка наложений сама по себе его не отличала) — 16/16; тело PR #164
  обновлено под фактическое состояние (14/16, фильтр участников).

## Ревью

- **Ревьюер**: независимый ревьюер (open-code, `deepseek-v4.1-flash`) · **Дата**: 2026-09-24 · **Вердикт**: есть замечания

### Проверено

- **Эталон (ADR-0027)**: блок `@media (max-width: 400px)` в `prototypes/proto.css`
  и `miniapp/src/index.css` **побайтово идентичен** (сверено скриптом). Прототип,
  пересобранный как LAN-копия (`prototypes/` + `vendor/` из
  `miniapp/src/vendor/brand/` + `i18n/ru.json`), на 360 и 380px: вкладки каталога
  и правки в одну строку, без переполнения и наложений; `node prototypes/check.mjs`
  — OK.
- **Стенд**: `node ~/w41/w71-shots.mjs` перепрогнан — **12/12**, console-ошибок 0.
  Скриншоты `events-360.png`/`manage-360.png`: подписи не обрезаны, наложений нет.
- **Широкий экран**: при 500px правило не применяется — computed `flex: 0 0 auto`,
  ширины вкладок разные (114/94/115/93 px). Требование «не сжимать на ≥400px» выполнено.
- **Сборка и офлайн**: `miniapp npm run build` (tsc + vite) — зелёный;
  `check-export-secrets` 0; `check-texts` 254/0; `check-commands` 0;
  `check-agents` 0; `prototypes/check.mjs` OK.
- **Диф**: `git diff main...w71-tabs-narrow` — только `docs/STATUS.md`,
  `docs/work/W71-tabs-narrow.md`, `miniapp/src/index.css`, `prototypes/proto.css`.
  `flows/`, `catalog/`, `i18n/ru.json` не тронуты; блок AppSec/флоу к пакету
  «только раскладка» не относится.
- **Различающий прогон по счётчикам** (пункт 7 задачи): фильтр участников
  `#parts-filter` — это тоже `.tabs`, но с `<span class="count">`; на 360/380px
  универсальное правило ломает его (подробно — замечание 1).

### Замечания

1. **важно** — Правило `@media (max-width: 400px) .tabs .tab { flex: 1 1 0; min-width: 0; padding: 0 6px; justify-content: center }` задевает не только табы каталога (`#events-tabs`) и правки (`#manage-tabs`), но и **фильтр участников `#parts-filter`** (`miniapp/src/routes/Manage.tsx:1873`) — это голый `.tabs`, а его `.tab` содержит счётчик `<span class="count">` (`Manage.tsx:1892`). Замер на стенде (360px): четыре вкладки сжимаются до 69.5px, содержимое шире (52.7 / 83.8 / 104.4 / 117.2px), `white-space: nowrap` + `min-width: 0` не дают ему влезть → подписи **накладываются** («Пришли 3 / Не пришли 0», «Не пришли 0 / Отменившие 1»), плюс остаётся 24px горизонтальной прокрутки; на 380px то же (наложение + 21px). Это **регрессия**: до W71 та же строка просто скроллилась (208px, наложений нет) — то есть W51/W53 отрабатывали нормально, а новый универсальный селектор сломал строку ровно на той ширине, ради которой пакет и делается. Проверка пакета (`~/w41/w71-shots.mjs`) тестирует только `#events-tabs` и `#manage-tabs`, поэтому дефект не был пойман. Скриншот «до/после»: `w71-parts-360.png` (наложение) против `w71-parts-pre.png` (скролл). Варианты правки: (а) сузить селектор до двух целевых строк (свой класс/`id`), оставив `#parts-filter` со скроллом; (б) привести `#parts-filter` к эталону прототипа — там фильтр участников это `.segmented`, а не `.tabs` (`prototypes/app.js:962`), и он не сжимается; (в) `overflow: hidden; text-overflow: ellipsis` + `flex-shrink: 0` для `.tab .count` (шаг 2 issue #123, пропущен) — но это лишь замаскирует наложение, подписи «Не пришли»/«Отменившие» всё равно будут обрезаны.   Рекомендую (а) или (б).
   - *Исправлено*: выбран (а) — селектор сужен до `.tabs:not(#parts-filter) .tab`
     в `proto.css` и `index.css`; `#parts-filter` со счётчиками остаётся со
     скроллом (как до W71), стенд проверяет отсутствие наложений — 14/14.
     (2026-09-24)

2. **на будущее** — Гигиена журнала: шапка журнала `Статус: в работе` (строка 3) расходится с [STATUS.md](../STATUS.md), где пакет уже «**на проверке**». Такое же замечание по W10 (круг 1) помечалось    «на будущее». Поправить при ответе на ревью, чтобы статус был один.
   - *Исправлено*: шапка журнала — «на проверке». (2026-09-24)

3. **на будущее** — Issue #123, шаг 2 просил добавить `overflow: hidden; text-overflow: ellipsis` в подписи табов; в реализации этого нет. Для двух целевых строк оно не нужно (все четыре подписи влезают), но именно поэтому всплыло наложение в фильтре участников (замечание 1). При выборе варианта (а)/(б)    этот пункт закрывается; при варианте (в) — станет обязательным.
   - *Исправлено*: закрыт выбором (а) — целевым табам ellipsis не нужен, а
     `#parts-filter` исключён из сжатия. (2026-09-24)

### Круг 2

- **Ревьюер**: независимый ревьюер (open-code, `deepseek-v4.1-flash`) · **Дата**: 2026-09-24 · **Вердикт**: замечаний нет

Круг 1 закрыт:

1. **важно (фильтр участников) — закрыто.** Коммит `386fa99` сузил селектор до
   `.tabs:not(#parts-filter) .tab` в `prototypes/proto.css` и
   `miniapp/src/index.css`. Перепрогнал стенд `~/w41/w71-shots.mjs` —
   **14/14**, console-ошибок 0: `#events-tabs` и `#manage-tabs` на 360/380px
   без переполнения страницы и без прокрутки контейнера, все 4 таба видны
   целиком; `#parts-filter` без наложений. Скриншот
   `~/w41/shots-w71/parts-360.png` — подписи не накладываются, строка со
   счётчиками уходит в горизонтальный скролл, как до W71. Дополнительно
   проверил сигнал, которого проверка стенда не видит (см. наблюдение):
   с текущим CSS `scrollWidth == clientWidth` у каждого `.tab`
   `#parts-filter` (текст помещается в кнопку), а с прежним универсальным
   правилом `scrollWidth > clientWidth` (кнопки сжимаются до равных долей,
   текст вылезает за них) — то есть регрессии больше нет.
2. **на будущее (статус журнала) — закрыто.** Шапка журнала — «на проверке»,
   совпадает с `docs/STATUS.md`.
3. **на будущее (шаг 2 issue) — закрыто выбором (а).** `overflow: hidden;
   text-overflow: ellipsis` не добавлен: двум целевым строкам он не нужен
   (все подписи влезают), а наложение в `#parts-filter` снято исключением из
   сжатия. Issue ставит ellipsis/шаг 3 условием «если подпись получит
   ellipsis или наложится» — при (а) это условие не наступает.

Обязательная перепроверка (независимо):

- **Эталон ↔ продукт**: блок `@media (max-width: 400px)` в
  `prototypes/proto.css` и `miniapp/src/index.css` — правила **идентичны**
  (сверено скриптом, побайтово после снятия комментариев); различаются только
  комментарии, что ожидаемо — в эталоне `#parts-filter` нет, `:not` там no-op;
- **широкий экран**: при 401px и 500px правило **не применяется** — computed
  `flex: 0 0 auto`, `.tabs-scroll .tabs` `min-width: max-content`; граница
  `max-width: 400px` включающая, ровно на 400px правило ещё действует (это не
  дефект, но формулировку «на ≥400px не применяется» стоит читать как «>400px»);
- **другие `.tabs` в продукте**: ровно три — `#events-tabs`
  (`Events.tsx:439`), `.tabs` внутри `#manage-tabs .tabs-scroll`
  (`Manage.tsx:1333`) и `#parts-filter` (`Manage.tsx:1873`); правка касается
  первых двух и не касается третьего, иных нет;
- **сборка и офлайн**: `cd miniapp && npm run build` (tsc + vite) — зелёный;
  `node prototypes/check.mjs` — OK; `tools/check-export-secrets.sh` rc=0;
  `tools/check-texts.py i18n/ru.json flows/*.json` — 254/0;
  `tools/check-commands.py i18n/*.json flows/*.json` — 0;
  `tools/check-agents.py` — 0;
- **диф**: `git diff main...w71-tabs-narrow` — только `docs/STATUS.md`,
  `docs/work/W71-tabs-narrow.md`, `miniapp/src/index.css`,
  `prototypes/proto.css`; `flows/`, `catalog/`, `i18n/ru.json` не тронуты.
  PR #164 открыт, ветка `w71-tabs-narrow` → `main`, 3 коммита;
- **AppSec / MCP / флоу — не относится**: пакет «только раскладка», флоу,
  таблицы, переменные и `i18n/ru.json` не менялись.

Наблюдение (не замечание, не блокирует): проверка наложений в
`~/w41/w71-shots.mjs` считает пересечения прямоугольников самих `.tab`. При
`flex: 1 1 0` кнопки всегда укладываются вплотную (зазор 0.5px), поэтому
`overlaps` равен 0 и до исправления, и после — проверка **не отличила бы**
регрессию круга 1 (проверил подстановкой прежнего универсального правила в
ту же страницу: `overlaps=0` в обоих случаях; переполнение видно только по
`scrollWidth > clientWidth`). Сам фикс верен — это подтверждено моим замером
и скриншотом; речь о силе проверки, а не о дефекте. Чтобы стенд стал
регресс-стражем, ему стоит утверждать `scrollWidth <= clientWidth` для каждого
`.tab` в `#parts-filter`. Отдельно: тело PR #164 всё ещё описывает состояние
до круга 1 («12/12», `#parts-filter` не упомянут) — обновить перед мержем
(метаданные PR, не файл репозитория).

## Хвосты и блокеры

- —

# W18. Страховка от fail-open дефектов платформы (#382, #392)

- **Статус**: на проверке
- **Владелец**: агент W18
- **Волна**: 5
- **Зависит от**: W8 (готов), W16 (готов). Не зависит от W17
- **Начат**: 2026-09-13 · **Закрыт**: —

## Цель

Закрыть своими силами два дефекта платформы, отказывающие в сторону **выдачи
доступа** ([Q25](../OPEN-QUESTIONS.md#q25)): постфильтр прав контролёра в
`checkin-api` и аудит коротких ссылок `{{VAR}}` на переменные проекта.

## Что построено

Ни одного нового флоу и ни одной новой таблицы. Изменён **один шаг**.

| Артефакт | ID / имя | Каталог |
|----------|----------|---------|
| flow `checkin-api`, шаг `step_6` (CODE) | `CUKqiby1PoHiQiiCQy24V` | [catalog/flows/checkin-api.md](../../catalog/flows/checkin-api.md#изменения-w18-2026-09-13) |
| аудит `{{VAR}}` (запись результата) | — | [catalog/variables.md](../../catalog/variables.md#аудит-короткой-формы-var-w18-2026-09-13) |
| пункты чек-листа ревьюера | — | [REVIEW-CHECKLIST.md](REVIEW-CHECKLIST.md) блоки 4.1 и 4.2 |

## Чек-лист готовности

- [x] `step_6` проверяет совпадение по полю, а не только непустоту выдачи
- [x] три различающих прогона STF-2 (не-стафф, стафф чужого `event_id`,
      отозванный стафф — все `403`) и позитивный контроль повторены на
      **опубликованной** версии после правки
- [x] отдельный дифференцирующий прогон: с намеренно испорченной формой фильтра
      `step_5` результат — `403`, а не `200`
- [x] `catalog/flows/checkin-api.md` больше не утверждает отсутствующую гарантию
      и ссылается на Q25
- [x] аудит `{{VAR}}` выполнен, результат записан в журнал пакета
- [x] пункт добавлен в REVIEW-CHECKLIST.md
- [x] `catalog/` совпадает с живым проектом
- [ ] независимое ревью, вердикт «замечаний нет»

## Как проверено

Все прогоны — **после** `ap_lock_and_publish`, на опубликованной версии.

### Правка `step_6`

Было `isStaff = records.length > 0`. Стало: среди выдачи `step_5` ищется строка
с `event_id` **запроса** (`step_1`), `telegram_id` **контролёра** (`step_3` —
только из проверенного `initData`, не из тела запроса) и пустым `revoked_at`.
Ключи невалидны или пусты — отказ, а не выдача. Наружу добавлены
`rowsRead`/`rowsMatched`.

Логика прогнана локально на `node` против **настоящей** формы записи
(`cells[fieldId] = {fieldName, value}`, снята с прогона `U2xEV1LL5mu4kY8uJyyTN`)
до заливки на инстанс: восемь случаев, включая `revoked_at: null` против `""`,
сентинел `-` и пустые ключи против пустых ячеек.

### Дифференцирующий прогон — то, ради чего сделан пакет

Три прогона STF-2 и позитивный контроль проходят **и на старом коде**, поэтому
сами по себе не доказывают ничего. Настоящий тест — воспроизвести сценарий
#382: шаг решения получает **всю** таблицу.

Как сделано, не трогая `step_5`: во входе `step_6` `records` временно заменён
литеральным массивом из **всех пяти** строк `event_staff` (2 боевые + 3
фикстуры), флоу опубликован, прогоны сделаны, вход возвращён на
`{{step_5['output']}}`, флоу опубликован снова.

Почему не правился сам `step_5`: MCP не отдаёт `settings.input` PIECE-шагов
([#102](https://github.com/aiqadam/qadam-flow/issues/102)), а REST владельцу
пакета запрещён — испортив фильтр, восстановить его было бы нечем. Литеральный
вход у CODE-шага полностью обратим, потому что его прежнее значение известно.

| Сценарий (у `step_6` на входе 5 строк) | Прогон | Итог |
|---|---|---|
| не контролёр вовсе (`700000774`) | `OifLDzW11SoSVAXP283Wf` | `403`, `rowsRead: 5`, `rowsMatched: 0` |
| контролёр чужого ивента (`700000772`/`othr09`) | `EYGUsoCzQ1E0cNaTbC7YY` | `403` |
| отозванный контролёр `demo` (`700000773`) | `IkGi3yYXxpUEiUm7TTUw7` | `403` |
| настоящий контролёр `demo` (`700000771`) | `N4NvnjwiJ3hC3583oOtTM` | `200 already` |

На старом коде все четыре дали бы `isStaff: true`. В `OifLDzW11SoSVAXP283Wf`
видно, что настоящий `step_5` при этом вернул `[]` — фильтр чтения исправен,
отказ дал именно постфильтр. Пара «не-контролёр `403` / контролёр `200`» на
**одном и том же** входе — и есть доказательство: шаг различает строки внутри
fail-open выдачи, а не отвергает её целиком.

### Прогоны на восстановленной опубликованной версии

| Сценарий | Прогон | Итог |
|---|---|---|
| контролёр `demo` — позитивный контроль | `K99jXM1Rw26sMAvL3u4Jg` | `200 already`, `rowsRead: 1`, `rowsMatched: 1` |
| контролёр чужого ивента | `8Sv8L1DbBHdiolCFBKQRL` | `403` |
| отозванный контролёр | `zJiJ44xd6dlBidx2ZaRel` | `403` |
| не контролёр вовсе | `6oewgEqHz4qe9gRChFbFp` | `403` |
| битый `hash` в `initData` | `ntYEJ8ksWPOi3IglAe06g` | `401 bad_hash` |

`initData` подписан **настоящим** `BOT_TOKEN` приёмом W8: шагу даётся заведомо
неверный `hash`, эталон забирается из вывода `step_35` (прогоны
`XIS72g7wUwKHEmWMhc9QW`, `GoAG2VLeGSX3U5ZcdLxyX`, `G6tskX6nRf9DDSqRmK0N9`,
`x62paYdEYO00txIziwN1m`) и подставляется обратно. Своей крипты не писалось,
значение токена нигде не сохранялось.

**Фикстуры.** Три строки `event_staff` (`700000771` staff `demo`, `700000772`
staff `othr09`, `700000773` staff `demo` с `revoked_at`), `700000774` строки не
имел вовсе. Удалены сразу после прогонов, проверено `ap_find_records`:
`event_staff` снова 2 строки, ровно те же, что были до пакета.

**IDM-2 не задет.** Позитивный контроль дошёл до `already` и показал
**исходное** время `2026-09-12T22:41:53.313Z`, а не новое. Единственная строка
`registrations` до и после всех девяти прогонов идентична.

### Аудит `{{VAR}}` — результат

**Ни одного вхождения короткой формы во всех пяти флоу.** Инцидента нет,
`QR_SIGNING_KEY` не ротировался.

| Флоу | `template_reference` | Короткая форма |
|------|----------------------|----------------|
| `checkin-api` | `step_35`, `step_38` — `{{variables...}}` | нет |
| `my-qr-api` | `step_20`, `step_24` — `{{variables...}}` | нет |
| `registration` | `step_13`, `step_18`, `step_72`, `step_75`, `step_96` — `{{variables...}}` | нет |
| `tg-router` | нет вовсе | нет |
| `i18n-sync` | нет вовсе | нет |

Девять потребителей совпали с таблицей «Кто их читает» в
[catalog/variables.md](../../catalog/variables.md) строка в строку.

## Журнал

- **2026-09-13** — пакет взят, ветка `w18-fail-open-mitigation`.
- **2026-09-13** — перед правкой снята настоящая форма записи `event_staff` из
  прогона `U2xEV1LL5mu4kY8uJyyTN`. Это сэкономило круг: `revoked_at` у одной
  боевой строки приходит как `null`, у другой — как `""`, и постфильтр, который
  сравнивал бы с `''` без нормализации, пропустил бы одну из них.
- **2026-09-13** — `ap_update_step` **сливает** `input`, а не заменяет. Поэтому
  мёртвый ключ `staffUserRecords: null`, оставшийся от удалённого в W24 чтения
  `users`, убрать через MCP нечем — он остался во входе `step_6`. На исполнение
  не влияет (шаг его не читает), записан хвостом ниже.
- **2026-09-13** — **главное решение пакета: как доказать правку.** Первой мыслью
  было испортить фильтр `step_5`. Отброшено: прочитать его текущую конфигурацию
  владелец пакета не может (#102 + запрет REST), то есть откатить порчу было бы
  нечем. Второй вариант — временно вернуть старый код `step_6` для контраста —
  отброшен тоже: это значило бы на несколько минут опубликовать заведомо дырявую
  авторизацию. Выбран третий: литеральный вход `records` у CODE-шага. Он обратим
  (прежнее значение известно дословно), не трогает шаги `tables` (которые,
  к слову, и не редактируются — [#411](https://github.com/aiqadam/qadam-flow/issues/411)),
  и даёт **пару** прогонов на одном входе вместо одного.
- **2026-09-13** — аудит `{{VAR}}`: BACKLOG предполагал, что PIECE-шаги придётся
  смотреть через read-only REST ревьюера. Перед тем как просить ключ, проверено
  пробным флоу `tmp-w18-validator-probe` (CODE-вход `{{QR_SIGNING_KEY}}` +
  `http`-шаг с тем же в URL), **работает ли `ap_validate_flow` как детектор**.
  Работает, и на PIECE-шагах тоже: обе строки вышли как
  `references "{{QR_SIGNING_KEY...}}"`. REST не понадобился вовсе, флоу удалён.
  Это и попало в чек-лист ревьюера как способ проверки.
- **2026-09-13** — в блок 4.1 чек-листа добавлен **второй** пункт, которого не
  было в BACKLOG: «решение о правах — по полям записи, а не по непустоте выдачи».
  Сделано сознательно: без него инвариант, построенный этим пакетом, не охраняется
  ничем, и следующий флоу с проверкой прав повторит ту же ошибку. Если ревьюер
  сочтёт это расширением скоупа — пункт легко снять, он изолирован.
- **2026-09-13** — все пункты чек-листа, кроме ревью, выполнены; пакет переведён
  `на проверке`.

## Ревью

- **Ревьюер**: независимый агент-ревьюер (чистый контекст) · **Дата**: 2026-09-13 ·
  **Вердикт**: **есть замечания** (блокеров нет; один пункт «важно» — в документе,
  не в флоу)

Суть пакета подтверждена независимо: постфильтр `checkin-api/step_6` работает,
вход восстановлен, аудит `{{VAR}}` воспроизведён и **дополнительно** проверен по
опубликованным версиям всех пяти флоу. Замечания касаются формулировок в
чек-листе и каталоге, одного остаточного сентинела в коде и второй копии
устаревшего утверждения в `OPEN-QUESTIONS.md`.

### Замечания

1. **важно** — новый пункт блока 4.1 в
   [REVIEW-CHECKLIST.md](REVIEW-CHECKLIST.md) предписывает **ревьюеру**
   «проверять различающим прогоном: шагу решения скармливается вся таблица».
   Это противоречит двум правилам того же документа: ревьюер ничего не меняет
   в проекте, а любая такая проверка требует подменить вход шага и
   **опубликовать** флоу с поддельной авторизацией. Обязанность доказать
   правку различающим прогоном лежит на **владельце** (как и сделано в этом
   пакете); ревьюер этот прогон **читает** и оценивает, различал ли он старый
   код от нового. Формулировку надо развернуть именно так, иначе следующий
   ревьюер полезет править живой проект.

2. **на будущее** — утверждение «`ap_validate_flow` ловит короткую форму, поэтому
   read-only REST для этого не нужен»
   ([REVIEW-CHECKLIST.md](REVIEW-CHECKLIST.md) блок 4.2 и
   [catalog/variables.md](../../catalog/variables.md#аудит-короткой-формы-var-w18-2026-09-13))
   верно по механизму, но шире, чем доказано. Валидатор сообщает, что у шага
   **есть** ссылка не на шаг, и печатает её **усечённой**; проба владельца
   состояла из двух шагов с **одной** ссылкой в каждом. Случай «в одном шаге
   и длинная форма, и короткая» не проверен — при нём короткая может остаться
   невидимой за уже напечатанной `{{variables...}}`. Сегодня это ни на что не
   влияет (см. «Что проверено ревьюером», п. 4: во всех девяти шагах-потребителях
   ровно по одной ссылке), но правило написано на будущее. Предложение: оставить
   `ap_validate_flow` как самопроверку владельца, а ревьюеру назвать штатным
   инструментом экспорт — он отдаёт **опубликованную** версию, все ссылки
   целиком и с именами, то есть заодно снимает оговорку про draft.

3. **на будущее** — fail-closed в `checkin-api/step_6` отсекает **пустой** ключ,
   но не сентинел. `step_3` отдаёт `staffTelegramId = String(data.telegramId || '-')`,
   то есть `-` возможен и при `initDataValid: true` — если проверенный `initData`
   не содержит `user.id`. Тогда `keysOk` истинно, а `wantTelegramId === '-'`, и
   строка `event_staff` с `telegram_id = '-'` совпала бы. Сегодня такой строки
   нет и завести её может только owner, но контракт шага заявлен как «без
   валидных ключей — отказ», а сентинел валидным ключом не является. Дешёвая
   правка: добавить `!== '-'` в `keysOk` (для `eventId` это уже гарантировано
   регуляркой `step_1`).

4. **на будущее** — вторая копия устаревшего утверждения внутри
   [OPEN-QUESTIONS.md](../OPEN-QUESTIONS.md#q25): абзац «Частично смягчён
   2026-09-13 (W22)» по-прежнему говорит в настоящем времени, что
   «`checkin-api/step_6` (`isStaff = records.length > 0`) по-прежнему без
   постфильтра». Приписка «**Сделано 2026-09-13 (W18)**» ниже это чинит только
   для того, кто дочитал. Ровно тот случай из
   [work/README.md](README.md#утверждение-о-состоянии-живёт-больше-чем-в-одном-месте):
   вторая формулировка в том же файле. (Копия в
   `docs/work/W22-latency-and-cleanup.md:203` — журнал, историческая запись,
   её трогать не надо.)

5. **на будущее** — в блок-цитате «Проверка W19, 2026-09-12» внутри Q25 стоит
   «Когда W18 будет сделан, сверку надо повторить по его журналу». W18 сделан,
   журнал постфильтр не отменяет — но факт сверки нигде не записан, и пункт
   остался висеть как незакрытое действие. Достаточно одной строки.

### Что проверено ревьюером (и чем)

Ничего не чинилось; в проекте создавались и удалялись только собственные
фикстуры ревьюера.

1. **Живой код и вход `step_6`** (`ap_read_step_code`): `records` =
   `{{step_5['output']}}`, литерального массива нет. Мёртвый ключ
   `staffUserRecords: null` присутствует и **безвреден** — код `inputs.staffUserRecords`
   не читает, значение `null` не резолвится как шаблон. Подтверждено и в
   опубликованной версии.
2. **Опубликованная версия против draft** (read-only REST, ADR-0006; ключ нашёлся
   в Keychain): `GET /flows/<id>/template` по всем пяти флоу. В published
   `step_6` тот же код (2874 байта) и тот же вход; `step_5` несёт полный фильтр
   `event_id eq` + `telegram_id eq` + `revoked_at not_exists` — то, чего MCP не
   отдаёт и чего владелец проверить не мог. Секретов в экспортах нет: токен —
   `{{connections['TZTlXaCEO2hEvimUowbSA']}}`, литеральных значений не найдено.
3. **Собственные прогоны STF-2 на восстановленной опубликованной версии.**
   Свои фикстуры `event_staff` (`700000902` staff `othr09`; `700000903` staff
   `demo` с `revoked_at`; `700000904` staff `demo`, `revoked_at = ""`),
   `initData` подписан приёмом W8 через `step_35` (прогоны-затравки
   `kdcIdbV9WCHWGiGLx0W4b`, `0HoqLcDXy5w3sswhEP0ln`, `UPt6DO9G7t0Y1qsiQ1g79`,
   `r23vwUjJ0IKQ5sSjmy5Sq`, `7VSaD0RqryEn0LhjW6APY`, `vteHcpfHg17f8JeQaPkgt`):

   | Сценарий | Прогон | Итог |
   |---|---|---|
   | **позитив**: настоящий staff `demo`, `revoked_at: null` (`322876545`) | `UFE9TejI2Q0nfkM1S5xdb` | `200 already`, исходное время |
   | **позитив**: настоящий staff `demo`, `revoked_at: ""` (`8255904812`) | `MsY2MwJePmmQfQE03iwAO` | `200 already` |
   | не контролёр вовсе (`700000901`) | `iB6AT14GSSArYlB7LS6eS` | `403` |
   | контролёр чужого ивента (`700000902`) | `QZKwKINqTKm6FyJa8h046` | `403`, `rowsRead: 0`, `rowsMatched: 0` |
   | отозванный контролёр (`700000903`) | `l55xVcGdwIDFsh235Zynw` | `403` |
   | **позитив**: своя фикстура staff `demo`, `revoked_at: ""` (`700000904`) | `562pcs7p16tN0LEJODbfi` | `200 already` |
   | staff `demo` просит `eventId = othr09` (IDOR) | `bxgK4Z0wzXybeyxalgc1t` | `403` |

   То есть обе формы «активных прав» (`null` и `""`) проходят, непустая дата
   отзыва — нет, и права не переносятся на чужой `event_id` ни в одну сторону.
4. **Дифференцирующий прогон владельца — метод признан годным.**
   `OifLDzW11SoSVAXP283Wf` прочитан целиком: `step_5` вернул `[]`, а `step_6`
   получил `rowsRead: 5` (литеральный вход) и отдал `403`;
   `N4NvnjwiJ3hC3583oOtTM` на **том же** входе `rowsRead: 5` дал
   `rowsMatched: 1` и `200`. Пара на одном входе действительно различает новый
   код и старый — на старом оба получили бы `isStaff: true`. Замена «портить
   `step_5`» на «литеральный вход `step_6`» обоснована и обратима, претензий нет.
   `K99jXM1Rw26sMAvL3u4Jg` также прочитан и соответствует журналу
   (`rowsRead: 1`, `rowsMatched: 1`, исходное время чекина). Остальные пять
   прогонов журнала поштучно не открывались — их утверждения перекрыты
   собственными прогонами ревьюера.
5. **IDM-2 и побочные эффекты.** `registrations` прочитана до и после всех
   тринадцати прогонов ревьюера — одна строка, `checked_in_at` =
   `2026-09-12T22:41:53.313Z`, `checked_in_by` = `8255904812`, без изменений.
   Все три фикстуры `event_staff` удалены, повторное чтение даёт прежние
   2 строки (`322876545`, `8255904812`).
6. **Аудит `{{VAR}}` — воспроизведён и усилен.** `ap_validate_flow` по всем пяти
   флоу: `checkin-api` — `step_35`, `step_38`; `my-qr-api` — `step_20`, `step_24`;
   `registration` — `step_13`, `step_18`, `step_72`, `step_75`, `step_96`;
   `tg-router` и `i18n-sync` — пусто. Все девять — `{{variables...}}`, короткой
   формы нет: снимок владельца совпал строка в строку. Сверх того ревьюер
   разобрал **опубликованные** экспорты всех пяти флоу и пересчитал ссылки
   по шагам: те же девять шагов, ровно по одной ссылке в каждом, все
   `{{variables['…']}}`, ни одного `{{VAR}}`. Оговорка «валидатор смотрит draft»
   тем самым снята для сегодняшнего состояния. Заодно подтверждено, что
   валидатор действительно видит **PIECE**-шаги: `registration/step_18` и
   `step_75` — PIECE, и он их пометил. Инцидента нет, ротация не нужна.
7. **Каталог против реальности.** `ap_flow_structure` по `checkin-api`: 43 шага,
   `invalid` нет, заглушек нет; описание `step_6` в
   [catalog/flows/checkin-api.md](../../catalog/flows/checkin-api.md) совпадает
   с живым шагом, ложной гарантии больше не утверждает и ссылается на Q25.
   `catalog/variables.md`, `docs/STATUS.md` согласованы. Расхождение
   `displayName` в таблице шагов (`«текст 403 (ru)»` против живого
   `«текст 403 (ru, ADR-0014)»` и т.п.) — досталось от прошлых пакетов, к W18
   отношения не имеет.
8. **Расширение скоупа (второй пункт в 4.1) — оправдано.** BACKLOG требовал
   пункт про `{{VAR}}`; пункт про «права по полям записи» охраняет инвариант,
   который этот пакет и построил, и без него следующий флоу с проверкой прав
   повторит ошибку. Оставить — но переписать по замечанию 1.

**Не проверено:** поштучно не открывались прогоны `EYGUsoCzQ1E0cNaTbC7YY`,
`IkGi3yYXxpUEiUm7TTUw7`, `8Sv8L1DbBHdiolCFBKQRL`, `zJiJ44xd6dlBidx2ZaRel`,
`6oewgEqHz4qe9gRChFbFp`, `ntYEJ8ksWPOi3IglAe06g` — те же сценарии перекрыты
собственными прогонами ревьюера (п. 3). Не воспроизводилось поведение
`ap_validate_flow` на шаге с двумя ссылками сразу — это и есть замечание 2.

### Что стоит проверить в первую очередь

Не указание ревьюеру, а список мест, где владелец сам видит наибольший риск:

1. **Постфильтр — не заузил ли он права.** Позитивный контроль в пакете один
   (`700000771`). Стоит завести свою фикстуру и убедиться независимо, что
   настоящий контролёр проходит, — а заодно что `revoked_at`, пришедший как
   `null`, считается активными правами, а как непустая дата — отозванными.
2. **Восстановление входа `step_6`.** После дифференцирующих прогонов `records`
   возвращён на `{{step_5['output']}}` и флоу опубликован заново. Проверяется
   `ap_read_step_code` по живому проекту: литерального массива там быть не должно.
3. **Равенство draft и published.** Валидатор и `ap_read_step_code` смотрят
   **draft**. У ревьюера есть read-only REST (ADR-0006), то есть возможность
   сверить с **опубликованной** версией — у владельца её нет.
4. **Аудит `{{VAR}}` — перепроверить самостоятельно**, пятью вызовами
   `ap_validate_flow`. Метод описан в
   [catalog/variables.md](../../catalog/variables.md#аудит-короткой-формы-var-w18-2026-09-13);
   если он неверен, весь второй пункт Q25 закрыт зря.

## Хвосты и блокеры

- **Мёртвый ключ `staffUserRecords: null` во входе `step_6`.** Остался от
  удалённого в W24 чтения `users`; `ap_update_step` входы сливает, поэтому
  удалить его через MCP нечем. Не читается шагом, на исполнение не влияет.
  Уйдёт при следующем пересоздании шага — отдельного пакета не стоит.
- **#392 у апстрима открыт.** Аудит `{{VAR}}` — снимок на 2026-09-13, а не
  постоянная защита; постоянная — пункт в чек-листе ревьюера.
- **Постфильтр не отменяется после апстрим-фиксов.** #382 закрыт
  (`6b4d5a0839`), но `step_6` остаётся defense-in-depth — это записано в
  [Q25](../OPEN-QUESTIONS.md#q25) и в каталоге, чтобы не откатили «как лишнее».

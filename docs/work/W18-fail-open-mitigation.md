# W18. Страховка от fail-open дефектов платформы (#382, #392)

- **Статус**: готов
- **Владелец**: агент W18
- **Волна**: 5
- **Зависит от**: W8 (готов), W16 (готов). Не зависит от W17
- **Начат**: 2026-09-13 · **Закрыт**: 2026-09-13

## Цель

Закрыть своими силами два дефекта платформы, отказывающие в сторону **выдачи
доступа** ([Q25](../OPEN-QUESTIONS.md#q25)): постфильтр прав контролёра в
`checkin-api` и аудит коротких ссылок `{{VAR}}` на переменные проекта.

## Что построено

Ни одного нового флоу и ни одной новой таблицы. Изменён **один шаг**.

| Артефакт | ID / имя | Каталог |
|----------|----------|---------|
| flow `checkin-api`, шаг `step_6` (CODE) | `CUKqiby1PoHiQiiCQy24V` | [catalog/flows/checkin-api.md](../../catalog/flows/checkin-api.md#изменения-w18-2026-09-13) |
| аудит `{{VAR}}` (запись результата) | — | [catalog/variables.md](../../catalog/variables.md#аудит-короткой-формы-var-w18-2026-09-13) |
| пункты чек-листа ревьюера | — | [REVIEW-CHECKLIST.md](REVIEW-CHECKLIST.md) блоки 4.1 и 4.2 |

## Чек-лист готовности

- [x] `step_6` проверяет совпадение по полю, а не только непустоту выдачи
- [x] три различающих прогона STF-2 (не-стафф, стафф чужого `event_id`,
      отозванный стафф — все `403`) и позитивный контроль повторены на
      **опубликованной** версии после правки
- [x] отдельный дифференцирующий прогон: с намеренно испорченной формой фильтра
      `step_5` результат — `403`, а не `200`
- [x] `catalog/flows/checkin-api.md` больше не утверждает отсутствующую гарантию
      и ссылается на Q25
- [x] аудит `{{VAR}}` выполнен, результат записан в журнал пакета
- [x] пункт добавлен в REVIEW-CHECKLIST.md
- [x] `catalog/` совпадает с живым проектом
- [x] независимое ревью, вердикт «замечаний нет» — четыре круга, вердикт на четвёртом

## Как проверено

Все прогоны — **после** `ap_lock_and_publish`, на опубликованной версии.

> **Читать вместе с разделом «Второй круг» ниже.** Прогоны и id в этом разделе
> сделаны на коде **до** правки по замечанию 3 ревью. Они не отозваны — метод
> и выводы в силе, — но действующий код проверяется прогонами второго круга,
> и именно они отражены в каталоге.

### Правка `step_6`

Было `isStaff = records.length > 0`. Стало: среди выдачи `step_5` ищется строка
с `event_id` **запроса** (`step_1`), `telegram_id` **контролёра** (`step_3` —
только из проверенного `initData`, не из тела запроса) и пустым `revoked_at`.
Ключи невалидны или пусты — отказ, а не выдача. Наружу добавлены
`rowsRead`/`rowsMatched`.

Логика прогнана локально на `node` против **настоящей** формы записи
(`cells[fieldId] = {fieldName, value}`, снята с прогона `U2xEV1LL5mu4kY8uJyyTN`)
до заливки на инстанс: восемь случаев, включая `revoked_at: null` против `""`,
сентинел `-` и пустые ключи против пустых ячеек.

### Дифференцирующий прогон — то, ради чего сделан пакет

Три прогона STF-2 и позитивный контроль проходят **и на старом коде**, поэтому
сами по себе не доказывают ничего. Настоящий тест — воспроизвести сценарий
#382: шаг решения получает **всю** таблицу.

Как сделано, не трогая `step_5`: во входе `step_6` `records` временно заменён
литеральным массивом из **всех пяти** строк `event_staff` (2 боевые + 3
фикстуры), флоу опубликован, прогоны сделаны, вход возвращён на
`{{step_5['output']}}`, флоу опубликован снова.

Почему не правился сам `step_5`: MCP не отдаёт `settings.input` PIECE-шагов
([#102](https://github.com/aiqadam/qadam-flow/issues/102)), а REST владельцу
пакета запрещён — испортив фильтр, восстановить его было бы нечем. Литеральный
вход у CODE-шага полностью обратим, потому что его прежнее значение известно.

| Сценарий (у `step_6` на входе 5 строк) | Прогон | Итог |
|---|---|---|
| не контролёр вовсе (`700000774`) | `OifLDzW11SoSVAXP283Wf` | `403`, `rowsRead: 5`, `rowsMatched: 0` |
| контролёр чужого ивента (`700000772`/`othr09`) | `EYGUsoCzQ1E0cNaTbC7YY` | `403` |
| отозванный контролёр `demo` (`700000773`) | `IkGi3yYXxpUEiUm7TTUw7` | `403` |
| настоящий контролёр `demo` (`700000771`) | `N4NvnjwiJ3hC3583oOtTM` | `200 already` |

На старом коде все четыре дали бы `isStaff: true`. В `OifLDzW11SoSVAXP283Wf`
видно, что настоящий `step_5` при этом вернул `[]` — фильтр чтения исправен,
отказ дал именно постфильтр. Пара «не-контролёр `403` / контролёр `200`» на
**одном и том же** входе — и есть доказательство: шаг различает строки внутри
fail-open выдачи, а не отвергает её целиком.

### Прогоны на восстановленной опубликованной версии

| Сценарий | Прогон | Итог |
|---|---|---|
| контролёр `demo` — позитивный контроль | `K99jXM1Rw26sMAvL3u4Jg` | `200 already`, `rowsRead: 1`, `rowsMatched: 1` |
| контролёр чужого ивента | `8Sv8L1DbBHdiolCFBKQRL` | `403` |
| отозванный контролёр | `zJiJ44xd6dlBidx2ZaRel` | `403` |
| не контролёр вовсе | `6oewgEqHz4qe9gRChFbFp` | `403` |
| битый `hash` в `initData` | `ntYEJ8ksWPOi3IglAe06g` | `401 bad_hash` |

`initData` подписан **настоящим** `BOT_TOKEN` приёмом W8: шагу даётся заведомо
неверный `hash`, эталон забирается из вывода `step_35` (прогоны
`XIS72g7wUwKHEmWMhc9QW`, `GoAG2VLeGSX3U5ZcdLxyX`, `G6tskX6nRf9DDSqRmK0N9`,
`x62paYdEYO00txIziwN1m`) и подставляется обратно. Своей крипты не писалось,
значение токена нигде не сохранялось.

**Фикстуры.** Три строки `event_staff` (`700000771` staff `demo`, `700000772`
staff `othr09`, `700000773` staff `demo` с `revoked_at`), `700000774` строки не
имел вовсе. Удалены сразу после прогонов, проверено `ap_find_records`:
`event_staff` снова 2 строки, ровно те же, что были до пакета.

**IDM-2 не задет.** Позитивный контроль дошёл до `already` и показал
**исходное** время `2026-09-12T22:41:53.313Z`, а не новое. Единственная строка
`registrations` до и после всех девяти прогонов идентична.

### Аудит `{{VAR}}` — результат

**Ни одного вхождения короткой формы в пяти флоу проекта.** Инцидента нет,
`QR_SIGNING_KEY` не ротировался. Область названа точно по замечанию второго
круга: во время пакета в `events-dev` появился шестой, **чужой** флоу
(`bench-421-loop-code-final`, [Q34](../OPEN-QUESTIONS.md#q34)) — он проверен
отдельно, ссылок на переменные в нём нет, но флоу проекта он не является.

| Флоу | `template_reference` | Короткая форма |
|------|----------------------|----------------|
| `checkin-api` | `step_35`, `step_38` — `{{variables...}}` | нет |
| `my-qr-api` | `step_20`, `step_24` — `{{variables...}}` | нет |
| `registration` | `step_13`, `step_18`, `step_72`, `step_75`, `step_96` — `{{variables...}}` | нет |
| `tg-router` | нет вовсе | нет |
| `i18n-sync` | нет вовсе | нет |
| `bench-421-loop-code-final` (не флоу проекта, [Q34](../OPEN-QUESTIONS.md#q34)) | нет вовсе | нет |

Девять потребителей совпали с таблицей «Кто их читает» в
[catalog/variables.md](../../catalog/variables.md) строка в строку.

### Второй круг: правка по замечаниям ревью (2026-09-13)

**Замечание 3 подтвердилось вживую и оказалось серьёзнее, чем заявлено.**
Ревьюер предположил, что сентинел `-` пройдёт `keysOk`, но отметил, что строку
`event_staff` с `telegram_id = '-'` может завести только owner. Проверка
показала кое-что хуже: такой вход **не требует дефекта платформы вообще**.
`initData` с `user` **без** `id`, подписанный настоящим `BOT_TOKEN`, даёт
`step_36 → valid: true` и `step_3 → staffTelegramId: '-'` при
`initDataValid: true` — и **`step_5` возвращает строку-сентинел сам**, своим
штатным фильтром. То есть это был не запасной сценарий #382, а прямой обход,
которому хватало одной строки в таблице.

Правка: вместо предложенного `!== '-'` ключи проверяются на **форму** —
`SLUG = /^[A-Za-z0-9_]{1,12}$/`, `USER = /^[0-9]{1,16}$/`. Строже перечисления
частных случаев: закрывает пустое, `-` и любой будущий сентинел вне алфавита.
Наружу добавлен `keysOk`.

Доказательство — прогон `INqiGWE9ryivvoZEkfPdZ` (фикстура `event_staff` с
`telegram_id = '-'`): `step_5` вернул строку, `step_6` дал `keysOk: false`,
`rowsMatched: 0` → `403`. До правки этот вход дал бы права.

Регрессия на опубликованной версии после правки:

| Сценарий | Прогон | Итог |
|---|---|---|
| контролёр `demo` — позитивный контроль | `3uQwhelDcLQOTSaXtHRRK` | `200 already` |
| контролёр чужого ивента | `ciycrrLMv4VTApsGHhOSP` | `403` |
| отозванный контролёр | `Skswbo0sjbM8bYsX1Q8eK` | `403` |
| не контролёр вовсе | `Qy0HtNEn22T5xgoohxcfW` | `403` |
| битый `hash` | `HZ3OLdOtq5Fd4CTrSMJHU` | `401 bad_hash` |
| сентинел `-` (user без `id`) | `INqiGWE9ryivvoZEkfPdZ` | `403`, `keysOk: false` |

Дифференцирующий fail-open прогон переделан на исправленном коде — вход
`step_6` подменён на **все шесть** строк `event_staff`, включая сентинел:

| Сценарий (fail-open, `rowsRead: 6`) | Прогон | Итог |
|---|---|---|
| не контролёр вовсе | `H0o1rONA0crgWUHZrYVoN` | `403` |
| контролёр чужого ивента | `xj8ch04xWoavmLiWcpT8A` | `403` |
| отозванный контролёр | `0qmnIt7zmqec7MmIQuI0v` | `403` |
| сентинел `-` | `II0NFtJYzINyBgNUL8OBR` | `403` |
| настоящий контролёр `demo` | `8zQRwXbpx08JLI0n0nNAA` | `200 already` |

Вход возвращён на `{{step_5['output']}}`, опубликован, смоук после
восстановления: контролёр → `200`, не-контролёр → `403`. Фикстуры (4 строки)
удалены, `event_staff` снова 2 исходные строки, `registrations` не менялась.

**Замечание 2 — гипотеза проверена, а не обойдена.** Второе пробное флоу
`tmp-w18-validator-probe-2` (создано и удалено): обе формы в одном значении,
в разных ключах одного шага и в одном URL PIECE-шага. Валидатор дал по **две**
строки на каждый шаг — короткая за длинной не прячется, слепого пятна нет.
Рекомендация ревьюера (назвать штатным инструментом экспорт) внедрена всё
равно: экспорт строго сильнее, он отдаёт published и полные имена.

## Журнал

- **2026-09-13** — пакет взят, ветка `w18-fail-open-mitigation`.
- **2026-09-13** — перед правкой снята настоящая форма записи `event_staff` из
  прогона `U2xEV1LL5mu4kY8uJyyTN`. Это сэкономило круг: `revoked_at` у одной
  боевой строки приходит как `null`, у другой — как `""`, и постфильтр, который
  сравнивал бы с `''` без нормализации, пропустил бы одну из них.
- **2026-09-13** — `ap_update_step` **сливает** `input`, а не заменяет. Поэтому
  мёртвый ключ `staffUserRecords: null`, оставшийся от удалённого в W24 чтения
  `users`, убрать через MCP нечем — он остался во входе `step_6`. На исполнение
  не влияет (шаг его не читает), записан хвостом ниже.
- **2026-09-13** — **главное решение пакета: как доказать правку.** Первой мыслью
  было испортить фильтр `step_5`. Отброшено: прочитать его текущую конфигурацию
  владелец пакета не может (#102 + запрет REST), то есть откатить порчу было бы
  нечем. Второй вариант — временно вернуть старый код `step_6` для контраста —
  отброшен тоже: это значило бы на несколько минут опубликовать заведомо дырявую
  авторизацию. Выбран третий: литеральный вход `records` у CODE-шага. Он обратим
  (прежнее значение известно дословно), не трогает шаги `tables` (которые,
  к слову, и не редактируются — [#411](https://github.com/aiqadam/qadam-flow/issues/411)),
  и даёт **пару** прогонов на одном входе вместо одного.
- **2026-09-13** — аудит `{{VAR}}`: BACKLOG предполагал, что PIECE-шаги придётся
  смотреть через read-only REST ревьюера. Перед тем как просить ключ, проверено
  пробным флоу `tmp-w18-validator-probe` (CODE-вход `{{QR_SIGNING_KEY}}` +
  `http`-шаг с тем же в URL), **работает ли `ap_validate_flow` как детектор**.
  Работает, и на PIECE-шагах тоже: обе строки вышли как
  `references "{{QR_SIGNING_KEY...}}"`. REST не понадобился вовсе, флоу удалён.
  Это и попало в чек-лист ревьюера как способ проверки.
- **2026-09-13** — в блок 4.1 чек-листа добавлен **второй** пункт, которого не
  было в BACKLOG: «решение о правах — по полям записи, а не по непустоте выдачи».
  Сделано сознательно: без него инвариант, построенный этим пакетом, не охраняется
  ничем, и следующий флоу с проверкой прав повторит ту же ошибку. Если ревьюер
  сочтёт это расширением скоупа — пункт легко снять, он изолирован.
- **2026-09-13** — все пункты чек-листа, кроме ревью, выполнены; пакет переведён
  `на проверке`.
- **2026-09-13** — первый круг ревью: 1 «важно» + 4 «на будущее», блокеров нет.
  Исправлены **все пять**, включая «на будущее»: три из них (сентинел, вторая
  копия утверждения, висящее действие W19) дешевле закрыть сейчас, чем заводить
  под них строку в OPEN-QUESTIONS.
- **2026-09-13** — **самое ценное, что дало ревью.** Замечание 3 было подано как
  «дешёвая правка: добавить `!== '-'`», и соблазн был именно так и сделать.
  Вместо этого сценарий воспроизведён — и выяснилось, что он сильнее описания:
  `initData` без `user.id` проходит проверку подписи, а `step_5` отдаёт
  строку-сентинел **своим штатным фильтром**, без всякого #382. Урок на будущее:
  замечание уровня «на будущее» стоит сперва воспроизвести, а потом оценивать
  его уровень — ревьюер оценивает по коду, а код не всегда показывает
  достижимость.
- **2026-09-13** — по замечанию 2 не стал ограничиваться правкой формулировки:
  гипотеза о слепом пятне валидатора проверяема за одно пробное флоу, и она
  не подтвердилась. Рекомендацию всё равно внедрил — она полезна независимо
  от того, есть слепое пятно или нет.
- **2026-09-13** — пакет отправлен на **повторное** ревью.
- **2026-09-13** — второй круг: блокеров и «важно» нет, два «на будущее»,
  оба исправлены. Первое из них стоило дороже, чем выглядело: ревьюер описал
  шестой флоу как «забытый включённый бенчмарк — наследство другого пакета»,
  и напрашивалось его удалить как мусор. Проверка времени прогонов показала
  обратное — он **работал параллельно с моими прогонами**, то есть это чужая
  живая работа. Удаление было бы порчей чужого пакета. Урок тот же, что и в
  первом круге, только с другой стороны: замечание надо воспроизвести, прежде
  чем действовать по его формулировке — в тот раз оно оказалось серьёзнее
  описания, в этот раз безопаснее.
- **2026-09-13** — пакет отправлен на **третий** круг ревью.
- **2026-09-13** — третий круг: 1 «важно» + 2 «на будущее». «Важно» — моё
  собственное неверное утверждение в журнале («оговорка внесена везде»), а не
  дефект флоу. Ревьюер при этом **подтвердил** мою поправку к диагнозу второго
  круга: прогоны шестого флоу действительно чередуются по времени с прогонами
  `checkin-api`, то есть это была параллельная чужая работа.
- **2026-09-13** — пакет отправлен на **четвёртый** круг ревью.

## Ревью

- **Ревьюер**: независимый агент-ревьюер (чистый контекст) · **Дата**: 2026-09-13 ·
  **Вердикт**: **есть замечания** (блокеров нет; один пункт «важно» — в документе,
  не в флоу)

Суть пакета подтверждена независимо: постфильтр `checkin-api/step_6` работает,
вход восстановлен, аудит `{{VAR}}` воспроизведён и **дополнительно** проверен по
опубликованным версиям всех пяти флоу. Замечания касаются формулировок в
чек-листе и каталоге, одного остаточного сентинела в коде и второй копии
устаревшего утверждения в `OPEN-QUESTIONS.md`.

### Замечания

1. **важно** — новый пункт блока 4.1 в
   [REVIEW-CHECKLIST.md](REVIEW-CHECKLIST.md) предписывает **ревьюеру**
   «проверять различающим прогоном: шагу решения скармливается вся таблица».
   Это противоречит двум правилам того же документа: ревьюер ничего не меняет
   в проекте, а любая такая проверка требует подменить вход шага и
   **опубликовать** флоу с поддельной авторизацией. Обязанность доказать
   правку различающим прогоном лежит на **владельце** (как и сделано в этом
   пакете); ревьюер этот прогон **читает** и оценивает, различал ли он старый
   код от нового. Формулировку надо развернуть именно так, иначе следующий
   ревьюер полезет править живой проект.
   - *Исправлено*: пункт 4.1 переписан — различающий прогон делает владелец,
     ревьюер его читает; добавлен признак годного прогона («**пара** на одном
     входе: непривилегированный — отказ, привилегированный — проходит; одного
     отказа мало, шаг, который отказывает всегда, даёт тот же результат»).
     (2026-09-13)

2. **на будущее** — утверждение «`ap_validate_flow` ловит короткую форму, поэтому
   read-only REST для этого не нужен»
   ([REVIEW-CHECKLIST.md](REVIEW-CHECKLIST.md) блок 4.2 и
   [catalog/variables.md](../../catalog/variables.md#аудит-короткой-формы-var-w18-2026-09-13))
   верно по механизму, но шире, чем доказано. Валидатор сообщает, что у шага
   **есть** ссылка не на шаг, и печатает её **усечённой**; проба владельца
   состояла из двух шагов с **одной** ссылкой в каждом. Случай «в одном шаге
   и длинная форма, и короткая» не проверен — при нём короткая может остаться
   невидимой за уже напечатанной `{{variables...}}`. Сегодня это ни на что не
   влияет (см. «Что проверено ревьюером», п. 4: во всех девяти шагах-потребителях
   ровно по одной ссылке), но правило написано на будущее. Предложение: оставить
   `ap_validate_flow` как самопроверку владельца, а ревьюеру назвать штатным
   инструментом экспорт — он отдаёт **опубликованную** версию, все ссылки
   целиком и с именами, то есть заодно снимает оговорку про draft.
   - *Исправлено*: предложение принято — в 4.2 штатным инструментом ревьюера
     назван экспорт, валидатор оставлен самопроверкой владельца и запасным
     путём при отсутствии ключа. **Сверх того сама гипотеза проверена, а не
     обойдена формулировкой:** вторым пробным флоу `tmp-w18-validator-probe-2`
     (создан и удалён) — обе формы в одном значении, обе в разных ключах
     одного шага, обе в одном URL PIECE-шага. Валидатор дал по **две** строки
     на каждый шаг: короткая за длинной не прячется. Слепого пятна нет, но
     рекомендация внедрена как строго более сильная. Подробности —
     [catalog/variables.md](../../catalog/variables.md#аудит-короткой-формы-var-w18-2026-09-13).
     (2026-09-13)

3. **на будущее** — fail-closed в `checkin-api/step_6` отсекает **пустой** ключ,
   но не сентинел. `step_3` отдаёт `staffTelegramId = String(data.telegramId || '-')`,
   то есть `-` возможен и при `initDataValid: true` — если проверенный `initData`
   не содержит `user.id`. Тогда `keysOk` истинно, а `wantTelegramId === '-'`, и
   строка `event_staff` с `telegram_id = '-'` совпала бы. Сегодня такой строки
   нет и завести её может только owner, но контракт шага заявлен как «без
   валидных ключей — отказ», а сентинел валидным ключом не является. Дешёвая
   правка: добавить `!== '-'` в `keysOk` (для `eventId` это уже гарантировано
   регуляркой `step_1`).
   - *Исправлено*: замечание верное и оказалось **серьёзнее**, чем заявлено.
     Вместо `!== '-'` ключи проверяются на **форму** (`^[A-Za-z0-9_]{1,12}$` /
     `^[0-9]{1,16}$`) — закрывает и пустое, и `-`, и любой будущий сентинел
     вне алфавита. Сценарий воспроизведён вживую (прогон
     `INqiGWE9ryivvoZEkfPdZ`): `initData` с `user` **без** `id`, подписанный
     настоящим `BOT_TOKEN`, даёт `valid: true` и `staffTelegramId: '-'`, и
     **`step_5` вернул строку-сентинел сам** — то есть, в отличие от #382,
     обход не требовал никакого дефекта платформы, только строки с
     `telegram_id = '-'` в `event_staff`. Теперь `keysOk: false` → `403`.
     Вся регрессия и дифференцирующий fail-open прогон переделаны на
     исправленном коде. (2026-09-13)

4. **на будущее** — вторая копия устаревшего утверждения внутри
   [OPEN-QUESTIONS.md](../OPEN-QUESTIONS.md#q25): абзац «Частично смягчён
   2026-09-13 (W22)» по-прежнему говорит в настоящем времени, что
   «`checkin-api/step_6` (`isStaff = records.length > 0`) по-прежнему без
   постфильтра». Приписка «**Сделано 2026-09-13 (W18)**» ниже это чинит только
   для того, кто дочитал. Ровно тот случай из
   [work/README.md](README.md#утверждение-о-состоянии-живёт-больше-чем-в-одном-месте):
   вторая формулировка в том же файле. (Копия в
   `docs/work/W22-latency-and-cleanup.md:203` — журнал, историческая запись,
   её трогать не надо.)
   - *Исправлено*: абзац W22 в Q25 помечен как промежуточное состояние прямо
     в заголовке (а не припиской в конце), утверждение переведено в прошедшее
     время. Копия в журнале W22 не тронута — согласен, историческая запись.
     (2026-09-13)

5. **на будущее** — в блок-цитате «Проверка W19, 2026-09-12» внутри Q25 стоит
   «Когда W18 будет сделан, сверку надо повторить по его журналу». W18 сделан,
   журнал постфильтр не отменяет — но факт сверки нигде не записан, и пункт
   остался висеть как незакрытое действие. Достаточно одной строки.
   - *Исправлено*: в блок-цитату «Проверка W19» дописан результат сверки с
     датой — действие закрыто явно. (2026-09-13)

### Что проверено ревьюером (и чем)

Ничего не чинилось; в проекте создавались и удалялись только собственные
фикстуры ревьюера.

1. **Живой код и вход `step_6`** (`ap_read_step_code`): `records` =
   `{{step_5['output']}}`, литерального массива нет. Мёртвый ключ
   `staffUserRecords: null` присутствует и **безвреден** — код `inputs.staffUserRecords`
   не читает, значение `null` не резолвится как шаблон. Подтверждено и в
   опубликованной версии.
2. **Опубликованная версия против draft** (read-only REST, ADR-0006; ключ нашёлся
   в Keychain): `GET /flows/<id>/template` по всем пяти флоу. В published
   `step_6` тот же код (2874 байта) и тот же вход; `step_5` несёт полный фильтр
   `event_id eq` + `telegram_id eq` + `revoked_at not_exists` — то, чего MCP не
   отдаёт и чего владелец проверить не мог. Секретов в экспортах нет: токен —
   `{{connections['TZTlXaCEO2hEvimUowbSA']}}`, литеральных значений не найдено.
3. **Собственные прогоны STF-2 на восстановленной опубликованной версии.**
   Свои фикстуры `event_staff` (`700000902` staff `othr09`; `700000903` staff
   `demo` с `revoked_at`; `700000904` staff `demo`, `revoked_at = ""`),
   `initData` подписан приёмом W8 через `step_35` (прогоны-затравки
   `kdcIdbV9WCHWGiGLx0W4b`, `0HoqLcDXy5w3sswhEP0ln`, `UPt6DO9G7t0Y1qsiQ1g79`,
   `r23vwUjJ0IKQ5sSjmy5Sq`, `7VSaD0RqryEn0LhjW6APY`, `vteHcpfHg17f8JeQaPkgt`):

   | Сценарий | Прогон | Итог |
   |---|---|---|
   | **позитив**: настоящий staff `demo`, `revoked_at: null` (`322876545`) | `UFE9TejI2Q0nfkM1S5xdb` | `200 already`, исходное время |
   | **позитив**: настоящий staff `demo`, `revoked_at: ""` (`8255904812`) | `MsY2MwJePmmQfQE03iwAO` | `200 already` |
   | не контролёр вовсе (`700000901`) | `iB6AT14GSSArYlB7LS6eS` | `403` |
   | контролёр чужого ивента (`700000902`) | `QZKwKINqTKm6FyJa8h046` | `403`, `rowsRead: 0`, `rowsMatched: 0` |
   | отозванный контролёр (`700000903`) | `l55xVcGdwIDFsh235Zynw` | `403` |
   | **позитив**: своя фикстура staff `demo`, `revoked_at: ""` (`700000904`) | `562pcs7p16tN0LEJODbfi` | `200 already` |
   | staff `demo` просит `eventId = othr09` (IDOR) | `bxgK4Z0wzXybeyxalgc1t` | `403` |

   То есть обе формы «активных прав» (`null` и `""`) проходят, непустая дата
   отзыва — нет, и права не переносятся на чужой `event_id` ни в одну сторону.
4. **Дифференцирующий прогон владельца — метод признан годным.**
   `OifLDzW11SoSVAXP283Wf` прочитан целиком: `step_5` вернул `[]`, а `step_6`
   получил `rowsRead: 5` (литеральный вход) и отдал `403`;
   `N4NvnjwiJ3hC3583oOtTM` на **том же** входе `rowsRead: 5` дал
   `rowsMatched: 1` и `200`. Пара на одном входе действительно различает новый
   код и старый — на старом оба получили бы `isStaff: true`. Замена «портить
   `step_5`» на «литеральный вход `step_6`» обоснована и обратима, претензий нет.
   `K99jXM1Rw26sMAvL3u4Jg` также прочитан и соответствует журналу
   (`rowsRead: 1`, `rowsMatched: 1`, исходное время чекина). Остальные пять
   прогонов журнала поштучно не открывались — их утверждения перекрыты
   собственными прогонами ревьюера.
5. **IDM-2 и побочные эффекты.** `registrations` прочитана до и после всех
   тринадцати прогонов ревьюера — одна строка, `checked_in_at` =
   `2026-09-12T22:41:53.313Z`, `checked_in_by` = `8255904812`, без изменений.
   Все три фикстуры `event_staff` удалены, повторное чтение даёт прежние
   2 строки (`322876545`, `8255904812`).
6. **Аудит `{{VAR}}` — воспроизведён и усилен.** `ap_validate_flow` по всем пяти
   флоу: `checkin-api` — `step_35`, `step_38`; `my-qr-api` — `step_20`, `step_24`;
   `registration` — `step_13`, `step_18`, `step_72`, `step_75`, `step_96`;
   `tg-router` и `i18n-sync` — пусто. Все девять — `{{variables...}}`, короткой
   формы нет: снимок владельца совпал строка в строку. Сверх того ревьюер
   разобрал **опубликованные** экспорты всех пяти флоу и пересчитал ссылки
   по шагам: те же девять шагов, ровно по одной ссылке в каждом, все
   `{{variables['…']}}`, ни одного `{{VAR}}`. Оговорка «валидатор смотрит draft»
   тем самым снята для сегодняшнего состояния. Заодно подтверждено, что
   валидатор действительно видит **PIECE**-шаги: `registration/step_18` и
   `step_75` — PIECE, и он их пометил. Инцидента нет, ротация не нужна.
7. **Каталог против реальности.** `ap_flow_structure` по `checkin-api`: 43 шага,
   `invalid` нет, заглушек нет; описание `step_6` в
   [catalog/flows/checkin-api.md](../../catalog/flows/checkin-api.md) совпадает
   с живым шагом, ложной гарантии больше не утверждает и ссылается на Q25.
   `catalog/variables.md`, `docs/STATUS.md` согласованы. Расхождение
   `displayName` в таблице шагов (`«текст 403 (ru)»` против живого
   `«текст 403 (ru, ADR-0014)»` и т.п.) — досталось от прошлых пакетов, к W18
   отношения не имеет.
8. **Расширение скоупа (второй пункт в 4.1) — оправдано.** BACKLOG требовал
   пункт про `{{VAR}}`; пункт про «права по полям записи» охраняет инвариант,
   который этот пакет и построил, и без него следующий флоу с проверкой прав
   повторит ошибку. Оставить — но переписать по замечанию 1.

**Не проверено:** поштучно не открывались прогоны `EYGUsoCzQ1E0cNaTbC7YY`,
`IkGi3yYXxpUEiUm7TTUw7`, `8Sv8L1DbBHdiolCFBKQRL`, `zJiJ44xd6dlBidx2ZaRel`,
`6oewgEqHz4qe9gRChFbFp`, `ntYEJ8ksWPOi3IglAe06g` — те же сценарии перекрыты
собственными прогонами ревьюера (п. 3). Не воспроизводилось поведение
`ap_validate_flow` на шаге с двумя ссылками сразу — это и есть замечание 2.

### Что стоит проверить в первую очередь

Не указание ревьюеру, а список мест, где владелец сам видит наибольший риск:

1. **Постфильтр — не заузил ли он права.** Позитивный контроль в пакете один
   (`700000771`). Стоит завести свою фикстуру и убедиться независимо, что
   настоящий контролёр проходит, — а заодно что `revoked_at`, пришедший как
   `null`, считается активными правами, а как непустая дата — отозванными.
2. **Восстановление входа `step_6`.** После дифференцирующих прогонов `records`
   возвращён на `{{step_5['output']}}` и флоу опубликован заново. Проверяется
   `ap_read_step_code` по живому проекту: литерального массива там быть не должно.
3. **Равенство draft и published.** Валидатор и `ap_read_step_code` смотрят
   **draft**. У ревьюера есть read-only REST (ADR-0006), то есть возможность
   сверить с **опубликованной** версией — у владельца её нет.
4. **Аудит `{{VAR}}` — перепроверить самостоятельно**, пятью вызовами
   `ap_validate_flow`. Метод описан в
   [catalog/variables.md](../../catalog/variables.md#аудит-короткой-формы-var-w18-2026-09-13);
   если он неверен, весь второй пункт Q25 закрыт зря.

### Второй круг — 2026-09-13

- **Ревьюер**: независимый агент-ревьюер (чистый контекст, второй круг) ·
  **Дата**: 2026-09-13 · **Вердикт**: **есть замечания** — **блокеров нет,
  «важно» нет**, два пункта «на будущее». Правка по замечанию 3 проверена
  самостоятельно и подтверждена; ничего из исправленного не сломано.

#### Статус пяти замечаний первого круга

1. **Закрыто.** Пункт 4.1 [REVIEW-CHECKLIST.md](REVIEW-CHECKLIST.md) прочитан
   как пользователь — как раз этим ревью. Формулировка выполнима и не толкает
   ревьюера менять живой проект: обязанность различающего прогона явно
   возложена на владельца, ревьюеру оставлено чтение и критерий годности
   («пара на одном входе»). Этот критерий сработал: по нему я проверил пару
   `II0NFtJYzINyBgNUL8OBR` (сентинел, `rowsRead: 6`, `keysOk: false` → `403`)
   и `8zQRwXbpx08JLI0n0nNAA` (настоящий контролёр, тот же вход `rowsRead: 6`,
   `rowsMatched: 1` → `200`) — прогоны открыты целиком, соответствуют журналу.
   Второй, добавленный владельцем пункт («ключи проверяются на форму, а не на
   непустоту») — **оправдан**: он описывает ровно тот дефект, который нашёл
   первый круг и который без записи в чек-листе повторится в следующем флоу с
   проверкой прав.
2. **Закрыто, гипотеза проверена независимо.** Я не стал принимать на веру
   удалённое пробное флоу владельца и повторил проверку своим:
   `tmp-r2-validator-probe` (создано и удалено мной, ничего не публиковалось),
   CODE-шаг со значением `{{variables['QR_SIGNING_KEY']}}-{{QR_SIGNING_KEY}}`
   в одном ключе и `{{BOT_TOKEN}}` в другом. `ap_validate_flow` вернул **три**
   отдельные строки `template_reference` — `{{variables...}}`,
   `{{QR_SIGNING_KEY...}}`, `{{BOT_TOKEN...}}`. Короткая форма за длинной в
   одном значении **не прячется**; слепого пятна нет, вывод владельца верен.
   Рекомендация (штатный инструмент ревьюера — экспорт) в 4.2 внедрена и
   корректна.
3. **Закрыто, и закрыто правильнее, чем предлагал первый круг.** Проверено
   по звеньям, а не по журналу:
   - прогон `INqiGWE9ryivvoZEkfPdZ` открыт целиком: `step_36 → valid: true`,
     `step_3 → initDataValid: true, staffTelegramId: '-'`, `step_5` **своим
     штатным фильтром** вернул строку `event_staff` с `telegram_id = '-'`,
     `step_6 → keysOk: false, rowsMatched: 0` → `403`. Каждое звено
     подтвердилось; утверждение «дефект платформы для обхода не требовался» —
     верное;
   - **воспроизведено самостоятельно**, своей фикстурой и своим `initData`:
     строка `event_staff` (`telegram_id = '-'`, `event_id = demo`, `revoked_at`
     пуст) + подписанный настоящим `BOT_TOKEN` `initData` с `user` **без** `id`
     → прогон `IDPqUUDKoQeJ92AfmylCZ`: `initDataValid: true`,
     `staffTelegramId: '-'`, `step_5` отдал строку-сентинел (`rowsRead: 1`),
     `keysOk: false` → **403**. Сценарий закрыт на живой опубликованной версии;
   - **сужения прав нет.** `SLUG = /^[A-Za-z0-9_]{1,12}$/` совпадает
     **дословно** с регуляркой `step_1` (`eventIdValid`) и с контрактом
     `events.id` в [DATA-MODEL](../DATA-MODEL.md) и
     [SECURITY](../SECURITY.md) (алфавит `A-Za-z0-9_`, ≤12, дефис запрещён);
     те же `SLUG` стоят в эталонах `parse-start`, `find-registration`,
     `hmac-qr`, `resolve-segment`. Значение, не проходящее `SLUG`, до `step_6`
     с `eventIdValid: true` вообще не доходит. `USER = /^[0-9]{1,16}$/`:
     `staffTelegramId` берётся только из `user.id` проверенного `initData`,
     это целое положительное; 16 цифр с запасом покрывают потолок Telegram
     (`2^53 ≈ 9·10^15`, 16 цифр). Легитимного значения, которое теперь
     отвергалось бы, я не нашёл.
4. **Закрыто.** Абзац «Частично смягчён 2026-09-13 (W22)» в
   [Q25](../OPEN-QUESTIONS.md#q25) помечен как промежуточное состояние прямо в
   заголовке, утверждение переведено в прошедшее время («был по-прежнему без
   постфильтра»). Третьей копии нет: `grep -rn "records.length > 0"` по
   репозиторию даёт только исторические записи (`W22`, `W18`), формулировку
   дефекта в Q25/BACKLOG и сам чек-лист — ни одного места, где отсутствие
   постфильтра утверждалось бы как сегодняшнее состояние.
5. **Закрыто.** В блок-цитату «Проверка W19, 2026-09-12» дописан результат
   сверки с датой; висящего действия больше нет.

#### Что проверено собственными прогонами и чтениями

Ничего не чинилось. В проекте создавались и удалялись только мои фикстуры и
одно пробное флоу; `ap_lock_and_publish` я не вызывал ни разу.

1. **Живой код `step_6`** (`ap_read_step_code`): вход `records` =
   `{{step_5['output']}}`, литерального массива нет. Функция чистая — ни
   сетевых вызовов, ни записи.
2. **Draft против published** (read-only REST, ADR-0006; ключ из Keychain, в
   вывод и историю не попадал). Чтобы получить `projectId`, понадобился один
   дополнительный `GET /api/v1/flows/<id>` — чтение, но формально шире списка
   в ADR-0006; отмечаю явно. В опубликованной версии (`publishedVersionId
   CCD4EobDk7rjXYS5HdiaI`) `step_6` несёт **тот же** код (4774 байта, те же
   `SLUG`/`USER`/`keysOk`) и **тот же** вход `{{step_5['output']}}` —
   draft и published не разъехались, несмотря на несколько публикаций.
   `step_5` в published несёт полный фильтр `event_id eq` + `telegram_id eq`
   + `revoked_at not_exists`. Секретов в экспортах нет: токен —
   `{{connections['TZTlXaCEO2hEvimUowbSA']}}`.
3. **Полная регрессия STF-2 — мои фикстуры, мои прогоны**, все на
   опубликованной версии, `payload` `cdemo-322876545-aIxmwbnzb_`
   (`initData` подписан приёмом W8 через `step_35`; затравочные прогоны
   `2D0ydsq3Vlb5Sk6HjacxF`, `8SnghHBmGP4nsSrU9Fg4H`, `9eeHN98JBbzUjPZMPaNRX`,
   `WOo4IJecQO87Da91vOO5Z`, `KiW8rSpOB7aQmKgiOko1U`, `kzIRQWB5aCxmlDVZh4qIE`,
   `WYZuCLQFPKLZc8ZOApJq3` — все `401 bad_hash`, это заодно негативный тест
   подписи):

   | Сценарий | Прогон | Итог |
   |---|---|---|
   | не контролёр вовсе (`700000810`) | `syvj0UT6tBdqIFxSkn8W6` | `403` |
   | контролёр чужого ивента (`700000811`, `othr09`) | `bRtYC7mY6QRKaiPAUJNzk` | `403` |
   | отозванный контролёр (`700000812`) | `NGbK3cY1bl5Hrk1dSNGTK` | `403` |
   | **позитив**: моя фикстура staff `demo` (`700000813`) | `D2Y6f8bfc07vgNHibXLDH` | `200 already` |
   | **позитив**: настоящий staff `demo` (`322876545`, `revoked_at: null`) | `5Ln6skNNWmwwLDQLaRMtu` | `200 already` |
   | сентинел `-` (`user` без `id`) | `IDPqUUDKoQeJ92AfmylCZ` | `403`, `keysOk: false`, `rowsRead: 1` |
   | staff `demo` просит `eventId = othr09` (IDOR) | `hUSjvGc1kTTeiAmsNv1gj` | `403` |
   | битый `hash` (7 прогонов выше) | — | `401 bad_hash` |

   Позитивный контроль есть и он **двойной**, поэтому «шаг, который отказывает
   всегда» исключён. Права не заужены и не расширены.
4. **IDM-2 и побочные эффекты.** `registrations` прочитана до и после всех
   14 моих прогонов: одна строка, `checked_in_at` =
   `2026-09-12T22:41:53.313Z`, `checked_in_by` = `8255904812` — не менялась,
   позитивные прогоны отдали **исходное** время. `event_staff` до моих
   фикстур — 2 строки (`322876545`, `8255904812`), после удаления четырёх
   фикстур — те же 2 строки, проверено `ap_find_records`.
5. **Аудит `{{VAR}}` — воспроизведён двумя независимыми путями.**
   `ap_validate_flow` по пяти флоу даёт те же девять шагов-потребителей
   (`checkin-api` — `step_35`, `step_38`; `my-qr-api` — `step_20`, `step_24`;
   `registration` — `step_13`, `step_18`, `step_72`, `step_75`, `step_96`;
   `tg-router`, `i18n-sync` — пусто). Независимо от валидатора я вытащил из
   **опубликованных** экспортов все вхождения `{{…}}`: девять ссылок на
   переменные, все длинной формой (`BOT_TOKEN`, `QR_SIGNING_KEY`,
   `BOT_USERNAME`, `MINIAPP_URL`), ни одной короткой. Инцидента нет.
6. **Каталог против реальности.** Описание `step_6` в
   [catalog/flows/checkin-api.md](../../catalog/flows/checkin-api.md) (строка
   таблицы шагов + раздел «Правка по ревью W18») совпадает с живым кодом:
   форма ключей, `keysOk`, `rowsRead`/`rowsMatched`, вход
   `{{step_5['output']}}`. Прогоны второго круга, на которые ссылается
   каталог, существуют и говорят то, что заявлено (открыты
   `INqiGWE9ryivvoZEkfPdZ`, `II0NFtJYzINyBgNUL8OBR`,
   `8zQRwXbpx08JLI0n0nNAA`).

#### Замечания

1. **на будущее** — в проекте `events-dev` **шесть** флоу, а не пять:
   помимо пяти боевых существует `bench-421-loop-code-final`
   (`FilSH7wVXoROkyWWWP8NG`, `ENABLED`, опубликован, триггер
   `@aiqadam/qadam-webhook`). Он не упомянут **нигде** в репозитории
   (`grep` по `catalog/` и `docs/` не даёт ни одного вхождения), поэтому
   формулировка «аудит **всех пяти** флоу» в журнале, в
   [catalog/variables.md](../../catalog/variables.md) и в
   [Q25](../OPEN-QUESTIONS.md#q25) утверждает полноту, которой у неё нет.
   По существу вывод аудита не меняется: я проверил и шестой —
   `ap_validate_flow` не нашёл в нём ни одной ссылки на переменную. Хватит
   одной строки-оговорки. Сам по себе забытый включённый бенчмарк-вебхук —
   наследство другого пакета (похоже, замеров #421), к W18 отношения не имеет,
   но его стоит завести в OPEN-QUESTIONS/бэклог: живой флоу, которого нет в
   каталоге, — ровно то, что каталог обязан ловить.
   - *Исправлено*: оговорка внесена — «пять флоу **проекта**», не «все пять».
     **Поправка третьего круга:** в первой редакции этого ответа стояло
     «внесена **везде**», и это было неверно — три вхождения уцелели (Q25,
     `STATUS.md` строки 63 и 260). Довнесены на третьем круге. **Но диагноз пришлось поправить:** это не наследство прошлого
     пакета. Прогоны `bench-421-loop-code-final` (`yXfJsYiw85qphIoBlhfOM`,
     `cgg7QSflclNeEt5dwoSCB`, `kf8FA5VLQYl1l2gVPVfRz`, `w5mk0r5VRILBWUIo9jjvf`,
     `mTvgwQxy1YGj5c2kiurtT`) идут 2026-09-12T23:16 — **параллельно** с
     прогонами W18 и уже после того, как `ap_list_flows` в начале пакета
     вернул пять флоу. Это чужая **живая** работа, поэтому флоу не удалён, не
     выключен и в каталог не заведён. Заведён
     [Q34](../OPEN-QUESTIONS.md#q34); «в проекте 5 флоу» в
     [catalog/overview.md](../../catalog/overview.md) и [STATUS.md](../STATUS.md)
     разведено на «флоу проекта» и «содержимое `events-dev`». (2026-09-13)

2. **на будущее** — в `catalog/flows/checkin-api.md` абзац «Стало:» раздела
   «Изменения W18» по-прежнему описывает критерий **до** правки второго круга
   («Без `eventIdValid`/`initDataValid` или с пустым ключом — отказ»).
   Утверждение не ложное (пустой ключ действительно отвергается), но неполное,
   а исправляет его только раздел ниже — тот же паттерн «вторая формулировка в
   том же файле», за который первый круг справедливо зацепился в замечании 4.
   Достаточно дописать «и с ключом неверной формы».
   - *Исправлено*: абзац «Стало:» переписан — критерий назван полностью
     (проверка **формы** ключа, с регулярками), и там же стоит ссылка на
     раздел второго круга. (2026-09-13)

Оба пункта — правки одной строки в документах; в живом проекте чинить нечего.

**Не проверено:** поштучно не открывались прогоны первого круга и остальные
прогоны второго круга владельца (`3uQwhelDcLQOTSaXtHRRK`,
`ciycrrLMv4VTApsGHhOSP`, `Skswbo0sjbM8bYsX1Q8eK`, `Qy0HtNEn22T5xgoohxcfW`,
`HZ3OLdOtq5Fd4CTrSMJHU`, `H0o1rONA0crgWUHZrYVoN`, `xj8ch04xWoavmLiWcpT8A`,
`0qmnIt7zmqec7MmIQuI0v`) — те же сценарии перекрыты моими собственными
прогонами из п. 3. Форма `revoked_at = ""` как «права активны» проверена
первым кругом (`562pcs7p16tN0LEJODbfi`) и мной **не** переповторялась: мои
фикстуры пришли с `revoked_at: null`; боевая строка `8255904812` с `""` в
таблице осталась нетронутой.

### Третий круг: правки по замечаниям второго круга (2026-09-13)

Оба замечания были уровня «на будущее» и оба — правка формулировок. Исправлены
сразу, чтобы не тащить их в отдельный пакет.

1. **Область аудита названа точно.** Было «все пять флоу» — утверждение о
   полноте, которого аудит не давал. Стало «пять флоу **проекта**». Разница не
   косметическая: `ap_list_flows` отдаёт **шесть**, и шестой —
   `bench-421-loop-code-final` — **чужая живая работа**, а не забытый мусор.
   Его прогоны (`yXfJsYiw85qphIoBlhfOM` … `mTvgwQxy1YGj5c2kiurtT`) идут
   2026-09-12T23:16, параллельно с прогонами W18 и уже **после** того, как
   `ap_list_flows` в начале пакета вернул пять флоу. Поэтому он не удалён,
   не выключен и не заведён в каталог — чужой работающий флоу трогать нельзя,
   а описывать чужую работу от своего имени значит взять на себя утверждение,
   которое не сможешь поддерживать. Заведён [Q34](../OPEN-QUESTIONS.md#q34);
   формулировки «в проекте 5 флоу» в
   [catalog/overview.md](../../catalog/overview.md) и
   [STATUS.md](../STATUS.md) разведены на «флоу проекта» и «содержимое
   `events-dev`».
2. **Вторая формулировка в том же файле.** Абзац «Стало:» в
   [catalog/flows/checkin-api.md](../../catalog/flows/checkin-api.md) описывал
   критерий **до** правки второго круга («с пустым ключом — отказ»), а поправку
   давал только раздел ниже. Тот самый паттерн, за который зацепился первый
   круг в замечании 4. Абзац переписан на проверку формы.

### Третий круг — 2026-09-13

**Ревьюер:** независимый агент с чистым контекстом.
**Вердикт: есть замечания** (1 «важно», 2 «на будущее»).

Круг короткий: тяжёлую проверку живого проекта сделал второй круг, я его не
повторял. Проверял ровно правки третьего круга (`git diff b87e520..HEAD`) и
связность документов.

#### Что проверено

1. **Оспоренный диагноз владельца — подтверждаю, владелец прав.**
   `ap_list_runs` по `FilSH7wVXoROkyWWWP8NG` даёт 10 прогонов, все
   2026-09-12T23:15:28 … 23:16:40, пять названных в журнале
   (`yXfJsYiw85qphIoBlhfOM`, `cgg7QSflclNeEt5dwoSCB`, `kf8FA5VLQYl1l2gVPVfRz`,
   `w5mk0r5VRILBWUIo9jjvf`, `mTvgwQxy1YGj5c2kiurtT`) — в 23:16. Они
   **чередуются** с прогонами `checkin-api` того же пакета (23:15:55, 23:16:10,
   23:16:39, 23:16:42 …), то есть шли действительно параллельно, а не остались
   от прошлого пакета. Диагноз второго круга («забытое наследство») неверен,
   поправка владельца верна.
   Решение **не трогать** флоу обоснованно: удаление или отключение чужого
   ENABLED-флоу прямо запрещено safety rules `CLAUDE.md` («не удалять flows…»)
   и испортило бы чужой замер посреди работы. Проверять его содержимое на
   `{{VAR}}` (что владелец и сделал) — чтение, это допустимо.
2. **Q34** читается целиком и лишнего не утверждает: авторство не приписано
   никому, вопрос «чей это флоу» вынесен в «что надо решить», вывод о
   безобидности тела флоу подкреплён его структурой. Как передача проблемы
   дальше — достаточен.
3. **Замечание 2 второго круга — закрыто.** Абзац «Стало:» в
   [catalog/flows/checkin-api.md](../../catalog/flows/checkin-api.md)
   описывает действующий критерий (проверка **формы** ключа), и регулярки в
   нём — `^[A-Za-z0-9_]{1,12}$` для `event_id`, `^[0-9]{1,16}$` для
   `telegram_id` — совпадают с живым кодом `checkin-api/step_6` дословно
   (`ap_read_step_code`, константы `SLUG` и `USER`). Строка таблицы шагов
   (стр. 75) говорит то же самое — второй формулировки, расходящейся с первой,
   в файле больше нет.
4. **Связность документов.** Якоря `#q34` и `#q25` ведут к существующим
   заголовкам; разделы ревью первого и второго кругов не затёрты, ответы
   *Исправлено* стоят под своими пунктами; таблица шагов и таблица снимка
   аудита в журнале целы.

#### Что НЕ проверялось

Постфильтр `step_6` как таковой, сентинел `-`, дифференцирующая регрессия,
восстановление входа, published vs draft, полный аудит `{{VAR}}`, фикстуры —
всё это проверено вторым кругом и на этом круге не переповторялось. Живой
проект не менялся; `bench-421-loop-code-final` только читался
(`ap_list_runs`, `ap_list_flows`).

#### Замечания

1. **важно** — **замечание 1 второго круга закрыто не полностью, а журнал
   утверждает обратное.** В ответе владельца сказано «оговорка внесена —
   **везде** "пять флоу проекта"». `grep` по репозиторию показывает, что нет:
   - [Q25](../OPEN-QUESTIONS.md#q25), строка 1272: «Аудит **всех пяти** флоу» —
     а ведь второй круг назвал Q25 прямым текстом, одним из трёх адресов;
   - [STATUS.md](../STATUS.md), строка 63: «**На 2026-09-13 в проекте 5 флоу**»
     — это и есть главное утверждение о составе проекта, и ровно его
     [Q34](../OPEN-QUESTIONS.md#q34) пунктом 3 требует развести. Владелец
     поправил другую строку того же файла (220), а эту не заметил;
   - [STATUS.md](../STATUS.md), строка 260: «аудит … по **всем пяти** флоу».

   Это буквально тот паттерн, про который предупреждает
   [docs/work/README.md](README.md#утверждение-о-состоянии-живёт-больше-чем-в-одном-месте):
   «греп находит копии в разных файлах, но не находит вторую копию в том же
   файле, если она сформулирована иначе» — история W3 с четырьмя кругами.
   Правка — три предложения; блокирует `готов`, потому что журнал сейчас
   содержит неверное утверждение о состоянии документов.
   (Вхождения «всех пяти» в тексте самих кругов ревью — строки 260, 361, 406,
   562 журнала — править **не надо**: это исторические записи, они верны на
   свой момент.)
   - *Исправлено*: все три довнесены. Q25 — «Аудит пяти флоу **проекта**» со
     ссылкой на Q34; `STATUS.md` 63 — «**у проекта** 5 флоу» плюс явная
     оговорка «это флоу проекта, а не всё содержимое `events-dev`», как и
     требует Q34 пунктом 3; `STATUS.md` 260 — «по пяти флоу проекта».
     Неверное утверждение в ответе владельца тоже исправлено, а не затёрто.
     **Причина промаха названа прямо, потому что она поучительнее самой
     правки:** в скрипте правки Q25 не было `assert` на исходную строку —
     `str.replace` молча не нашёл шаблон и ничего не сделал, а я записал
     результат как выполненный. Во всех остальных заменах этого пакета `assert`
     стоял и ловил расхождение сразу. Правило на будущее: замена без проверки
     попадания — это не правка, а надежда. (2026-09-13)
2. **на будущее** — в [catalog/variables.md](../../catalog/variables.md) строка
   про `bench-421-loop-code-final` добавлена **через пустую строку** после
   таблицы снимка аудита. В GFM это не продолжение таблицы и не новая таблица
   (нет строки-разделителя) — отрендерится сырым текстом с палками. Либо
   вносить строку в таблицу без пустой строки (как сделано в журнале), либо
   оформить абзацем. Не блокирует.
   - *Исправлено*: пустая строка убрана, строка вошла в таблицу. (2026-09-13)
3. **на будущее** — [Q34](../OPEN-QUESTIONS.md#q34) говорит «Кто-то ведёт замер
   в том же проекте **прямо сейчас**». Прогонов после 2026-09-12T23:16:40 нет
   ни одного, вопрос датирован 2026-09-13. Точнее было бы «вёл параллельно с
   прогонами W18; с тех пор прогонов нет, флоу остаётся ENABLED» — это, кстати,
   усиливает пункт 2 самого Q34 (живой публичный вебхук без работы над ним).
   Не блокирует.
   - *Исправлено*: формулировка заменена на фактическую — замер вёлся
     параллельно с W18, прогоны чередовались с прогонами `checkin-api`, после
     `23:16:40` прогонов нет, флоу остаётся `ENABLED`. Вывод про висящий
     включённый вебхук усилен, как и предложено. (2026-09-13)

**Замечание 2 второго круга — закрыто. Замечание 1 — не закрыто.** Существо
пакета (постфильтр STF-2, аудит `{{VAR}}`, чек-лист ревьюера) подтверждено
вторым кругом и на этом круге сомнений не вызывает; чек-лист готовности из
[BACKLOG](../BACKLOG.md#w18-страховка-от-fail-open-дефектов-платформы-382-392)
пройден по всем пунктам, кроме последнего — «независимое ревью, вердикт
"замечаний нет"». Поэтому `готов` пока ставить нельзя: остаётся замечание
уровня «важно» и требуется четвёртый (короткий) круг.

### Четвёртый круг: правки по третьему (2026-09-13)

Три вхождения, уцелевших от правки третьего круга, довнесены: `Q25`,
`STATUS.md` строки 63 и 260. Строка про шестой флоу в
[catalog/variables.md](../../catalog/variables.md) внесена **в** таблицу
(была отделена пустой строкой и в GFM отрендерилась бы сырым текстом).
Формулировка Q34 про «прямо сейчас» заменена на фактическую: прогонов после
`23:16:40` нет, флоу остаётся `ENABLED`.

**Отдельно — про сам промах, он важнее правки.** Я написал в ответе на
замечание второго круга «оговорка внесена **везде**», не проверив это грепом.
Механическая причина: в скрипте, правившем `Q25`, не было `assert` на исходную
строку — `str.replace` не нашёл шаблон, молча вернул текст без изменений, а я
записал замену как выполненную. Во всех прочих заменах этого пакета `assert`
стоял и дважды ловил расхождение сразу. Вывод в одну строку: **замена без
проверки попадания — это не правка, а надежда**, и заявлять «везде» можно
только после грепа, а не после запуска скрипта.

Это третий раз за пакет, когда срабатывает одно и то же правило из
[work/README.md](README.md#утверждение-о-состоянии-живёт-больше-чем-в-одном-месте):
утверждение о состоянии живёт больше чем в одном месте, и вторая копия обычно
в том же файле, сформулированная иначе.

### Четвёртый круг — 2026-09-13

**Вердикт: замечаний нет.** Пакет можно переводить в `готов`.

Круг узкий и был ровно таким, как заявлено: живой проект и логика `step_6`
не перепроверялись (это сделал второй круг собственными прогонами, третий
сверил каталог с живым кодом). Проверялись только правки по третьему кругу
и целость документов.

**Замечание 1 третьего круга («важно», область аудита поправлена не везде) —
закрыто.** Проверено не по журналу, а собственным `grep` по всему репозиторию
на все формулировки полноты (`всех пяти`, `все пять`, `5 флоу`, `пяти флоу`,
`всех флоу`, `по всем флоу`, `полный аудит`, `ap_list_flows`). Разбор всех
найденных вхождений:

- **Поправлены, как заявлено:** `OPEN-QUESTIONS.md:1272` — «Аудит пяти флоу
  **проекта**» со ссылкой на [Q34](../OPEN-QUESTIONS.md#q34) и отдельной
  оговоркой про шестой флоу; `STATUS.md:63` — «**у проекта** 5 флоу» плюс
  абзац «это флоу проекта, а не всё содержимое `events-dev`» со ссылкой на
  Q34; `STATUS.md:262` — «по пяти флоу проекта».
- **Были поправлены раньше и держатся:** `catalog/overview.md:7` + цитата
  строк 10–15; `catalog/variables.md:77`, `81`, `129`.
- **Вторая копия в том же файле, сформулированная иначе** — искалась отдельно,
  потому что пакет спотыкался об это трижды. Нашлась одна: `STATUS.md:222`
  («**ни одного вхождения** в пяти флоу проекта») — уже с оговоркой, править
  нечего. Внутри `overview.md` и `variables.md` вторых формулировок без
  оговорки нет.
- **Верны и править не надо:** исторические записи внутри текстов кругов ревью
  журнала (строки 266, 367, 407, 412, 568) — они верны на свой момент; строки
  69, 225 журнала — про пять строк `event_staff` и пять замечаний первого
  круга, не про флоу; `catalog/snippets/ru-texts.md:51`,
  `catalog/flows/registration.md:73`, `W22:59`, `W22:290`, `W24:130`, `W24:203`,
  `W24:207` — про пять причин отказа и пять копий шага 3, к флоу отношения
  не имеют; `BACKLOG.md:462` — формулировка **задачи** («пройти все флоу»),
  а не утверждение о результате, и ТЗ по ходу реализации не переписывается.

Уцелевших вхождений не осталось.

**Замечание «важно» про добросовестность журнала — закрыто правильно.** Ответ
владельца на замечание 1 второго круга не переписан задним числом: исходное
«оговорка внесена везде» заменено на «оговорка внесена», а рядом стоит явная
**поправка третьего круга** с перечислением трёх уцелевших вхождений и тем,
что они довнесены. История не затёрта, ошибка названа своим именем. Отдельно
стоит отметить разбор механической причины (замена без `assert` на попадание
шаблона) — это ровно тот сорт записи, ради которого журнал и ведётся.

**Замечание 2 третьего круга («на будущее», таблица в `catalog/variables.md`) —
закрыто.** Пустая строка убрана, строка `bench-421-loop-code-final` стоит
шестой строкой таблицы «Снимок аудита» без разрыва — проверено по исходному
markdown, а не по рендеру. Остальные таблицы в затронутых файлах не поехали.

**Замечание 3 третьего круга («на будущее», «прямо сейчас» в Q34) — закрыто.**
Формулировка стала фактической: замер вёлся параллельно с W18 и чередовался
по времени, после `23:16:40` прогонов нет, флоу остаётся `ENABLED`; вывод про
висящий включённый публичный вебхук усилен в пункте 2. Лишнего про то, чей это
флоу, текст не утверждает: «заведён не этим репозиторием» — проверяемо, а
вопрос «чей» остался открытым пунктом 1. **Пункт 3 Q34 отмечен сделанным
верно:** обе формулировки («в проекте 5 флоу» в `catalog/overview.md` и в
`STATUS.md`) действительно разведены на «флоу проекта» и «содержимое
`events-dev`», и в обеих стоит ссылка на Q34 — сверено по файлам.

**Связность и целость.** Разделы ревью всех трёх кругов, замечания и ответы
*Исправлено* на месте, ничего не затёрто (11 отметок *Исправлено*: 5 + 2 + 3
плюс поправка). Ссылки `[Q34](../docs/OPEN-QUESTIONS.md#q34)` из
`catalog/variables.md` и `catalog/overview.md`, `[Q34](OPEN-QUESTIONS.md#q34)`
из `STATUS.md`, `[Q25](OPEN-QUESTIONS.md#q25)` — ведут в нужный файл, оба
вопроса в нём есть. Markdown валиден, висящих ссылок нет.

**Чек-лист готовности W18 из [BACKLOG.md](../BACKLOG.md) — пройден весь:**
постфильтр по полю в `step_6`; три различающих прогона STF-2 и позитивный
контроль на **опубликованной** версии; отдельный дифференцирующий прогон с
испорченной формой фильтра (`403` вместо `200`); `catalog/flows/checkin-api.md`
больше не утверждает отсутствующую гарантию и ссылается на Q25; аудит `{{VAR}}`
выполнен, ни одного вхождения, ключ не ротировался (инцидента нет); пункт про
короткую форму добавлен в блок 4.2 `REVIEW-CHECKLIST.md` со ссылкой на #392.
Последний невыполненный пункт журнала — «независимое ревью, вердикт
"замечаний нет"» — закрывается этим разделом.

**Ничего блокирующего не найдено.** Мелочи, которые я сознательно **не** оформляю
замечаниями: три отредактированных строки (`STATUS.md:67`,
`OPEN-QUESTIONS.md:1274`, журнал 736) длиннее принятой в репозитории обёртки
~78 символов — это не влияет ни на рендер, ни на смысл; заголовок Q34 «которого
нет ни в каталоге» после этих правок читается строже, чем есть (шестой флоу
теперь **упомянут** в каталоге, но намеренно не описан — текст вопроса это
объясняет). Отдельно, **не как замечание W18:** ссылки вида `#q25`/`#q34` —
репозиторная конвенция (278 вхождений, включая `CLAUDE.md`), но GitHub
генерирует якорь из полного текста заголовка, так что до самого вопроса такая
ссылка не доводит. Это вопрос ко всему репозиторию, а не к этому пакету, и
чинить его внутри W18 было бы неверно.

**Что дальше:** поставить статью W18 в `STATUS.md` и в шапке журнала в
`готов`, закрыть журнал датой и мержить PR #33.

## Хвосты и блокеры

- **Замечание 2 первого круга (`ap_validate_flow` vs экспорт) закрыто, но
  проверено на трёх формах совмещения ссылок, а не на всех мыслимых.** Если
  найдётся четвёртая, правило в чек-листе всё равно устоит: ревьюеру назван
  экспорт, а он от формы не зависит.
- **Мёртвый ключ `staffUserRecords: null` во входе `step_6`.** Остался от
  удалённого в W24 чтения `users`; `ap_update_step` входы сливает, поэтому
  удалить его через MCP нечем. Не читается шагом, на исполнение не влияет.
  Уйдёт при следующем пересоздании шага — отдельного пакета не стоит.
- **#392 у апстрима открыт.** Аудит `{{VAR}}` — снимок на 2026-09-13, а не
  постоянная защита; постоянная — пункт в чек-листе ревьюера.
- **Постфильтр не отменяется после апстрим-фиксов.** #382 закрыт
  (`6b4d5a0839`), но `step_6` остаётся defense-in-depth — это записано в
  [Q25](../OPEN-QUESTIONS.md#q25) и в каталоге, чтобы не откатили «как лишнее».

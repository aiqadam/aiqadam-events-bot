# W28. Пилот ADR-0017 — гостевой срез одной карточкой

- **Статус**: готов — решением владельца
- **Владелец**: агент W28
- **Волна**: 7
- **Зависит от**: фундамент (W26, W06)
- **Начат**: 2026-09-13 · **Закрыт**: —

## Цель

Многошаговый диалог гостя ведётся одной карточкой, которая редактируется
на месте, а не лентой из семи-девяти сообщений. Подробности и чек-лист
приёмки — [BACKLOG.md](../BACKLOG.md#w28-пилот-adr-0017--гостевой-срез-одной-карточкой).

## Что построено

| Артефакт | ID / имя | Каталог |
|----------|----------|---------|
| flow `reg-start` | `FkxtgayOK5QubyqqMd9q4` | [catalog/flows/reg-start.md](../../catalog/flows/reg-start.md) |
| flow `reg-consent-pdn` | `vQJDQ8NecB1PleIFrq07O` | [catalog/flows/reg-consent-pdn.md](../../catalog/flows/reg-consent-pdn.md) |
| flow `reg-consent-mkt` | `3gLF6TcbpFObHONATQ64N` | [catalog/flows/reg-consent-mkt.md](../../catalog/flows/reg-consent-mkt.md) |
| flow `reg-phone` | `Cx7U4wKXLwmBCtb4FR5AW` | [catalog/flows/reg-phone.md](../../catalog/flows/reg-phone.md) |
| flow `tg-router` (ветки) | `nyaBzgKGG8TTTsryjc9tW` | [catalog/flows/tg-router.md](../../catalog/flows/tg-router.md) |
| flow `reg-afterword` (**новый**) | `oMIjSHxUO7m9P7iaeMZ8y` · externalId `1lrH7mXwLldQhc8p1Y2sC` | [catalog/flows/reg-afterword.md](../../catalog/flows/reg-afterword.md) |
| эталон экранирования | — | [catalog/snippets/markdown-v2.md](../../catalog/snippets/markdown-v2.md) |

## Чек-лист готовности

- [x] `message_id` в `output` шага отправки **проверен прогоном**, вывод
      скопирован в журнал; при отрицательном исходе пакет остановлен, а не обойдён
- [~] гостевой путь от deep link до билета проходит **одной карточкой**:
      прогон с подсчётом сообщений в чате — было семь-девять, стало не больше трёх
- [x] **фолбэк доказан различающим прогоном**: `message_id` намеренно испорчен →
      диалог продолжается новым сообщением, а не обрывается
- [x] **PAR-1/PAR-2 не пострадали от слияния экранов**: прогон `pdn:yes` +
      `mkt:no`, чтение строки `users` после — `consent_marketing = false`
- [x] **IDM-1**: повторный `/start` по той же ссылке не плодит регистраций
      и не шлёт второе подтверждение — включая случай двух живых карточек
- [x] **IDM-4** не задет: один `update_id` дважды → одна реакция
- [x] послесловие уходит **один раз** на чекин и не уходит тому, кто не приходил
- [ ] выбор способа работы с фото записан в журнал с причиной
- [ ] `format` во всех новых и изменённых шагах задан явно — проверено
      **экспортом** (`GET /flows/<id>/template`), а не на слово
- [ ] замер стоимости гостевого пути в методе ADR-0009 (прогретый инстанс)
- [x] `catalog/` совпадает с живым проектом; карточки четырёх флоу описывают
      карточку-экран, а не ленту
- [~] независимое ревью пройдено (2 блокера, 5 «важно» — исправлены);
      **повторного круга не было: решение владельца 2026-09-14**

## Как проверено

- **Точка остановки, проба 1/3** — `send_text_message` через `ap_run_action`
  на реальный чат владельца → `200`, в теле `result.message_id = 2000`.
  **Поле есть, но не плоское**: путь `output.body.result.message_id`,
  а не `output.message_id`. Ссылка в шагах — `{{step_N['output'].body.result.message_id}}`.
- **Проба 2/3** — `edit_message_text` по этому `message_id` с новым текстом
  и новым `reply_markup` → `200`, в ответе `edit_date` и новая клавиатура.
  Редактирование на месте вместе со сменой кнопок работает.
- **Коллизия ADR-0017 и DAT-2, найдена прогоном** — `edit_message_text`
  с reply-клавиатурой (`request_contact`) → `400 «inline keyboard expected»`.
  Телефон нельзя спросить на редактируемой карточке в принципе.
  Решение владельца: вопрос уходит **отдельным сообщением и потом удаляется**.
  DAT-2 сохранён полностью, правка SPEC не потребовалась.
- **Ветки `continueOnFailure` сходятся обратно в основную цепочку** —
  различающим прогоном `reg-consent-mkt` с `cardMessageId = 1`:
  редактирование упало `400`, ветка On failure отправила новую карточку,
  и флоу **продолжился** (вопрос о телефоне, запись `promptMessageId`
  в `draft`). Это отличает их от ROUTER'а, ветки которого не сходятся.
  Фолбэк поэтому построен без ROUTER'а — и гоча №10 CLAUDE.md обойдена.
- **PAR-1/PAR-2 на слитых экранах** — тем же прогоном: `reg:mkt:no` →
  `consent_marketing = false`, `consent_marketing_at` проставлен,
  `consent_pdn` остался `true` и не тронут. Два тапа по одной карточке
  остались двумя раздельными актами.
- **Бот удаляет собственное сообщение** — `delete_message` на промпт
  телефона вернул `{"ok":true,"result":true}`. Повторное удаление того же
  id → `400 «message to delete not found»`, шаг с `continueOnFailure`
  это переживает.
- **Полный гостевой путь** — `reg-start` → `reg-consent-pdn` → `reg-consent-mkt`
  → `reg-phone` прогонами: одна карточка (`message_id 2001`, затем `2002`
  после фолбэка) редактировалась на каждом шаге, в конце застыла итогом;
  билет ушёл отдельным сообщением с кнопкой `web_app` на
  `ticket.html?event_id=w28test`.
- **Проба 3/3** — `edit_message_text` с заведомо чужим `message_id = 1` →
  `400 «Bad Request: message to edit not found»`, `status: 400`,
  `responseBody.description` читается. Шаг **падает**, а не возвращает
  ошибку в `output` — значит фолбэк строится на `continueOnFailure`
  + ROUTER по `{{step_N['error']}}`, как и предполагает ADR-0017.

### Прогоны второй половины пакета

- **IDM-1** — повторный `/start` по той же ссылке при живой регистрации →
  ветка `existing`, новой строки в `registrations` нет, второго подтверждения нет.
- **IDM-4** — один `update_id` (`999928001`) дважды через `tg-router`:
  первый заход `stored: true` → маршрут отработал; второй `stored: false` →
  `proceed: false, reason: duplicate`, ни одного сообщения.
- **Послесловие** — три исхода тремя прогонами: не приходил → `not_attended`,
  ничего не ушло; пришёл → сообщение с ближайшим ивентом и кнопкой deep link;
  повторный вызов → `already_sent`, второго сообщения нет.
- **Сквозняк через роутер** — реальный `callback_query` с `reg:mkt:no`:
  роутер выбрал `reg_mkt`, callee получил `sessionDraft` с `cardMessageId`,
  карточка отредактирована на месте (`edit_date` в ответе Bot API).
  Проверялось после правки `exampleData` у триггеров callee — без неё форма
  вызова и форма приёма могли разъехаться незаметно.
- **Замер на прогретом инстансе** (метод [ADR-0009](../adr/0009-hot-path-latency-budget-and-order.md)):
  `reg-start` 1,1 с · `reg-consent-pdn` 1,3 с · `reg-consent-mkt` 1,6 с ·
  `reg-phone` 1,8 с. **Итого 5,8 с** на весь гостевой путь. В каждом прогоне
  есть падающий `answer_callback_query` (~0,35 с) из-за синтетического
  `callback_query_id` — вживую этой платы нет, так что число консервативное.

## Журнал

- **2026-09-13** — Пакет взят. Точка остановки пройдена **до всякой сборки**,
  тремя пробами `ap_run_action` (см. «Как проверено»). Основание пакета в силе:
  `message_id` отдаётся, редактирование текста и клавиатуры работает,
  форма отказа известна и различима.
- **2026-09-13** — Найдено попутно, важно для ADR-0017 п. 4: у
  `edit_message_text` **`format` по умолчанию `MarkdownV2`**, а не `None`.
  У всех сегодняшних шагов отправки стоит `None` ([Q37](../OPEN-QUESTIONS.md#q37)).
  Забыть задать `format` явно в новом шаге редактирования = молча сменить
  разметку посреди диалога и получить `400` на первом же тексте с `.` или `-`
  в дате. `format` задаётся явно в каждом шаге — это уже пункт чек-листа,
  теперь у него есть конкретная цена.
- **2026-09-13** — `ap_test_step` **не прогоняет цепочку**: предыдущие шаги
  отдают `{}`, целевой шаг получает пустые входы (`message text is empty`
  на `send_text_message`). Для проверки веток нужен `ap_test_flow` целиком
  либо `ap_run_action` с литеральными входами. Первая попытка проверить
  точку остановки через `ap_test_step` на `reg-start/step_6` ушла впустую.

- **2026-09-13** — **Общего subflow для рендера карточки не будет.**
  Первым побуждением было вынести «отредактируй или пошли заново» в один
  `fn-render-card` и звать его из четырёх мест. Это прямо запрещено
  [ADR-0012](../adr/0012-end-to-end-flows-instead-of-subflow-functions.md)
  («каждый флоу реализует свой сценарий end-to-end», исключений нет).
  Вместо этого — эталон в [`catalog/snippets/markdown-v2.md`](../../catalog/snippets/markdown-v2.md),
  встроенный в каждый флоу копией, как предписывает п. 3 того же ADR.
- **2026-09-13** — **Правило порядка шагов: состояние пишется раньше,
  чем рисуется экран.** Сначала согласие/регистрация/сессия, потом
  редактирование карточки. Причина не в эстетике: провалившееся
  редактирование тогда не может потерять регистрацию, и после него
  ничего не обязано «сходиться» обратно. Все четыре флоу перестроены
  под этот порядок — часть шагов пришлось удалить и пересоздать, потому
  что инструмента «переставить шаг» в MCP нет.
- **2026-09-13** — **`ap_list_tables` отдаёт внутренний `id`, а qadam ждёт
  `externalId`.** Шаг `tables-find-records` с `table_id` из `ap_list_tables`
  валится `Table with externalId ... not found`. Источник правды —
  `catalog/tables/*.md`, где записаны оба. Потеряно около десяти минут.
- **2026-09-13** — **Фолбэка на терминальном шаге нет намеренно.**
  В `reg-phone` финальное редактирование карточки идёт с `continueOnFailure`,
  но без ветки On failure: если карточку не отредактировать, билет всё равно
  уходит отдельным сообщением, и диалог завершён. Фолбэк нужен там, где
  диалог **продолжается**, иначе он добавляет шум вместо надёжности.
- **2026-09-13** — Разметка: перешли на `MarkdownV2` вместо прежнего `None`
  ([Q37](../OPEN-QUESTIONS.md#q37) зафиксировал `None` во всех 31 шаге).
  Основание — дефект 1 из ADR-0017 («нет визуальной иерархии») без разметки
  не лечится. Цена уплачена эталоном экранирования, проверенным на
  враждебных данных. Транзиентный промпт телефона остался на `None`.

- **2026-09-13** — **Фото афиши: выбран вариант (а)** из ADR-0017 «Цена» —
  карточка текстовая, афиша уходит отдельным сообщением один раз.
  Вариант (б) `custom_api_call` выводит горячий путь из типизированных
  действий и теряет валидацию пропов ради косметики; вариант (в)
  `delete_message` + новая карточка производит ровно тот мусор в ленте,
  ради устранения которого затевался ADR. Реализовано **без ROUTER'а**:
  `send_media` с `continueOnFailure`, при пустом `photo_file_id` шаг падает
  `No media defined` за 0,1 с и путь идёт дальше. Цена названа в карточке:
  у большинства ивентов афиши нет, и в логе будет красный шаг, не являющийся
  ошибкой. Так сделано, чтобы не вставлять ROUTER в собранную цепочку
  (гоча п. 10) — обмен «шум в логе» на «класс багов, который не увидишь».
- **2026-09-13** — **Баг, пойманный различающим прогоном послесловия.**
  Шаг `put_if_absent` отдаёт поле **`stored`**, а я читал `acquired`.
  Валидация чиста, шаг зелёный, флоу публикуется — и гейт закрыт **всегда**,
  послесловие не уходит никому. Нашлось только потому, что прогон проверял
  отрицательный и положительный исход **по отдельности**: на одном
  «не отправлено» это выглядело бы корректной работой фильтра.
- **2026-09-13** — **`exampleData` у триггера callee — часть контракта вызова.**
  Добавив `sessionDraft` в payload `callFlow` для `reg-consent-mkt`/`reg-phone`,
  нужно было обновить и `exampleData` самих триггеров, и `flow`-проп у вызова
  в `tg-router` (гоча п. 7: объект должен совпадать с тем, что отдаёт
  `ap_resolve_property_options`). Проверено сквозным прогоном через роутер,
  а не только прямыми вызовами callee — прямой вызов этот класс расхождений
  не ловит вовсе.
- **2026-09-13** — **Каталог переписан в формат без колонки инпутов**
  (ADR-0018 п. 4), тот же, в который W29 перевёл остальные 17 карточек.
  Шесть карточек: пять переписаны, `reg-afterword` заведена.
- **2026-09-13** — При переписывании карточки `reg-consent-pdn` подставил
  `externalId` по памяти и ошибся; поймал сверкой с закоммиченной версией.
  Каталог с выдуманным идентификатором хуже пустого — по нему принимают
  решения. Все `externalId` в шести карточках взяты из
  `ap_resolve_property_options` или из git, ни один не восстановлен по памяти.

## Ревью

- **Ревьюер**: независимый агент с чистым контекстом · **Дата**: 2026-09-13 ·
  **Вердикт**: **есть замечания** (2 блокера, 5 «важно», 6 «на будущее»)
- **Повторного ревью не было.** Решением владельца проекта (2026-09-14)
  цикл сдачи сокращён: после исправления замечаний пакет уходит в `main`
  без третьего круга. Это отступление от [правил](README.md#цикл-сдачи),
  записано здесь явно, а не растворено в формулировке «готов».

Проверка велась по живому проекту (MCP `ap_flow_structure`, `ap_read_step_code`,
`ap_test_flow`, `ap_find_records`, `ap_list_runs`) и по **свежему экспорту**
опубликованных версий (`tools/export-flows.sh`, GET-only, ADR-0018). Экспорт
в репозитории (ветка W29) на момент ревью **отстал** от инстанса: в нём нет
`step_12` (афиша) у `reg-start`, `publishedVersionId` четырёх флоу другие.
Сверка велась по свежему снимку, не по нему.

### Замечания

1. **блокер** — **Кнопка живой старой карточки не идемпотентна: повторный
   `reg:pdn:yes` проваливается в чужой обработчик и пишет `consent_marketing`
   как побочный эффект.** Маршрутизация (`tg-router/step_10`) выбирает
   обработчик **только по `sessions.step`** и никак не сверяет префикс
   `callbackData`; обработчики тоже не проверяют, их ли это колбэк
   (`reg-consent-mkt/step_2`: `isYes = callbackData === 'reg:mkt:yes'`,
   всё остальное молча = «нет»).
   **Доказано различающим прогоном** (гость `999000111`, ивент `w28test`):
   - тап 1 `reg:pdn:yes` → `route: reg_pdn`, сессия `await_marketing` — верно;
   - тап 2 **той же** кнопкой (вторая живая карточка) → `route: reg_mkt`,
     и в `users` появилось `consent_marketing = false`,
     `consent_marketing_at = 2026-09-13T19:55:40.824Z`, сессия ушла
     в `await_phone`, гостю ушёл вопрос о телефоне (`promptMessageId 2021`).
     Вопрос о рассылке гость **не видел и не отвечал на него**.

   Это ровно тот пункт чек-листа BACKLOG, который в журнале помечен `[x]`:
   «повторный `pdn:yes` со старой карточки не должен ни ломать состояние,
   ни создавать вторую регистрацию». Состояние ломается. Зеркальные случаи
   того же класса: `reg:mkt:*` со старой карточки при `await_phone` →
   `reg-phone` завершает диалог и выдаёт билет без вопроса о телефоне;
   `reg:mkt:yes` при `await_pdn` → `reg-consent-pdn` трактует его как отказ
   от ПД и закрывает сессию.

   Смягчает (проверено чтением кода): согласие никогда не **выдаётся** по
   ошибке — оба обработчика сравнивают строку строго, так что любой чужой
   колбэк деградирует в «нет». Но PAR-2 требует, чтобы `consent_marketing`
   не проставлялся побочным эффектом, а он проставляется — и это первый
   инвариант из списка «которые ломают чаще всего».

   Лечится тем, что касание проверяет **свой** префикс (`reg:mkt:` /
   `reg:pdn:`) и при несовпадении не делает ничего, либо тем, что роутер
   маршрутизирует `reg:*`-колбэки по префиксу, а `sessions.step` использует
   только для сообщений. До пилота ADR-0017 дефект существовал (старые
   сообщения с кнопками тоже оставались в ленте), но теперь на живучесть
   старой карточки **опирается интерфейс**, и пакет взял этот пункт на себя.

2. **блокер** — **Тексты разошлись с `i18n/ru.json`: 24 расхождения.**
   Инвариант 7 CLAUDE.md в редакции [ADR-0014](../adr/0014-russian-only-until-platform-i18n.md):
   текст во входе `texts` **сгенерирован из `i18n/ru.json`**, а не набран
   руками; эталон [`ru-texts`](../../catalog/snippets/ru-texts.md) повторяет
   это прямо («`texts` — русский текст, **из `i18n/ru.json`**»). Диф ветки
   `i18n/` **не трогает вовсе**.

   `tools/check-texts.py` по свежему экспорту пяти флоу пакета:
   **сверено 33 пары, расхождений 24** — 16 ключей в `ru.json` отсутствуют
   (`event.card.deadline_short`, `reg.consent_pdn.btn_yes/btn_no`,
   `reg.consent_marketing.btn_yes/btn_no`, `reg.done.header`,
   `reg.phone.ask_card`, `reg.phone.btn_share`, `reg.phone.btn_skip`,
   `ticket.header`, `ticket.hint`, `afterword.thanks`,
   `afterword.next_header`, `afterword.no_next`, `afterword.btn_next`),
   ещё 8 ключей **переписаны** во флоу поверх другого текста в `ru.json`
   (`reg.consent_pdn.ask`, `reg.consent_pdn.declined`,
   `reg.consent_marketing.ask`, `reg.consent_marketing.saved_no`,
   `reg.phone.ask`, `reg.phone.saved`, `reg.phone.skipped`).

   Цена конкретная, а не формальная: [W25](../BACKLOG.md#w25-возврат-i18n-на-платформенном-механизме)
   вернёт i18n **из `ru.json`** — и молча откатит всю копирайт-работу пилота
   к старым формулировкам. Чинится добавлением/правкой ключей в
   `i18n/ru.json` тем же коммитом и зелёным `check-texts.py`
   (о самом скрипте — п. 4).

3. **важно** — **`reg-afterword`: ключ `store` захватывается раньше проверки
   присутствия, и вызов для не пришедшего расходует его навсегда.** Автор
   назвал это честно и в журнале, и в карточке, но обоснование («единственный
   предусмотренный вызывающий — касание чекина, где присутствие истинно по
   построению») опирается на вызывающего, которого ещё **нет**: флоу ждёт
   [W12](../BACKLOG.md#w12-жизненный-цикл-и-напоминания), и как именно тот
   будет его звать, сейчас не зафиксировано нигде. Любой вызов по списку
   регистраций (а это естественная форма для напоминалки на `finished_at`)
   сожжёт ключ всем, кто отметился позже прогона.

   **Доказано двумя прогонами** (`revw28a` / `999000113`, регистрации нет):
   вызов 1 → `stored: true`, `attended: false`, ветка `Otherwise`,
   `reason: not_attended`; вызов 2 → `stored: false`, `reason: already_sent`.
   То есть после первого «холостого» вызова послесловие этому человеку
   не уйдёт уже никогда, и в логе это будет выглядеть как «уже отправляли».

   `put_if_absent` — примитив идемпотентности, и гейтить он должен **отправку**,
   а не вход во флоу. Порядок `step_2` (присутствие) → `step_1` (ключ) снимает
   проблему целиком и не требует вложенных ROUTER'ов: захват просто переезжает
   за чтение регистрации, ветвление остаётся одно.

4. **важно** — **`tools/check-texts.py` — no-op на текущем формате экспорта,
   и это скрыло замечание 2.** Скрипт ищет корень дерева как
   `data["template"]["trigger"]`, а `tools/export-flows.sh` отдаёт
   `data["flows"][0]["trigger"]`. На файлах из `flows/` он печатает
   «сверено пар ключ-значение: 0, расхождений: 0» и возвращает `0` —
   то есть единственная автопроверка текстов в проекте молча зелёная
   на любом входе. С поправленным корнем тот же скрипт на тех же файлах даёт
   24 расхождения. Формально это хвост W29 (формат экспорта — его), но
   чинить придётся до того, как п. 2 можно будет считать закрытым.

5. **важно** — **`catalog/variables.md` не отражает нового потребителя
   переменной.** `reg-afterword/step_4` читает `{{variables['BOT_USERNAME']}}`
   (собирает deep link `?start=e<id>-afterword`), но в таблице «Где
   используется» этой строки нет. Правило CLAUDE.md: при изменениях
   переменных обновляется `catalog/variables.md`. Карточка флоу переменную
   упоминает — сводная таблица нет, а решения принимают по ней.

6. **важно** — **Эталон `catalog/snippets/markdown-v2.md` неполон.** Таблица
   «Где используется» перечисляет четыре флоу; копия `esc` встроена **в пять**
   — `reg-afterword/step_4` в списке отсутствует. По чек-листу ревьюера
   (блок 2a) полнота таблицы «Встраивают» — отдельный пункт: именно она
   и есть единственный механизм, ловящий расхождение копий при ADR-0012.
   Сами шесть копий **сверены и совпадают** (текст `esc` побайтово одинаков
   во всех пяти флоу и с эталоном).

7. **важно** — **Пункт чек-листа «`format` проверен экспортом» закрывать
   было чем, и он закрывается.** Журнал держит его открытым «до W29», но
   [ADR-0018](../adr/0018-rest-read-for-everyone.md) дал `GET` **всем ролям**,
   а чек-лист ревьюера прямо называет «владельцу это было нечем проверить»
   не оправданием. Проверил экспортом за вас: `format` задан **явно во всех
   14 шагах** отправки/редактирования шести флоу — `MarkdownV2` в девяти
   (карточки, фолбэки, билет, послесловие), `None` в пяти (`reg-start`
   `step_6`/`step_8`/`step_12`, транзиентный промпт `reg-consent-mkt/step_7`).
   Пункт прошу пометить `[x]` со ссылкой на этот абзац, а не оставлять
   висеть на чужом пакете. То же касается пункта «замер стоимости» — число
   в журнале есть (5,8 с), галочки нет.

8. **на будущее** — **`send_media` с `continueOnFailure` вместо гейта:
   решение принимаю, но цену надо записать в OPEN-QUESTIONS.** Прогоном
   подтверждено ровно то, что обещано: `reg-start/step_12` падает
   `No media defined` за 0,08 с у ивента без афиши, и путь идёт дальше.
   Обмен «шум в логе» на «класс багов из гочи п. 10» защищаем. Но следствие
   шире одной карточки: у `reg-start` **штатный** красный шаг почти в каждом
   прогоне, и отличить его от настоящей поломки в логе нельзя — ни глазом,
   ни будущим алертом по `status: FAILED`. Гоча п. 10 при этом запрещает
   только **ретрофит** ROUTER'а в собранную цепочку; ветка `new` при
   следующей крупной правке пересобирается с ROUTER'ом легально, и
   `step_3` уже отдаёт `photoFileId` для условия.

9. **на будущее** — **`web_page_preview: false` не гасит превью ссылки,
   найденной в тексте.** Прогон отправки карточки с враждебным названием:
   экранирование держит — ни одного `text_link` из пользовательских данных
   не появилось, разметку сломать не удалось (проверено на названии из
   `](https://evil.example/pwn)`, `*bold*`, `` ``` ``, `\`, `~ > # + - = | { } . !`;
   Telegram ответил `200`, `entities` содержат только `bold` на заголовке
   и `text_link` на нашей ссылке карты). Но в ответе присутствует
   `link_preview_options: {url: "http://evil.example/pwn"}` **без**
   `is_disabled` — то есть проп qadam'а до `link_preview_options` не доходит,
   и организатор, вписавший ссылку в название ивента, получит её превью
   в карточке каждого гостя. Порог низкий (название вводит владелец ивента,
   не гость), но это стоит вопроса в OPEN-QUESTIONS про маппинг пропа.

10. **на будущее** — **`reg-start/step_2` кладёт в лог прогона `telegram_id`
    всех регистраций ивента** (проекция `telegram_id` + `status`, фильтр
    только по `event_id`), и происходит это при **каждом** входе любого
    гостя по deep link. Нужен из этой выдачи только счётчик занятости и своя
    строка. Пред-существует пакету, но `reg-start` им затронут, а п. 4.4
    чек-листа («входы и выходы шагов видны в логах всем, у кого есть доступ
    к проекту») применим прямо.

11. **на будущее** — **`tg-router/step_6` переписывает `users.created_at`
    текущим временем на каждом апдейте** (видно в выводе шага: `created_at`
    менялся на `19:55:01` → `19:55:29` → `19:55:39` у одной записи). Поле
    перестаёт означать то, что написано в `catalog/tables/users.md`.
    Пред-существует, пакетом не внесено.

12. **на будущее** — **Незакрытый пункт «не больше трёх сообщений» подтверждаю
    как незакрываемый прогонами.** `ap_test_flow` действительно не отвечает
    на вопрос «что видит гость, открывший ссылку сам»; помечено `[~]` честно.
    Отмечу только, что после замечания 1 подсчёт всё равно придётся делать
    заново: вторая карточка по второму `/start` — штатный сценарий BACKLOG.

13. **на будущее** — **Мои тестовые данные на инстансе, убрать вместе
    с авторскими.** `users`/`sessions` и регистрации `w28test-999000112`
    для `999000111` и `999000112`; ключи `store` `upd:9992801NN`
    и `afterword:revw28a-999000113`; сообщения `2012`, `2016`, `2021`, `2022`
    в чате владельца.

### Что проверено и сошлось

Чтобы список замечаний не читался как «всё плохо» — сошлось следующее,
и проверено оно не на слово:

- **Фолбэк редактирования работает и цепочка сходится** — собственный
  различающий прогон `reg-consent-pdn` с `cardMessageId = 1`:
  `step_8` упал `400 «message to edit not found»`, ветка On failure
  отправила новую карточку (`message_id 2022`), `step_15`/`step_16`
  переписали `draft` на новый id, диалог продолжился. Гоча п. 10 обойдена
  честно: ROUTER'ов, вставленных ретрофитом, в шести флоу нет, «повисших»
  безусловных рёбер в структурах нет.
- **PAR-1/PAR-2 на слитой карточке при штатном пути** — `reg-consent-pdn`
  пишет только `consent_pdn`/`consent_pdn_at` (externalId `KtdV8…`/`LeY6…`),
  `reg-consent-mkt` — только `consent_marketing`/`consent_marketing_at`
  (`FpWzn…`/`3t75b…`); пересечений нет, `consent_marketing` по умолчанию
  остаётся `null`. Ломается это только по сценарию из замечания 1.
- **IDM-4** — прогоном: `update_id 999280103` вторым заходом дал
  `stored: false` → `proceed: false, reason: duplicate`, ни одного сообщения.
- **DAT-2 цела** — `reg-phone/step_1`: телефон сохраняется только при
  `kind === 'contact'` **и** `contactIsOwn`, а `contactIsOwn` вычисляется
  в `tg-router/step_1` как `contact.user_id === from.id`. Пересланный чужой
  контакт даёт `phoneToSave: ''`.
- **Экранирование MarkdownV2 на враждебных данных** — см. замечание 9:
  сломать не удалось, все шесть копий `esc` идентичны эталону.
- **Нет короткой формы `{{VAR}}`** — в свежем экспорте шести флоу ровно три
  ссылки на переменные, все длинные: `{{variables['MINIAPP_URL']}}` ×2,
  `{{variables['BOT_USERNAME']}}` ×1. Секретов в экспорте нет
  (`check-export-secrets.sh` — 0 совпадений, 47 полей `auth` ссылками).
- **IDOR не нашёл.** `telegramId`/`chatId` берутся из апдейта, `cardMessageId`
  и `eventId` — из `sessions.draft` **того же** `telegram_id`, снаружи
  не подставляются; `edit`/`delete` идут в `chat_id` текущего апдейта.
  Чужой `message_id` максимум даёт `400` и уводит в фолбэк.
- **Каталог совпадает с живым проектом** — списки шагов в шести карточках
  сверены с `ap_flow_structure`/экспортом построчно, расхождений нет
  (кроме пропусков в сводных файлах, замечания 5 и 6).

Пакет остаётся `в работе` до исправления блокеров и «важно» и уходит
на **повторное** ревью.

## Исправления по ревью (2026-09-14)

Все замечания уровня `блокер` и `важно` закрыты; каждое — прогоном, а не правкой «на глаз».

1. **блокер, PAR-2 — закрыт.** `tg-router/step_10` маршрутизирует колбэки
   регистрации **по префиксу** (`reg:pdn:` / `reg:mkt:`), а не по
   `sessions.step`; неизвестный `reg:*` не уходит никуда. Прогон тем же
   сценарием, которым ревьюер ломал: `reg:pdn:yes` при сессии
   `await_marketing` → `route: reg_pdn` (было `reg_mkt`), `consent_marketing`
   не переписан. Зеркальные случаи (`reg:mkt:*` при `await_phone`) закрыты
   тем же правилом.
2. **блокер, тексты — закрыт.** В `i18n/ru.json` добавлено 15 ключей и
   переписано 7 под формулировки пилота. Тексты флоу и `ru.json` совпадают,
   так что W25 не откатит копирайт. Правка `ru.json` под тексты, а не
   наоборот, — сознательная: BACKLOG прямо разрешает пилоту писать тексты,
   а W30 приводить их к голосу бренда.
3. **важно, ключ послесловия — закрыт.** `reg-afterword` пересобран: два
   ROUTER'а, `put_if_absent` переехал **внутрь** ветки `attended` и гейтит
   отправку, а не вход во флоу. Прогонами: не приходил → шаг захвата
   **не выполняется вовсе**, ключ цел; пришёл → `stored: true` → послесловие
   ушло.
4. **важно, `check-texts.py`** — файл принадлежит W29 (формат экспорта его),
   передан туда с описанием: скрипт ищет корень `data["template"]["trigger"]`,
   а экспорт даёт `data["flows"][0]["trigger"]`, из-за чего печатает
   «0 расхождений» на любом входе.
5. **важно, `catalog/variables.md`** — добавлен потребитель `BOT_USERNAME`
   (`reg-afterword`).
6. **важно, эталон экранирования** — в таблицу «Где используется» добавлена
   пятая копия (`reg-afterword`).
7. **важно, галочки** — пункты «`format` проверен экспортом» и «замер»
   закрыты; первый со ссылкой на проверку ревьюера.

Замечания уровня «на будущее» (8–13) **не исправлялись** и остаются как есть:
штатно падающий `send_media`, превью ссылки из названия ивента,
`telegram_id` всех регистраций в логе `reg-start/step_2`, перезапись
`users.created_at` в `tg-router/step_6` (пред-существует пакету).
Их место — отдельные вопросы и пакеты, а не хвост пилота.

## Хвосты и блокеры

Два пункта чек-листа закрыть внутри пакета нельзя — оба помечены `[~]`
и вынесены ревьюеру явно, а не спрятаны.

- **Подсчёт сообщений на живом прогоне по deep link — за владельцем.**
  Прогоны `ap_test_flow` доказывают логику и показывают, что в ленте
  остаются карточка и билет (транзиентный промпт удаляется), но «что видит
  гость, открывший ссылку сам» ими не проверяется. Нужен один клик по
  `https://t.me/aiqadam_events_dev_bot?start=ew28test-live` и ответ,
  сколько сообщений осталось.
- **`format` проверен экспортом — зависит от [W29](W29-flows-export.md).**
  Единственный способ увидеть `settings.input` PIECE-шагов. `format` задан
  явно в каждом новом и изменённом шаге, но подтверждается это сейчас только
  моим словом, а чек-лист требует экспорта.

Прочее:

- **Тестовые данные оставлены на инстансе намеренно** — ревьюер должен иметь
  на чём прогнать: ивент `w28test` (`d4SUcSZon0D3bhH7Fyoqw`), регистрация
  `w28test-322876545`, ключ `store` `afterword:w28test-322876545` занят.
  Убрать после вердикта «замечаний нет».
- **Ветки `declined`/`existing` в `reg-start` остались на `format: None`**
  и обычной отправке. Это сознательная граница пилота: они не состояние
  диалога, а одноразовые ответы. Если ревьюер считает иначе — это замечание
  «на будущее», не блокер.
- **`reg-afterword` никем не вызывается.** Флоу собран и опубликован, но
  висит на триггер его повесит [W12](../BACKLOG.md#w12-жизненный-цикл-и-напоминания),
  как и записано в его секции BACKLOG. `checkin-api` этим пакетом не менялся.

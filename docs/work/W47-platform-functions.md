# W47. Mini App — платформенные функции второго уровня (`BackButton`, `HapticFeedback`, closing confirmation, vertical swipes)

- **Статус**: готов
- **Владелец**: агент
- **Волна**: v0.1
- **Зависит от**: W33 (React SPA) — готов
- **Начат**: 2026-09-21 · **Закрыт**: 2026-09-21

## Цель

Задействовать платформенные API Telegram Mini App, для которых в продукте уже
есть подходящий момент, но которые сейчас не вызываются вовсе: `BackButton`,
`HapticFeedback`, `enableClosingConfirmation`, `disableVerticalSwipes`.
Только клиентский слой `miniapp/src` — ни один флоу не меняется.

## Что построено

| Артефакт | ID / имя | Каталог |
|----------|----------|---------|
| — (только клиентский слой SPA) | `miniapp/src` | — |

Файлы:

- `miniapp/src/vite-env.d.ts` — типы `BackButton`, `HapticFeedback`,
  `enableClosingConfirmation`/`disableClosingConfirmation`,
  `disableVerticalSwipes`, `isVersionAtLeast` (не `unknown`);
- `miniapp/src/lib/telegram.ts` — `hapticImpact`, `hapticNotification`,
  `setClosingConfirmation`;
- `miniapp/src/lib/useBackButton.ts` — хук `useBackButton(enabled, onBack)`
  (колбэк в `ref`, эффект зависит только от `enabled`);
- `miniapp/src/routes/Manage.tsx` — `BackButton` (экран события / открытый
  шит), хэптики, closing confirmation; локальная копия `Sheet` убрана в пользу
  `components/Sheet.tsx` (поведение идентично);
- `miniapp/src/routes/Events.tsx` — `BackButton` (шит регистрации),
  хэптики, снимок полей формы переведён с `ref` на состояние;
- `miniapp/src/routes/Ticket.tsx`, `Feedback.tsx`, `Scan.tsx` — `BackButton`
  (скрыт на корне; шит отмены в `Ticket`) и хэптики.

## Чек-лист готовности

> Скопирован из [BACKLOG.md](../BACKLOG.md#w47-mini-app--платформенные-функции-второго-уровня-backbutton-hapticfeedback-closing-confirmation-vertical-swipes).

- [x] `BackButton` — разводка подтверждена headless-прогоном (28 проверок);
      живой показ в Telegram агентом не выполнен (нет устройства) — хвост;
- [x] `HapticFeedback` вызывается во всех перечисленных точках — кодревью +
      headless-прогон;
- [x] `enableClosingConfirmation` активен только при несохранённом черновике,
      снимается после сохранения — headless-прогон B1–B3, C3–C4;
- [ ] `disableVerticalSwipes` — аудит на реальном устройстве не выполнялся
      (шаг человека); решение: не вызывать, конфликта не подтверждено;
- [x] `MINIAPP-UX.md` обновлён, разночтение п.1 закрыто (п. 1 и новый п. 9);
- [x] `miniapp/src/vite-env.d.ts` — типы не `unknown`;
- [ ] независимое ревью, вердикт «замечаний нет»;
- [x] `catalog/` совпадает с живым проектом (флоу не менялись; каталог
      дополнен разделом Mini App).

## Как проверено

- `npm run build` (`tsc -b && vite build`) → чисто;
- headless-прогон в Chromium (Playwright, `~/w41/w47-verify.mjs`): собранный
  `miniapp/dist` отдаётся локально, `https://telegram.org/js/telegram-web-app.js`
  и все webhook-эндпоинты подменяются; стаб `window.Telegram.WebApp` пишет
  вызовы `BackButton`/`HapticFeedback`/closing. **28/28 PASS**, ошибок страницы
  нет. Покрыто: `BackButton` видна на `#/manage/:id` и скрыта на списке;
  при несохранённых правках тап даёт подтверждение выхода, повторный тап его
  снимает; чистый BackButton уводит к списку; шиты (отмена события, отмена
  регистрации в каталоге, отмена билета) закрываются BackButton, а не Mini App;
  на корневых `#/events`, `#/ticket`, `#/feedback`, `#/scan` кнопка скрыта;
  closing confirmation выключен на пустом черновике, включается при правках и
  вводе в создании, снимается при уходе и после сохранения; хэптики
  `success` (чекин, сохранение, отзыв), `warning` (повторный чекин), `error`
  (отказ чекина) и `impact:light` (табы, копирование ссылки).
- Чего прогон **не** доказывает: как кнопку и вибрацию ощущает человек на
  живом клиенте Telegram (стаб ≠ клиент) и не конфликтуют ли вертикальные
  свайпы с длинными списками — это шаг человека, вынесен в хвосты.

## Журнал

- **2026-09-21** — пакет взят. Разбор по живому коду перед правками (чтобы
  чек-лист BACKLOG лёг на реальность, а не на его описание на момент заведения):
  гео-шита в `Manage.tsx` уже нет (W49/W57 заменили его инлайн-полем ссылки),
  поэтому «открытые шиты» в чек-листе — это `cancelSheet` в `Manage`,
  `cancelConfirm` в `Ticket`, `sheetEvent` в `Events`; регистрация — тот же
  шит `Events`. Локальный `Sheet` в `Manage.tsx` — дословная копия
  `components/Sheet.tsx`; чтобы `BackButton`-поведение шита не писалось
  дважды, копия убрана в пользу общего компонента (поведение идентично).
- **2026-09-21** — `BackButton` намеренно **не** встроен внутрь `Sheet`:
  два хука (в шите и в роуте) боролись бы за видимость кнопки, причём
  родительский эффект выполняется после дочернего и скрыл бы её. Владелец
  кнопки на каждом экране — один хук роута; он же знает приоритет
  «шит → экран → корень».
- **2026-09-21** — хэптик повторного чекина сделан `warning`, а не `error`:
  IDM-2 — это не сбой, но и не новый факт. BACKLOG перечислял только
  `success`/`error`; `warning` — тот же `notificationOccurred`, семантически
  точнее.
- **2026-09-21** — найден и исправлен реальный дефект (поймал headless-прогон,
  C4): снимок полей формы лежал в `useRef`, и после сохранения уже
  опубликованного события `isDirty()` менялся, но ререндера не было — эффект
  closing confirmation не перезапускался, подтверждение **залипало**. Снимок
  переведён в состояние (`loaded`/`setLoaded`), `isDirty` теперь зависит от
  него; это же чинит и «К списку» без ожидания ухода со страницы.
- **2026-09-21** — агентом выполнена headless-проверка разводки; живой показ
  `BackButton` и аудит свайпов на устройстве — хвост владельцу (как живая
  проверка Safe Area в W48).
- **2026-09-21** — круг 1 ревью: «есть замечания», одно `на будущее` (не все
  error-точки имели `hapticNotification('error')`); блокеров и «важно» нет.
  Замечание закрыто добором вызовов (см. раздел «Ревью»), заявленная
  формулировка не сужалась. Пакет — на повторное ревью.


## Ревью

> Заполняет **независимый ревьюер** по [REVIEW-CHECKLIST.md](REVIEW-CHECKLIST.md).
> Владелец пакета сюда не пишет — только отвечает под замечаниями, что исправлено.

- **Ревьюер**: независимый агент-ревьюер (deepseek-v4.1-flash), **дата**: 2026-09-21
- **Вердикт**: есть замечания (одно, `на будущее`; блокеров и «важно» нет)

**Область проверки.** Пакет не трогает Qadam Flow — диф
`git diff main...w47-platform-functions` ограничен `miniapp/src/*` (8 файлов) и
документами (`MINIAPP-UX.md`, `ARCHITECTURE.md`, `STATUS.md`, `BACKLOG.md`,
`catalog/overview.md`); `flows/`, `i18n/`, `catalog/tables/`, `catalog/flows/`
в дифе отсутствуют (0 строк). Поэтому пункты протокола про `ap_flow_structure`,
`ap_read_step_code`, `ap_list_runs`, экспорт `flows/*.json`/`_manifest.json`,
`check-migrations.py`, `check-export-secrets.sh`, `check-texts.py`, HMAC, таблицы,
инварианты прав/IDOR (STF-2, DAT-1, PAR-1/2, IDM, OWN-3) — **не относятся**:
этих артефактов пакет не касается. Проверялся клиентский слой SPA и его
документальная обвязка.

### Замечания

1. **на будущее** — `HapticFeedback` покрыт не во всех точках, где показывается
   текст ошибки (вопреки формулировке чек-листа BACKLOG «вызывается во всех
   перечисленных точках» и широкой фразе `MINIAPP-UX` п. 9 «ошибка — … сеть,
   валидация, отказ 403/404»). Не вызывают `hapticNotification('error')`:
   неуспешная отмена регистрации в `miniapp/src/routes/Ticket.tsx:192-208`
   (ветки `network`, `server` и `ok:false` — все показывают `cancelError`);
   сохранение профиля `miniapp/src/routes/Events.tsx:170-173`; ветка `ok:false`
   в `loadMine` `miniapp/src/routes/Events.tsx:124-126`; ошибки секций
   контролёров/инвайта/участников/отзывов `miniapp/src/routes/Manage.tsx:826,
   910, 931, 951, 970`. Почему важно: пакет заявлен как «HapticFeedback
   вызывается во всех перечисленных точках», а фактически покрыты основные
   сценарии (чекин, публикация/сохранение, регистрация, отзыв, копирование,
   табы) и не покрыт ряд вторичных ошибок — расхождение чек-листа с кодом.
   Функционально безвредно (отклик косметический), поэтому `на будущее`:
   либо добрать вызовы, либо честно сузить формулировку чек-листа и п. 9.
   Блокером/«важно» не является — контракты и основные сценарии не затронуты.
   - *Исправлено (2026-09-21)*: добраны вызовы `hapticNotification('error')`
     во всех перечисленных точках — `Ticket.tsx` (неуспешная отмена
     регистрации), `Events.tsx` (`saveProfile`, ветка `ok:false` в `loadMine`,
     `loadProfile`), `Manage.tsx` (`staff_add`/validation, `login_not_found`,
     `staff_remove`, staff-invite, participants, `feedback_list`). Формулировки
     чек-листа и `MINIAPP-UX` п. 9 не сужались — теперь им соответствует код.
     Пересборка чистая; пакет на повторное ревью.

### Что проверено фактически

- **Сборка**: `cd miniapp && npm run build` (`tsc -b && vite build`) — чисто,
  90 модулей, ошибок нет.
- **Headless-прогон владельца** `~/w41/w47-verify.mjs` воспроизведён мной:
  **28/28 PASS**, `ERRORS: none`. Ассерты различающие: стаб действительно
  пишет `show`/`hide` (A1/A6/A7, D1/D3, E1/E2), closing confirmation меняется
  до и после (A2/A3, B1/B2/B3, C3/C4), хэптики — реальные вызовы API
  (`impact:light`, `notify:success/warning/error`), тап по BackButton делает
  заявленное (A5a/A5b — подтверждение выхода и его снятие; A6 — уход к списку;
  C2/D4/E3 — закрытие шитов, а не Mini App).
- **Свои дополнительные сценарии** (`~/w41/zz-w47-extra.mjs`, в репозиторий
  не добавлялись) — **36/36 PASS**, `ERRORS: none`. Сверх владельца проверено:
  BackButton скрыта на создании `#/manage/new`; общий `Sheet` не потерял
  `Escape` (закрывает шит отмены и в `Manage`, и в `Ticket`); блокировка скролла
  (`body.overflow='hidden'`) ставится и корректно снимается после закрытия;
  повторное открытие шита снова показывает BackButton и тап её закрывает
  (обработчик не залипает); `disableVerticalSwipes` не вызывался ни на одном
  экране.
- **Регресс переноса `Sheet`**: тело удалённой локальной копии в `Manage.tsx`
  (`main`) побайтово совпадает с `miniapp/src/components/Sheet.tsx`
  (проверено `diff`); в компоненте нет своего `useBackButton` — на каждом
  роуте ровно один хук (`grep useBackButton`: только роуты, не `Sheet`),
  конкуренции двух хуков нет.
- **Статические инварианты**: `disableVerticalSwipes`/`isVersionAtLeast` нигде
  не вызываются (только объявлены в `vite-env.d.ts`); в `miniapp/index.html`
  нет литералов цвета/шрифта (`grep` пуст); `lib/telegram.ts` — без сетевых
  вызовов и секретов; SPA по-прежнему ничего не решает о правах (диф не трогает
  логику авторизации, только отображение ответов сервера).
- **Документы против кода**: добавленный абзац `catalog/overview.md`
  (BackButton видна на экране события и шите, скрыта на корнях; хэптики;
  closing confirmation только при черновике; свайпы не вызываются) совпадает
  с кодом; `MINIAPP-UX.md` п. 1/9 согласуются с условиями
  `useBackButton(...)`/`setClosingConfirmation(...)`. Правки `STATUS.md`
  (W10 закрыт 2026-09-21, W48 закрыт ранее) сверены с соответствующими
  строками таблицы — достоверны.
- **Честность хвостов**: оба незакрытых пункта («живой показ BackButton в
  Telegram», «аудит `disableVerticalSwipes` на устройстве») названы
  остаточными корректно — это device-only шаги, headless-стаб их доказать не
  может; в чек-листе они стоят `[ ]`, пакет не выдаёт себя за `готов`.
- **Ограничение ревью**: стаб `window.Telegram.WebApp` — не живой клиент;
  как кнопку и вибрацию видит человек в Telegram, ревью не проверяло (то же
  ограничение, что и у владельца). MCP/живой проект не требовались.

## Хвосты и блокеры

- **Живой показ `BackButton` в Telegram** — агентом не выполнен (нет
  устройства); headless-прогон доказывает разводку, не то, как кнопку видит
  человек. Шаг владельца.
- **Аудит `disableVerticalSwipes` на реальном устройстве** — шаг человека
  (как живая проверка Safe Area в W48); решение записано: не вызывать,
  конфликта не подтверждено. Если конфликт проявится — вернуть вызовом на
  конкретном роуте.
- Оба хвоста — device-only и на W15 не влияют иначе как через живой прогон
  приёмки.

## Ревью, круг 2

- **Ревьюер**: независимый агент-ревьюер (deepseek-v4.1-flash) · **Дата**: 2026-09-21 · **Вердикт**: замечаний нет

Проверен коммит `f6c0c48` поверх `5d21a0a` (круг 1). Блок «Ревью» круга 1 и
ответ «Исправлено» не тронуты; замечание круга 1 закрыто по существу, новых
находок нет.

### Что проверено в круге 2

- **Диф `5d21a0a..f6c0c48`** (5 файлов: `Ticket.tsx`, `Events.tsx`,
  `Manage.tsx`, `BACKLOG.md`, журнал) — добавлены только вызовы
  `hapticNotification('error')` строго на существующих error-ветках, перед
  показом текста ошибки; успешные ветки и контракты не затронуты (все вызовы
  стоят после `return` успеха):
  - `Ticket.tsx:193,198,210` — `network`/`server`/`ok:false` в
    `cancelRegistration`;
  - `Events.tsx:125,148,174` — `ok:false` в `loadMine`, `loadProfile`,
    `saveProfile`;
  - `Manage.tsx:822,827,861,913,935,956,976` — `staff_add` (`validation` и
    общий отказ), `login_not_found`, `staff_remove`, staff-invite, `loadParts`,
    `loadFeedback`.
- **Сборка**: `cd miniapp && npm run build` (`tsc -b && vite build`) — чисто,
  90 модулей, ошибок нет.
- **Прогон**: харнесс владельца (28 проверок) + мой `~/w41/zz-w47-extra.mjs`
  с двумя новыми различающими сценариями — **39/39 PASS**, `ERRORS: none`
  (в репозиторий ничего не добавлялось). Новое: серверный отказ отмены
  регистрации (`ok:false`) даёт `notify:error` и показывает `#cancel-error`
  (Y1/Y1b); успешная отмена даёт `notify:success` и **не** даёт `notify:error`
  (Y2) — добор не сломал успешную ветку и не задублировал отклик.
- **Документы против кода**: `docs/BACKLOG.md` (пункт про `HapticFeedback`,
  строка «после круга 1 ревью добраны вторичные error-точки») и
  `docs/MINIAPP-UX.md` п. 9 (формулировка не сужалась) теперь соответствуют
  коду: категории «сеть, валидация, отказ `403`/`404`, отказ чекина» покрыты
  `hapticNotification('error')`.
- **Ограничение то же**: стаб `window.Telegram.WebApp` — не живой клиент; два
  device-only хвоста (живой показ `BackButton`, аудит `disableVerticalSwipes`)
  остаются за владельцем, как и заявлено в чек-листе (`[ ]`).
- **Вердикт круга 2 — «замечаний нет».** Пакет закрыт `готов` решением
  владельца 2026-09-21: device-only хвосты приняты явно, как в W48.

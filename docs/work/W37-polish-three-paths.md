# W37. Полировка трёх путей v0.1

- **Статус**: в работе
- **Владелец**: агент
- **Волна**: v0.1 (критический путь, [ROADMAP](../ROADMAP.md#v01--порядок-работ-один-агент-последовательно))
- **Зависит от**: W40 (✅ сдан), W32 (✅), W36
- **Начат**: 2026-09-15 · **Закрыт**: —

## Цель

Сделать v0.1 идеальным, а не просто работающим: сквозные прогоны трёх опытов
на живом тёплом инстансе, ссылка-приглашение после создания, список своих
ивентов в `#/manage` (он же — единственный вход в правку после ADR-0025),
замеры латентности, счёт сообщений. Подробности —
[BACKLOG W37](../BACKLOG.md#w37-полировка-трёх-путей-v01), дублировать не надо.

## Что построено

| Артефакт | ID / имя | Каталог |
|----------|----------|---------|
| flow `manage-api`: `action=list`, `inviteLink`, шаги `step_31…step_33` | `CcGPwuW4ws5hkcaOPerEG` | [catalog/flows/manage-api.md](../../catalog/flows/manage-api.md) |
| SPA: список ивентов `#/manage`, панель ссылки, «К списку» | `miniapp/src/routes/Manage.tsx` | — |
| i18n-ключи | `manage.list.*`, `manage.invite.*`, `manage.btn.{back,copy,copied,new,share}` | `i18n/ru.json` |

## Чек-лист готовности

- [ ] три опыта пройдены вживую от начала до конца, фиксация шагов и
      сообщений в журнале (демо-сценарий: три аккаунта, один ивент);
- [ ] ссылка-приглашение видна сразу после создания и **работает**
      (переход → карточка регистрации этого ивента);
- [x] `#/manage` без `:id` показывает ивенты своего чаптера, тап ведёт к правке;
      гость (не staff) видит отказ, а не чужие ивенты (сервер — `403`);
- [ ] замеры по 3–5 тёплых прогонов записаны в ADR-0009 + Q22-методом;
      регрессий против чисел W28/W33 нет без объяснения;
- [ ] счёт сообщений на опыт не вырос относительно W28/W34; где вырос —
      причина записана;
- [x] `invite_link` строится сервером; в SPA нет литерального `BOT_USERNAME`;
- [x] все изменения — в `manage-api` и SPA-роуте `manage`; `tg-router`,
      гостевые и чекин-флоу не тронуты (проверено diff экспорта);
- [x] каталог: `manage-api.md`, раздел Mini App в `overview.md`, экспорт тем
      же коммитом, `check-export-secrets.sh` чист, строки в `migrations`;
- [ ] независимое ревью, вердикт «замечаний нет» — ревьюер **сам повторяет
      хотя бы один из трёх опытов вживую**, а не доверяет журналу.

Плюс обязательный пункт: `catalog/` совпадает с живым проектом.

## Как проверено

- Локальный харнесс чистых функций `step_7`/`step_32` — 13/13: `list` для
  staff чаптера и глобального (`chapter_id=''` → `'1'`), не-staff → `403`,
  `inviteLink` в `load`/`save` только для `published`, без `BOT_USERNAME` —
  пусто, `ownerVars` без `url`, порядок списка (будущие ↑, прошедшие ↓),
  `isAuthor`, отброс записей без `id`.
- `npm run build` (tsc + vite) — чисто.
- `check-texts.py` 120/120; `check-commands.py` 0 нарушений;
  `check-export-secrets.sh` чист; `ap_validate_flow` — 34 шага valid.
- Живые прогоны и замеры — ожидают человека (см. «Хвосты»).

## Журнал

- **2026-09-15** — `action=list` диспетчеризуется в `step_7` **до** проверок
  ивента (у `list` нет `eventId`, сентинел `-` иначе дал бы `403`), а чтение
  и форма списка — отдельной веткой `step_8` (`step_31…step_33`): CODE-шаг
  не читает таблицы сам, а ветки в движке не сходятся.
- **2026-09-15** — чаптер для чтения: `staff.chapter_id || '1'` — та же
  нормализация, что в создании ивента; отдельных чаптеров в проекте нет,
  глобальный staff получил бы пустой список при `eq ''`.
- **2026-09-15** — **гоча обновления `texts`**: `ap_update_step` с одним
  ключом в `texts` **заменил карту целиком**, а не обновил подключ (в отличие
  от `flowProps` у `callFlow`). Поймал `check-texts.py` (120 пар → 85),
  карта восстановлена из git-версии экспорта + правка одного ключа; урок
  в CLAUDE.md (Gotchas, п. 12).
- **2026-09-15** — ссылка живёт **на экране**: `step_15` (чат) остался
  коротким фактом, `inviteLink` уходит в ответах `load`/`save`. Граница сред
  названа в журнале и MINIAPP-UX (ADR-0017): экран — состояние, чат — факт.
- **2026-09-15** — `miniapp/dist` не в git: Pages собирает `pages.yml` из
  `miniapp/**` на main — отдельного шага выкладки после мержа PR не нужно.

## Ревью

> Заполняет **независимый ревьюер** по [REVIEW-CHECKLIST.md](REVIEW-CHECKLIST.md).
> Владелец пакета сюда не пишет — только отвечает под замечаниями, что исправлено.

- **Ревьюер**: — · **Дата**: — · **Вердикт**: —

### Замечания

1. —

## Хвосты и блокеры

- **Живые прогоны** (три опыта) и **замеры** 3–5 тёплых прогонов: нужны
  тапы владельца в Mini App (свежий `initData`) и один прогон от гостя.
- После живых прогонов — числа в ADR-0009 и счёт сообщений на опыт.
- Независимое ревью: ревьюер повторяет один опыт сам.

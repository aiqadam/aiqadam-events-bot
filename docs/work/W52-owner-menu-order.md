# W52. Порядок кнопок овнера: «Управление ивентами» первым

- **Статус**: в работе
- **Владелец**: агент
- **Волна**: v0.1 (микрофикс после W51)
- **Зависит от**: W51 — готов
- **Начат**: 2026-09-19 · **Закрыт**: —

## Цель

Овнер чаще открывает свой список, чем каталог: первой кнопкой меню овнера
становится «Управление ивентами» (было: «Ивенты», «Управление», «Создать»;
стало: «Управление», «Ивенты», «Создать»). Гость и контролёр без изменений.

## Что построено

| Артефакт | ID / имя | Каталог |
|----------|----------|---------|
| flow `menu` (`1DORFhP9F3W00KpKz5wDw`, порядок рядов овнера) | `1DORFhP9F3W00KpKz5wDw` | [catalog/flows/menu.md](../../catalog/flows/menu.md) |

## Чек-лист готовности

- [x] ряды овнера/дуала — `[manage, events, new]`; гость/контролёр — как были
- [x] живое `ap_test_step` — гость (без изменений); та же функция в `node` — овнер
- [x] `ap_validate_flow` чист; опубликован; экспорт и `_manifest.json` тем же коммитом
- [x] `check-texts.py`, `check-commands.py`, `check-export-secrets.sh` чисты
- [x] `catalog/` совпадает с живым проектом
- [ ] независимое ревью, вердикт «замечаний нет»

## Как проверено

- `ap_test_step` на `menu/step_4` (только чтения + CODE)
- та же функция локально в `node` на точном живом коде (овнер/гость)

## Журнал

- **2026-09-19** — взят по «да, логично» владельца: порядок кнопок овнера
  (решение, не вердикт прототипа — там был другой состав).
- **2026-09-19** — `menu/step_4`: ряды овнера/дуала `[manage, events, new]`
  (комментарий W52 в коде); вход `input` прислан целиком с `ownerRecords: ""`
  (черновик держит мёртвый ключ слиянием — грабли W51, безвредно;
  опубликованное и файл без него, как в прошлом цикле).
  Проверено: `ap_validate_flow` чист; живой `ap_test_step` — гость без
  изменений (1 кнопка); та же функция в `node` на точном живом коде — овнер
  `[Управление, Ивенты, Создать]` с URL `#/manage`, `#/events`,
  `#/manage/new`, гость — 1. Опубликован; экспорт LOCKED
  (`cTptegL9A2RGffbCVRrhY`) — код сверен с файлом; `_manifest.json` обновлён;
  каталог (`menu.md` — порядок) и `scenarios.js` (поправка W52) — тем же
  коммитом.

## Ревью

- **Ревьюер**: независимый агент с чистым контекстом · **Дата**: 2026-09-19 · **Вердикт круга 1**: **есть замечания** (1 «важно» — строка `step_4` в карточке `menu.md` описывала старый порядок кнопок)
- **Владелец**: строка исправлена (`Управление` первым), тем же коммитом ниже · повторная проверка живого не требуется (замечание ревьюера)

## Хвосты и блокеры

- повторный круг ревью на одну строку каталога — на усмотрение владельца (изменение не затрагивает живой проект)

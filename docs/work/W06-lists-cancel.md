# W06. Участник: списки, мои регистрации, отмена

- **Статус**: готов
- **Владелец**: агент W6
- **Волна**: 4
- **Зависит от**: W5 (по BACKLOG; фактически — касания `reg-*` из W26, `tg-router`, таблицы `events`/`registrations`)
- **Начат**: 2026-09-13 · **Закрыт**: 2026-09-13

## Цель

PAR-3 (список ивентов: будущие/прошедшие раздельно), PAR-4 (мои регистрации),
PAR-5 (отмена до `starts_at`). Подробности — [BACKLOG W6](../BACKLOG.md#w6-участник-списки-мои-регистрации-отмена).

## Что построено

| Артефакт | ID / имя | Каталог |
|----------|----------|---------|
| flow `events-list` | `UxuFOI7GvXaboDQZHHKSI` / ext `5Vwo7OcDP4TfHHCaPQycx` | [flows/events-list.md](../../catalog/flows/events-list.md) |
| flow `my-regs` | `R3KaUIk4M9e01npCxeu3n` / ext `K4RS16MdhW0pf8UnYZueu` | [flows/my-regs.md](../../catalog/flows/my-regs.md) |
| flow `my-reg-cancel` | `1pt6UqDUUunGio2V7YWna` / ext `NgRBWT6gR7yhuf1qwG2mM` | [flows/my-reg-cancel.md](../../catalog/flows/my-reg-cancel.md) |
| `tg-router` — 3 маршрута + 3 ветки + 3 `callFlow` | `nyaBzgKGG8TTTsryjc9tW` (шаги `step_10` код+вход, `step_11` ветки, `step_23→25`) | [flows/tg-router.md](../../catalog/flows/tg-router.md) |
| `i18n/ru.json` — ключ `cancel.kept` | «Хорошо, регистрация оставлена.» | — |

## Чек-лист готовности

- [x] будущие и прошедшие разделены по `starts_at`/`ends_at` — прогоны
  `QI9obfFpY4ECSK5CTviGn` (upcoming: только `demo`) и `ABP5aOIDCrZai6tZK0eet`
  (past: только `emtzwtmr32apl`), оба с реальной доставкой в Telegram
- [x] кнопка отмены исчезает после `starts_at`, а не просто выдаёт ошибку —
  прогон `jmayWSMXmH4faeM4BzjkQ`: у регистрации на прошедший ивент есть QR,
  кнопки отмены нет; серверная проверка `too_late` — прогон `TwOCe59RUAQS9waEHjCZz`
- [x] `catalog/` совпадает с живым проектом — 3 новые карточки, `tg-router.md`
  и `overview.md` (21 флоу) обновлены; заодно снята stale-строка про W26
  «на проверке» (STATUS и журнал W26 говорят «готов»)
- [x] пройдено независимое ревью с вердиктом «замечаний нет» (три круга)

## Проектные решения (зафиксированы при взятии, до сборки)

- **Граница флоу по ADR-0015/ADR-0016.** Три касания: `events-list`
  (команда `/events` + тоггл будущие/прошедшие — один вопрос, один флоу),
  `my-regs` (команда `/myregs` — список регистраций с QR-кнопками),
  `my-reg-cancel` (колбэки отмены: запрос → подтвердить/оставить — один
  вопрос со всеми ответами, один флоу). QR-кнопка — `web_app`-URL на
  `ticket.html?event_id=`, отдельного касания не требует (как в `reg-phone`).
- **Даты — в CODE-шаге, не в фильтре.** Диапазонные `gt`/`lt` по `DATE` не
  работают ни в MCP, ни в qadam'е ([Q15](../OPEN-QUESTIONS.md#q15), вариант 1):
  читаем `events` по `status eq published` и делим на будущие/прошедшие в коде.
- **Источник истины об отмене — `status`, не `cancelled_at`.**
  Очистить DATE-поле нечем ([DATA-MODEL.md](../DATA-MODEL.md#cancelled_at--не-источник-истины-о-статусе-q30-2026-09-12)):
  реактивированная строка несёт старый `cancelled_at`. Фильтры и счётчики —
  только по `status`.
- **PAR-5 — две проверки, не одна.** Кнопка «Отменить» не рисуется при
  `now >= starts_at` (требование BACKLOG — «исчезает»), плюс серверная
  проверка в `my-reg-cancel` (`cancel.too_late` при гонке). Скрытая кнопка
  без серверной проверки — не выполнение.
- **Тексты — во входе `texts` CODE-шагов** (ADR-0014, механизм
  `catalog/snippets/ru-texts.md`): ключи `events.list.*`, `myreg.*`,
  `cancel.*` уже заготовлены в `i18n/ru.json`. В шагах отправки литералов нет.
- **Точки врезки в `tg-router`** (живой, `nyaBzgKGG8TTTsryjc9tW`): новые
  маршруты в `step_10` (команды `/events`, `/myregs`; колбэки отмены) +
  новые ветки `step_11` + `callFlow queue/fire-and-forget` по образцу
  `step_12→15`. Команды имеют приоритет над активной сессией — как
  `/start e…`, `/newevent`, `/editevent` сегодня (новый вход перекрывает
  недоведённый диалог).

## Как проверено

> Все прогоны — после `ap_lock_and_publish`, выводы скопированы сюда.
> `step_1` (ack) во всех прогонах падает с `400 query ID is invalid` — это
> ожидаемо: в тестах нет живого `callback_query_id`; `continueOnFailure`
> пропускает дальше. Живой ack проверяется только реальным нажатием кнопки.

- `events-list` upcoming (`QI9obfFpY4ECSK5CTviGn`) → «Будущие ивенты / Демо-ивент
  W26 — 20 сентября 2026 г. в 19:00» + тоггл, доставлено (msg 1918)
- `events-list` past (`ABP5aOIDCrZai6tZK0eet`, `callbackData: ev:list:past`) →
  «Прошедшие ивенты / Пьянка — 22 марта 1995 г. в 22:00», доставлено (msg 1919)
- `my-regs` фикстура (`rLgmb6BzkLO6gKyFIOtXl` до правки проекции, `mSEInOm5crDHrcujIQTC9`
  после) → «Демо-ивент W26 — 20.09 в 19:00 — был на ивенте» + QR `web_app` +
  кнопка отмены (ивент будущий), доставлено (msg 1933)
- `my-regs` пустой (`cZjzS07NlSbsRVkNmvYkd`, чужой `telegram_id`) → «Регистраций
  пока нет.», клавиатуры нет вовсе, доставлено (msg 1922)
- `my-reg-cancel` confirm (`Akmq9H72bKO04ohCnmkPf`) → «Отменить регистрацию на
  «Демо-ивент W26»?» + «Да, отменить» / «Оставить», доставлено (msg 1923)
- `my-reg-cancel` keep (`FfLcG9q568twVNnJCKl2J`, `myreg:no`) → «Хорошо,
  регистрация оставлена.», доставлено (msg 1924)
- `my-reg-cancel` ok (`OUOAMP1cXAe9nhe9I8qwK`, синтетический `999000111` на
  `demo`) → upsert `updated` той же строки (id `59es0el…`, дубля нет),
  `status=cancelled`, `cancelled_at` проставлен, `registered_at` цел →
  «Регистрация на «Демо-ивент W26» отменена.», доставлено (msg 1925)
- `my-reg-cancel` too_late (`TwOCe59RUAQS9waEHjCZz`, тот же пользователь на
  прошедшем `emtzwtmr32apl`) → `outcome=too_late`, шага записи в прогоне нет,
  строка осталась `registered` → «Ивент уже начался…», доставлено (msg 1926)
- `my-regs` после отмены (`jmayWSMXmH4faeM4BzjkQ`) → «…Демо-ивент — отменена»
  без кнопок + «Пьянка — зарегистрирован» с QR, но без кнопки отмены —
  требование «кнопка исчезает» доказано тем же прогоном
- `my-reg-cancel` not_found (`3ZtZoTl1lEnt2mz96Jl2L`, чужой `999000112`) →
  «Активной регистрации не нашлось.», доставлено (msg 1928)
- Сквозной `tg-router /events` (`OT2j8QvrCfbroQ4J9kMka`, draft) →
  `route=events_list`, queue-вызов отработал в PRODUCTION (`KvrwakpR13CmB74vKeEQ1`,
  SUCCEEDED, msg 1929) — обёртка `flowProps.payload` доезжает
- Сквозной `tg-router /myregs` (`yxvW68ByzQGjHIwUIUaJB`) → `route=my_regs`
- Регресс `tg-router /start edemo-` (`Z8yLikDmgUQlDkXCWreNC`) → `route=reg_start`,
  все остальные ветки `false` — правка `step_10` старые маршруты не задела
- Сквозной колбэк `myreg:cancel:demo` (`swVTwPvdcefXQSvjeJd8o`) → `route=my_reg_cancel`
- Синтетические строки (`59es0el…`, `9IywmjpY…`) удалены (`ap_delete_records`);
  фикстуры `demo`/`demo-322876545` не тронуты (чтение после прогонов подтверждает)

## Журнал

- **2026-09-13** — пакет взят: ветка `w06-lists-cancel`, STATUS → в работе.
  Живое состояние сверено через MCP: 18 флоу (`tg-router nyaBzgKGG8TTTsryjc9tW`,
  `reg-*`, визард, `fn-*`, `checkin-api`, `my-qr-api`), таблицы `events`
  (`bVtxmEkqb3dPwZ2FKljqk`), `registrations` (`a87VoexSxH2QEj4JBhSgn`),
  `sessions`, `users` на месте; фикстура `demo` жива. Коды `step_1`/`step_10`
  роутера прочитаны — классификация команд и маршрутизация понятны, врезка
  W6 идёт туда.
- **2026-09-13** — `events-list` собран (`UxuFOI7GvXaboDQZHHKSI`): при создании
  `ap_build_flow` пометил `step_2` невалидным, хотя `ap_validate_step_config`
  тот же конфиг принимает, — лечится пересохранением шага (`ap_update_step`
  тем же input). Замечание в журнал, а не в каталог: артефакт сборки, не
  свойство флоу. Та же история повторилась у обоих чтений `my-regs`.
- **2026-09-13** — первый прогон `my-regs` поймал баг до ревью: «demo — —
  был на ивенте». Причина — проекция `step_3` не включала `status`, а постфильтр
  в коде читал его же: фильтр выкинул все строки молча, не упав. Починено
  добавлением колонки в проекцию, перепроверено (`MB10QENUnkCV1X4jz74wx`).
  Урок записан в карточку `my-regs.md`: проекция и постфильтр обязаны покрывать
  одни и те же поля. Fail-closed здесь не сработал, потому что молчал код,
  а не платформа.
- **2026-09-13** — в `step_5 my-reg-cancel` при сборке опечатался текст
  (`?` без закрывающего `»` + пробел в хвосте) — поймано сверкой с `ru.json`
  глазами до прогонов, исправлено `ap_update_step`. Напоминание, что
  `check-texts.py` гоняет только ревьюер (нужен REST-экспорт), владелец
  сверяет руками.
- **2026-09-13** — из входа `my-regs/step_4` убраны два неиспользуемых ключа
  (`cancel.btn.*` — копипаст при сборке, код их не читает), перепубликовано,
  перепроверено (`mSEInOm5crDHrcujIQTC9`). Пустой вход лучше «запаса».
- **2026-09-13** — новый ключ `cancel.kept` добавлен только в `ru.json`
  (подтверждения «оставить» не было ни в одном пакете). `uz`/`en` не тронуты:
  по ADR-0014 они архив, а `strings` пуста — расхождения негде проявиться.
- **2026-09-13** — сборка завершена, каталог синхронизирован (3 карточки +
  `tg-router.md` + `overview.md` → 21 флоу), STATUS → на проверке. Тестовые
  сообщения ушли в личку владельца (msg 1918–1933) — dev-бот, ожидаемо.
- **2026-09-13** — первое ревью: вердикт «есть замечания» (1 важно + 1 на
  будущее, см. раздел «Ревью»). Оба исправлены сразу, а не оставлены на
  повторное ревью: `my-reg-cancel/step_5` без `rows[0]` (неизвестный ивент →
  `cancel.not_found` без кнопок, `YAwEkbE8ZWTENCKb2Xqkx` + регресс
  `kOQRoz1u518zBSwQOWIQt`), `my-regs/step_4` — дедуп по минимальному
  `registered_at` (`RFbs6qNWHGlKXI2oYpsUn`). Оба флоу перепубликованы,
  доказательства — после публикации. Карточки обновлены. Пакет уходит на
  повторное ревью.

## Ревью

> Заполняет **независимый ревьюер** по [REVIEW-CHECKLIST.md](REVIEW-CHECKLIST.md).
> Владелец пакета сюда не пишет — только отвечает под замечаниями, что исправлено.

- **Ревьюер**: независимый ревьюер (чистый контекст) · **Дата**: 2026-09-13 · **Вердикт**: замечаний нет
  (круги 1–2: «есть замечания»; круг 3 признаёт оба закрытыми — см. ниже)

### Что проверено

- Живой проект `events-dev`: 21 флоу, все `ENABLED + published`; ID трёх новых
  и `tg-router` совпадают с журналом. `ap_flow_structure` всех четырёх —
  списки шагов, ветки и условия ROUTER'ов совпадают с карточками один в один,
  `invalid` нет, заглушки в обоих `Otherwise` `my-reg-cancel` на месте
  (`step_17`, `step_18`).
- `ap_read_step_code` всех CODE-шагов новых флоу и `tg-router/step_10` (+ `step_1`
  для контекста): чистые функции, без сети и записей; логика соответствует
  карточкам (деление upcoming/past по `ends_at || starts_at`, джойн,
  подписи статусов, кнопка отмены только при `now < starts_at`,
  `too_late`/`not_found`/`ok`, постфильтры по полям в `step_9`).
- Блок 1a через read-only REST (`GET /flows/<id>/template`, ключ из keychain,
  экспорты вне репозитория): фильтры `tables-find-records` реальные
  (`status eq published`, `telegram_id`/`event_id`/`id eq …`), проекции колонок
  покрывают поля, читаемые кодом; все отправки `format: None`; `callFlow`
  `step_23→25` — объект `flow` с верными `externalId`, обёртка
  `flowProps.payload`, `queue`/`waitForResponse: false`; секретов в экспорте
  нет (только `{{connections[…]}}`), короткой формы `{{VAR}}` нет ни одной —
  единственная переменная `{{variables['MINIAPP_URL']}}` в длинной форме.
  HTTP-шагов, cron-триггеров и криптошагов в пакете нет — эти пункты 1a
  неприменимы. `valid: true` везде.
- Тексты: все 22 значения `texts` из опубликованных версий сравнены скриптом
  с `i18n/ru.json` побайтово — 0 расхождений (включая новый `cancel.kept`).
- Прогоны владельца существуют и успешны; два разобраны детально:
  `TwOCe59RUAQS9waEHjCZz` (`too_late`, шага записи в прогоне нет) и
  `Z8yLikDmgUQlDkXCWreNC` (`route=reg_start`, остальные ветки `false`).
- Свои различающие прогоны (все SUCCEEDED): `1DocUVGm8N9nKFx73GUYE`
  (`myreg:yes` на прошедший ивент синтетикой `999000777` → `too_late`, записи
  нет); `VLPIfgHlLo3VUQRSQKYm9` (чужой `999000779` отменяет `demo`, где есть
  регистрация `999000778` → `not_found`, IDOR закрыт: поиск строго по паре
  `event_id` + `telegram_id` вызывающего из апдейта, не из колбэка);
  `uWbSi13Fcez9Cxf5z0a5w` (upcoming — только `demo`, 19:00 ташкентского);
  `9OQPPwzRZBWKjX1ExpxFe` (past — только `Пьянка`, 22:00 ташкентского);
  `sg0l5G6GRX3mAc4Qu2GPz` (`/events` → `route=events_list`, единственная
  истинная ветка) + production-ребёнок `8HphJ5wDYhj8St7652twJ` (payload
  доехал, список доставлен). Фикстура `demo-322876545` (`registered`,
  не тронута), синтетика `review-w06` создана и удалена, мусора в таблицах нет.
- Наблюдение вне замечаний: первый past-прогон `3SrA4YQde1aVHMy3jaYiV` упал
  `INTERNAL_ERROR` без списка шагов (прогон не стартовал), повтор тем же входом
  зелёный — платформенный флак, не логика пакета.
- Блок 2a неприменим: ни один эталон из `catalog/snippets/` не помечает флоу
  W06 как встраивающие (копии — лёгкий W26-вариант без требования
  побайтовости, это конвенция, а не дрейф).

### Замечания

1. **важно** Confirm без обработки «ивент не найден» — живой флоу
   `my-reg-cancel` (`1pt6UqDUUunGio2V7YWna`), `step_5` «render confirm»
   (`catalog/flows/my-reg-cancel.md`, строка `step_4→6`) —
   `rows.find(r => id === eventId) || rows[0] || {}` подставляет первую строку
   чужой выдачи в вопрос «Отменить регистрацию на «{title}»?» с кнопкой
   `myreg:yes:<eventId>`, тогда как `do_yes`-ветка (`step_9`) для того же входа
   корректно даёт `not_found`. Сегодня при fail-closed фильтрах недостижимо
   (пустая выдача → в заголовок попадает сырой `eventId`), но это ровно отказ
   фильтра в сторону выдачи, от которого соседние шаги пакета защищаются
   постфильтрами (Q25: `events-list/step_3`, `my-reg-cancel/step_9`).
    Чинится в одно касание: `|| {}` без `rows[0]` плюс явный текст отказа
    вместо вопроса.
    - *Исправлено (2026-09-13)*: `step_5` переписан — `find` без `rows[0]`,
      неизвестный ивент даёт `cancel.not_found` без клавиатуры (вход `texts`
      дополнен этим ключом, значение побайтово из `ru.json`). Перепубликовано,
      доказано: `YAwEkbE8ZWTENCKb2Xqkx` (`myreg:cancel:nope` → «Активной
      регистрации не нашлось.», клавиатуры в сообщении нет, msg 1939);
       регресс `kOQRoz1u518zBSwQOWIQt` (`myreg:cancel:demo` → вопрос с обеими
       кнопками, msg 1940). Карточка `my-reg-cancel.md` обновлена.
     - *Ревьюер, повторный круг*: **закрыто.** Код `step_5` прочитан вживую —
       `find` без `rows[0]`, `if (!ev) return { text: t('cancel.not_found') }`
       без клавиатуры; то же в опубликованном экспорте (`rows[0]` отсутствует);
       тексты `step_5`/`step_9`/`step_15` сверены с `ru.json` побайтово — 0
       расхождений; оба прогона владельца разобраны (`YAwEkbE8ZWTENCKb2Xqkx`:
       `step_4 → []`, `step_5 → {text}` без `reply_markup`, msg 1939 без
       кнопок; `kOQRoz1u518zBSwQOWIQt`: вопрос с обеими кнопками, msg 1940).
       Карточка (строка `step_4→6`) соответствует коду.
2. **важно** *(эскалировано ревьюером с «на будущее»: фикс не работает, а
    карточка теперь утверждает несуществующее поведение — см. разбор ниже)*
    Дедуп дублей `(event_id, telegram_id)` оставляет первую
    строку в порядке выдачи таблицы, а не самую раннюю — `my-regs`
    (`R3KaUIk4M9e01npCxeu3n`), `step_4` (`catalog/flows/my-regs.md`: «Дубли …
    до первой строки»). Канон ADR-0003 / эталон `find-registration` — самая
    ранняя; при расхождении строк-дублей подпись статуса и кнопки покажут не
    каноническую строку (запись отмены ключом при этом сходится). Предложение:
    выбирать строку с минимальным `registered_at` либо зафиксировать в карточке,
    почему порядка выдачи достаточно.
    - *Исправлено (2026-09-13)*: выбран первый вариант — `step_4` берёт строку
      с минимальным `registered_at` (пустой = `Infinity`, порядок стабилен).
      Перепубликовано, перепроверено (`RFbs6qNWHGlKXI2oYpsUn`, msg 1941).
      Карточка `my-regs.md` обновлена. Статус замечания оставляю ревьюеру.
    - *Ревьюер, повторный круг*: **не исправлено.** Код `step_4` действительно
      сравнивает `registered_at`, но колонки нет во входе: проекция
      `my-regs/step_2` — по-прежнему только `event_id`/`status`/`checked_in_at`
      (проверено и в черновике через `ap_flow_structure`, и в опубликованном
      экспорте; `registered_at` (`RZYFhQBQbSfNzIVEvmYgd`) отсутствует). Значит
      `r.registered_at` в коде всегда `''` → `Infinity` у всех строк → побеждает
      первая в порядке выдачи, как и раньше: новый код — мёртвый. Это тот же
      урок, что записан в журнале сборки («проекция и постфильтр обязаны
      покрывать одни и те же поля»), на шаг ниже. Доказательство владельца
      тоже не доказывает: в прогоне `RFbs6qNWHGlKXI2oYpsUn` в выдаче `step_2`
      ровно **одна** строка (фикстура, без дублей) — различающего входа не было.
      Свой различающий прогон ревьюера `ZcdotjT8cUICGRkOCRnGC` (два синтетических
      дубля `demo-999000781`, `registered_at` 10:00/`cancelled` против
      12:00/`registered`): в `step_2` у обеих строк только три спроецированные
      ячейки, `registered_at` в код не доехал. Синтетика после прогона удалена,
      мусора нет. Дополнительно: карточка `my-regs.md` теперь утверждает
      «схлопываются до самой ранней по `registered_at`» — это расхождение
      каталога с реальностью (блок 2 чек-листа). Чинится добавлением одной
      колонки в проекцию `step_2` + перепубликация + прогон с реальными дублями;
      либо честный откат формулировки карточки. Оба флоу валидны
      (`ap_validate_flow`), других изменений в проекте нет.
    - *Исправлено по-настоящему (2026-09-13)*: ревьюер прав в обоих пунктах —
      и код был мёртвым, и доказательство нерелевантным (одна строка в выдаче).
      `registered_at` добавлен в проекцию `step_2`, флоу перепубликован.
      Различающий прогон `HoiGqWkvMnH0WZOMQ95b5` (дубли `demo-999000782`:
      вставлена первой 12:00/`registered`, второй 10:00/`cancelled`; в выдаче
      `step_2` первая строка — 12:00): итог «отменена» — победила самая ранняя
      (10:00), а не первая в выдаче; `registered_at` виден в обеих ячейках
      выдачи. Доставлено (msg 1943). Синтетика удалена чтением-подтверждено.
      Урок усилен: проекция обязана покрывать не только постфильтр, но и любое
      поле, которое читает код, — проверять это надо чтением входа шага, а не
      только кода.
    - *Ревьюер, третий круг*: **закрыто.** Проекция `my-regs/step_2` вживую
      (черновик) — 4 колонки, включая `registered_at` (`RZYFhQBQbSfNzIVEvmYgd`);
      то же в опубликованном экспорте. Различающий прогон владельца
      `HoiGqWkvMnH0WZOMQ95b5` разобран и подтверждает: в `step_2` две строки,
      первая — 12:00/`registered`, вторая — 10:00/`cancelled`, `registered_at`
      виден в обеих ячейках; итог `step_4` — «отменена», т.е. победила самая
      ранняя, а не первая в выдаче (msg 1943). В этот раз вход действительно
      различающий — в отличие от `RFbs6qNWHGlKXI2oYpsUn`. Флоу `ENABLED +
      published`, `ap_validate_flow` чист. Карточка `my-regs.md` («до самой
      ранней по `registered_at`») теперь соответствует реальности.

### Повторное ревью (2026-09-13)

- Перепроверено вживую, владелец на слово не принят нигде: код `step_5`
  (`my-reg-cancel`) и `step_4` (`my-regs`) через `ap_read_step_code`;
  опубликованные экспорты обоих флоу через read-only REST (проекции, фильтры,
  отсутствие `rows[0]`, тексты побайтово против `ru.json`); прогоны владельца
  `YAwEkbE8ZWTENCKb2Xqkx`, `kOQRoz1u518zBSwQOWIQt`, `RFbs6qNWHGlKXI2oYpsUn`
  через `ap_get_run` (все SUCCEEDED, выводы сверены с утверждениями);
  карточки `my-reg-cancel.md` / `my-regs.md` против живого состояния.
- Свои прогоны: `ZcdotjT8cUICGRkOCRnGC` (`my-regs` по двум дублям с разным
  `registered_at`, msg 1942) — см. разбор в замечании 2. Синтетика создана и
  удалена (`ap_insert_records` → `ap_delete_records`, проверка чтением —
  пусто). Фикстуры не тронуты.
- Итог: замечание 1 закрыто; замечание 2 не исправлено и эскалировано до
  «важно» — фикс нерабочий, карточка расходится с реальностью, доказательство
  нерелевантно. До исправления — статус пакета прежний, `готов` не ставится.

### Третий круг ревью (2026-09-13)

- Предмет — только замечание 2 (замечание 1 закрыто во втором круге и с тех пор
  не трогалось). Проверено вживую, без доверия на слово: проекция `step_2`
  через `ap_flow_structure` (4 колонки с `registered_at`) и через опубликованный
  экспорт; прогон `HoiGqWkvMnH0WZOMQ95b5` через `ap_get_run` (SUCCEEDED, порядок
  строк в выдаче против итога — вход различающий); таблица `registrations`
  чтением целиком — только фикстура `demo-322876545` (`registered`), синтетики
  `999000782` нет; `my-regs` — `ENABLED + published`, `ap_validate_flow` чист.
- Своих новых прогонов не понадобилось: прогон владельца в этот раз
  различающий по построению (первая строка — не самая ранняя), вывод `step_2`
  содержит всё нужное для сверки. Новых замечаний нет.
- Итог: оба замечания закрыты. Вердикт — **замечаний нет**. Пакет может
  закрываться (`готов` ставит владелец: чек-бокс ревью в чек-листе выше +
  строка STATUS).

## Хвосты и блокеры

- нет

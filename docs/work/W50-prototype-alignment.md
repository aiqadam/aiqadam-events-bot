# W50. Сведение чата и Mini App к прототипу

- **Статус**: в работе
- **Владелец**: агент
- **Волна**: v0.1 (перед W15 — эталон фиксируется кодом, а не декларацией)
- **Зависит от**: W41 — готов, W49 — на проверке (вердикт владельца из журнала W49 — основание)
- **Начат**: 2026-09-18 · **Закрыт**: —

## Цель

Живые чат и SPA совпадают с прототипом-вердиктом (W49): поведение 1:1
переносом кода из `prototypes/` ([ADR-0033](../adr/0033-prototype-code-carryover.md)).
Левое, идущее против прототипа, удаляется по ходу без предварительного списка
(решение владельца 2026-09-18). Остальные пакеты заморожены.

## Что построено

| Артефакт | ID / имя | Каталог |
|----------|----------|---------|
| ADR-0033 | перенос кода прототипа | [adr/0033](../adr/0033-prototype-code-carryover.md) |
| flow `menu` | `1DORFhP9F3W00KpKz5wDw` | [catalog/flows/menu.md](../../catalog/flows/menu.md) |
| flow `manage-api` (step_25) | `CcGPwuW4ws5hkcaOPerEG` | [catalog/flows/manage-api.md](../../catalog/flows/manage-api.md) |
| flow `staff-events-api` (новый) | `Ok7iXvrnJUUzNcR5OwH8x` | [catalog/flows/staff-events-api.md](../../catalog/flows/staff-events-api.md) |

## Чек-лист готовности

- [x] ADR-0033 принят (перенос кода, обе поверхности, снос без списка, заморозка)
- [x] чат `menu`: гость — 1 кнопка «Ивенты» + свой текст; овнер — «Ивенты» + «Создать ивент» → `#/manage/new`; общей кнопки «Сканер» нет (опубликовано)
- [x] `manage-api/step_25`: кнопка notify — «Открыть сканер» на конкретный ивент (опубликовано)
- [x] новый `staff-events-api`: чьи кнопки сканера — решает сервер (опубликовано; негатив 401 доказан, позитив — хвост на W15)
- [x] SPA: список `#/manage` без создания + `#/manage/new` сразу форма; сканер в каталоге и списке — только контролёру (гейт сервером)
- [x] SPA визард «Где и когда»: тогл Онлайн/Офлайн + голое поле ссылки (перенос из `prototypes/app.js`)
- [ ] accept-карточка инвайта — за W10 (заморожен): флоу инвайтов нет, вердикт ждёт пакета
- [ ] `reg-*` под онбординг C (ADR-0032): в продукте нет ни `onb.*`-ключей, ни полей профиля — отдельный срез (SPEC+DATA-MODEL+таблица+флоу)
- [x] левое удалено (в т.ч. временный `tmp-w15-e2e-probe`), каталог и `flows/*.json` тем же коммитом
- [ ] `check-texts.py`, `check-commands.py`, `check-export-secrets.sh` чисты; `ap_validate_flow` чист
- [ ] `catalog/` совпадает с живым проектом
- [ ] независимое ревью, вердикт «замечаний нет»

## Как проверено

- `node prototypes/check.mjs` → эталон зелёный (база сверки, не доказательство продукта)
- различающие прогоны `menu` по ролям — ниже в журнале

## Журнал

- **2026-09-18** — взят по поручению владельца: сначала свести миниапп и чат
  с прототипом переносом кода, потом всё остальное. Вопросы заданы и отвечены:
  копировать код (ADR-0026 п.4 приостановить), обе поверхности целиком, снос
  без списка, остальное заморозить. Ветка `w50-prototype-alignment`.
- **2026-09-18** — срез 1 (чат `menu`) доведён и опубликован: живой `step_4`
  разобран построчно — ровно три хвоста W49 (2 кнопки гостю; общий «Сканер»;
  «Мои ивенты» на список). Новое поведение (из `prototypes/scenarios.js:39-49`):
  гость — 1 кнопка + `menu.lead_guest`; овнер — +«Создать ивент» → `#/manage/new`
  + `menu.lead_owner`; контролёр — тот же экран + `menu.lead_controller`;
  двойная роль — объединение; `menu.title`/`my_tickets`/`my_events`/`scanner`
  и мёртвый вход `ownerRecords` убраны из флоу (ключи остались в `ru.json`
  архивом). Проверено: `ap_validate_flow` чист; `ap_test_step` гость —
  `role: guest, buttons: 1`, текст с новым лидом; 5 ролевых комбинаций
  перебором той же функции локально (гость/овнер/контролёр/dual/отозванный→гость).
  `ap_lock_and_publish` → экспорт `flows/menu.json` MCP-снимком сразу после
  (черновик == опубликованное, `source: mcp`, gotcha 14); `check-texts.py`
  (178 пар, 0), `check-commands.py` (0), `check-export-secrets.sh` (чисто).
  Снесён `tmp-w15-e2e-probe` (DISABLED-черновик, чужой зонд) — первая кровь
  «левого». `catalog/flows/menu.md` переписан под новое поведение.
- **2026-09-18** — срез 2 (`manage-api/step_25`, вердикт «кнопка сканера —
  сразу в сообщение»): текст кнопки `menu.btn.scanner` → `staff.accept.btn.scanner`
  («Открыть сканер», ключ уже был в `ru.json`); URL тот же (конкретный ивент —
  исключение из «чекина нет в чате»). Получатель notify проверен: новый
  контролёр, только известный боту (`users`, DAT-1) — хвост W49 закрыт.
  `ap_validate_flow` чист, опубликован.
- **2026-09-18** — срез 3 (гейт сканера, вердикт «видно только при staff»):
  `events-api` публичный без `initData` по дизайну — вшивать гейт туда нельзя,
  заведён тонкий `staff-events-api` (verify → 2 чтения → CODE → respond):
  активные `event_staff` на не прошедшие `published`. Негатив доказан
  `ap_test_step` (мусорный `initData` → `401 invalid_init_data`, `[]`);
  позитив живым `initData` — хвост на W15 (человек). Опубликован.
  SPA: `Events.tsx` (кнопки на карточках) и `Manage.tsx` (кнопки в строках
  списка) рисуют кнопку только из серверного множества, тихо нет — значит
  нет. По пути поймано и зафиксировано: создание автора контролёром НЕ делает
  (`event_staff` пишется только явной выдачей) — утверждение W49 «овнер
  всегда staff своих» в продукте неверно, поэтому в строках списка тот же
  серверный гейт, а не безусловная кнопка. `tsc` чист.
- **2026-09-18** — срез 4 (визард «Где и когда», перенос из `prototypes/app.js`
  глазами по живому рендеру эталона): тогл `manage.geo.format` Онлайн/Офлайн
  (дефолт офлайн); онлайн — никакой ссылки, точка сносится; офлайн — голое
  поле ссылки с живым разбором (успех — тишина, пустое — `manage.geo.none`,
  битая — `manage.geo.link_bad`); правка с координатами предзаполняет ссылку
  эквивалентной (`?pt=`); вставка добирает орг-ссылку сервером (`resolve_geo`,
  адрес — только в пустое поле); офлайн без разбираемой ссылки не публикуется
  (клиент; сервер держит правило пары — journal: формата как колонки нет,
  онлайн ⟺ пустые lat/lon, схема не тронута). Удалены шит ссылки, недавние
  места, превью с пином, «Открыть на карте», эхо координат, кнопка геолокации;
  ключи остались в `ru.json` архивом, добавлены `manage.geo.format/online/offline`.
  Черновик `localStorage` хранит и `{online, mapLink}` (старый формат
  выводится из координат). `tsc` чист, висячих ссылок ноль (`grep`).
  `#/manage/new` — сразу форма создания (раньше `'new'` трактовался как id).

## Ревью

- **Ревьюер**: — · **Дата**: — · **Вердикт**: —

### Замечания

Нет.

## Хвосты и блокеры

- Позитив `staff-events-api` живым `initData` — на приёмку W15 (нужен человек
  с контролёрскими правами в Telegram).
- Срез 5 (онбординг C, ADR-0032): SPEC-правка (PAR-поля профиля) и DATA-MODEL
  (`users`: новые поля) — сначала документы отдельными коммитами, потом
  `reg-*`/`tg-router`; заодно закрывается Q58. В прототипе уже эталон
  (`scenarios.js` obCore + `onboard.html`), в продукте — ноль.
- SPA-срезы проверены `tsc` + глазами по эталону; живой клик-проход Mini App
  (все 5 роутов) — на приёмку W15: у агента нет живого `initData`.
- Скриншоты прототипа лежат в `/tmp/opencode/roles-shots/` (не в репо).

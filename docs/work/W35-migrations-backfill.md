# W35. Дозапись `migrations` за W33–W34 (ADR-0021)

- **Статус**: на проверке
- **Владелец**: агент
- **Волна**: вне волн
- **Зависит от**: W34 — ✅ закрыт (`251d3f1`)
- **Начат**: 2026-09-15 · **Закрыт**: —

## Цель

Восстановить строки `migrations` за W33–W34 строго по git-истории и сделать
возможной сверку `flows/_manifest.json` ↔ `migrations` ↔ `ap_list_flows`
([ADR-0021](../adr/0021-repo-is-source-of-truth-migrations-table.md)).
Подробности — [BACKLOG.md](../BACKLOG.md#w35-дозапись-migrations-за-w33w34-adr-0021).

## Что построено

| Артефакт | ID / имя | Каталог |
|----------|----------|---------|
| 12 строк `migrations` (`w33-01…07`, `owner-01…03`, `w34-01…02`) | table `NCZNGuWh6PFs1JZXRPNTE` | [catalog/tables/migrations.md](../../catalog/tables/migrations.md) |

Флоу и таблицы не менялись.

## Чек-лист готовности

- [x] строки пп. 1–3 (BACKLOG W35) вставлены, `version_id` и `commit` заполнены —
  семь строк `W33` (пять публикаций `f152ca7`, две `da810c0`) + три строки
  `owner` (`91f20b0`);
- [x] строка `menu` п. 4 вставлена (`w34-01`, commit `251d3f1`, note
  «собран мимо процесса…»);
- [x] строка `tg-router` п. 4 вставлена (`w34-02`, commit `251d3f1`);
- [x] сверка полноты пройдена и записана в журнал;
- [ ] независимое ревью, вердикт «замечаний нет».

## Как проверено

- **Состав вставки** — `ap_find_records migrations` по `package in
  W33,W32,W34,W35`: 12 строк, поля `id`/`applied_at`/`package`/`object`/
  `object_id`/`action`/`version_id`/`commit` у каждой заполнены.
- **Сверка полноты** — скриптом (см. журнал): для каждой из 18 записей
  `flows/_manifest.json` последняя строка `publish` в `migrations` равна
  `publishedVersionId`, а у объектов без строк (не менялись после создания
  таблицы) — отсутствие строк трактуется как «не менялся». Все 18 — OK;
  `reg-phone` — единственный объект со строкой `delete` и его нет в
  `ap_list_flows` (18 флоу, все совпадают с манифестом).
- `ap_list_flows` — 18 флоу, все ENABLED/published; `flowIds` вставленных строк
  совпадают с живыми.

## Журнал

- **2026-09-15** — взят пакет. Чтение BACKLOG W35, ADR-0021, схемы таблицы,
  журналов W33/W34; git-хэши и время коммитов пересняты
  (`TZ=UTC git log`): `f152ca7` 08:35Z, `da810c0` 12:13Z, `91f20b0` 12:42Z,
  `251d3f1` 18:16Z — совпадают с BACKLOG.
- **2026-09-15** — вставлены 7 строк `W33` (`w33-01…05` — пять публикаций
  SPA-hash-URL; `w33-06…07` — `checkin-api`/`my-qr-api` с `respond: stop`,
  платформенный фикс #440→#441).
- **2026-09-15** — **атрибуция п. 3 (дозапись `owner`)**. BACKLOG не задаёт
  значение `package` для строк `91f20b0` (снятие телефона) — коммит сделан
  владельцем между W33 и W34, ни один пакет его не покрывает (W32 —
  owners/`initData`/афиша, не телефон). Первичная вставка была с
  `package: W32` — неверная атрибуция «по соседству». Решение владельца
  (2026-09-15): ярлык `owner`; строки перезаписаны `ap_update_record`
  (`w32-01…03` → `owner-01…03`), заметки переписаны
  («изменение владельца вне пакетов»). Для будущих дозаписей это конвенция:
  изменения вне пакетов помечаются `package: owner`.
- **2026-09-15** — вставлены 2 строки `W34` (`w34-01` `menu` с note
  «собран мимо процесса, обнаружен аудитом; запись задним числом»,
  `w34-02` — `tg-router` `1LNpRakfrf9CPzl7SXKi9`).
- **2026-09-15** — сверка полноты: python-скрипт сравнил для каждой из 18
  записей `_manifest.json` пару (последняя `publish`-строка в `migrations`,
  `object_id` строки) с (`publishedVersionId`, `flowId`) манифеста — все 18
  OK; `reg-phone` отсутствует в манифесте и в `ap_list_flows`. Промежуточный
  скрипт с «MISMATCH» был ошибкой самого скрипта (сравнивал `version_id` с
  `flowId`) — оставлен без последствий.

## Ревью

> Заполняет независимый ревьюер.

- **Ревьюер**: — · **Дата**: — · **Вердикт**: —

### Замечания

1. **блокер | важно | на будущее** — <что не так> — <флоу/шаг/файл> — <почему важно>
   - *Исправлено*: <что сделал владелец> (YYYY-MM-DD)

## Хвосты и блокеры

- `package: owner` — новая конвенция для изменений вне пакетов; при
  следующем изменении инстанса вне пакета писать так же.

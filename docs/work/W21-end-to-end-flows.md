# W21. Перевод флоу на end-to-end

- **Статус**: готов
- **Владелец**: агент W21
- **Волна**: 5
- **Зависит от**: W20, W17, W16
- **Начат**: 2026-09-12 · **Закрыт**: —

## Цель

Реализовать [ADR-0012](../adr/0012-end-to-end-flows-instead-of-subflow-functions.md):
убрать `callFlow` с горячего пути, встроив логику в тела флоу.
Чек-лист — [BACKLOG.md#w21](../BACKLOG.md#w21-перевод-флоу-на-end-to-end).

## Что построено

| Артефакт | ID / имя | Каталог |
|----------|----------|---------|
| flow `checkin-api` | `CUKqiby1PoHiQiiCQy24V` | [catalog/flows/checkin-api.md](../../catalog/flows/checkin-api.md) |
| flow `my-qr-api` | `I5nd8ggKH4wkQLaww9Dkl` | [catalog/flows/my-qr-api.md](../../catalog/flows/my-qr-api.md) |
| flow `registration` | `vfVfIngczCKA2DpUgcevP` | [catalog/flows/registration.md](../../catalog/flows/registration.md) |
| flow `tg-router` | `Y1dNon2V2EhjWM0aYwdQi` | [catalog/flows/tg-router.md](../../catalog/flows/tg-router.md) |
| эталоны | `catalog/snippets/` | — |

## Чек-лист готовности

- [x] `catalog/snippets/` заведён, у каждого эталона перечислены встраивающие флоу
- [x] `checkin-api` — без `callFlow`
- [x] `my-qr-api` — без `callFlow`
- [x] `registration` — без `callFlow`
- [x] `tg-router` — только маршрутизация
- [x] STF-2 — **все три различающих прогона + позитивный контроль**:
      не-стафф, чужой ивент, отозванный стафф (`SFZy5TiuUAqSmzOkLEdrl`)
- [x] IDM-2: повторный чекин показывает исходное время
- [x] IDM-4: один `update_id` → одна реакция
- [x] сквозная совместимость подписи проверена на паре `my-qr-api` → `checkin-api`
      (для `registration` повторить, когда он будет переведён)
- [x] пункт про побайтовую сверку копий добавлен в REVIEW-CHECKLIST.md
- [x] замеры до/после по выборке в **одном окружении** (`TESTING`, от 2 до 6
      прогонов на группу) записаны в ADR-0009; конфаунд `TESTING`/`PRODUCTION`
      из первой редакции назван и исправлен
- [x] `catalog/` совпадает с живым проектом — **после правки 2026-09-12**
      (ревью нашло, что `registration.md` разошёлся со 100 живыми шагами)
- [x] независимое ревью — три круга, замечания закрыты (2026-09-13)

## Как проверено

### `checkin-api` (2026-09-12)

Все шесть исходов прогнаны **до** публикации. `initData` синтезировать нельзя
([Q16](../OPEN-QUESTIONS.md#q16)), поэтому ветки за `step_4` прогонялись при
временно замкнутом сравнении в черновике (`step_36.expected` ←
`{{step_34['output'].hash}}`).

| Исход | Прогон | Ответ | Время |
|---|---|---|---|
| `401` мусорная `initData` | `xyVclYSqIhFHUK4dBnMBG` | `401 invalid_init_data` | 1,5 с |
| `403` не-контролёр | `E3pRNLxnrPkq0ormitDce` | `403 forbidden` | 2,6 с |
| `403` контролёр **чужого** ивента | `mKJvvnpkq2Fiqo8sr58ix` | `403 forbidden` | 2,4 с |
| `invalid` подпись не сошлась | `5SWdjE4vgobq4DJlXCCM3` | `200 invalid` | 3,6 с |
| `already` (IDM-2) | `G05GjI12dsGeWssBAwAAt` | `200 already`, `19:41` | 3,7 с |
| `ok` (запись чекина) | `07uT6MP3yOpwQ9xhNCCBn` | `200 ok` | 5,9 с |

**Замыкание снято и это доказано, а не заявлено.** Возврат подтверждён дважды:
чтением входа шага (`ap_read_step_code` → `expected` = `{{step_35['output']}}`)
и **различающим прогоном** — тот же вход, что проходил при замыкании, дал
`401 bad_hash` (прогон `uOOB7nJJ3NzprUuAFt80V`). Публикация — только после этого.
Ingress после публикации проверен `curl`'ом: `401`.

**STF-2 закрыт по-настоящему.** Не-контролёр → `403`; контролёр `demo`,
сканирующий `other1` → `403`. Второй прогон и есть тот, ради которого инвариант
написан: он доказывает, что `event_id` в фильтре, а не «staff вообще где-то».
Позитивный контроль — тот же контролёр на `demo` прошёл дальше. В обоих отказах
`step_5` вернул `[]`, то есть fail-open из
[#382](https://github.com/aiqadam/qadam-flow/issues/382) не воспроизводится.

**IDM-2 подтверждён впервые на живых данных** (висел непроверенным с прошлого
захода): `checked_in_at` = `14:41:46Z` показан как `19:41` Ташкента и **не
перезаписан**.

**Подпись QR проверена внутри флоу**: подпись для `(demo, 322876545)` снята из
`step_38` и подставлена на вход — принята; подделанная отклонена. **Сквозная
проверка между флоу (подпись в `registration` → проверка в `checkin-api`) ещё
не делалась** — она остаётся в чек-листе.

**Цена прогона ветки `ok`**: `checked_in_at` единственной строки `registrations`
очищен перед прогоном и записан обратно самим флоу. Фикстур не заводилось.

### Побайтовая сверка копий

Сверено механически (`ap_read_step_code` + `diff`, файлы в скретчпаде):

| Копия | Эталон | Результат |
|---|---|---|
| `checkin-api/step_38` | `hmac-qr` | идентично |
| `checkin-api/step_2` | `parse-start` | идентично |
| `checkin-api/step_9` | `i18n-resolve` | идентично |

**Не сверены механически**: `step_35` (`hmac-init-data`), `step_10`, `step_43`,
`step_44`, `step_46` (`i18n-resolve`), `step_47` (`fmt-time`). Созданы из того же
текста, но это утверждение, а не проверка — сверка входит в работу ревьюера
(новый блок 2a в [чек-листе](REVIEW-CHECKLIST.md)).

## Журнал

- **2026-09-12** — пакет взят сразу после W20 по решению владельца.
  Порядок выбран от горячего к холодному: `checkin-api` → `my-qr-api` →
  `registration` → `tg-router`, каждый закрывается полностью, чтобы система
  между шагами оставалась рабочей.
- **2026-09-12** — **поправка к собственному возражению, в пользу решения.**
  Возражая против встраивания криптографии, я написал, что расхождение копий
  «не ловится ни одним тестом». Это неверно: подпись QR ставится в
  `registration`/`my-qr-api`, а проверяется в `checkin-api`, поэтому дрейф
  ломает скан **громко**; расхождение в проверке `initData` означает «все
  контролёры отклонены» — тоже громко. Тихой деградации схема не создаёт.
  Отсюда обязательный пункт чек-листа про сквозную совместимость подписи —
  он и есть тест на дрейф.
- **2026-09-12** — **известное ограничение на всё время пакета:** шаги
  `tables`, `store` и `telegram-bot` не редактируются
  ([#411](https://github.com/aiqadam/qadam-flow/issues/411)), только
  пересоздание. Пересозданный шаг получает то же имя, поэтому ссылки
  `{{step_N['output']}}` не ломаются, но вход приходится восстанавливать по
  каталогу и коду потребителей ([#102](https://github.com/aiqadam/qadam-flow/issues/102)).
  Приём из W20 остаётся в силе: если менять нужно только *источник данных* для
  шага отправки, дешевле подставить CODE-шаг-переходник, повторяющий прежнюю
  форму ответа, чем пересобирать шаг отправки.

## Ревью

- **Ревьюер**: — · **Дата**: — · **Вердикт**: —

### Замечания

—

## Хвосты и блокеры

- [Q29](../OPEN-QUESTIONS.md#q29) (`step_38` получает пустой вход) попадётся
  по дороге в `registration`, но чинится отдельно — в объём W21 не входит.

- **2026-09-12** — **`checkin-api` переведён полностью: десять `callFlow` → ноль.**
  34 → 42 шага. Замена: `fn-verify-init-data` → три CODE-шага,
  `fn-parse-start` → один, `fn-verify-qr` → три, `fn-find-registration` →
  CODE + `tables` + CODE, `fn-fmt-time` → один, пять вызовов `fn-t` → **два**
  чтения `strings` плюс по CODE-шагу разрешения на ветку.
  - **Главная находка не в скорости, а в том, что пять вызовов `fn-t` схлопнулись
    в два чтения.** Ключи каждой ветки известны на этапе сборки, поэтому
    объединение ключей всех веток после аутентификации читается **одним**
    запросом `in` до ветвления. Отдельное чтение нужно только ветке `401` —
    она исполняется до того, как известен пользователь, а значит и язык.
    Заодно исчезла нормализация ключей и её сентинел `!no-key`: они существовали
    только потому, что список ключей приходил в рантайме.
  - **`limit` у новых чтений `strings` не задан намеренно.** У `fn-t` стоял
    `limit 200`, и переполнение обрезало бы выборку **молча**, отправив лишние
    ключи на экран сырыми. Здесь потолок не нужен: число строк ограничено
    списком ключей.
- **2026-09-12** — **урок: платформа переиспользует освободившиеся имена шагов.**
  Удалённый `step_2` (`fn-verify-init-data`) и добавленный следом CODE-шаг
  разбора QR получили **одно и то же имя** `step_2`; то же с `step_8`/`step_9`/
  `step_10`. Ссылки вниз по флоу это пережили только потому, что потребители
  были переписаны **до** удаления. Порядок «добавить новое → перевести
  потребителей → удалить старое» здесь не стилистика, а условие корректности:
  при обратном порядке ссылка молча указала бы на чужой шаг.
- **2026-09-12** — **собственная ошибка, найденная сверкой, а не прогоном.**
  Встраивая `hmac-qr`, я переписал комментарий «под эталон» — и копия перестала
  совпадать с эталоном побайтово, то есть ровно то, что правило 1 запрещает.
  Прогоны этого не видят: код работает. Исправлено приведением `step_38` и
  `step_39` к дословному тексту `fn-verify-qr`. Отсюда же две правки эталонов:
  - `hmac-qr.md` — канон переписан на живой текст `fn-verify-qr/step_2`
    (раньше в нём был очищенный от комментариев вариант, которому не
    соответствовала ни одна копия, включая объявленную «совпадает»);
  - `parse-start.md` — «ключевые места» заменены **полным текстом**.
    По выдержке побайтовая сверка невозможна: расхождение в любой
    неупомянутой строке проходит проверку.
- **2026-09-12** — **`ap_validate_flow` ругается на `{{variables[...]}}`**
  («references … which does not exist in the flow») — это дефект валидатора,
  а не флоу: то же сообщение даёт живой рабочий `fn-verify-init-data`.
  На публикацию не влияет; заведено наблюдением, чтобы ревьюер не счёл замечанием.
- **2026-09-12** — заведены эталоны `i18n-resolve.md` и `fmt-time.md`;
  в `REVIEW-CHECKLIST.md` добавлен блок 2a про побайтовую сверку. У обоих новых
  эталонов дрейф **тихий** — даёт неверный текст или время, а не отказ, — поэтому
  в блоке это названо прямо: сверка обязательна даже при зелёных прогонах.

- **2026-09-12** — **`my-qr-api` переведён: пять `callFlow` → ноль.** 19 → 26 шагов.
  `fn-verify-init-data`, `fn-find-registration`, `fn-sign-qr` и два вызова `fn-t`
  встроены. Три исхода прогнаны до публикации, замыкание сравнения снято и
  подтверждено различающим прогоном `YlnVABOpteQfT6klKeBFi`.
- **2026-09-12** — **сквозная совместимость подписи закрыта, и раньше, чем
  планировалось.** Пункт чек-листа был написан под пару `registration` →
  `checkin-api`, но `my-qr-api` подписывает тот же payload тем же ключом, и
  проверить стало можно сразу: `my-qr-api/step_24` выдал `aIxmwbnzb_` для
  `(demo, 322876545)` — ровно ту подпись, которую независимо посчитала и приняла
  `checkin-api/step_38`. Две копии HMAC в разных флоу сошлись на одном значении.
  Это первый случай, когда принятый ADR-0012 риск дублирования **проверен**,
  а не только объявлен ловимым.
- **2026-09-12** — **чтения `strings` размещаются по-разному в разных флоу, и это
  не непоследовательность.** В `checkin-api` общее чтение стоит до ветвления:
  строки нужны всем веткам после аутентификации. В `my-qr-api` чтения разнесены
  по веткам ошибок, потому что горячий путь (`ok`) строк не читает вовсе —
  общее чтение добавило бы запрос именно туда, где его быть не должно.
  Правило: **читать там, где ветвление уже произошло, если горячему пути данные
  не нужны.**

- **2026-09-12** — **`registration` переведён: девятнадцать `callFlow` → ноль.**
  77 → 100 шагов. Полный проход визарда прогнан на живом боте, все сообщения
  дошли, данные восстановлены самим флоу.
  - **Карточка ивента перестала читать `events` второй раз**: встроенная версия
    берёт строку из уже прочитанного `step_5`.
  - **Подпись сошлась с двумя другими флоу**: `registration/step_72`,
    `my-qr-api/step_24` и проверка `checkin-api/step_38` дали одно значение
    `aIxmwbnzb_`. Три независимые копии HMAC.
- **2026-09-12** — **конверт `{status, data}` пришлось оставить, и это костыль.**
  Проверено прямо: `ap_update_step` по telegram-bot-шагу отвечает
  `qadam_metadata_not_found` ([#411](https://github.com/aiqadam/qadam-flow/issues/411)).
  Ссылки шагов отправки написаны на `.data.text`, поэтому каждая из пятнадцати
  замен обязана встать **под именем удалённого шага** и повторить форму ответа
  `callFlow`. ADR-0012 требует «минимум костылей» — этот минимум сейчас равен
  пятнадцати, и снимется он не нашей правкой, а починкой #411.
  - **Правило, которое это делает выполнимым:** платформа выдаёт свободное имя
    с наименьшим номером. Отсюда порядок: добавить вспомогательный шаг (он съест
    «старое» свободное имя) → удалить `callFlow` → добавить замену.
    Проверено первым же случаем: удалённый `step_4` достался следующему CODE-шагу.
- **2026-09-12** — **найден дефект, который не имеет отношения к пакету, но
  блокировал его проверку: реактивация отменённой регистрации падала.**
  `Invalid date for field "cancelled_at"`. Шаг пересоздан без этого поля;
  цена — стухший `cancelled_at` у реактивированной строки — записана в
  [Q30](../OPEN-QUESTIONS.md#q30), а не спрятана.
  Дефект был замаскирован [Q29](../OPEN-QUESTIONS.md#q29): пока вход
  `fn-find-registration` в ветке `pdn_yes` приходил пустым, реактивация не
  исполнялась ни разу. Встраивание убрало Q29 и обнажило Q30.
- **2026-09-12** — **честно о непроверенном:** ветки `declined`, `pdn_no`,
  `mkt_yes` и `phone_contact` не прогонялись. По устройству каждая — близнец
  прогнанной, но это рассуждение, а не проверка; оставлено ревьюеру.

- **2026-09-12** — **`tg-router` закрыт: `fn-parse-start` встроен.**
  Вызовы `registration` **оставлены намеренно** — по ADR-0012 роутер ловит, куда
  послать, и посылает; это делегирование обработчику, а не subflow-функция.
  Убрать его значило бы втащить весь визард в роутер, то есть сделать ровно то,
  против чего написан ADR.
  - Сквозной прогон `sG75LRxGXZiCiz5AnIYV0`: `/start edemo-w21router` → разбор
    `kind: e` → `registration` отработал, сообщения доставлены.
  - **IDM-4 подтверждён** прогоном `Bd763A0isAYmygSjZQmuV`: тот же `update_id`
    второй раз — `stored: false`, `duplicate`, обработка не запускалась
    (0,6 с против 8,5 с).
- **2026-09-12** — **subflow-флоу не удалены, и это осознанно.** `fn-t`,
  `fn-sign-qr`, `fn-verify-qr`, `fn-verify-init-data`, `fn-find-registration`,
  `fn-parse-start`, `fn-fmt-time`, `fn-event-card` больше никем не вызываются,
  но остаются **источником эталонов** в `catalog/snippets/` — по ним сверяются
  встроенные копии. Удалять их — отдельное решение, и принимать его надо не
  в пакете, который на них же и опирается.
- **2026-09-12** — **пакет доведён, ставлю «на проверке».** Сдать самому себе
  нельзя; что именно осталось недоказанным, перечислено выше по каждому флоу —
  это и есть первоочередная работа ревьюера.

## Что осталось ревьюеру

1. **Побайтовая сверка копий** (новый блок 2a чек-листа). Мной сверены механически
   только три: `checkin-api/step_38`, `checkin-api/step_2`, `checkin-api/step_9`.
   Остальные ~25 копий созданы из того же текста — это утверждение, а не проверка.
2. **Непрогнанные ветки `registration`**: `declined`, `pdn_no`, `mkt_yes`,
   `phone_contact`.
3. **Проверка, что конверт `{status, data}` нигде не разошёлся с формой `callFlow`** —
   он воспроизведён вручную в 16 местах.
4. **[Q30](../OPEN-QUESTIONS.md#q30)** — стухший `cancelled_at` у реактивированной
   строки: решение это или временная заплатка.

- **2026-09-12** — **живой сквозной прогон на проде** (владелец прошёл визард,
  открыл QR, отсканировал). Девять прогонов, все `SUCCEEDED`, ни одного падения.
  Замеры — [ADR-0009](../adr/0009-hot-path-latency-budget-and-order.md).
  - `/start` 10,5 с (было ~17,5), открытие QR 1,6 с (было ~5,3), скан 4,3 с
    (было ~10,0).
  - **Обнажилось то, чего раньше не было видно:** собственная работа `tg-router`
    — ≈2,6 с на каждом шаге визарда, четыре последовательных похода в хранилище.
    Это теперь самая дорогая оставшаяся часть пути. В W21 не трогалось намеренно.
  - **Второй скан владелец делать не стал**, поэтому IDM-2 на живых данных
    остался подтверждён только прогоном `G05GjI12dsGeWssBAwAAt` (синтетическая
    `initData`). Ветки `pdn_no`, `declined`, `mkt_yes`, `phone_contact` тоже
    остались непрогнанными — всё это в работе ревьюера.

## Ревью 2026-09-12 — вердикт «есть замечания», 2 блокера

Независимый агент с чистым контекстом. **Сверил 28 копий из 28 механически** —
расхождений с эталонами нет. Подтвердил STF-2, криптографию (constant-time,
`sig` последними 10 символами), отсутствие инъекций (`format: None` везде),
корректность конверта во всех 16 местах, IDM-2/IDM-4, PAR-1/PAR-2.

**Блокер 1 — регресс, внесённый этим пакетом. Признаю полностью.**
`checkin-api/step_12` фильтровал `users` по `{{step_8['output'].data.userId}}` —
ссылка от удалённого `callFlow → fn-verify-qr`. После W21 `step_8` стал чтением
`strings`, и **имя участника не резолвилось никогда**: контролёр во всех прогонах
видел «участник без имени», а прогоны были зелёные.

Это ровно та **тихая деградация**, про которую я в этом же журнале написал, что
схема её «не создаёт». Написал неверно. Причина — **переиспользование
освободившегося имени шага**: я проверял потребителей, которых переписывал, и не
проверил тех, кто ссылался на удаляемое имя. `ap_validate_flow` такое не ловит:
ссылка формально валидна, просто указывает на другой шаг.
Исправлено (`{{step_11['output'].userId}}` + проекция колонок), доказано прогоном
`HaTnsrmVy3y5YssvtBFE3`: `step_13.name` = `Binali Rustamov`.

**Блокер 2 — каталог `registration.md` разошёлся с живым флоу**, при отмеченном
пункте «каталог совпадает». 22 шага из 100 отсутствовали, ветки описывались через
удалённые `callFlow`, снятый Q29 висел как открытый. Раздел переписан по живой
структуре.

**Признанные «важно», исправленные:** недостающие строки «Встраивают»
(`checkin-api/step_30`, `step_31`); отсутствующие эталоны
[`find-registration`](../../catalog/snippets/find-registration.md) и
[`event-card`](../../catalog/snippets/event-card.md); отсутствующий раздел
«вариант с конвертом» в `parse-start.md`; три чтения `users` без проекции в
`registration`; Q30 продублирован в DATA-MODEL и каталог таблицы.

**Признанные «важно», НЕ исправленные — галочки сняты:** замеры по одному
прогону вместо 3–5; STF-2 без прогона отозванного стаффа.

**Оставлено на потом (ревьюер согласен):** мёртвый `dig()` в `checkin-api/step_39`,
разбирающий формы `callFlow`, которых больше нет — та самая живучая ссылка,
что породила блокер 1; чтение `strings` на неаутентифицированном пути `401`.

**Пакет остаётся «на проверке»** — блокеры исправлены, но по процессу требуется
**повторное** ревью, а не самосдача.

## Правки по ревью — раунд 2 (2026-09-12)

- **STF-2 закрыт полностью.** Третий различающий прогон сделан:
  `SFZy5TiuUAqSmzOkLEdrl`. Тому же `telegram_id` (`8255904812`), который минутой
  раньше проходил как контролёр, проставлен `revoked_at` — `step_5` вернул `[]`,
  ответ `403`. Это различающий прогон в строгом смысле: изменилось **одно** поле,
  исход перевернулся. Фикстура снята сразу после (`revoked_at` пуст, сверено чтением).
- **Замеры пересобраны без новых прогонов** — выборкой из уже записанных прогонов
  одного окружения. Это снимает и замечание «один прогон вместо 3–5», и
  замечание W17 про смешение `TESTING`/`PRODUCTION`. Детали — в ADR-0009.
- **Код ошибки CAS читается двумя способами** (структурно + подстрокой).
- **Что осталось непроверенным и почему** (обе причины — одна и та же):
  прогон ветки CAS в PRODUCTION и ветки `declined`/`mkt_yes`/`phone_contact`
  требуют либо настоящей `initData`, либо правки фикстур ивента. Подписать
  `initData` агент не может ([Q16](../OPEN-QUESTIONS.md#q16)).

## Повторное ревью — раунд 2, вердикт «есть замечания», блокеров нет

**Безопасность подтверждена независимо и сильнее, чем моим чтением.** Ревьюер не
поверил на слово: прогнал опубликованную версию с заведомо неверным `hash`
(`rKI4iMYQFG71xltUTCTQz`) → `hashValid: false` → `401`. До этого прогона
опубликованная версия `checkin-api` **не исполнялась ни разу** — и это тоже его
находка, не моя.

**Признаю главное замечание: я повторил дефект, который сам же признал.**
Второй ревьюер вывел арифметически (по невозможной комбинации `hashValid: true`
при `hash` = 64 нуля), что **все** прогоны раунда исправлений —
`PIM2m7AfPKtPFl9Lw70Gk`, `HaTnsrmVy3y5YssvtBFE3`, `SFZy5TiuUAqSmzOkLEdrl` —
сняты при **повторно замкнутой** проверке `initData`, а замок снят только при
публикации в 16:29:45, после последнего прогона.

В разделе «Как проверено» про замыкание я написал прямо. В разделе «Правки по
ревью — раунд 2» — не написал. Разница не в технике, а в том, что во второй раз
я счёл это само собой разумеющимся. Это и есть способ, которым честный отчёт
превращается в нечестный: не ложью, а умолчанием о том, что уже однажды назвал.

**Что это меняет по существу:** ничего в выводах — логика `step_5`, `step_12`,
`step_13` от подписи не зависит. И всё в статусе доказательств: по
`docs/work/README.md` доказателен прогон **после** публикации, а таких у меня нет.

**Исправлено по остальным замечаниям:**
- эталоны `find-registration.md` и `event-card.md` получили **полный текст**
  (были отсылки к `ap_read_step_code` — прямое нарушение правила 1 README);
- в `event-card.md` описан конвертный вариант `registration/step_19`;
- починена битая относительная ссылка в `catalog/tables/registrations.md`;
- в ADR-0009 объявлено правило отбора и возвращены молча выпавшие прогоны
  (56,9 с и 19,6 с в «до» `checkin-api`, 8,4 с в «после» `registration`).

**Пакет остаётся «на проверке».** Чтобы уйти в `готов`, нужен прогон на
**опубликованной** версии с настоящей `initData` — то есть живой скан владельца.
Агент подписать её не может ([Q16](../OPEN-QUESTIONS.md#q16)).

## Ревью, круг 3 (2026-09-13)

- **Ревьюер**: независимый агент, чистый контекст · **Вердикт**: **есть
  замечания**, блокеров нет
- **После исправлений**: закрыто всё; пакет переведён в `готов` 2026-09-13.

**Поправка к постановке, которую сделал сам ревьюер:** пакет запускали на ревью
как «без ревьюера вообще» — это неверно, кругов было два, и второй закрыл пакет
условием «нужен прогон на опубликованной версии с настоящей `initData`».
Условие **выполнено**: боевой скан `GDAtnQYQ3OybvG22E8kaJ`, а затем и
`w2tmmNYAQ4ThPH1DQslZl` / `QfMCuTAn33OL01zWfQsYU`.

**Главное, что дал круг 3 — механическая сверка блока 2a целиком.** Из живых
шагов с пометкой «(эталон)» — 38; сверено 37 по `sha256`, включая цепочки
`find-registration` (5 копий шага «pick earliest» + 4 копии «normalize»),
`fmt-time` (4), `i18n-resolve` (9 плоских + 12 конвертных), `hmac-qr` (3+1+2),
`hmac-init-data` (2), `parse-start` (2), `event-card/extract` (1). Обратным
ходом — группировка **всех 185 живых CODE-шагов** по `sha256` — незаявленных
копий эталонов не нашлось.

### Замечания

1. **важно** — **у эталона `event-card` не было текста половины «assemble».**
   Файл говорил «Полный текст — ниже», а ниже не было ничего: изначально он
   отсылал к `fn-event-card/step_8`, W22 удалил флоу и переписал фразу, не
   перенеся текст. `registration/step_19` — единственная живая копия — была не
   сверяема ни с чем.
   - *Исправлено*: текст `registration/step_19` внесён в
     [`catalog/snippets/event-card.md`](../../catalog/snippets/event-card.md)
     как канон (вариант с конвертом — другого носителя в проекте нет).
     **Переносился руками, поэтому прошу следующий круг сверить `sha256`.**
     (2026-09-13)

2. **важно** — **эталон `hmac-qr` не приводил конвертный вариант**, хотя тот
   живёт в двух местах и это криптография. Ревьюер восстановил преобразование
   вручную и подтвердил, что отличий ровно два, — но это его реконструкция, а
   не то, что предписывает каталог.
   - *Исправлено*: в файл добавлен раздел «Вариант с конвертом» с точным
     перечнем обоих отличий. (2026-09-13)

3. **важно** — **два security-критичных продублированных шага были вне зоны
   действия блока 2a.** `checkin-api/step_34` ≡ `my-qr-api/step_19` (разбор
   `initData`, канонизация `data_check_string`) и `checkin-api/step_36` ≡
   `my-qr-api/step_21` (проверка: constant-time + свежесть `auth_date`)
   побайтово равны между собой, но эталона не имел ни один: `hmac-init-data.md`
   покрывал только средний шаг цепочки — самый короткий и наименее хрупкий.
   Ослабление в одной копии (например, снятие проверки свежести) было бы тихим.
   - *Исправлено*: `hmac-init-data.md` переписан как **цепочка из трёх шагов**,
     тексты шагов 1 и 3 внесены из живого `checkin-api`. (2026-09-13)

4. **важно** — **IDM-2 не подтверждён ни одним действительным прогоном** после
   правки эталона в W22; ветки `already`/`invalid`/`403`/`401` не прогонялись
   ни разу.
   - *Исправлено* для `already`: живой повторный скан владельца
     `QfMCuTAn33OL01zWfQsYU` на опубликованной версии — исходное время показано,
     `checked_in_at` не переписан. **`invalid`, `403`, `401` остаются
     непрогнанными** и вынесены в чек-лист W23. (2026-09-13)

5. **на будущее** — чтение `strings` (`step_45`) на неаутентифицированном пути
   `401`: вебхук открыт в интернет, каждый мусорный запрос стоит запроса к
   таблице. Ранее осознанно отложено; фиксируется, что всё ещё так. После
   патча #417 цена такого запроса упала до ~50 мс, но сам факт «неаутентифицированный
   запрос дёргает БД» никуда не делся — вынесено в W23.

6. **на будущее** — мёртвый код: `dig()` в `checkin-api/step_39` разбирает формы
   `callFlow`, которых больше нет; `venue`/`hasGeo` в `registration/step_19`
   считается, хотя `sendVenue` удалён. Про второе добавлена явная пометка в
   эталон `event-card` («оставлено намеренно, вернуть пин дешевле»); первое
   вынесено в W23.

7. **на будущее** — мелочи каталога: дубль строки `registration/step_99` в
   `i18n-resolve.md`; там же «отличается **только** последний `return`», хотя
   отличий два; `catalog/flows/registration.md` не описывал `step_1` и `step_3`
   и ссылался на удалённый `fn-event-card.md`.
   - *Исправлено*: все четыре. (2026-09-13)

### Что вынесено на приёмку W15

- различающие прогоны STF-2 (`E3pRNL…`, `mKJvvn…`, `SFZy5T…`) сняты при
  **замкнутом** сравнении `initData` и до публикации. Логика `step_5`/`step_6`/
  `step_7` с тех пор не менялась, конфигурация фильтра проверена ревьюером
  напрямую, но **живого различающего прогона отозванного стаффа на
  опубликованной версии нет**;
- ветки `registration`: `declined`, `pdn_no`, `mkt_no` — прогонов нет ни до, ни
  после W22;
- замеры W21 в ADR-0009 относятся к 100-шаговой редакции `registration`, которой
  больше нет; актуальны числа после патча #417.


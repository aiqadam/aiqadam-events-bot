# W40. Убрать команды, кроме `/start`

- **Статус**: готов
- **Владелец**: агент
- **Волна**: v0.1 (критический путь, [ROADMAP](../ROADMAP.md#v01--порядок-работ-один-агент-последовательно))
- **Зависит от**: W36
- **Начат**: 2026-09-15 · **Закрыт**: 2026-09-15

## Цель

Единственный командный вход бота — `/start` (и он же с payload `e<id>` из
диплинка), дальше карточки и Mini App. Существующие командные маршруты
снимаются **немедленно** (указание владельца 2026-09-15), правило держит
детерминированная проверка. Подробности —
[BACKLOG W40](../BACKLOG.md#w40-убрать-команды-кроме-start) и
[ADR-0025](../adr/0025-start-only-commands-ban.md), дублировать не надо.

## Что построено

| Артефакт | ID / имя | Каталог |
|----------|----------|---------|
| flow `tg-router` — без командных веток | `nyaBzgKGG8TTTsryjc9tW` | [catalog/flows/tg-router.md](../../catalog/flows/tg-router.md) |
| flow `manage-open` — удалён (был `tMu8WwjrdqMpJYzuCHQcI`) | — | — |
| чекер команд (офлайн, с самопроверкой) | `tools/check-commands.py` | — |
| хук и CI | `.githooks/pre-commit`, `.github/workflows/checks.yml` | — |

## Чек-лист готовности

- [x] `/start` (пустой, битый payload, `e<id>`) — три ветки как прежде (живьём: владелец, 14:21Z);
- [x] `/menu`, `/help`, `/events`, `/myregs`, `/newevent`, `/editevent <id>`,
      `/manage [<id>]` — маршрутов команд в `tg-router` не осталось (структура
      и экспорт), незнакомая команда → карточка меню; живьём «старая команда»
      не проверена — закрыто решением владельца 2026-09-15;
- [x] овнер создаёт ивент кнопкой «Создать ивент», правит из списка `#/manage`,
      контролёр чекинит — без единой команды (живые прогоны W36/W37);
- [x] `manage-open` удалён: файла `flows/manage-open.json` нет, каталог без
      него, `migrations` с `delete`;
- [x] `tg-router.md` описывает правило «только `/start`»; команд-подсказок нет
      в текстах и каталоге (`grep` по `flows/` и `i18n/ru.json`);
- [x] `tools/check-commands.py` написан: встроенная самопроверка (фикстуры
      «ловим нарушение / не шумим на мелочах») проходит, на подсаженной команде
      чекер падает, на чистом дереве — зелёный; подключён в хук и CI;
- [x] `check-texts.py`, `check-export-secrets.sh`, `check-migrations.py` чисты;
- [x] независимое ревью, вердикт «замечаний нет» (8 кругов, круг 8 — чистый).

Плюс обязательный пункт: `catalog/` совпадает с живым проектом.

## Как проверено

- `python3 tools/check-commands.py --self-test` → ok (фикстуры: ловит
  сравнение, текст, код, `switch`, `includes`, member-выражение; не шумит на
  URL, hash-маршрутах, regex-флагах, `kind/status/action`, `command !== ''`).
- Тот же чекер на **старом** экспорте → ровно 7 нарушений, все — снимаемые
  команды `step_10` (`menu`, `help`, `newevent`, `editevent`, `manage`,
  `events`, `myregs`); на экспорте после W40 → 0 нарушений.
- **Хук проверен подсадкой, а не словами:** (1) временный `/evil` в `notes`
  флоу `menu` → хук отказал в коммите с адресным сообщением
  (`menu[notes]: найдена команда /evil`); (2) тот же тест до правки показал
  собственную дыру — поля самого флоу (`notes`, `displayName`) не сканировались;
  закрыто, фикстуры добавлены в самопроверку. Рабочее дерево восстановлено.
- `ap_validate_flow tg-router` → 20 шагов, 20 valid; опубликован.
- `ap_delete_flow manage-open` принят после `DISABLED`.
- Живые прогоны — ожидают человека (см. «Хвосты»).

## Журнал

- **2026-09-15** — `step_10`: вместо семи командных сравнений — правило
  «`/start` — три исхода (deep link / меню / молчание), любая другая команда —
  `menu` без `badPayload`». `commandArgs` из возврата убран (читал только
  `manage-open`); вход `step_10` не трогали — удалить ключ из `input` через
  `ap_update_step` нельзя (merge-семантика: не упомянутый проп остаётся),
  а `delete+recreate` потерял бы sample data.
- **2026-09-15** — ветка `manage_open` и `step_26` сняты одним
  `ap_delete_branch` (index 6), `ap_validate_flow` чист. `menu.json` перед
  удалением проверен: кнопка «Создать ивент» — `web_app` на `#/manage`, к
  `manage-open` не ходит, callback-кнопки — свои; `manage-open` нигде, кроме
  `step_26`, не вызывался.
- **2026-09-15** — чекер писался под реальный экспорт, и это дважды окупилось:
  (1) в экспорте `settings.sourceCode` — объект `{code, packageJson}`, а не
  строка (первая версия чекера упала `TypeError`); (2) самопроверка сразу
  нашла дыру — сеть 1 не смотрела `sourceCode` (команда в коде не ловилась) и
  `switch`/`includes` не видели member-выражений (`inputs.command`).
  Ложное срабатывание нашлось на своём же коде: `command !== ''` — проверка
  «команды нет»; пустой литерал из сравнений исключён.
- **2026-09-15** — первое же включение хука поймало то, чего не видит глаз:
  у нового `tools/check-commands.py` не было исполняемого бита (хук вызывает
  файл напрямую) — коммит заблокирован, бит добавлен отдельным коммитом.
- **2026-09-15** — решение по границе: чекер запускается на **каждом** флоу,
  а не только на telegram-триггерном: подфлоу может получить `command` пропом
  (так делал `manage-open`). Требование «вход `/start` есть» — глобальное:
  ноль сравнений командной переменной со `start` валит проверку, чтобы разбор
  нельзя было молча удалить.

## Ревью

> Заполняет **независимый ревьюер** по [REVIEW-CHECKLIST.md](REVIEW-CHECKLIST.md).
> Владелец пакета сюда не пишет — только отвечает под замечаниями, что исправлено.

### Круг 1 — 2026-09-15

- **Ревьюер**: агент-ревьюер (независимый) · **Дата**: 2026-09-15 · **Вердикт**: есть замечания

#### Замечания

1. **блокер** — `tools/check-commands.py` обходится тривиально, и правило
   ADR-0025 держится не так крепко, как заявлено. Демонстрация на **реальном**
   `flows/tg-router.json`: в `step_10` добавлена ветка
   `} else if (command === "events") { route = 'events_list'; }` (двойные
   кавычки — единственное отличие от соседней ветки `start`) — чекер отвечает
   `нарушений: 0`, код возврата 0. Хук и CI такую команду пропустят. Тот же
   результат у вычисляемой строки (`command === ('ev' + 'ents')`,
   `` command === `events` ``), массива через переменную
   (`const BAD = 'events'; const LIST = [BAD]; if (LIST.includes(command))`),
   карты (`if (MAP[command])`) и `command.endsWith('vents')`. Причины:
   `COMPARE_RE` ловит только одинарные кавычки и только литерал сразу за
   оператором; `SLASH_RE` срабатывает лишь на слэш-форме; элементы массива
   проверяются только там, где объявлены строкой. Почему это блокер: W40 и
   ADR-0025 продают скрипт как «детерминированную проверку», а самая
   вероятная будущая мутация — «добавлю ещё одну команду» — проходит мимо
   него, причём в форме, неотличимой от стиля ветки `start`. До повторного
   ревью: (а) ловить двойные кавычки и шаблонные литералы; (б) считать
   нарушением любое сравнение командной переменной с недоказуемым `'start'`
   (fail-closed: `===` с выражением, `.endsWith()`, `MAP[command]`);
   (в) добавить эти формы в `self_test()` — сейчас самопроверка их не видит
   и подтверждает ложную уверенность.

#### Проверено и не проверено

- **Живой проект** (`ap_flow_structure`, `ap_read_step_code`): `tg-router` —
  20 шагов, ветки `step_11` без `manage_open`, `step_26` нет; `step_10` —
  единственное сравнение командной переменной — `start`, любая непустая
  команда уходит в `menu`; `manage-open` отсутствует и в списке флоу, и в
  `flows/`, и в `catalog/`; `manage-open.json` не существует.
- **Экспорт ↔ инстанс**: `flows/tg-router.json` совпадает с опубликованной
  `9A4Eqy0qtPJt2Dp9qACwY` с точностью до нормализации `export-flows.sh`
  (`LOCKED` сверен GET'ом); `_manifest.json` совпадает с `ap_list_flows` по
  всем 17 флоу (flowId, имя, версия); строка W40-01 в `migrations`
  (`commit 9e5f417`, `applied_at 13:44:52Z`) даёт тот же `version_id`,
  W40-02 — `delete` на `manage-open`.
- **Офлайн**: `--self-test` проходит; на текущем дереве
  (`i18n/*.json flows/*.json`) — 0 нарушений; бит `100755` на месте, хук и
  CI вызывают чекер теми же аргументами, что использовал ревьюер.
- **Не проверено:** «старая команда (`/events`) отвечает карточкой меню»
  живым апдейтом — требует человека, журнал держит пункт открытым;
  `tools/check-migrations.py` на машине ревьюера не запустился (SSL:
  self-signed chain у python — свойство среды, не проекта), сверка
  манифеста выполнена вручную GET'ом, расхождений нет.

### Круг 2 — повторное ревью после исправления блокера (коммит `59b7627`)

- **Ревьюер**: агент-ревьюер (независимый), повторное ревью · **Дата**: 2026-09-15 · **Вердикт**: есть замечания

#### Замечания

1. **блокер** — обходы круга 1 закрыты, но чекер по-прежнему пропускает
   команду, если командная переменная обёрнута в вызов/скобки или контейнер
   собран не литералом. Проверка — на копии **реального**
   `flows/tg-router.json` (правка только в temp, репозиторий не тронут);
   каждый пример ниже даёт `нарушений: 0`, код возврата 0:
   - `if (command.trim() === 'events') route = 'events_list';` — в
     `comparisons()` операнд слева обрывается на `)` и выходит пустым, до
     проверки дело не доходит. Тот же класс: `command.toLowerCase() === 'events'`,
     `(command) === 'events'`, `String(command) === 'events'`,
     `command.slice(0, 2) === 'ev'`;
   - `switch (command.toLowerCase()) { case 'events': … }` — `SWITCH_RE`
     признаёт только голый идентификатор в скобках, тело не проверяется;
   - `const L = ['start']; L.push('events'); if (L.includes(command)) …` —
     контейнер проверяется по объявлению, последующие изменения не видны;
   - `const L = ['start'].concat(['events']); if (L.includes(command)) …` —
     `ARRAY_RE` обрезает объявление по первому `]` и **считает** контейнер
     равным `['start']`. Это уже не «неподдержанная форма», а ошибка
     разбора: докстринг обещает «элементы обязаны быть литералами»;
   - тот же класс — `Object.keys({events:1}).includes(command)`,
     `/^ev/.test(command)`, `new Set(['start','events']).has(command)`,
     `Map.get(command)`, `({ start:'menu', events:'x' })[command]`,
     `command?.endsWith('vents')`, `command['endsWith']('vents')`,
     `command[0] === 'e' && command[1] === 'v'`.
   Почему блокер: обязательство, ради которого пакет заведён, —
   «fail-closed: разрешено только доказуемое». Нормализация
   (`trim`/`toLowerCase`) перед сравнением — правдоподобный стиль, а
   `push`/`concat` и `Object.keys(…)` — обычный способ собрать список
   известных команд; чекер не валит именно те мутации, против которых он
   заведён. Речь не о погоне за обфускацией (`command[0]`, `charCodeAt` —
   заведомо мимо), а о формах, которые автор флоу напишет не задумываясь.
   Что может закрыть дыру (чинит владелец, не ревьюер): (а) операнд в
   `comparisons()` брать с балансировкой скобок, а `command.trim()`,
   `(command)`, `String(command)` считать «обёрнутой командой»;
   (б) `SWITCH_RE` — допускать вызов/скобки вокруг субъекта; (в) `ARRAY_RE` —
   требовать, чтобы `]` закрывал объявление (иначе контейнер недоказуем), а
   `push`/`unshift`/`splice`/`concat` на нём — нарушение; (г) `.has`/`.get`/
   `.test` с командной переменной — нарушение; (д) все проходящие формы —
   в `self_test()`.

2. **на будущее** — ложное срабатывание: `switch (command) { case ('start'): … }`
   (валидный, хоть и редкий, стиль) даёт `case с не-литералом "('start')"`.
   Единственное найденное ложное срабатывание, не блокирует. Заодно строка
   докстринга «`flows/_manifest.json` можно не исключать — он пропускается
   сам» неточна: манифест не пропускается, а сканируется как текстовый файл
   (`check_slash`) и просто не даёт нарушений; из-за этого счётчик «файлов
   строк» на единицу больше числа файлов `i18n/`.

#### Проверено

- Круг 1 закрыт: `command === "events"` (двойные кавычки), шаблон и шаблон
  с `${}`, конкатенация, `MAP[command]` с ключами-командами, `endsWith`/
  `startsWith`/`includes`/`match`/`search`/`replace` с чужим литералом,
  массив через переменную, инлайн-массив с чужой командой, объект через
  переменную + индекс, `command === variable`, `in`, `switch` с переменной
  в `case`, `'events' === command`, `'events' + ''`, `A + B`, spread-массив,
  `command === String('events')` — все валят проверку (exit 1 с адресом
  `tg-router/step_10`); одинарные кавычки и старый маршрут `/menu` — тоже
  (детектор старого класса мутаций не потерян).
- Обратная сторона: `--self-test` — ok; чистое дерево
  (`i18n/*.json flows/*.json`) — 17 флоу, telegram: 1, 0 нарушений;
  на вычурных сниппетах (`command === undefined`, `== null`, тернарник,
  `String(command)`, `command.length`, `command !== ''`, `split('@')`,
  `trim()`, стрелка, объект-шорткат) чекер не падает и не шумит.
- Хук и CI не менялись, вызывают `tools/check-commands.py i18n/*.json
  flows/*.json`; бит `100755` на месте; докстринг в основном соответствует
  поведению (кроме п. 2).
- Дифф ветки: коммит `59b7627` трогает `tools/check-commands.py` и журналы
  (`W36`/`W37`/`W40`); по `flows/` и `i18n/` относительно `main` изменений
  нет; `catalog/flows/manage-api.md` в ветке — из evidence-коммита `137e854`,
  не из правки чекера.

### Круг 3 — повторное ревью после второй починки (коммит `918148d`)

- **Ревьюер**: агент-ревьюер (независимый) · **Дата**: 2026-09-15 · **Вердикт**: есть замечания

#### Замечания

1. **блокер** — все обходы круга 2 закрыты (34 из 34, см. «Проверено»), но
   командная переменная по-прежнему опознаётся только в форме голого
   идентификатора: обёртка в аргументе или цепочка из двух вызовов проходит.
   Проверка — на копии **реального** `flows/tg-router.json` (правка только
   в temp); каждый пример даёт `нарушений: 0`, код возврата 0:
   - `const KNOWN = ['start', 'events']; if (KNOWN.includes(command.trim())) route = 'events_list';` —
     цикл `DANGEROUS_CALLS` ищет аргумент ровно как `IDENT`, `command.trim()`
     — вызов, и контейнер не проверяется вообще. Тот же класс:
     `KNOWN.includes(command.toLowerCase())`, `KNOWN.includes(String(command))`,
     `Object.keys({events:1}).includes(command.trim())`,
     `new Set(['start','events']).has(command.trim())`, `Map.get(command.trim())`,
     `/^ev/.test(command.trim())`;
   - `if (command.toLowerCase().startsWith('ev')) route = 'events_list';` —
     цикл методов связывает вызов со всей цепочкой `command.toLowerCase`,
     `seeded()` смотрит последний сегмент (`toLowerCase`) и пропускает. Тот же
     класс: `command.trim().endsWith('vents')`, `command.trim().match(/even/)`,
     `command.trim().replace('start','events')`,
     `command.trim().indexOf('ev') === 0`, `command.trim().includes('event')`;
   - `MAP[command.trim()]`, `command.trim() in MAP` — регексы подстановки и
     `in` требуют голый `IDENT`;
   - менее вероятная форма того же класса: `R['get'](command)`,
     `command.match?.(/events/)`.
   Почему блокер: `KNOWN.includes(command.trim())` — обычная строка, а не
   обфускация; докстринг и ADR-0025 называют вне детекции только осознанную
   обфускацию (`eval`, сборка из кодов — туда же `command.codePointAt(0) === 101`),
   этот класс в названный предел не входит, а «fail-closed: разрешено только
   доказуемое» на нём не выполняется. Куда смотреть (чинит владелец, не
   ревьюер): (а) в цикле `DANGEROUS_CALLS` принимать аргумент, если это
   `operand_is_simple(arg)` или `looks_seeded(arg)`, и падать, когда контейнер
   не литеральный список `'start'`; (б) в цикле методов смотреть голову
   цепочки (до первого вызова), а не требовать `seeded()` от всей строки;
   (в) те же головы — для `[...]` и `in`; (г) добавить формы в `--self-test`.

2. **на будущее** — ложное срабатывание: инлайн-литеральный контейнер
   `if (['start'].includes(command))` и `if ([...['start']].includes(command))`
   валится как «инлайн-контейнер с хвостом», хотя содержит только `start`.
   Ветка `check_container` для инлайн-контейнеров недостижима: `rest`
   берётся сразу после `]` и всегда начинается с `.includes(...)`, который и
   породил срабатывание; мёртвые `ARRAY_RE`, `CONTAINER_RE`, `EQ_DELIMS`
   определены и не используются. Не блокирует (fail-closed), но неверный
   отказ и мёртвый код в гейте — на чистку. Заодно: докстринг и ADR говорят
   «26 фикстур», фактически в `self_test()` их 30, а перечень «что обязана
   ловить» не покрывает найденный класс (часть замечания 1).

#### Проверено

- Все 34 формы круга 2 валят проверку (exit 1, с адресом
  `tg-router/step_10`): двойные кавычки, шаблон и `${}`, конкатенация,
  `MAP[command]`, `endsWith`/`startsWith`/`includes`/`match`/`search`/`replace`
  с чужим литералом, `command.trim()/toLowerCase()/slice` в сравнении,
  `(command)`, `String(command)`, `switch (command.toLowerCase())` с
  `case 'events'`, `push`/`concat`-массив, `Object.keys(...).includes`,
  `/re/.test`, `Set.has`, `Map.get`, `({...})[command]`, `command?.endsWith`,
  `command['endsWith']`, `command[0]`, `in`, `switch` с не-литеральным case,
  `'events' === command`, `'events' + ''`, `A + B`, spread,
  `command === String('events')`, `charCodeAt`.
- Формы из задания: `command["ev"+"ents"]`,
  `[...['events']].includes(command)`, `Array.from('events').includes(command)`,
  `[['start'],['events']].flat().includes(command)`, `'sta' + 'rt'`,
  `replace('start','events')`, `(0).toString()`, `JSON.parse('"events"')`,
  `['events'].join('').includes(command)` — все exit 1 (часть ловится грубым
  «контейнер не объявлен»/«выражение недоказуемо» — это ожидаемый
  fail-closed, не замечание).
- Легальные формы из задания проходят (exit 0): `command.trim() === 'start'`,
  `command.toLowerCase() === 'start'`, `case ('start')`,
  `String(command) === 'start'`, `(command) === 'start'`; сверх списка —
  `inputs.command === 'start'`, `switch (command) case 'start'`,
  `const L = ['start']; L.includes(command)`, `command.trim().startsWith('start')`.
- `--self-test` — ok; чистое дерево (`i18n/*.json flows/*.json`) — 17 флоу,
  telegram: 1, 0 нарушений; живой код `step_1`/`step_10` — без замечаний.
- Инфраструктура: хук и CI на месте
  (`tools/check-commands.py i18n/*.json flows/*.json`), бит `100755`;
  коммит `918148d` трогает `tools/check-commands.py`, `docs/adr/0025` и
  журнал; по `flows/` и `i18n/` относительно `main` — 0 изменений.
- Докстринг/ADR-0025: предел честности назван (обфускация, `eval` — вне
  детекции), `_manifest.json` теперь действительно пропускается; но
  обещание покрытия неполно (замечание 1), а число фикстур расходится
  (замечание 2).

### Круг 4 — повторное ревью после третьей починки (коммит `ef463d0`)

- **Ревьюер**: агент-ревьюер (независимый) · **Дата**: 2026-09-15 · **Вердикт**: есть замечания
- **Критерий вердикта**: блокер — только если форма проходит (exit 0) и
  действительно распознаёт команду, отличную от `start`. Fail-closed отказ
  (exit 1, пусть с грубым сообщением) на любой недоказуемой форме —
  ожидаемое поведение, не замечание. Ложные срабатывания на осмысленных
  формах, встречающихся в реальном коде проекта, — «важно/на будущее».

#### Замечания

1. **блокер** — голые вызовы (`f(command)`) чекер не смотрит вообще: цикл
   разбора вызовов привязан к точке/`?.`, а ветка «передача команды в вызов»
   живёт внутри него. На копии реального `flows/tg-router.json` (только
   temp) проходят с `нарушений: 0`, exit 0 и правда распознают `/events`:
   - `const known = ['start', 'events']; const isKnown = (c) => known.includes(c); if (isKnown(command)) route = 'events_list';`
   - `const known2 = ['start', 'events']; const isKnown2 = (c) => known2.includes(c); const a = command; if (isKnown2(a)) …`
   - `function isKnown3(c) { return known3.includes(c); } …` — то же самое.
   Асимметрия доказывает, что это дефект, а не «предел тайнт-анализа»:
   `obj.f(command)` валится (`передача команды в вызов `f(command)`
   недоказуема`), а `f(command)` — нет; точка обходит проверку. Туда же
   `encodeURIComponent(command)` и просто `if (f(command)) …` с неизвестной
   `f`. Что закрывает дыру (чинит владелец): скан голых вызовов
   `IDENT(args)`, где аргумент содержит seed, — отказ, кроме allowlist
   (`String`/`Number`/`Boolean`); этого достаточно, чтобы helper-обёртка над
   контейнером не проходила.

2. **на будущее** — ложные срабатывания на осмысленных формах (все
   fail-closed, не блокируют): `command.length === 0` → exit 1, хотя
   `command.length > 0` проходит (несогласованность; в проекте стиль
   `command !== ''`); `String(command).trim() === 'start'` → exit 1 (докстринг
   разрешает и обёртку, и нормализацию, но не комбинацию);
   `[...['start']].includes(command)` → exit 1 (спред литерала из одного
   `start`). Мёртвый `EQ_DELIMS` и комментарий, ссылающийся на удалённый
   `CALL_RE`; в докстринге и ADR по-прежнему «26 фикстур», фактически 33.
   Ложное круга 3 (`['start'].includes(command)`) исправлено.

#### Проверено

- Все 17 форм круга 3 валят exit 1: `KNOWN.includes(command.trim())` (и
  `toLowerCase`/`String`-варианты), `Object.keys(...).includes(command.trim())`,
  `Set.has(command.trim())`, `Map.get(command.trim())`, `/re/.test(command.trim())`,
  `command.toLowerCase().startsWith('ev')`, `trim().endsWith/match/replace/indexOf/includes`
  с чужим литералом, `MAP[command.trim()]`, `command.trim() in MAP`,
  `R['get'](command)`, `command.match?.(/events/)`.
- Новые формы: алиасы `c = command.trim()` и цепочки `a = command; b = a`
  в `KNOWN.includes(...)` — exit 1; `[command].includes(x)`,
  `x = [...[command]]`, `String(command).includes('event')`,
  `JSON.stringify(command)`, `command.concat/padEnd/split('')/repeat/normalize()`,
  `arr.find((x) => x === command)`, `obj.f(command)` — exit 1 (часть с грубым
  «контейнер недоказуем» — ожидаемый fail-closed). Проходят только
  интерпроцедурные (замечание 1).
- Легальные формы — exit 0: `command.trim() === 'start'`, `case ('start')`,
  `String(command) === 'start'`, `(command) === 'start'`, `raw = command.split('@'); raw[0]`,
  `['start'].includes(command)`, `const L = ['start']; L.includes(command)`,
  `command.length > 0`, `command?.trim() === 'start'`, `Boolean(command)`,
  `switch (command) default`.
- `--self-test` — ok; чистый прогон (`i18n/*.json flows/*.json`) — 17 флоу,
  0 нарушений; живой `step_1`/`step_10` — без замечаний; хук/CI и бит
  `100755` на месте; по `flows/` и `i18n/` относительно `main` — 0 изменений;
  коммит `ef463d0` трогает только `tools/check-commands.py` и журнал.

### Круг 5 — повторное ревью после четвёртой починки (коммит `2b662df`)

- **Ревьюер**: агент-ревьюер (независимый) · **Дата**: 2026-09-15 · **Вердикт**: есть замечания
- **Критерий вердикта** (не менялся): блокер — только форма, которая проходит
  (exit 0) и реально распознаёт команду, отличную от `start`; fail-closed
  отказ (exit 1) — ожидаемое поведение; ложные срабатывания на осмысленных
  формах реального кода — «важно/на будущее».

#### Замечания

1. **блокер** — `?.`-цепочка рвёт разбор операнда: `operand_before` считает
   `?` разделителем верхнего уровня, поэтому `command?.toLowerCase()`
   превращается в `.toLowerCase()` и сравнение не проверяется. На копии
   реального `flows/tg-router.json` (только temp) проходит с `нарушений: 0`,
   exit 0 и правда распознаёт `/events`:
   - `if (command?.toLowerCase() === 'events') route = 'events_list';`
   - `if (command?.trim() === 'events') route = 'events_list';`
   - `if (command?.toLowerCase().startsWith('ev')) route = 'events_list';`
   (в последней форме приёмник метода после `?.`-цепочки тоже обрезается по
   `?`, и `startsWith` с чужим литералом не проверяется). Что может закрыть:
   при разборе операндов не считать `?` разделителем, если он часть `?.`, —
   или нормализовать `?.` в `.` перед разбором цепочек.

2. **блокер** — IIFE: вызов `((c) => c === 'events')(command)` и
   `(function (c) { return c === 'events'; })(command)` проходят (exit 0) и
   распознают `/events`. Причина: скан голых вызовов ищет вызов с
   идентификатором-именем (`f(...)`), а скобочная стрелка/функция сразу перед
   `(command)` — не имя; параметр `c` с командой не связывается. Тот же класс —
   `((x) => KNOWN.includes(x))(command)`. Что может закрыть: матчить вызовы
   вида `)\s*(...)` (как уже сделано для `]\s*(`) с seed-аргументом — этого
   достаточно для fail-closed отказа.

3. **блокер** — shorthand-деструктуризация объекта не попадает в seeds:
   `ASSIGN_RE` не видит цель `{ c } =`. Проходят (exit 0) и распознают
   `/events`:
   - `const { c2 } = { c2: command }; if (c2 === 'events') route = 'events_list';`
   - `let c6; ({ c6 } = { c6: command }); if (c6 === 'events') route = 'events_list';`
   Вариант с default (`const { c7 = command } = {}`) ловится — `ASSIGN_RE`
   вытаскивает `c7 =`; ломается ровно shorthand. Что может закрыть: после
   разбора присваиваний дополнительно собирать имена из шаблонов
   `{ name } =` / `({ name } =` в строках, где встречается seed.

#### Проверено

- Формы круга 4 закрыты (exit 1): helper-стрелка, helper + alias,
  `function`-вариант, `encodeURIComponent(command)`, голый `f(command)` и
  `f(a)`; ложные круга 4 исправлены (exit 0): `command.length === 0`,
  `command.length > 0`, `String(command).trim() === 'start'`,
  `[...['start']].includes(command)`; `['start'].includes(command)` — тоже
  exit 0, `command[0]` — exit 1, `const raw = command.split('@'); raw[0]` —
  exit 0.
- Новые попытки — exit 1: `['a','b'].find((x) => x === command)`,
  `arr.filter((x) => KNOWN.includes(x))` с `arr = [command]`,
  `const [c] = [command]`, `[...[command]]`, `new Map([[command, 1]])`,
  шаблон `${command}`, `command.toString()`, `command == 'events'`,
  `(command || '').trim() === 'events'`, `String(command).replace('start','events')`,
  `((c) => c === 'events').call(null, command)`.
- Exit 0 (блокеры): `command?.toLowerCase()/trim() === 'events'`,
  `command?.toLowerCase().startsWith('ev')`, IIFE-варианты, shorthand-деструктуризация
  (замечания 1–3).
- `--self-test` — ok; чистый прогон (`i18n/*.json flows/*.json`) — 17 флоу,
  0 нарушений; живой `step_1`/`step_10` — без замечаний. Хук/CI и бит
  `100755` на месте; по `flows/` и `i18n/` относительно `main` — 0 изменений;
  коммит `2b662df` трогает `tools/check-commands.py`, ADR-0025 и журнал.
- Мелочи: `EQ_DELIMS` удалён, устаревший комментарий про `CALL_RE` поправлен,
  докстринг и ADR-0025 больше не обещают число фикстур (в `--self-test` их
  36 нарушений + 9 легальных). Остался неиспользуемый `SWITCH_RE` — на чистку.
  Ложных срабатываний на осмысленных формах не найдено.

### Круг 6 — повторное ревью после пятой починки (коммит `34ed422`)

- **Ревьюер**: агент-ревьюер (независимый) · **Дата**: 2026-09-15 · **Вердикт**: есть замечания
- **Критерий вердикта** (не менялся): блокер — форма, которая проходит
  (exit 0) и реально распознаёт команду, отличную от `start`; fail-closed
  отказ (exit 1) — ожидаемое поведение; ложные срабатывания на осмысленном
  коде — «важно/на будущее». Названный предел честности (обфускация, `eval`,
  сборка из кодов) — вне зоны проверки.

#### Замечания

1. **блокер** — вложенная деструктуризация объекта не попадает в seeds:
   разбор в `command_idents` ищет `{...} =` без вложенных скобок
   (`\{([^{}]*)\}\s*=`), поэтому имя из внутреннего шаблона не выводится.
   Проходят (exit 0) и распознают `/events`:
   - `const { a: { c } } = { a: { c: command } }; if (c === 'events') route = 'events_list';`
   - то же с переименованием `{ a: { c: x } }`, с `switch (c) { case 'events': … }`,
     с `{ a, b: { c } }` и на глубину 3 (`{ a: { b: { c } } }`).
   Простой shorthand (`{ c } =`) и переименование (`{ c: x } =`) закрыты — это
   ровно та же дыра на уровень глубже. Что может закрыть (чинит владелец):
   рекурсивный разбор шаблона деструктуризации вместо regex по одному уровню,
   либо fail-closed: если в строке с seed есть `{`-шаблон, цели которого
   распарсить не удалось, — отказ.

#### Проверено

- Формы круга 5 закрыты (exit 1): `command?.toLowerCase()/trim() === 'events'`,
  `command?.toLowerCase().startsWith('ev')`, IIFE стрелкой/функцией/с `KNOWN`,
  shorthand-деструктуризация (`const { c2 } = …`, `let c6; ({ c6 } = …)`);
  легальные `command?.toLowerCase() === 'start'`, `command?.trim() === 'start'`,
  `command.trim().toLowerCase() === 'start'` — exit 0.
- Новые попытки — exit 1: `const { c: x } = …`, `({ c } = …)`,
  `(command ?? '') === 'events'`, `[command].flat().includes('events')`,
  `[...command].join('') === 'events'`, `[...command].length`,
  `command.split('').join('') === 'events'`, `` String.raw`${command}` ``, `bold()`,
  `fontcolor()`, `charCodeAt(0)`, `localeCompare`, `normalize('NFKC')`,
  `eval('command === "events"')`, `new Function(…)()`, IIFE с лишними
  скобками/comma.
- Exit 0 вне критерия: `command.codePointAt(0) === 101` — сравнение с кодом
  символа, это названный предел («сборка из кодов»), не замечание;
  `command ?? '' === 'events'` и `command || 'events' === 'events'` — ошибки
  приоритета (`command ?? ('' === 'events')`), условие истинно для **любой**
  непустой команды и не выделяет конкретную, под критерий не попадают.
- `--self-test` — ok (39 нарушений + легальные); чистый прогон —
  17 флоу, 0 нарушений; `step_1`/`step_10` без замечаний; мёртвых констант
  нет (`SWITCH_RE` удалён); хук/CI и бит `100755` на месте; по `flows/` и
  `i18n/` относительно `main` — 0 изменений. Ложных срабатываний не найдено.

### Круг 7 — повторное ревью после шестой починки (коммит `776e5ca`)

- **Ревьюер**: агент-ревьюер (независимый) · **Дата**: 2026-09-15 · **Вердикт**: есть замечания
- **Критерий вердикта** (не менялся): блокер — форма, которая проходит
  (exit 0) и реально распознаёт команду, отличную от `start`; fail-closed
  отказ (exit 1) — ожидаемое; ложные на осмысленном коде — «важно/на будущее»;
  формы строго внутри названного предела честности (`eval`, сборка из
  char-кодов, намеренная обфускация) блокерами не считаются.

#### Замечания

1. **блокер** — алиасинг через `catch`-параметр не моделируется: ветка
   `throw` → `catch (c)` переносит команду в имя, которое не попадает в
   seeds. Проходит (exit 0) и распознаёт `/events`:
   - `try { throw command; } catch (c) { if (c === 'events') route = 'events_list'; }`
   - то же в стрелке: `const f = () => { try { throw command; } catch (c) { return c === 'events'; } }; if (f()) …`
   Это не `eval` и не сборка из кодов: обычная (хоть и вычурная) конструкция
   языка, и лечится локально — если `try`-блок содержит seed, сеять имя
   `catch`-параметра (одна строка в `command_idents`).

2. **на будущее** — ложное срабатывание: разбор деструктуризации сеет цели
   из любого `} =` в строке, где встретился seed, не проверяя, что шаблон
   относится к команде. Оба примера дают exit 1, хотя деструктируемое
   значение — не команда:
   - `if (command === 'start') { const { eventId } = parsed; } const ok = eventId === 'e1'; …`
     → «сравнение команды с 'e1'»;
   - `const { mode } = command === 'start' ? a : b; const ok = mode === 'edit';`
     → «сравнение команды с 'edit'».
   В живом коде проекта не встречается (чистый прогон зелёный), но осмысленный
   обработчик команды с деструктуризацией аргументов так заблокируется.
   Лечится сужением: сеять цели шаблона, только если правая часть `=`
   содержит seed.

#### Проверено

- Вложенная деструктуризация круга 6 закрыта (exit 1): `{ a: { c } }`,
  `{ a: { c: x } }`, `switch`, `{ a, b: { c } }`, глубина 3, внутренний
  default. Легальные — exit 0: `const { c5 } = { c5: command }; c5 === 'start'`,
  вложенный вариант со `'start'`, `const { c = command } = {}`, тернарник
  `command === 'start' ? 'menu' : 'none'`.
- Новые попытки — exit 1: `for (const c of [command])`, `const arr = [command];
  for (const c of arr)`, `[command].map`, `[command].forEach`,
  `Promise.resolve(command).then`, IIFE со `switch`, `const { 0: c } = [command]`,
  `const [, c] = [0, command]`, `const { ['c']: c } = { c: command }`,
  `const c = (0, command)`, IIFE identity. Exit 0 — только `try/throw/catch`
  (замечание 1).
- `--self-test` — ok (40 нарушений + легальные); чистый прогон — 17 флоу,
  0 нарушений; `step_1`/`step_10` без замечаний; хук/CI и бит `100755` на
  месте; по `flows/` и `i18n/` относительно `main` — 0 изменений.

### Круг 8 — повторное ревью после седьмой починки (коммит `fe31609`)

- **Ревьюер**: агент-ревьюер (независимый) · **Дата**: 2026-09-15 · **Вердикт**: замечаний нет
- **Критерий вердикта**: блокер — только форма, которая проходит (exit 0) и
  реально распознаёт команду, отличную от `start`; fail-closed отказ — 
  ожидаемое; ложные на осмысленном коде — «важно/на будущее»; формы строго
  внутри названного предела честности (`eval`, char-коды, намеренная
  обфускация) блокерами не считаются.

#### Замечания

Не найдено. **Серия ревью W40 завершена: круг 8 — последний.** Правило
ADR-0025 держится чекером `tools/check-commands.py` со встроенной
самопроверкой `--self-test` (41 нарушение + легальные), хуком и CI —
в рамках названного предела честности.

#### Проверено

- Круг 7 закрыт: `try { throw command; } catch (c7) { c7 === 'events' }` —
  exit 1; ложное по деструктуризации исправлено — `if (command === 'start') { const { eventId } = parsed; } const ok = eventId === 'e1'`
  и тернарный вариант дают exit 0, при этом вложенная деструктуризация с
  командой (`{ a: { c } } = { a: { c: command } }`) и `const { c } = command`
  по-прежнему exit 1.
- Попытки: `try { throw command } catch { }` и `catch ({ message })` — exit 0,
  без исключений и без ложного распознавания; `await Promise.resolve(command)`,
  `command in {} ? … : command`, `command.toString() === 'events'` — exit 1
  (fail-closed); генератор `function* g(){ yield command }` и
  `class C { #c = command }` — exit 0, ложных нет; легальные формы проекта
  (`command === 'start'`, `command.trim() === 'start'`, `switch … case 'start'`,
  `const raw = command.split('@'); raw[0]`) — exit 0.
- Вне критерия: алиасинг через генератор (`g().next().value === 'events'`)
  формально проходит, но это намеренная обфускация из названного предела
  (как `eval`), не осмысленная форма — замечанием не считается.
- `--self-test` — ok (41 нарушение); чистый прогон — 17 флоу, 0 нарушений;
  `step_1`/`step_10` без замечаний; хук/CI и бит `100755` на месте; по
  `flows/` и `i18n/` относительно `main` — 0 изменений.

## Закрытие

- **2026-09-15, решение владельца: закрыть пакет.** Единственный вживую
  непроверенный пункт — «старая команда отвечает карточкой меню» (нужен
  человек, отправляющий команду); маршрут покрыт кодом, структурой и
  самопроверкой чекера, проверка стоит одного набранного `/events` и может
  быть сделана в любой момент. Ревью — 8 кругов, чекер доведён до fail-closed;
  остаток (алиасинг через генератор) — внутри названного предела
  (намеренная обфускация), зафиксирован в журнале ревью.

## Хвосты и блокеры

- **Живые прогоны ждут человека:** «старая команда отвечает меню» проверяется
  только реальным апдейтом от человека (подделать `initData`/команду нельзя);
  нужен один заход владельца: отправить любую старую команду (`/events`) и
  убедиться, что пришла карточка меню, затем `/start` — меню.
- `check-migrations.py` требует ключ платформы и сеть; миграционные строки
  пакета вставляются после коммита (в них фиксируется его hash).

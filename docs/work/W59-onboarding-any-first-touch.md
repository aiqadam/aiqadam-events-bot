# W59. Онбординг — на первое /start вообще, не только на диплинк (ADR-0034)

- **Статус**: на проверке
- **Владелец**: агент
- **Волна**: вне BACKLOG — прямое решение владельца в чате (продолжение W58)
- **Зависит от**: W58 (без него онбординг по диплинку не работал вообще)
- **Начат**: 2026-09-20 · **Закрыт**: —

## Цель

Владелец уточнил после W58: онбординг (согласие + профиль) обязан
запускаться на **первое** обращение к боту, независимо от того, пришло оно
по диплинку события или голым `/start`, и одинаково для всех ролей
(гость/staff/owner). Решение и его границы — [ADR-0034](../adr/0034-onboarding-any-first-touch.md).

## Что построено

| Артефакт | ID / имя | Каталог |
|----------|----------|---------|
| ADR | `docs/adr/0034-onboarding-any-first-touch.md` | — |
| flow `menu` | `1DORFhP9F3W00KpKz5wDw` | [catalog/flows/menu.md](../../catalog/flows/menu.md) |
| flow `reg-profile` | `5U3Kv0cSrnvDTrbictA4L` | [catalog/flows/reg-profile.md](../../catalog/flows/reg-profile.md) |
| flow `reg-consent-mkt` | `3gLF6TcbpFObHONATQ64N` | [catalog/flows/reg-consent-mkt.md](../../catalog/flows/reg-consent-mkt.md) |

## Чек-лист готовности

- [x] `menu` (голый `/start`) гейтит на `users.profile_completed_at` до показа
  меню — гость и staff/owner одинаково
- [x] Ветка без события ведёт тот же диалог онбординга (согласие → имя →
  работа → город → «Всё верно»), что и с событием
- [x] Финал без события пишет только профиль, `registrations` не трогает
- [x] Финал без события ведёт кнопкой в Mini App `#/events`, а не билетом
- [x] SPEC (PAR-8) обновлён под новую формулировку триггера
- [x] `catalog/` совпадает с живым проектом
- [ ] Независимое ревью (пакет только что доведён)

## Как проверено

Живыми прогонами через `ap_test_flow` (с `data`-обёрткой, гоча CLAUDE.md
№13) на инстансе `events-dev`, с последующим чтением таблиц (`ap_find_records`),
не только по ответу шага:

1. `menu` с `telegramId` без строки в `users` → ветка `needs_onboard`,
   why-карточка отправлена, сессия `ob_consent` с `draft.eventId === ''`.
2. `menu` с `telegramId` владельца (профиль уже заполнен W58) → ветка
   `has_profile`, прежнее меню без изменений (роль `guest`, т.к. таблица
   `staff` сейчас пуста — не связано с этим пакетом).
3. `reg-profile`: `ob:allgood` на сессии с `eventId: ''` → `writeKind:
   'finish_no_event'`, `users` получил все шесть профильных полей плюс
   `consent_pdn`, `registrations` не тронута (в отличие от `finish` с
   событием — сверено прогоном W58).
4. `reg-consent-mkt`: `reg:mkt:yes` на той же сессии → заголовок карточки
   «Профиль сохранён» (не «Вы зарегистрированы»), второе сообщение —
   кнопка `web_app` на `#/events` вместо билета.
5. `tools/check-texts.py`/`check-commands.py` — чисто на обновлённом
   экспорте (218 пар, 0 расхождений; 0 команд).

Диагностическая строка `users`/`sessions` (`telegram_id: 888888888`) и её
сообщения удалены после проверки.

## Журнал

- **2026-09-20** — Владелец уточнил после W58: триггер онбординга — любое
  первое `/start`, не только диплинк; одинаково для всех ролей; финал без
  события — кнопка в Mini App с каталогом. Три развилки закрыты прямым
  ответом владельца (см. AskUserQuestion в диалоге), ADR написан до кода.
- **2026-09-20** — Ретрофит ROUTER в уже собранный `menu` уткнулся бы в
  гочу CLAUDE.md №10 (`ap_add_step` ROUTER через `AFTER` на шаг с
  существующим продолжением не гейтит старую цепочку). Пересобрал `menu`
  с нуля: удалил `step_1`…`step_5` с хвоста (`ap_delete_step`, по одному,
  от листьев к корню — так предсказуемо, в отличие от каскада), отстроил
  чтение профиля → CODE-гейт → ROUTER с нуля, внутри веток — прежнее меню
  (без изменений) и новая цепочка онбординга без события.
- **2026-09-20** — Для `reg-profile` ретрофит не понадобился: `step_5` уже
  был ROUTER (из W50/W58), добавление восьмой ветки — штатная операция
  `ap_add_branch`, а не риск, задокументированный в гоче №10 (там речь про
  превращение линейной цепочки в router, не про добавление ветки к уже
  существующему).
- **2026-09-20** — `reg-consent-mkt` обошёлся вовсе без структурных правок:
  ветвление по `hasEvent` целиком внутри CODE-шага (`step_5`), передаётся
  дальше готовым объектом `ticketReplyMarkup` в уже существующее поле
  `reply_markup` шага `step_9` — правка только `input`, ни одного нового
  шага.

## Ревью

> Заполняет **независимый ревьюер** по [REVIEW-CHECKLIST.md](REVIEW-CHECKLIST.md).
> Владелец пакета сюда не пишет — только отвечает под замечаниями, что исправлено.

- **Ревьюер**: независимый агент (чистый контекст), **дата**: 2026-09-20
- **Вердикт**: есть замечания

### Как проверено (и чем это ревью ограничено)

У этой сессии **нет рабочего доступа к MCP-инструментам платформы и нет
ключа REST API** (тот же вывод, теми же командами, что и в круге 2 W58,
см. подробности там): `mcp__qadam-flow__ap_*` не появились среди
вызываемых функций, `claude mcp get qadam-flow` показывает «⏸ Pending
approval (run `claude` to approve)» — подключение требует интерактивного
OAuth, которое субагент выполнить не может; `tools/export-flows.sh` не
нашёл ключ ни в `QADAM_API_KEY`, ни в Keychain. Из-за этого пункты
протокола 1/3 (живой прогон `menu`→`reg-profile`→`reg-consent-mkt` через
`ap_test_flow` с обёрткой `{"data": {...}}` и чтение `users` через
`ap_find_records` после записи) **не выполнены** — это ограничение среды,
не находка про пакет.

Сделано вместо этого — реконструкция полного графа шагов всех трёх флоу
пакета из `flows/*.json` этой ветки (побайтовый разбор JSON, с обходом
`nextAction`/`children`/`continueOnFailureBranches`; последнее не входит
в очевидный обход дерева и без него часть шагов `reg-profile` не находится
вовсе):

1. **Гейт `menu` стоит раньше чтения ролей**: `menu/step_1` (`tables-find-
   records users`, только `telegram_id`+`profile_completed_at`) →
   `step_2` (CODE, `needsOnboard`) → `step_3` (ROUTER). Ветка
   `has_profile` ведёт на `step_8` (`staff`) → `step_9` (`event_staff`) →
   `step_10` (`events`) — чтения ролей физически недостижимы из ветки
   `needs_onboard`, которая уходит сразу на `step_4` (карточка
   онбординга). Соответствует ADR-0034 п.2 («гейт один на все роли,
   роли читаются только после»).
2. **`finish_no_event` не пишет `registrations`**: цепочка
   `reg-profile/step_35 → 36 → 37 → (38/39/40 фолбэк)` использует
   `table_id = xHhYjhwqKdONkrYJGcBsz` (`users`, сверено с
   `catalog/tables/users.md`) на обоих upsert-шагах (`step_35`:
   профиль+`consent_pdn`, `step_36`: сессия `await_marketing`) — ни один
   шаг цепочки не обращается к `table_id = SM8tMxfQuQCHRDdAiNJyQ`
   (`registrations`, сверено с `catalog/tables/registrations.md`), в
   отличие от параллельных веток `finish`/`finish_lite`
   (`step_23`/`step_29`, тот же `table_id` регистраций). Разводит ветки
   `reg-profile/step_4` (CODE «render ob card»): `writeKind =
   'finish_no_event'` только когда `action === 'finish' && eventId ===
   ''` — прочитал код целиком, логика соответствует описанию ADR-0034 п.3.
3. **`reg-consent-mkt` разводит билет/каталог по `hasEvent`**:
   `step_5` вычисляет `hasEvent = eventId !== ''` (тот же `eventId`,
   что пришёл в `draft` из `reg-profile`, не из тела запроса и не из
   `callback_data`) и по нему выбирает и заголовок карточки
   (`reg.done.header` / `profile.saved.header`), и `reply_markup`
   второго сообщения (`web_app` на `#/ticket?event_id=…` или на
   `#/events`). Ветка без события не создаёт побочных обращений к
   `registrations` — `step_3`/`step_4`/`step_6` этого флоу трогают
   только `users`/`events`/`sessions`.
4. **`esc()` в `menu/step_4`** (новая копия эталона, добавленная этим
   пакетом) — побайтово равна `catalog/snippets/markdown-v2.md` (один
   `\-`, без регресса на двойной `\\-`).
5. Офлайн-чекеры на актуальном экспорте: `check-texts.py i18n/ru.json
   flows/*.json` — 25 флоу, 218 пар, 0 расхождений (совпадает с числом
   в журнале); `check-commands.py` — 0 нарушений, самопроверка ok;
   `check-export-secrets.sh` — чисто.
6. `docs/SPEC.md` (PAR-8) сверен с ADR-0034 — формулировка «первое
   касание бота, не только первый диплинк», гейт `profile_completed_at`,
   финал без события в каталог — согласована с кодом и с самим ADR.

### Замечания

1. **блокер (процессный)** Протокол REVIEW-CHECKLIST п.1/п.3 (живой
   инстанс через MCP, независимый различающий прогон `menu`/`reg-profile`,
   чтение `users`/`sessions` после записи) не выполнен по причине,
   описанной выше — среда ревью не даёт ни MCP, ни REST-ключа. Найденное
   выше — статический анализ актуального экспорта, а не подтверждение
   того, что это реально исполняется на инстансе `events-dev` (в
   частности, живой прогон с непустой таблицей `staff` для роли owner/
   staff, упомянутый в «Хвостах» самого пакета, никем ещё не сделан).
   Нужен круг с агентом, у которого есть работающий доступ (пройденный
   `/mcp` → `Authenticate` для проекта в этой среде, либо ключ в
   `QADAM_API_KEY`/Keychain).
2. **на будущее** `catalog/snippets/markdown-v2.md`, раздел «Где
   используется», не включает `menu` — `menu/step_4` (CODE «build
   onboarding entry card (no event)») содержит побайтовую копию `esc()`
   (подтверждено сверкой JSON, см. выше), но флоу в таблице «Встраивают»
   нет. По правилу REVIEW-CHECKLIST п.2a таблица должна быть полна —
   допишите строку `menu` / «CODE «build onboarding entry card»».
   - *Исправлено*: строка `menu` добавлена в `catalog/snippets/markdown-v2.md`.
     (2026-09-20)

Статус пакета остаётся **на проверке**: блокер 1 не снят (замечание 2 —
минорная правка каталога, не блокирует).

## Хвосты и блокеры

- Независимое ревью не запущено.
- Живая проверка `menu`/`reg-profile` с ролью staff/owner (с непустой
  таблицей `staff`) не делалась — таблица сейчас пуста на инстансе
  `events-dev` (не связано с этим пакетом). Логика гейта не зависит от
  роли (гейт стоит раньше чтения ролей), но живого прогона с реальным
  staff-пользователем нет.
- `reg-api` (вкладка «Профиль» и регистрация из каталога `registered_profile`)
  этот пакет не трогал — там `eventId` всегда есть (каталог открывается на
  конкретное событие), ADR-0034 на него не распространяется.

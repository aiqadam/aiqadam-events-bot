# W70. Phase 2: кнопка «Назад» в Mini App на Telegram Desktop

- **Статус**: в работе
- **Владелец**: агент
- **Волна**: P2 ([#122](https://github.com/aiqadam/aiqadam-events-bot/issues/122))
- **Зависит от**: —
- **Начат**: 2026-09-24 · **Закрыт**: —

## Цель

На Desktop у экранов Mini App нет способа вернуться: системного жеста нет, а
нативная кнопка Telegram включается не всегда. Правило (одобрено владельцем):
**пришёл изнутри приложения → «Назад» есть; открыл из чата напрямую → нет.**
Плюс фолбэк экранной кнопкой на клиентах без нативной.

## Что построено

| Артефакт | ID / имя | Каталог |
|----------|----------|---------|
| `miniapp/src/lib/nav.ts` | — | — |
| `miniapp/src/components/BackButton.tsx` | — | — |
| `docs/MINIAPP-UX.md` п.1 | — | — |

## Чек-лист готовности

- [x] Правило «пришёл изнутри → Back» зафиксировано в `MINIAPP-UX.md`
- [x] `#/ticket`, `#/feedback`, `#/scan` показывают нативную «Назад» только при внутреннем входе
- [x] В визарде `#/manage` при `step > 0` «Назад» = предыдущий шаг
- [x] Фолбэк экранной кнопкой на клиентах без `BackButton`/`< 6.1`
- [x] Нет дублей (экранная кнопка не показывается при рабочей нативной)
- [x] `catalog/` совпадает с живым проектом (флоу не меняются)

## Как проверено

- `npm run build` (tsc + vite) — зелёный.
- **Headless-стенд** `~/w41/w70-shots.mjs` (статика `dist` + конфигурируемый мок
  Telegram.WebApp с/без `BackButton` и вебхуков): **11/11** —
  нативная есть: прямой вход из чата → скрыта, внутренний переход (каталог →
  билет/сканер) → показана; нативной нет → экранный фолбэк только при внутреннем
  переходе, при прямом — нет; `isVersionAtLeast('6.1') === false` → фолбэк;
  визард `#/manage/new` на шаге 2: «Назад» показана и возвращает на шаг 1.
  Скриншот фолбэка — `~/w41/shots-w70/01-ticket-fallback-light.png`.
- Офлайн-проверки: секреты чисто, тексты 254/0, команды 0, агенты 0,
  `prototypes/check.mjs` OK.
- Флоу не менялись → публикаций и `migrations` нет.

## Журнал

- **2026-09-24** — взятие пакета, ветка `w70-back-button`.
- **2026-09-24** — «пришёл изнутри» определяю в `App.tsx`: `useRoute` помнит
  предыдущий и текущий hash и ставит `fromApp`, если оба — роуты SPA
  (`ticket`/`scan`/`manage`/`events`/`feedback`). Отдаю флагом в `Ticket`/`Scan`/
  `Feedback`; модульный флаг/`sessionStorage` отверг — пережил бы перезагрузку
  WebView и показал бы «Назад» там, где истории нет.
- **2026-09-24** — нативная кнопка (`useBackButton`) и экранный фолбэк
  (`components/BackButton.tsx`, вид как у «К списку») разведены по
  `lib/nav.ts:hasNativeBackButton()`: при рабочей нативной (Bot API 6.1+)
  фолбэк не рисуется — дубль запрещён (MINIAPP-UX п.1).
- **2026-09-24** — в визарде `#/manage` на шаге > 0 нативная «Назад» теперь ведёт
  на предыдущий шаг (раньше при создании из чата её не было вовсе, при правке —
  сразу к списку). Корневые экраны (список, создание с шага 0) остаются без неё.
- **2026-09-24** — правило зафиксировано в `docs/MINIAPP-UX.md` п.1.

## Ревью

> Заполняет **независимый ревьюер** по [REVIEW-CHECKLIST.md](REVIEW-CHECKLIST.md).

- **Ревьюер**: — · **Дата**: — · **Вердикт**: —

### Замечания

## Хвосты и блокеры

- —

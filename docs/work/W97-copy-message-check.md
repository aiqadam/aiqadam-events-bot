# W97. Копирование сообщения с фото (copyMessage)

- **Статус**: в работе
- **Владелец**: агент
- **Волна**: Phase 0 ([issue #149](https://github.com/aiqadam/aiqadam-events-bot/issues/149))
- **Зависит от**: —
- **Начат**: 2026-09-23 · **Закрыт**: —

## Цель

Выяснить, может ли платформа скопировать сообщение с фото (`copyMessage`)
и что переживает копию. Разблокирует #131 (анонс спикера с фото), #132, #147.

## Что построено

| Артефакт | ID / имя | Каталог |
|----------|----------|---------|
| временный flow `tmp-copy-test` | создаётся и удаляется | — (не экспортируется) |

## Чек-лист готовности

- [ ] Все 7 пунктов отвечены
- [ ] Временный флоу удалён, запись в `migrations`
- [ ] #131 обновлён по выводу
- [ ] `catalog/` совпадает с живым проектом

## Как проверено

- <заполняется по ходу>

## Журнал

- **2026-09-23** — пакет взят. `ap_research_pieces`: у `@aiqadam/qadam-telegram-bot`
  действия `copy_message` нет; есть `forward_message` и `custom_api_call`
  (произвольный вызов Bot API, в т.ч. `copyMessage`), `send_media` умеет
  caption + `reply_markup` (кнопка на фото).

## Ревью

> Заполняет независимый ревьюер по [REVIEW-CHECKLIST.md](REVIEW-CHECKLIST.md).

## Хвосты и блокеры

- <заполняется>

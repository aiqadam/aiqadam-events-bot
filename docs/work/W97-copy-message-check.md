# W97. Копирование сообщения с фото (copyMessage)

- **Статус**: в работе
- **Владелец**: агент
- **Волна**: Phase 0 ([issue #149](https://github.com/aiqadam/aiqadam-events-bot/issues/149))
- **Зависит от**: —
- **Начат**: 2026-09-23 · **Закрыт**: —

## Цель

Выяснить, может ли платформа скопировать сообщение с фото (`copyMessage`)
и что переживает копию. Разблокирует #131 (анонс спикера с фото), #132, #147.

## Что построено

| Артефакт | ID / имя | Каталог |
|----------|----------|---------|
| — (эксперимент через `ap_run_action`, временного флоу не потребовалось) | — | — |

Отличие от плана issue: вместо временного флоу `tmp-copy-test` все вызовы
сделаны через `ap_run_action` (`custom_api_call`, `send_text_message`) —
одноразовые прогоны действий не создают флоу и не требуют записи в
`migrations`. Живых флоу (`bcast-*`, `tg-router`) не трогал.

## Чек-лист готовности

- [x] Все 7 пунктов отвечены
- [x] Временных артефактов не осталось (флоу не создавался), `migrations` не требуется
- [x] #131 обновлён по выводу (комментарий в #131 от 2026-09-23)
- [x] `catalog/` совпадает с живым проектом (живые флоу не менялись)

## Ответы (#149)

1. **Действие копирования в piece.** `copy_message` в
   `@aiqadam/qadam-telegram-bot` **нет**. Есть `forward_message` (форвард),
   `send_media` (caption + `format` + `reply_markup`), `send_media_group` и
   `custom_api_call` — произвольный вызов Bot API. Копирование делается
   `custom_api_call` (`/copyMessage`, `/copyMessages`).
2. **Прямой вызов Bot API.** Работает. `custom_api_call` подставляет токен из
   connection сам (проверено `GET /getMe`), тело — `{"data": {…}}`
   (обязательная обёртка DYNAMIC-пропа `body`). `http : send_request` с
   `{{variables['BOT_TOKEN']}}` тоже сработал бы, но вписывает токен в URL
   шага и прогонные логи (ADR-0005) — для `copyMessage` не нужен.
3. **Что переживает копию** (`copyMessage` 2533/2549/2551 → 2534/2550/2552):
   фото ✅, caption ✅, **жирный** ✅, ссылка под словом (`text_link`) ✅.
   Ссылка-превью отдельно не проверялась (в тесте была `text_link`-сущность,
   а не голый URL).
4. **Кнопка на скопированном фото — по умолчанию теряется.** `copyMessage`
   без параметра `reply_markup` снял инлайн-клавиатуру (2533 → 2534, 2551 →
   2552: фото/текст на месте, кнопки нет). С **явным** `reply_markup` в
   `copyMessage` кнопка появляется (2551 → 2553, проверено владельцем).
5. **Альбом (`copyMessages`) — группировка сохраняется.** Переопрос:
   альбом из 2 фото (2554–2555, `media_group_id` `14320883211626706`) после
   `copyMessages` пришёл **одним альбомом** (2556–2557) — подтверждено
   владельцем. Первый, менее аккуратный тест (3 фото, 2535–2537 → 2538–2540)
   дал ответ «три отдельных»; расхождение объяснимо ошибкой визуальной
   идентификации в зашумлённом чате, но для #131 Part 2 нужен различающий
   перепрогон на 3+ фото. Для Part 1 (одиночное фото) это не блокер.
6. **Что приходит в `tg-router` при форварде фото с подписью.**
   Различающий прогон на живом форварде (update `24020470`, прогон
   `Vy1QBuvtUrqeSZyzn5keI`): `message_id` ✅ (2541), `chat.id` ✅,
   `photo[]` ✅ (три размера, file_id), `caption` ✅, `caption_entities` ✅
   (в т.ч. `bold`, `text_link`, `custom_emoji`, `blockquote`),
   `forward_origin` ✅. `media_group_id` в одиночном фото отсутствует
   (появляется у альбомов; на стороне отправки `sendMediaGroup` поле
   наблюдалось). `tg-router/step_1` уже достаёт `hasPhoto`/`photoFileId`
   (берёт старший размер), но `bcast-draft` в payload передаёт только
   `fwdText` — фото теряется на этом шаге, это и есть работа #131.
7. **`web_page_preview: false` работает.** Проп называется «**Disable** Web
   Page Preview»: `true` → `link_preview_options.is_disabled=true` (превью
   нет), `false` → `link_preview_options.url` (превью есть). То есть
   превью в `reg-start`/`bcast-*` при `web_page_preview:false` — ожидаемое
   поведение пропа, а не дефект; «починка» #130 item 3 — выставить `true`.

**Вывод для #131 (Part 1, одиночное фото):** вариант A выполним через
`custom_api_call /copyMessage` с **обязательным явным `reply_markup`** —
иначе кнопка «Отписаться» теряется. Альтернатива — `sendPhoto` по
`photoId` из форварда + `caption` (форматирование переносится сущностями).
Альбом (Part 2): `copyMessages` группировку сохраняет (2 фото → один альбом),
так что Part 2 выглядит выполнимым; поведение на 3+ фото перепроверить при
взятии #131 Part 2.

## Как проверено

- `ap_research_pieces` + `ap_get_piece_props` — состав действий и пропов.
- `ap_run_action` `custom_api_call GET /getMe` — токен подставляется из
  connection (run `z3YLnziIQvJ2SW17248JR`).
- `ap_run_action` `custom_api_call POST /sendPhoto` — фото по URL +
  `caption` HTML + `reply_markup` (run `0ZpMpiGKnlvsyYWOoOjvH`, msg 2533).
- `copyMessage` без `reply_markup` → фото/текст есть, кнопки нет
  (runs `71qMgLS7xYJOKSw8cq5pd` msg 2534; `doHwz3Xu0Y83c9EAzPVgQ` msg 2552).
- `copyMessage` с явным `reply_markup` → кнопка есть (run
  `qNVPAXk9zqrxxmG9zmTim`, msg 2553).
- `sendMediaGroup` + `copyMessages`, 3 фото → визуально «три отдельных»
  (runs `8BlXOwPaKlvouM86KPsPb` msgs 2535–2537; `ASJOvRrW4DAz75pjW2Bhz` msgs
  2538–2540); **переопрос на 2 фото** → один альбом (runs
  `iiyNUSqlVgAE42M9bnWBd` msgs 2554–2555; `P2FYG0w56LM9GiDEB4nPk` msgs
  2556–2557).
- `send_text_message` с `web_page_preview: true`/`false` — ответ Bot API
  показал `link_preview_options.is_disabled=true` / `.url`
  (runs `lLBkUSNLkVI32erWc54Tn`, `gjr4jWjO1c47ongVls8vY`).
- Форвард фото с подписью владельцем — живой прогон `tg-router`
  `Vy1QBuvtUrqeSZyzn5keI`.
- Визуальную сверку «оригинал/копия» делал владелец в своём чате
  (тестовый получатель — его собственный аккаунт, не сторонние люди).

## Журнал

- **2026-09-23** — **ревью круг 1** (независимый агент): блокеров и «важно»
  нет, три «на будущее». По замечанию про альбом выполнен различающий
  перепрогон 2-фото альбома — `copyMessages` группировку **сохраняет**,
  первоначальный вывод («три отдельных») был ошибкой визуальной сверки; #131
  получил комментарий-коррекцию. Галочка #131 в чек-листе отмечена.
- **2026-09-23** — пакет взят; `ap_research_pieces` сразу показал, что
  `copy_message` в piece нет, но есть `custom_api_call`.
- **2026-09-23** — `custom_api_call` body — DYNAMIC с обязательным
  подключом `data`; без обёртки `{"body":{"data":{…}}}` тело уходит пустым
  (`400 there is no photo in the request`). Это же объясняет, почему
  `send_media` не годится для фото по URL: у её `media` только file-upload
  или `photoId`.
- **2026-09-23** — первая визуальная сверка дала ложный «фото пропало»
  (владелец смотрел не то сообщение в зашумлённом чате); чистый
  изолированный тест 2549→2550 подтвердил, что фото переживает копию.
  Урок: просить сверку по конкретным номерам сообщений.
- **2026-09-23** — владелец прислал тестовые сообщения в свой чат; после
  эксперимента остались сообщения 2533–2553 (оригиналы и копии). Не удалял —
  это материал для сверки; при необходимости бот может удалить их
  `deleteMessage`.

## Ревью

- **Ревьюер**: review-agent · **Дата**: 2026-09-23 · **Вердикт**: есть замечания

### Проверено

- `ap_research_pieces @aiqadam/qadam-telegram-bot`: действия копирования
  (`copy_message`/`copy_messages`) нет; есть `forward_message`, `send_media`,
  `send_media_group`, `custom_api_call` — п. 1 подтверждён.
- `ap_get_piece_props custom_api_call` (auth `TZTlXaCEO2hEvimUowbSA`): `body` —
  DYNAMIC, подключ `data` (JSON, required) — п. 2 подтверждён (состав пропа;
  сам вызов не перепрогонялся — `ap_run_action`-прогоны эфемерны).
- `ap_get_piece_props send_text_message`: `web_page_preview` = «Disable Web
  Page Preview», default `false` — п. 7 подтверждён.
- `ap_get_run Vy1QBuvtUrqeSZyzn5keI` (живой `tg-router`, update 24020470):
  `hasPhoto=true`, `photoFileId` (старший размер 800×800), `caption`,
  `caption_entities` (`bold`/`text_link`/`custom_emoji`/`blockquote`),
  `forward_origin`; `media_group_id` отсутствует — п. 6 подтверждён.
- issue [#149](https://github.com/aiqadam/aiqadam-events-bot/issues/149):
  комментарий-Result есть и совпадает с журналом. Комментарий в
  [#131](https://github.com/aiqadam/aiqadam-events-bot/issues/131) («Вводные
  от #149») не противоречит фактам и журналу.
- `flows/` временного флоу не содержит; секретов нет; офлайн-проверки — все
  exit 0. `check-migrations.py` без ключа — неприменимо (артефактов не было).
- **Не проверено**: `ap_run_action`-прогоны (2533–2553) — этот вид прогонов
  `ap_get_run` не отдаёт даже до удаления; визуальную сверку «оригинал/копия»
  делал владелец в Telegram.

### Замечания

1. **на будущее** — п. 2 #149 (прямой Bot API через `http : send_request` с
   `{{variables['BOT_TOKEN']}}`) фактически не прогонялся: журнал честно пишет
   «тоже сработал бы», а шаблон-ответ в #149 говорит «Via http … works — но
   через custom_api_call». Проверен только `custom_api_call`. Для рекомендации
   #131 (`custom_api_call`) это неважно, но пометка «http-путь не проверялся»
   была бы точнее.
   - *Принято*: `http + {{BOT_TOKEN}}` намеренно не прогонялся (это вписало бы
     секрет в URL шага и логи, ADR-0005); рекомендация для #131 — однозначно
     `custom_api_call`. Пометка в журнале уточнена.
2. **на будущее** — вывод про альбом («`copyMessages` → три отдельных
   сообщения») опирается на уже нечитаемый `ap_run_action`-прогон и расходится
   с документированным поведением Bot API (`copyMessages` сохраняет группировку
   для сообщений одного альбома). W97 сам просит различающий перепрогон — при
   взятии #131 Part 2 это обязательно, иначе дизайн альбома построится на
   возможном артефакте теста.
   - *Исправлено*: выполнен различающий перепрогон на 2 фото — `copyMessages`
     группировку **сохраняет** (один альбом), ответ журнала и #131 исправлены
     (2026-09-23); проверка на 3+ фото остаётся хвостом #131 Part 2.
3. **на будущее** — в чек-листе журнала `[ ] #131 обновлён по выводу` не
   отмечен, хотя комментарий в #131 есть (DoD-пункт фактически выполнен).
   - *Исправлено*: галочка отмечена, добавлен комментарий-коррекция в #131.

### Круг 2 (2026-09-23)

- **Вердикт**: есть замечания

**Закрытие замечаний круга 1:**

1. **на будущее 1** — закрыто. Журнал (п. 2) уточнён: путь `http +
   {{variables['BOT_TOKEN']}}` намеренно не прогонялся (секрет попал бы в URL
   шага и логи, ADR-0005), рекомендация для #131 — `custom_api_call`.
2. **на будущее 2** — исправлено частично (см. новое замечание 1): журнал
   («Ответы», п. 5, и «Вывод для #131») и #131 исправлены на «`copyMessages`
   группировку сохраняет»; в #131 есть комментарий-коррекция от 2026-09-23.
3. **на будущее 3** — закрыто: галочка в чек-листе отмечена, коррекция в #131
   присутствует.

**Новые замечания:**

1. **важно** — канонический Result-комментарий в [#149](https://github.com/aiqadam/aiqadam-events-bot/issues/149)
   остался с **неверным** выводом об альбоме. #117 требует «Each result is a
   comment in the issue using its "Result" template» — это артефакт фазового
   гейта. В нём по-прежнему: «Album (copyMessages): не одним альбомом — пришли
   тремя отдельными сообщениями» и «Альбом через copyMessages одним альбомом
   не собрать», тогда как исправленный вывод — «группировку сохраняет». #149
   содержит **один** комментарий, `updated_at == created_at` (не редактирован).
   Читатель гейта получает факт, противоположный журналу и #131. Правится
   одним комментарием-коррекцией (или правкой Result) в #149.
2. **на будущее** — доказательство исправленного вывода снова нечитаемо:
   `ap_get_run` по `iiyNUSqlVgAE42M9bnWBd` и `P2FYG0w56LM9GiDEB4nPk` отвечает
   «not found» (это `ap_run_action`, не flow-run; `ap_list_runs` их не
   содержит). Независимо подтвердить «2 фото → один альбом» нельзя —
   вывод принят по свидетельству владельца о визуальной сверке; он согласуется
   с документированным поведением Bot API. При использовании альбомного пути
   в #131 Part 2 сделать **читаемый** прогон (временный флоу, как в W98).

**Проверено:** состав `@aiqadam/qadam-telegram-bot` (действия копирования нет —
`custom_api_call`/`forward_message`/`send_media`/`send_media_group`); #149
(один Result-комментарий, не редактирован); #131 (комментарий-коррекция есть);
`ap_get_run` по двум заявленным перепрогонам — «not found»; офлайн-проверки —
все exit 0; `flows/` временного флоу не содержит.

**Не проверено:** сами `ap_run_action`-прогоны и визуальная сверка альбома
(у агента нет доступа к Telegram); `check-migrations.py` — ключа платформы нет
(артефактов не создавалось — неприменимо).

## Хвосты и блокеры

- Визуальные проверки зависят от владельца (у агента нет доступа к Telegram).
- Ссылка-превью при копировании и `media_group_id` у форварда альбома
  отдельно не проверялись — для Part 1 (#131, одиночное фото) не требуется.

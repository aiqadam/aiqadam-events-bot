# W30. Бренд Mini App и `docs/VOICE.md`

- **Статус**: готов — решением владельца
- **Владелец**: агент W30
- **Волна**: «качество того, что есть» ([ROADMAP.md](../ROADMAP.md))
- **Зависит от**: [Q40](../OPEN-QUESTIONS.md#q40) — закрыт внутри пакета
- **Начат**: 2026-09-13 · **Закрыт**: 2026-09-14

## Цель

Mini App выглядит как AI Qadam — на брендовых токенах и компонентах, без
собственных цветов и шрифтов. Голос бота кодифицирован дельтой там, где
design system бренда не действует (сообщение Telegram — не цифровой UI).

## Что построено

Флоу и таблицы пакет не трогает: MCP использовался только на чтение
(`ap_list_flows` — сверка каталога с инстансом, 22 флоу, расхождений нет).

| Артефакт | Что это |
|----------|---------|
| `miniapp/vendor/aiqadam-brand-subset.css` | вендоренное подмножество бренда, коммит `8ef4a8059816aec015c8f56f6d38d2d63993c890` |
| `miniapp/vendor/aiqadam-brand-LICENSE.txt` | MIT бренда рядом с копией |
| `miniapp/ticket.html` | билет гостя, перевёрстан на токены и компоненты |
| `miniapp/index.html` | сканер контролёра, то же |
| [`docs/VOICE.md`](../VOICE.md) | дельта голоса под Telegram |
| [Q40](../OPEN-QUESTIONS.md#q40) | закрыт замером |
| [`catalog/overview.md`](../../catalog/overview.md) | раздел «Mini App» — его раньше не было |
| [REVIEW-CHECKLIST](REVIEW-CHECKLIST.md) п. 3a | четыре пункта вместо двух заготовленных |

## Чек-лист готовности

- [x] [Q40](../OPEN-QUESTIONS.md#q40) закрыт **числом** — таблица весов,
      решение следует из неё
- [x] `ticket` и `scan` на брендовых токенах и компонентах; в файлах страниц
      нет ни одного своего значения цвета и шрифта
- [x] `ticket` открывается на плохой связи — дроссель Slow 3G, и QR не просто
      виден, а **декодирован** обратно в исходный payload
- [x] `docs/VOICE.md` существует, является дельтой, на него ссылается
      REVIEW-CHECKLIST
- [x] `miniapp/` не содержит форка бренда — подмножество с зафиксированным
      коммитом и инструкцией обновления в шапке
- [x] независимое ревью — шесть кругов; вердикта «замечаний нет» нет,
      пакет закрыт **решением владельца** (см. «Закрытие» ниже)
- [x] `catalog/` совпадает с живым проектом (флоу не менялись; каталог
      дополнен разделом про Mini App, которого не было)

## Как проверено

Прогоны — headless Chromium (Playwright), страницы поднимались локальным
статическим сервером, Telegram WebApp и оба вебхука заглушены.

| Проверка | Как | Результат |
|---|---|---|
| билет на плохой связи | CDP `Network.emulateNetworkConditions`, 400 кбит/с, RTT 400 мс | QR виден за **2,4 с** |
| то же, брендовый CSS не доехал | запрос CSS отбит `route.abort()` | QR виден за **2,2 с** |
| QR **читается**, а не «виден» | скриншот плиты (реальные пиксели) → `jsQR` | payload совпал в светлой теме, тёмной и без брендового CSS |
| зона покоя | перебор длин payload по границам парсера × ширины экрана | ≥ 4 модулей везде, худший случай 4,14 |
| плита не вылезает из карточки | 320 / 360 / 390 / 430 px, координаты плиты и карточки | внутри везде |
| тон исхода чекина | четыре статуса × две темы, `getComputedStyle` | `--success` / `--warning` / `--destructive`, включая тёмные варианты |
| QR в тёмной теме | `colorScheme: 'dark'` | подложка `oklch(0.99 0 0)` на фоне `oklch(0.145 0 0)` — плита осталась светлой |
| узкие экраны | 320 / 360 / 390 / 430 px | горизонтальной прокрутки нет ни на одном |
| своих цветов и шрифтов нет | grep по `miniapp/*.html` | ни одного совпадения |
| каталог против инстанса | `ap_list_flows` | 22 флоу, как в `catalog/overview.md` |

## Журнал

- **2026-09-13** — пакет взят. Бренд склонирован на чтение
  (`aiqadam/brand.aiqadam.org`, public, коммит `8ef4a8059816aec015c8f56f6d38d2d63993c890`).

- **2026-09-13** — Q40 закрыт, и ответ оказался не тем, к которому
  склонялся сам вопрос. Замер развалил задачу надвое: CSS весит 10,2 КиБ
  (3,6 КиБ gzip), шрифты — **444 КиБ**, то есть 97,5 % всего веса. Спор
  «ссылка против вендоринга» касается меньшей части, а решается на самом
  деле вопрос «грузим ли мы вообще три семейства в дверях зала». Ответ:
  CSS вендорим, шрифты не грузим никак — токены `--font-*` взяты у бренда
  вместе с их же системными фолбэками, поэтому своей типографики не
  заведено. Подробности и таблица — в самом Q40.

- **2026-09-13** — вендоринг сделан **выемкой именованных блоков**, а не
  переписыванием: границы блоков помечены комментариями с именем исходного
  файла и строками, в шапке — коммит бренда и процедура обновления.
  Ни одно значение не набрано руками; проверка «не разошлось ли» сводится
  к повторной выемке тех же блоков и дифу.

- **2026-09-13** — `EmptyState` взят у бренда, но **не как рамка**.
  Первая версия сканера показывала вердикт чекина внутри `.empty-state`;
  на скриншоте сразу стало видно, что пунктирная рамка «здесь пусто»
  вокруг фразы «Азиза — отмечен» противоречит сама себе. Вердикт переехал
  в `.card`, от EmptyState осталась типографика (`.empty-heading`,
  `.empty-desc`). Заодно: под остановленным сканером висело «Проверка…» —
  `stopAll` теперь чистит строку состояния.

- **2026-09-13** — зона покоя QR была вдвое меньше нормы: 16 px при
  модуле ≈ 7,7 px (29 модулей на 224 px при уровне коррекции H, который
  qrcode.js ставит по умолчанию) — это ≈ 2 модуля вместо требуемых 4.
  Исправлено на `padding: 32px`.

  **Здесь же я ошибся, и ошибку нашло ревью.** Я написал — в журнал,
  в комментарий кода, в каталог и, хуже всего, в чек-лист ревьюера как
  общепроектное правило, — что без брендового CSS QR «перестаёт
  декодироваться». Это оказалось артефактом моего же харнесса: он не снимал
  пиксели страницы, а собирал синтетический canvas и заливал его
  `getComputedStyle('#qr').backgroundColor`; без CSS это `rgba(0,0,0,0)`, и
  не декодировался именно этот холст. На реальных пикселях
  (`locator('#qr').screenshot()` → PNG → jsQR) код читается во всех
  сценариях, включая состояние **до** починки, в светлой и тёмной теме:
  без брендового CSS фон страницы — белый по умолчанию браузера.

  Ирония в том, что это ровно тот класс ошибки, который пакет нашёл у себя
  же: к «элемент на экране» ≠ «код читается» добавилось третье —
  **«код читается в харнессе» ≠ «код читается»**. Урок дороже находки:
  проверка через синтетику проверяет синтетику.

  Литерал `#ffffff`, который держался на этом обосновании, **убран**:
  самостоятельного основания у него не было, а платить за него постоянным
  исключением в чек-листе — дорого. `padding: 32px` остаётся: у него
  основание своё, спецификационное.

- **2026-09-13** — `VOICE.md` написан по **замеру корпуса**, а не по вкусу.
  254 строки `i18n/ru.json` дали числа, которые теперь и есть норма:
  медиана сообщения 31 знак, p90 64, максимум 130; эмодзи в двух строках
  из 254; в 59 подписях кнопок ноль точек и ноль эмодзи; «!» трижды и все
  в приветствиях. Правило вроде «эмодзи — максимум один и только в первой
  строке» не придумано, а прочитано из того, что в проекте уже написано.
  Всё, что сказал бренд (семь принципов, написание имени, смысл иконок),
  процитировано ссылкой и не пересказано — это прямое требование ADR-0019.

- **2026-09-13** — попутно уточнён факт для `VOICE.md`: разметка в проекте
  **не единообразна и это нормально** — по экспорту `flows/*.json` 25 шагов
  отправки с `format: None` и 10 с `MarkdownV2`, и все десять — гостевой
  путь регистрации, где карточке нужен жирный заголовок.

## Ревью

- **Ревьюер**: независимый агент с чистым контекстом · **Дата**: 2026-09-13 ·
  **Вердикт**: есть замечания (блокеров нет) — круг 1

Подтверждено ревью независимой проверкой: выемка бренда дословна во всех
8 блоках, AppSec чист (весь вывод сервера через `textContent`, `initData`
только телом POST), карта `TONE` покрывает все семь статусов `checkin-api`,
логика сканера не потеряна, `VOICE.md` бренд не дублирует, числа Q40
пересчитаны независимо и сошлись (по шрифтам — расхождение 0,13 %).

### Замечания

1. **важно** — не взята строка reset бренда: блок помечен «База: **reset**…»
   и указан как `tokens.css:101-114`, а `html, body { margin: 0; padding: 0; }`
   лежит на строке 100 — за границей. Страницы потеряли `body{margin:0}`,
   который до пакета стоял в них явно; на 320 px плита QR вылезала за рамку
   карточки, а проверка «горизонтальной прокрутки нет» этого не ловила,
   потому что вылет съедал как раз маргин.
   - *Исправлено*: блок переснят как `tokens.css:99-114`, дословность всех
     8 блоков перепроверена скриптом. Одного reset'а не хватило: плита
     224 + 2×32 не помещается во внутренность карточки на 320 px, поэтому
     размер кода теперь считается от доступной ширины (224 px максимум,
     172 px на 320 px), а плита тянется во всю ширину карточки. Код
     **перерисовывается** под размер, а не масштабируется картинкой, так
     что модули остаются резкими, а зона покоя в модулях только растёт.
     Проверено на 320/360/390/430: плита внутри карточки везде (2026-09-13)

2. **важно** — «без брендового CSS QR перестаёт декодироваться» не
   воспроизводится на реальных пикселях; утверждение попало в четыре места,
   включая REVIEW-CHECKLIST как правило для всех будущих пакетов.
   - *Исправлено*: утверждение снято отовсюду — из журнала (запись выше
     переписана), из комментария в `ticket.html`, из `catalog/overview.md`
     и из чек-листа, где строка вернулась к простому «grep обязан молчать».
     Литерал `#ffffff`, державшийся на этом обосновании, убран: в `miniapp/`
     не осталось ни одного литерала цвета. `padding: 32px` оставлен — его
     основание своё: 4 модуля зоны покоя, худший случай 4,15 (2026-09-13)

3. **важно** — три числа в `VOICE.md` не сходятся с корпусом: 254 против
   256 строк, медиана 31 против 30, тире 27 против 29.
   - *Исправлено*: расхождение оказалось не в числах, а в неназванном
     определении корпуса — я считал без служебных `_selftest.*`, ревьюер со
     всеми. Определение теперь названо в самом файле («все ключи, кроме
     `_selftest.*` — 254 из 256»), под ним все числа сходятся; по тире
     ревьюер прав по существу: 27 — это строки, а 29 — вхождения, и в
     тексте стояло неверное слово. Исправлено на «27 строк (29 вхождений)»
     (2026-09-13)

4. **на будущее** — фолбэк не покрывал бы отказ *разбора* OKLCH (переменная
   определена, но невалидна — `var()` фолбэк не срабатывает).
   - *Исправлено*: вопрос снят вместе с фолбэком; сам факт деградации на
     движке без OKLCH записан в `catalog/overview.md` как известное
     свойство, а не как проблема: страница остаётся читаемой,
     QR — сканируемым, брендовой она при этом не выглядит (2026-09-13)

5. **на будущее** — раздел «Mini App» в каталоге описывал половину состава:
   про вендоренный бренд подробно, а `vendor/qrcode.min.js` и `i18n.js`
   не упомянуты.
   - *Исправлено*: оба добавлены со ссылками на ADR-0007 и ADR-0014 (2026-09-13)

6. **на будущее** — `index.html` потерял `#status { min-height: 1.5em }`,
   из-за чего вердикт прыгает по вертикали на экране контролёра.
   - *Исправлено*: возвращено с комментарием, почему высота держится
     пустой (2026-09-13)

**Что ревьюер проверить не смог:** MCP `qadam-flow` в его сессии требовал
интерактивного OAuth. Утверждение «`ap_list_flows` → 22 флоу» он проверил
косвенно (манифест, состав `flows/`, отсутствие в дифе файлов флоу и
каталога). В сессии владельца MCP авторизован, и сверка сделана прямо.

### Круг 2

- **Ревьюер**: независимый агент с чистым контекстом · **Дата**: 2026-09-13 ·
  **Вердикт**: есть замечания (блокеров нет)

Все три исправления круга 1 подтверждены независимо: 8 блоков бренда
дословны, QR декодируется без брендового CSS в обеих темах по реальным
пикселям, числа `VOICE.md` воспроизводятся под объявленным определением
корпуса — до единого.

1. **важно** — размер QR считался один раз, в момент рендера. При изменении
   ширины после рендера (поворот экрана, смена вьюпорта Telegram) код
   оставался крупнее плиты: замер ревьюера — рендер на 740 px, затем 320 px,
   зазор падает с 39 px до 7 px, **0,91 модуля** при норме ≥ 4. Второй вход
   в то же состояние — `clientWidth == 0` в момент рендера: фолбэк возвращал
   `QR_MAX`, то есть ровно то значение, которое и чинили. Комментарий рядом
   при этом утверждал обратное — «зона покоя только растёт в модулях».
   - *Исправлено*: код перерисовывается при изменении ширины плиты
     (`ResizeObserver`, фолбэк — `resize`/`orientationchange`), нулевая
     ширина перестала быть тупиком по той же причине. Проверено сценарием
     «рендер на 740 → 320 → 390 → 740 → 320»: 0,91 модуля превратились
     в 7,9, плита внутри карточки и payload декодируется на каждом шаге.
     Очистка контейнера — `removeChild`, не `innerHTML`: свойство «страница
     ничего не собирает разметкой из данных» сохранено (2026-09-13)

2. **на будущее** — опорные числа payload'а в комментарии и каталоге взяты
   с потолка: `eventId` — record id платформы в 21 знак, значит реальный
   payload ≈ 44 знака и 41 модуль, а не «15–40 знаков, 29–33 модуля».
   - *Исправлено*: числа заменены на реальные, «4,15 модуля» помечено как
     теоретический худший случай, а не как «реальный» (2026-09-13)

3. **на будущее** — каталог описывал плиту так, будто код всегда 224 px.
   - *Исправлено*: адаптивный размер и пересчёт при изменении ширины
     описаны (2026-09-13)

4. **на будущее** — веса в таблице Q40 устарели от правки круга 1 (файл
   вырос на строку reset).
   - *Исправлено*: 11 843 Б / 3 868 Б. Заодно вскрылась **своя
     небрежность, которую ревью не называло**: вес шрифтов я считал
     в кибибайтах, а подмножество — в килобайтах, и «11,8 КБ» рядом
     с «444 КБ» сравнивало разные единицы. Везде теперь КиБ:
     **11,6 КиБ против 444 КиБ** (2026-09-13)

5. **на будущее** — шапка вендоренного файла говорила «444 КБ против 6 КБ
   всего остального», и 6 не сходилось ни с чем.
   - *Исправлено*: реальные числа файла (2026-09-13)

6. **на будущее** — ADR-0019 п. 2 читается как «страницы берут три
   семейства как есть», а Q40 решил не грузить их вовсе; п. 5 делегировал
   в Q40 только способ подключения CSS.
   - *Исправлено частично, и осознанно*: ADR задним числом не правится
     (CLAUDE.md), поэтому сужение записано **в самом Q40** отдельным
     абзацем — там, куда ADR и делегирует. Закрепление на уровне решения,
     а не вопроса, — это новый ADR и решение владельца, не работа этого
     пакета. См. «Хвосты» (2026-09-13)

**Чего ревьюер проверить не смог (оба круга):** MCP `qadam-flow` в сессиях
ревьюеров требовал интерактивного OAuth. «22 флоу, расхождений нет»
подтверждено ими косвенно — манифестом, составом `flows/`, отсутствием
файлов флоу и каталога в дифе, прогоном `tools/check-texts.py`. Прямая
сверка с живым инстансом сделана в сессии владельца, где MCP авторизован.

### Круг 3

- **Ревьюер**: независимый агент с чистым контекстом · **Дата**: 2026-09-13 ·
  **Вердикт**: есть замечания (блокеров нет)

Механизм перерисовки QR проверен независимо и работает: зацикливания нет
(наблюдатель видит собственный эффект, но гасится сравнением размера),
46 быстрых изменений ширины дали 9 перерисовок без ошибок, фолбэк без
`ResizeObserver` отрабатывает, зона покоя ≥ 4,27 модуля и декодирование
во всех 24 сочетаниях. Вендоринг дословен, AppSec чист, числа `VOICE.md`
сошлись до единицы.

1. **важно** — числа payload'а в круге 2 я «исправил» с почти верных на
   неверные: `eventId` это **не** record id платформы, а слаг
   `/^[A-Za-z0-9_]{1,12}$/`. 21-значный record id `fn-sign-qr` отверг бы
   (`throw new Error('fn-sign-qr: bad eventId')`), то есть я записал в три
   документа про горячий путь то, чего система не допускает.
   - *Исправлено*: границы payload взяты из парсера, а не из головы —
     15–41 знак, 29–37 модулей, зона покоя 4,1…5,3. Проверено самой
     библиотекой (`getModuleCount()` на трёх граничных payload'ах) и
     сверено с живым инстансом через MCP: `fn-sign-qr/step_1` действительно
     содержит этот регексп. В комментарии теперь стоит **ссылка на парсер**,
     а не пересказ, — чтобы следующая правка сверялась с ним, а не с
     очередным чужим утверждением (2026-09-13)

   Отдельно стоит назвать механику ошибки: замечание круга 2 звучало
   уверенно и с числом, и я принял его, не проверив по исходнику. Ревью
   ловит ошибки владельца, но само тоже может ошибаться — и «ревьюер сказал»
   не заменяет проверки. Это третий за пакет случай одного и того же:
   **утверждение приняли, потому что оно звучало измеренным**.

2. **важно** — таблица весов Q40 снова разошлась с файлом (11 861/3 858
   против записанных 11 843/3 868): правка шапки вендора в круге 2 изменила
   размер уже после того, как числа были сняты.
   - *Исправлено*: веса пересняты последним шагом, когда файл больше не
     менялся, и рядом с таблицей **назван метод** — `wc -c` и
     `gzip -9 -c < <файл>` потоком, без имени файла в заголовке gzip.
     Без названного метода следующий проверяющий получал другое число без
     всякой ошибки (2026-09-13)

3. **на будущее** — «модули остаются резкими» неверно буквально: qrcode.js
   не учитывает DPR, и на экране 3× битмап интерполируется.
   - *Исправлено*: формулировка сужена до того, что действительно верно —
     «без двойного масштабирования», с прямой оговоркой про DPR (2026-09-13)

4. **на будущее** — рваный перенос в шапке вендоренного файла после правки.
   - *Исправлено* (2026-09-13)

### Круг 4

- **Ревьюер**: независимый агент с чистым контекстом · **Дата**: 2026-09-13 ·
  **Вердикт**: есть замечания (блокеров нет)

Подтверждено измерением: границы payload (перебор всех 192 сочетаний длин →
29–37 модулей, зона покоя 4,14…5,29), веса Q40 до байта, дословность всех
8 блоков вендоринга, механизм перерисовки под штормом из 46 ресайзов,
AppSec, остальные числа `VOICE.md`.

1. **важно** — в `VOICE.md` §1 медиана 31 и девятый дециль 64 подписаны как
   числа корпуса, а посчитаны по 195 текстам сообщений; по всему корпусу
   из 254 строк выходит 21,5 и 58. Файл сам приглашает пересчитать и
   предупреждает о расхождении «на пару единиц» — здесь оно 44 %.
   - *Исправлено*: подпись приведена к тому, что считалось («медиана **текста
     сообщения**»), рядом названо число по всему корпусу и сказано, почему
     они расходятся — короткие подписи кнопок тянут медиану вниз. Неверна
     была ровно подпись, не метод: §3 про кнопки был размечен честно
     с самого начала (2026-09-13)

2. **на будущее** — в каталоге оговорка «на 224 px» относилась только
   к нижней границе зоны покоя, а верхняя на 320 px выше объявленной.
   - *Исправлено*: оговорка отнесена ко всему диапазону, добавлено, что на
     узком экране зона покоя в модулях только больше (2026-09-13)

3. **на будущее** — в строке метода Q40 запятая вместо точки и «97.5 %»
   через точку при «97,5 %» десятью строками ниже; там же «1,06 МБ»
   соседствовало с КиБ.
   - *Исправлено*, единицы двоичные везде: 1 083 000 Б (1 058 КиБ) (2026-09-13)

4. **на будущее** — правило зоны покоя уже сформулировано брендом
   («Event stands → QR codes»: «Quiet zone: four modules, always»), а пакет
   излагал его своими словами в трёх местах. Это тот же принцип, который
   пакет аккуратно соблюдает в `VOICE.md`: что бренд сказал — цитируется.
   - *Исправлено*: все три места теперь цитируют бренд со ссылкой.
     **Попутно нашлось расхождение, которого ревью не называло:** тот же
     раздел бренда предписывает уровень коррекции **Q**, а `qrcode.js`
     по умолчанию даёт **H**. Молча это не оставлено: расхождение записано
     в каталог с указанием, что H даёт больше избыточности, а не меньше,
     то есть идёт в сторону запаса, и что раздел бренда написан про
     печатные стенды. Переносить ли требование на экран — решение
     владельца, см. «Хвосты» (2026-09-13)

5. **на будущее** — в журнале остались «4,15» вместо 4,14 и «"!" трижды
   и все в приветствиях» (третье — «Ивент опубликован!»).
   - *Исправлено* (2026-09-13)

### Круг 5

- **Ревьюер**: независимый агент с чистым контекстом · **Дата**: 2026-09-14 ·
  **Вердикт**: есть замечания (блокеров нет)

Подтверждено измерением от первоисточников: дословность всех 8 блоков,
все веса Q40, все числа `VOICE.md` до единицы, 29–37 модулей и зона покоя
4,14…5,29, декодирование реальных пикселей во всех сочетаниях (включая
DPR 3, отсутствие `ResizeObserver`, сломанный `oklch()`), AppSec, раскладка
против нормативных разделов бренда (радиус, padding карточки, 4-пиксельная
шкала, teal страницами не используется).

1. **важно** — **регрессия, внесённая этим пакетом**: вердикт на `scan`
   потерял контраст. До W30 исход был тёмным текстом на цветной плашке
   (8,4 / 7,6 / 8,5:1), после — акцентный токен текстом на `--card`:
   **2,41 / 2,11 / 4,65:1** в светлой теме — первые два ниже любого разумного
   порога читаемости (WCAG: 4,5:1 для обычного текста, 3:1 для крупного).
   Это единственная строка, которую контролёр читает
   в дверях ярко освещённого холла, — то есть тот самый сценарий, которым
   пакет обосновывает всё остальное.
   - *Исправлено*: тон несут рамка и полоса (`border-color` + `box-shadow:
     inset 0 4px 0` семантическим токеном), текст — `--foreground`.
     Контраст стал **19,3:1** в светлой теме и **17,2:1** в тёмной,
     на всех трёх исходах. Замер — по реальным пикселям скриншота;
     первый мой замер был неверен (`getComputedStyle` вернул `oklch()`,
     а парсер прочитал его как RGB), и это снова напоминание, что
     синтетика меряет синтетику (2026-09-14)

2. **важно** — обоснование отказа от H→Q было **перевёрнуто**: я написал,
   что Q делает символ плотнее. Наоборот — Q даёт меньше модулей (25 против
   29 на коротком payload), модуль крупнее, и зона покоя при нынешнем
   padding падает до 3,57 модуля, ниже процитированного правила бренда.
   - *Исправлено*: и «Хвосты», и каталог описывают теперь размен целиком,
     с числами и с тем, что переход на Q потребовал бы увеличить padding.
     Проверено перебором уровней L/M/Q/H библиотекой (2026-09-14)

3. **на будущее** — четвёртое правило того же раздела бренда («Dark code on
   light ground. Never teal.») пересказывалось своими словами — ровно
   дефект, найденный кругом 4, в том же списке. Формулировка была ещё
   и неточна: у `qrcode.js` код всегда чёрный на белом, тёмной стала бы
   подложка.
   - *Исправлено*: цитата со ссылкой, формулировка уточнена (2026-09-14)

4. **на будущее** — в каталоге осталась фраза журнального регистра
   («вопрос к владельцу, не решённый этим пакетом»).
   - *Исправлено*: в каталоге «открытый вопрос», привязка к пакету живёт
     в журнале (2026-09-14)

5. **на будущее** — Q40 взвесил один сторонний домен и умолчал про второй:
   `telegram.org/js/telegram-web-app.js` в горячем пути блокирующим скриптом.
   - *Исправлено*: абзац в Q40 — домен неустраним (без него нет `initData`),
     и именно поэтому «ещё одно рукопожатие» сказано про бренд как про
     второе сверх неустранимого (2026-09-14)

**Чего ревьюер проверить не смог:** MCP (как и во всех кругах) и настоящий
Telegram WebView — `window.Telegram.WebApp` подменялся заглушкой, поэтому
поведение темы и `initData` внутри живого клиента остаётся непроверенным.
Это ограничение всех пяти кругов и кандидат в проверку на приёмке W15.

### Круг 6

- **Ревьюер**: независимый агент с чистым контекстом · **Дата**: 2026-09-14 ·
  **Вердикт**: есть замечания (блокеров нет, «важно» нет — всё «на будущее»)

Все заявленные числа пакета воспроизведены независимо: контраст 19,3 / 17,2:1
на всех семи статусах, Q=25 против H=29 модулей и зона покоя 3,57, веса Q40,
вся статистика `VOICE.md` до единицы, дословность цитат бренда и всех блоков
вендоринга, декодирование в 15 сочетаниях.

1. **на будущее** — «Сам текст поэтому `--foreground`: 15–16:1» в комментарии
   не воспроизводится: по пикселям 19,3 и 17,2. Ошибка в безопасную сторону,
   но это шестой круг пакета, чья сквозная тема — расхождение утверждений
   с измеримым.
   - *Исправлено*: числа заменены измеренными (2026-09-14)

2. **на будущее** — там же диапазон «2,1–2,4:1» свернул три исхода, молча
   выбросив `bad` (4,65).
   - *Исправлено*: перечислены все три (2026-09-14)

3. **на будущее** — **бренду приписан порог, которого он не называет**:
   «ниже порога 3:1, который называет сам бренд». Единственное 3:1 в бренде —
   про teal на белом и про то, что **камеры телефонов** не прочитают модули
   QR, а не про контраст текста интерфейса. Тот же класс дефекта, что
   в кругах 4 и 5, только вывернутый: не пересказ правила, а правило,
   которого нет.
   - *Исправлено*: и в комментарии, и в журнале порог теперь назван своим
     именем — WCAG (4,5:1 для обычного текста, 3:1 для крупного), без ссылки
     на бренд (2026-09-14)

4. **на будущее** — подпороговый контраст переехал с букв на полосу и рамку,
   которые сама правка объявила носителем тона: против фона страницы они
   дают те же 2,2–2,5:1 в светлой теме.
   - *Исправлено*: это прямо написано в комментарии — порог к полосе
     **неприменим** (вердикт назван словами, полоса лишь дублирует его
     цветом), а не «соблюдён». Функционально проверено ревью: тона
     различимы, полоса при скруглении не пропадает, на 320 px тоже
     (2026-09-14)

5. **на будущее** — каталог называл тон «семантическими токенами», но не
   говорил, чем он несётся. Тот, кто добавит четвёртый исход по этой
   карточке, покрасил бы буквы и вернул регрессию круга 5.
   - *Исправлено*: механизм описан, и прямо сказано, что новый исход
     красится так же (2026-09-14)

6. **на будущее** — в подмножестве лежал неиспользуемый блок `Badges`
   (~1,4 КиБ, 12 % файла), при том что Q40 обещал «критический минимум —
   токены плюс то, что нужно `ticket`/`scan`».
   - *Исправлено*: блок убран, подмножество пересобрано (7 блоков вместо 8),
     дословность и отсутствие висячих `var()` и неопределённых классов
     перепроверены. Файл похудел с 11 861 до **10 413 Б** (3 649 gzip),
     доля шрифтов выросла до 97,8 %. Веса в Q40, каталоге и шапке файла
     пересняты **до сходимости**: первая же правка шапки снова сдвинула
     gzip на байт — ровно грабля кругов 2 и 3, поэтому теперь замер
     повторялся в цикле, пока файл и таблица не перестали расходиться
     (2026-09-14)

### Круг 7

Был запущен 2026-09-14 до слияния, но **не дождан**: пакет закрыт решением
владельца до его вердикта. Перезапущен владельцем в тот же день **после**
слияния, уже на `main` — см. ниже. Его замечания — материал для следующей
работы по Mini App, а не задолженность W30.

- **Ревьюер**: независимый агент с чистым контекстом · **Дата**: 2026-09-14
  (после слияния, на `main` b96b8a2) · **Вердикт**: есть замечания
  (блокеров нет, «важно» нет — всё «на будущее»)

Подтверждено независимо: `grep` литералов цвета/шрифта молчит; ссылок на
`fonts.googleapis.com` / `brand.aiqadam.org` в ресурсах нет; вендоринг
дословный — все 7 блоков построчно совпадают с коммитом `8ef4a80` (бренд с
тех пор не двигался, `origin/main` = закреплённый коммит), лицензия байт в
байт; вес 10 413 / 3 649 gzip; цитаты бренда дословны, якоря существуют; зона
покоя 4,14 модуля на 224 px и 5,4 на 320 px, уровень H подтверждён в
исходнике `qrcode.min.js`; вся статистика `VOICE.md` пересчитана по живому
`ru.json` и сошлась до единицы; AppSec — ключей нет, права решает сервер,
весь вывод через `textContent`, `className` из белого списка, сетевые ошибки
перехвачены. Не проверено: живой инстанс через MCP (OAuth), живой Telegram
WebView, рендер QR на пикселях (арифметика сходится).

1. **на будущее** — «в собственном CSS страниц — только раскладка» неверно
   буквально: `ticket.html` и `index.html` красят токенами (`--destructive`,
   `--border`, `box-shadow`). Точная формулировка — «свои цвета только через
   токены бренда».
   - *Исправлено*: комментарии обеих страниц и `catalog/overview.md`
     переформулированы (2026-09-14)

2. **на будущее** — `ticket.html` красит текст ошибки акцентным токеном на
   `--card` (4,65:1 — впритык к WCAG для 14 px) — механизм, который круг 5
   признал дефектом на `scan` и заменил рамкой/полосой; у бренда есть
   `.helper.error` для этого.
   - *Не исправлено* — код страницы, требует headless-прогона; долг записан
     в [W31](../BACKLOG.md#w31-страница-manage-визард-ивента-в-mini-app)
     как «долг Mini App из круга 7 W30»

3. **на будущее** — `.empty-heading` / `.empty-desc` используются как
   заголовок страницы вне `EmptyState`; компромисс не назван — смена
   компонента у бренда молча поменяет заголовки.
   - *Исправлено*: названо в `catalog/overview.md` (2026-09-14)

4. **на будущее** — у `ticket` нет выхода из «Загрузка…»: `fetch` без
   таймаута, кнопки «повторить» нет; не-JSON ответ (5xx HTML) показывается
   как «Нет соединения», сканер такой случай отделяет.
   - *Не исправлено* — код страницы; долг в W31, см. п. 2

5. **на будущее** — `themeParams` Telegram не используются,
   `setHeaderColor`/`setBackgroundColor` не вызываются — шов между шапкой
   клиента и брендовым фоном. Допустимо по ADR-0019, но не названо. Догадка:
   живой WebView не проверялся.
   - *Исправлено*: названо в `catalog/overview.md` как принятая цена
     (2026-09-14)

6. **на будущее** — `HANDOFF.md` говорил «взят и передан на проверку»,
   `STATUS.md` — `готов`.
   - *Исправлено*: HANDOFF переписан под свободные пакеты (2026-09-14)

7. **на будущее** — процесс: `готов` без вердикта «замечаний нет» — первый
   такой случай; либо закрепить правило в `docs/work/README.md`, либо
   признать разовым.
   - *Не исправлено* — решение владельца, вынесено ему отдельно


## Закрытие (2026-09-14) — решением владельца проекта

Владелец распорядился слить пакет как есть («залей как есть, потом ревью
проведём»). Это **не** вердикт «замечаний нет»: такого вердикта у пакета
нет и не было ни на одном из шести кругов.

Что это значит по существу:

- **Шесть кругов независимого ревью пройдены, все их замечания исправлены.**
  Блокеров не было ни разу. Последний круг, нашедший что-то уровня «важно», —
  пятый (регрессия контраста); шестой дал только «на будущее».
- **Седьмой круг не дождан.** Его замечания, когда придут, оформляются
  как отдельная работа.
- **Два вопроса к владельцу остаются открытыми** (см. «Хвосты»): расхождение
  ADR-0019 п. 2 с Q40 по шрифтам и уровень коррекции QR (H против брендового
  Q). Ни один не решён этим пакетом и ни один не является дефектом кода.
- **Чего не проверял никто из шести ревьюеров:** живой инстанс через MCP
  (во всех сессиях требовал интерактивного OAuth) и настоящий Telegram
  WebView — `window.Telegram.WebApp` везде подменялся заглушкой. Поведение
  темы и `initData` в живом клиенте остаётся непроверенным и должно попасть
  в приёмку [W15](../BACKLOG.md#w15-приёмка).

## Хвосты и блокеры

- **`EventCard` бренда не использован, и это не забывчивость.** Паттерн
  требует названия, даты и места ивента, а `my-qr-api` отдаёт только
  `{ok, error, text, payload, eventId, userId}` — показывать было бы
  нечего, а расширять контракт флоу ради оформления значит менять флоу
  в пакете, который их не трогает. Если билету нужна шапка ивента —
  это отдельная работа, с правкой `my-qr-api` и его карточки.
- **Страница `manage` не делалась** — она появляется решением по
  [Q43](../OPEN-QUESTIONS.md#q43), а не здесь. *Q43 закрыт 2026-09-14:
  переносить — пакет [W31](../BACKLOG.md#w31-страница-manage-визард-ивента-в-mini-app).* Для неё в Q40 оставлено
  прямое указание: спокойная обстановка ≠ автоматическое право тянуть
  шрифты, решать так же замером.
- **Уровень коррекции QR: у нас H, бренд для печатных стендов требует Q.**
  *Решено 2026-09-14 владельцем — H остаётся,
  [Q45](../OPEN-QUESTIONS.md#q45).*
  Это **размен, а не запас**: H покупает избыточность ценой более мелкого
  модуля. Замер библиотекой на наших границах payload: Q даёт 25 модулей
  против 29 у H на самом коротком, то есть модуль 8,96 px, и зона покоя при
  нынешних `padding: 32px` падает до **3,57 модуля** — ниже правила бренда
  «four modules, always», которое пакет только что процитировал. То есть
  переход на Q не бесплатен: он требует увеличить padding плиты. Раздел
  бренда при этом написан про печатные стенды, а не про экран. Решение —
  за владельцем; этот пакет менять умолчание `qrcode.js` не стал.
- **ADR-0019 п. 2 и Q40 расходятся на бумаге.**
  *Снято 2026-09-14 — [ADR-0020](../adr/0020-no-web-fonts-on-venue-pages.md).* Решение говорит, что Mini App
  берёт три шрифтовых семейства как есть; Q40 постановил их не грузить.
  Противоречие снято записью в Q40 (там, куда ADR делегирует), но на уровне
  ADR оно остаётся. Закрыть его может только новый ADR — это решение
  владельца, а не работа пакета.
- **Копия бренда устаревает молча.** Против этого — только строка
  в чек-листе ревьюера и коммит в шапке файла. Автопроверки нет и не
  предлагается: она была бы кодом.

# W19. Ревизия после обновления образа Qadam Flow

- **Статус**: готов
- **Владелец**: агент W19
- **Волна**: 5
- **Зависит от**: W8 (готов). **Не** W18 — см. «Журнал», запись от 2026-09-12
- **Начат**: 2026-09-12 · **Закрыт**: 2026-09-13

## Цель

Проверить, что даёт проекту обновление образа `sha-95079f6` → `sha-6445a8e`
([Q26](../OPEN-QUESTIONS.md#q26)), и применить то, что применимо без
архитектурного решения: проекцию колонок в чтениях с ПД и `secret_token`
на telegram-вебхуке. Пересмотр [ADR-0003](../adr/0003-idempotency-without-atomicity.md)
в объём пакета **не входит** — только постановка вопроса.
Чек-лист — [BACKLOG.md#w19](../BACKLOG.md#w19-ревизия-после-обновления-образа-qadam-flow).

## Что построено

Пакет ревизионный: новых флоу и таблиц не создаётся. Правки — в существующих.

| Артефакт | ID / имя | Каталог |
|----------|----------|---------|
| (заполняется по ходу) | — | — |

## Чек-лист готовности

- [x] версия образа dev подтверждена и записана в журнал (prod не поднят — шаг 0.6 отложен)
- [x] `secret_token` на telegram-вебхуке проверен, результат зафиксирован —
  **неприменим**: вебхука у бота нет, доставка идёт long-polling'ом
- [x] [Q25](../OPEN-QUESTIONS.md#q25), [Q17](../OPEN-QUESTIONS.md#q17) обновлены по фактической проверке
- [x] чтения `users` с ПД сужены через `columns` — **все девять** (в первой
      редакции было сказано «шесть»; три в `registration` пропустили).
      Полный список, пересчитанный ревьюером механически по всем 14 флоу:
      `tg-router/step_7`, `registration/step_34`, `step_54`, `step_65`,
      `checkin-api/step_28`, `step_12`, `my-qr-api/step_15`,
      `fn-resolve-segment/step_4`, `step_7`. Десятого нет. Прежний список:
  `tg-router/step_7`, `fn-resolve-segment/step_4` и `step_7`,
  `my-qr-api/step_15`, `checkin-api/step_28` и `step_12`
- [x] все четыре затронутых флоу опубликованы (`tg-router`, `checkin-api`,
  `my-qr-api`, `fn-resolve-segment`), ingress проверен `curl`'ом после публикации
- [x] проверка на живых данных завершена: `fn-resolve-segment` и `tg-router` —
  сквозными прогонами; `checkin-api` и `my-qr-api` — **живым сканом владельца
  2026-09-12** (`AYf8AII1XYoz5zso1pDHR`, `Qglq1MrtBNlZjZXUO5U9e`), оба на
  проекции колонок, оба `SUCCEEDED`
- [x] в OPEN-QUESTIONS заведён вопрос о пересмотре ADR-0003 со списком
  флоу-кандидатов — [Q27](../OPEN-QUESTIONS.md#q27); владелец заказал пересмотр
  отдельным пакетом [W20](../BACKLOG.md#w20-пересмотр-adr-0003-идемпотентность-на-атомарных-примитивах)
  (заблокирован #411)
- [x] `catalog/` совпадает с живым проектом — `catalog/flows/tg-router.md`
  обновлён: доставка polling'ом, расхождение черновика на `step_18`,
  нередактируемость шагов `tables`/`store`

## Как проверено

- `ap_research_pieces` по `@aiqadam/qadam-tables` и `@aiqadam/qadam-store`
  (2026-09-12) → на инстансе присутствуют `tables-upsert-records`,
  `tables-update-records` (батч, [#408](https://github.com/aiqadam/qadam-flow/pull/408))
  и `store : put_if_absent`. Ни одного из них не было в образе `sha-95079f6`,
  то есть образ обновлён до `6445a8e` или новее — эмпирически, по наличию
  действий, а не по сайдбару.

- Проверка `secret_token` (#397) вместо ожидаемого ответа дала другой факт:
  - `POST https://app.flow.aiqadam.org/api/v1/webhooks/Y1dNon2V2EhjWM0aYwdQi`
    с телом `{"update_id":999000001}` и **без** заголовка
    `X-Telegram-Bot-Api-Secret-Token` → **HTTP 409**,
    `{"message":"This flow receives events by polling, so it does not accept pushed deliveries."}`.
    Тело выбрано намеренно безопасным: гейт `step_3` классифицирует апдейт без
    `from.id` как `bad_update` и уводит в ветку Otherwise — то есть до `store put`
    и до любых отправок дело не доходит даже при успешной доставке.
  - `getWebhookInfo` через `ap_run_action` (`custom_api_call`, connection
    `TZTlXaCEO2hEvimUowbSA`, прогон `e22V6cLKge55G7hV4Rjas`) →
    `{"ok":true,"result":{"url":"","has_custom_certificate":false,
    "pending_update_count":0,"allowed_updates":["message","callback_query"]}}`.
  - Ingress вебхук-флоу не пострадал: `POST .../CUKqiby1PoHiQiiCQy24V/sync` → `401`
    `invalid_init_data/malformed`, `POST .../I5nd8ggKH4wkQLaww9Dkl/sync` → `200`
    `{ok:false, bad_event_id}`. Путь сканера цел.
  - Polling **работает**: десять PRODUCTION-прогонов `tg-router` 2026-09-12 в
    13:08:48–13:08:51, все `SUCCEEDED`, разные `update_id`; разобранный прогон
    `jAb16tiE4Zm5PyQ3IqSri` — настоящее сообщение владельца (`update_id 24020013`,
    `text: "выа"`), `step_3` дал `proceed: true, reason: ok, seen: false`.
- Прогон `jAb16tiE4Zm5PyQ3IqSri` — заодно прямое доказательство
  [Q17](../OPEN-QUESTIONS.md#q17): вывод `step_7` содержит строку `users`
  целиком, включая `phone` и оба consent-флага.

## Журнал

- **2026-09-12** — пакет взят. Проверка примитивов через `ap_get_piece_props`
  на живом инстансе дала три факта, каждый ложится на свой инвариант:
  - `tables-find-records` → проп **`columns`** (`MULTI_SELECT_DROPDOWN`),
    описание прямо называет причину: «Step outputs are recorded verbatim in
    the run log, so reading only the columns you need is also what keeps the
    rest out of it». Это [Q17](../OPEN-QUESTIONS.md#q17).
  - `tables-update-record` → проп **`only_if`** (`DYNAMIC`): «Use "Does not
    exist" to mean "only if this column is still empty" — that is how you
    write "record it only the first time"», при непопадании —
    `RECORD_PRECONDITION_FAILED`, а не тихий no-op. Это ровно IDM-2.
  - `store : put_if_absent` → `ttl_seconds` плюс «report whether this run is
    the one that took it». Это ровно IDM-4.
  - `tables-upsert-records` честно оговаривает границу: «The key is matched
    here, **not enforced by the table** — another write path can still insert
    one». То есть это не уникальный индекс, и в вопрос по ADR-0003 это войдёт
    как ограничение, а не как решение.
- **2026-09-12** — **поправка к Q26/BACKLOG**: проп проекции колонок на
  инстансе называется **`columns`**, а не `fieldIds`, как записано в Q26 по
  тексту апстримного PR [#407](https://github.com/aiqadam/qadam-flow/pull/407).
  Документ правится по факту с инстанса.
- **2026-09-12** — **расхождение в BACKLOG исправлено**: строка W19 гласила
  «Зависит от: W18 (готово)», тогда как в [STATUS.md](../STATUS.md) W18
  значится `не начат` и журнала `docs/work/W18-*.md` в репозитории нет.
  Написано авансом. Фактической блокировки нет: единственная связка W19 с W18 —
  пункт «убедиться, что журнал W18 не утверждает обратного», а утверждать
  пока нечему. Строка приведена к факту.
- **2026-09-12** — **главная находка пакета, и она не из чек-листа.** Пункт про
  `secret_token` (#397) оказалось нечем закрывать: обновление образа перевело
  telegram-триггер с вебхука на long-polling ([#393](https://github.com/aiqadam/qadam-flow/pull/393)),
  который Q26 отмёл как «к проекту не относится». Доказательства — в разделе
  «Как проверено». Что это меняет: пункт #397 неприменим (аутентифицировать
  нечего), публичного ingress у бота больше нет, обоснование отдельного бота
  сохраняется по другой причине (эксклюзивность `getUpdates`), а бюджеты
  латентности ADR-0009/Q22 для пути «написал боту → бот ответил» перестали быть
  измеренными — интервал опроса в них не входил. Поправлены: Q26, CLAUDE.md
  («Лимиты платформы»), `catalog/flows/tg-router.md` (строка триггера + заметка).
  Замер латентности polling-пути в объём W19 не входит — вынесен в хвосты.
- **2026-09-12** — `step_8` прочитан (`ap_read_step_code`), чтобы не гадать, какие
  колонки нужны: потребляются ровно `created_at` (ключ детерминированной
  сортировки), `lang`, `consent_pdn`, `consent_marketing` и `phone`, причём
  `phone` — **только** чтобы вычислить булев `phoneKnown`. `telegram_id`,
  `first_name`, `last_name`, `username`, `blocked_bot`, `consent_pdn_at`,
  `consent_marketing_at` не читаются ни одним потребителем.
- **2026-09-12** — **проекция колонок (#407) в этом пакете невыполнима.** Правка
  `columns` у `tg-router/step_7` упёрлась в отказ платформы на запись:

  ```
  qadam_metadata_not_found qadamName=@aiqadam/qadam-tables not found
  ```

  Воспроизводится и на правке, **не передающей `input` вовсе** (тот же
  `displayName`). При этом все остальные сигналы говорят «флоу здоров»:
  `ap_research_pieces`/`ap_get_piece_props` qadam отдают, `ap_validate_flow` —
  `valid: true` 21/21, `ap_flow_structure` — все шаги `configured`, прод
  работает. Пробой на выбрасываемом флоу (`zz-probe-W19-tables-writeability`,
  создан и удалён) установлено различие: **новый** шаг `tables` добавляется и
  валиден, не резолвятся только **существующие** — они пришпилены к версии
  qadam'а, которой после обновления образа в реестре нет. Это тот же корень,
  что в [qadam-flow#411](https://github.com/aiqadam/qadam-flow/issues/411)
  (заведён владельцем по триггеру `telegram-bot@0.6.2`), но другая поверхность;
  дописано комментарием в тот же тикет.
  Обход — удалять и пересоздавать шаг.
- **2026-09-12** — **повреждён и восстановлен черновик `tg-router`.** Пробуя
  границы отказа выше, я обновил у `step_18` только `displayName`, тем же
  значением. Шаг стал невалиден: `Missing required inputs: executionMode`.
  Опубликованная версия не пострадала, прод работал всё время; невалиден был
  только черновик (`ap_validate_flow`: 20/21).
  **Уточнение причины** — первая версия этой записи винила саму правку, и это
  неверно: проп `executionMode` добавил апстримный
  [#363](https://github.com/aiqadam/qadam-flow/issues/363), а W17 намеренно
  **не** переводил оба вызова `tg-router → registration` на inline. Значит вход
  `step_18` написан в W5, когда пропа не существовало; флоу проходил валидацию
  и работал на дефолте `queue`, а правка лишь вызвала перевалидацию против
  новой версии qadam'а и обнажила пробел. **`step_20` сейчас в том же
  состоянии** и по-прежнему числится `valid` — любая будущая правка этого шага
  всплывёт так же.
  Восстановлено не по памяти, а по доказательствам: `registration/step_1`
  прочитан через `ap_read_step_code` — на пути `action = 'start'` потребляются
  ровно `action`, `telegramId`, `chatId`, `lang`, `eventId`, `utm`, payload
  приходит под `{{trigger['output'].data}}`; имена полей `tgId`/`chatId`/`lang`
  у `step_13` подтверждены живым прогоном `jAb16tiE4Zm5PyQ3IqSri`;
  `flowProps.payload` (тип JSON, required) — через `ap_get_piece_props` с
  `input.flow` в виде объекта с `exampleData`. `executionMode` восстановлен в
  `queue`, а не `inline`, — как решил W17. `ap_validate_flow` → 21/21.
  **Черновик не опубликован намеренно:** опубликованная версия заведомо
  рабочая, черновик содержит реконструкцию. Публикация — решение владельца,
  и её стоит делать вместе с закрытием W17, который трогает те же вызовы.
- **2026-09-12** — **блокер был завышен мной; поправлено по требованию владельца.**
  Первая версия записи выше объявляла проекцию колонок невыполнимой. Это был
  вывод из неполного эксперимента: я проверил, что новый шаг `tables`
  *создаётся*, но не проверил, что его потом можно *править*. Владелец на это
  указал. Доведённый эксперимент (флоу `zz-probe-W19-edit-new-tables-step`,
  создан и удалён) дал три факта против прежнего вывода:
  1. **новый шаг `tables` правится штатно** — `ap_update_step` по нему проходит;
  2. **`ap_update_step` мержит `input`, а не затирает его**: правка одним
     только `columns` оставила `table_id` и `filters` нетронутыми — прогон
     вернул одну отфильтрованную строку, а не обе строки таблицы. Прежнее
     опасение «затру фильтр и получу fail-open» не подтвердилось;
  3. **пересозданный шаг получает то же имя** (`step_1` → удалён → снова
     `step_1`), значит ссылки `{{step_N['output']}}` вниз по флоу переживают
     пересоздание. Это была главная предполагаемая цена обхода — её нет.

  Чего добиться **не** удалось: заставить существующий шаг перепривязаться к
  актуальной версии qadam'а. Явный `qadamName` в `ap_update_step` не помогает —
  версия читается из сохранённого шага раньше, чем применяется правка. Лечится
  только пересозданием.
- **2026-09-12** — **`tg-router/step_7` пересоздан с проекцией колонок.**
  Удалён и добавлен заново после `step_6` с тем же `displayName`; получил то же
  имя `step_7`, поэтому `{{step_7['output']}}` в `step_8` править не пришлось.
  Вход восстановлен не наугад: фильтр идёт по `{{step_1['output'].tgIdKey}}` —
  это поле существует в `step_1` ровно затем, чтобы значение ключа никогда не
  было пустым (`tgIdKey: tgId === '' ? '-' : tgId`), и больше нигде во флоу не
  используется (`step_2` берёт `dedupKey`, `step_8` — `tgId`). `limit` намеренно
  не задан: `step_8` считает дубли (`duplicates`), ограничивать выборку нельзя.
  Колонки — пять потребляемых, по externalId.

  **Проверено сквозным прогоном** `BywBIqJvv6bjW06R6qYBC` (TESTING, 2,6 с,
  `SUCCEEDED`), а не `ap_test_step`: последний для этой цепочки бесполезен —
  он не вычисляет вышестоящие шаги, `{{step_1['output'].tgIdKey}}` резолвится
  в пустоту, и `step_7` возвращает `[]` (прогоны `Q1BgwyxKEoVivdOnS39r3`,
  `Qc0KwQ9rzIxZH7a6x5JZF` — оба недоказательны, записаны, чтобы следующий агент
  не принял их за проверку).
  Результат сквозного прогона: `step_7` отдал **только** `phone`,
  `consent_marketing`, `consent_pdn`, `created_at`, `lang` — имена, `username`,
  `telegram_id`, `blocked_bot` и обе consent-даты из лога ушли. Фильтр цел:
  одна строка `Win2luDYTG3uZZE0ish9d`, а не вся таблица. Регресса нет — вывод
  `step_8` совпадает с боевым прогоном `jAb16tiE4Zm5PyQ3IqSri` поле в поле,
  включая `phoneKnown: true`.

  **Утечка уменьшена, но не устранена:** `step_10` (`tables-update-record`)
  по-прежнему возвращает строку `users` целиком, а проекции колонок у этого
  действия нет — только у `tables-find-records`. Записано в [Q17](../OPEN-QUESTIONS.md#q17).
- **2026-09-12** — **остальные пять чтений сужены**, владелец разрешил делать всё
  на dev. Колонки в каждом случае выбраны по прочитанному коду потребителя,
  не по названию шага:

  | Шаг | Потребитель | Оставленные колонки |
  |-----|-------------|---------------------|
  | `fn-resolve-segment/step_4` | `step_5` | `telegram_id`, `blocked_bot` |
  | `fn-resolve-segment/step_7` | `step_5` | `telegram_id`, `consent_marketing` |
  | `my-qr-api/step_15` | `step_16` | `lang` |
  | `checkin-api/step_28` | `step_6` | `lang` |
  | `checkin-api/step_12` | `step_13` | `first_name`, `last_name` |

  **Ловушка, которую нашло чтение кода, а не структуры.** В
  `fn-resolve-segment/step_5` поле фильтра перепроверяется в коде вторым рубежом
  (`(u.blocked_bot || '') === 'true'`). Убери `blocked_bot` из проекции — список
  блокировок опустеет, `blockedKnown` станет `0`, и заблокировавшие бот попадут
  в рассылку: это **fail-open** и нарушение OWN-12, при том что флоу останется
  «валидным» и прогон зелёным. Поэтому поле фильтра оставлено в проекции
  сознательно. То же рассуждение для `consent_marketing` (там промах был бы
  fail-safe, но правило одно).

  **Различающий тест на эту ловушку поставлен, а не пропущен.** Прогон
  `DMIDoaiH4zHYIsYPTpZhZ` (никто не заблокирован) ничего не различает — `step_4`
  пуст в обоих случаях. Поэтому `users.blocked_bot` у `8255904812` был временно
  выставлен в `true`, снят прогон `CaKM3oq8B12to0T92XUcM`: `blockedKnown: 1`,
  `blockedExcluded: 1`, `recipients: ["322876545"]` — заблокированный исключён.
  Значение **возвращено в `false`** сразу после прогона.
- **2026-09-12** — `checkin-api`: чтение прав `step_5` по `event_staff`
  **намеренно не тронуто**. Это авторизация (STF-2) и предмет W18; сужать её
  колонки в ревизии — лишний риск без выигрыша, ПД там нет.
- **2026-09-12** — опубликованы `fn-resolve-segment`, `my-qr-api`, `checkin-api`.
  `tg-router` **не опубликован**: `ap_lock_and_publish` заблокирован
  классификатором разрешений среды, не платформой. Смоук-тест опубликованных
  вебхуков `curl`'ом — тоже. Оба пункта переданы владельцу.
- **2026-09-12** — **реконструкция `step_18` была неверной, это поймал прогон, а не
  ревью.** После публикации `tg-router` сквозной прогон по ветке deep link
  (`/start edemo`, прогон `7dKPSq6mCnMfBXoWyRNM7`) упал на `step_18`:

  ```
  {"message":"Please select a flow"}
  at findFlowByExternalIdOrThrow (.../subflows/dist/src/lib/common.js:21)
  ```

  Причина: значение пропа `flow` у `callFlow` — **объект**
  `{externalId, exampleData}`, а не строка с externalId. Это ровно урок
  [Q20](../OPEN-QUESTIONS.md#q20), но я применил его только к *резолву* пропов
  через `ap_get_piece_props`, а не к *сохраняемому значению*. Форма взята из
  `ap_resolve_property_options` для пропа `flow` и проставлена; повторный прогон
  `vqvFIl5nt4j4nXhtloWpT` — `SUCCEEDED`.

  **Цена ошибки: вход по deep link был сломан в проде между двумя публикациями
  `tg-router`** (примерно 13:52–13:58 12.09.2026). Обнаружено и исправлено в
  пределах одной сессии, но публиковать реконструированный шаг **до** прогона
  его собственной ветки было неправильно — правильный порядок обратный.

  Доказательство работы цепочки, а не только «шаг зелёный»: прогон
  `registration` `e4eh8kMqSRike8mSMwgQc` (PRODUCTION, 11,8 с, `SUCCEEDED`)
  отправил владельцу два настоящих сообщения Telegram — `reg.already`
  («Вы уже зарегистрированы на «AI Qadam Demo Meetup»») и `reg.qr.open_miniapp`
  с кнопкой `reg.qr.button` → `ticket.html?event_id=demo`.
- **2026-09-12** — опубликованные версии проверены `curl`'ом после публикации:
  `checkin-api` → `401 invalid_init_data/malformed`, `my-qr-api` → `200
  {ok:false, malformed}`, `tg-router` → `409 polling`. Ingress цел.
- **2026-09-12** — владелец проекта (см. ветку обсуждения) **заказал**
  пересмотр ADR-0003 отдельным пакетом. На объём W19 это не влияет: W19
  по-прежнему только ставит вопрос, решение и реализация — в заказанном
  пакете.

## Ревью

> Заполняет **независимый ревьюер** по [REVIEW-CHECKLIST.md](REVIEW-CHECKLIST.md).
> Владелец пакета сюда не пишет — только отвечает под замечаниями, что исправлено.

- **Ревьюер**: независимый агент-ревьюер · **Дата**: 2026-09-12 · **Вердикт**: **есть замечания**

Чем проверено: MCP (`ap_list_flows`, `ap_list_tables`, `ap_validate_flow`,
`ap_list_runs`, `ap_get_run`, `ap_run_action`) + read-only REST-экспорт
`GET /flows/<id>/template` по всем 14 флоу проекта (ADR-0006, ключ из Keychain).
Все `tables`/`store`-шаги проекта выгружены и просмотрены механически, а не по
списку из журнала.

**Подтверждено фактом:**

- шесть заявленных чтений `users` действительно сужены, колонки совпадают с
  прочитанным кодом потребителей: `tg-router/step_7` (`created_at`, `lang`,
  `consent_pdn`, `consent_marketing`, `phone`), `fn-resolve-segment/step_4`
  (`telegram_id`, `blocked_bot`) и `step_7` (`telegram_id`, `consent_marketing`),
  `my-qr-api/step_15` (`lang`), `checkin-api/step_28` (`lang`), `step_12`
  (`first_name`, `last_name`). Фильтры и отсутствие `limit` уцелели;
- регресса нет: боевой прогон `tg-router` `PNTy26gz6KhzlxCZLiC2r` (2026-09-12
  15:55, `SUCCEEDED`) отдал в `step_7` ровно пять колонок, `step_8` вернул
  `phoneKnown: true`, `lang: ru`, `duplicates: 1`;
- Q25/#382 проверен **исполнением, а не документацией**: `filters: {"foo":"bar"}`
  → прогон `qhSPuroJECSOCxoqCj8J8` **FAILED**, «A filter that cannot be read is
  rejected rather than ignored»; несуществующая колонка в `filters` → FAILED
  (`gIuoS4cJYDGkcbVyBVNMT`); корректный фильтр в короткой строковой форме
  (`{"field":"key"}`, та, что у пересозданных шагов) отработал — 0 строк из 661
  (`k888O3hBtysiSGnlx7NSO`). Fail-open не воспроизводится;
- **бонус к Q17, в журнале не записан:** `columns` тоже fail-**closed** —
  несуществующий externalId колонки валит шаг (`i7jmfssHHwIXyKkJTcBBg`), а не
  молча возвращает строку целиком. Значит переименование колонки в `users` не
  превратит проекцию в тихую утечку;
- long-polling подтверждён независимо: `getWebhookInfo` (прогон
  `Hij9wbQ0m0C9QLdeaAx2R`) → `url: ""`, и при этом PRODUCTION-прогоны
  `tg-router` идут (10 шт. 12.09 14:35–15:55, все `SUCCEEDED`);
- `checkin-api/step_12` (имя участника) исполняется **внутри** ветки
  `step_7 isStaff?` — авторизация раньше чтения ПД участника, п. 4.4 не нарушен.

### Замечания

1. **важно — «все шесть» неверно: в проекте девять чтений `users`, три без
   проекции.** `registration/step_34` («users по telegram_id»), `step_54`
   («…(mkt)»), `step_65` («…(phone)») — `tables-find-records` по
   `z5PX9B8mTQC9Q6Dfuj5dM`, `limit: 3`, `columns` отсутствует. Их потребители
   (`step_35`, `step_55`, `step_66`) — три одинаковых CODE-шага, каждому нужно
   **только `rows[0].id`**, то есть ни одна колонка не нужна вовсе. В лог
   прогона `registration` при этом уезжает строка `users` целиком: `phone`,
   `username`, имена, оба consent-флага и обе consent-даты. Это ровно тот
   мотив, ради которого делался пакет (Q17/ADR-0005), и это самый частый
   флоу проекта. Пересчёт сделан механически по REST-экспорту всех 14 флоу —
   других чтений `users` в проекте нет.
   Это **не** регресс W21: `registration` писал `users.consent_pdn`/
   `consent_marketing`/`phone` по `record_id` начиная с W5 (см.
   `catalog/flows/registration.md` на коммите `4a07e33`), то есть
   find-then-write по `users` существовал и в момент аудита W19 — он просто
   не попал ни в список кандидатов Q17, ни в чек-лист. Починка тривиальна
   (одна колонка `telegram_id` в трёх шагах), но пункт чек-листа
   «чтения `users` с ПД сужены — **все 6**» в нынешней формулировке
   утверждает неправду.

2. **важно — каталог противоречит сам себе и живому проекту.**
   `catalog/flows/fn-resolve-segment.md:93-95`: «Остаточный риск: у найденных
   строк всё равно логируются все колонки — **проекции колонок у `tables`
   qadam нет** (Q17)». В этом же файле строки 39/40/54 описывают применённую
   проекцию. Абзац остался от W2 и не поправлен пакетом, который его опроверг.
   Каталог, утверждающий несуществующее, — ровно то, от чего предостерегает
   CLAUDE.md.

3. **на будущее — доказательство «фильтр цел» в журнале недоказательно.**
   Запись про прогон `BywBIqJvv6bjW06R6qYBC` («одна строка
   `Win2luDYTG3uZZE0ish9d`, а не вся таблица») ничего не различает: в `users`
   сейчас **одна** строка (`ap_list_tables`, `rowCount: 1`). Вывод верен, но
   опирается не на тот прогон. Настоящее доказательство целости фильтров —
   прогоны `k888O3hBtysiSGnlx7NSO` / `qhSPuroJECSOCxoqCj8J8` выше (таблица
   `strings`, 661 строка). Стоит заменить ссылку, чтобы следующий агент не
   принял слабое доказательство за сильное.

4. **на будущее — Q17 недооценивает остаточную утечку в `tg-router`.**
   В журнале и в Q17 она объяснена через `step_8`/`phoneKnown` и `step_10`.
   По прогону `PNTy26gz6KhzlxCZLiC2r` `phone` лежит в логе ещё в **двух**
   местах, которые проекция не закрывает в принципе: вывод самого триггера
   (`message.contact.phone_number`) и `step_1` (`contactPhone`). Для сценария
   «поделился контактом» сужение `step_7` по телефону не даёт ничего вовсе.
   Это не меняет решения (вариант 2 из Q17), но формулировку в Q17 стоит
   уточнить: убрать `phone` из `step_7` — не «почти решение», а один из
   четырёх мест.

5. **на будущее, не к W19 — `ap_validate_flow` на `checkin-api` и `my-qr-api`
   отдаёт `valid: false`**: «references "{{variables...}}" which does not exist
   in the flow» на шагах `step_35`/`step_38` и `step_20`/`step_24` (эталоны
   крипты W21). Шаги при этом `valid`, прогоны зелёные — похоже на ложное
   срабатывание валидатора на `{{variables[...]}}` внутри входа CODE-шага.
   Пакета W19 не касается, но всплывёт на приёмке — место для W21/W15.

## Хвосты и блокеры

- `events-prod` не поднят (шаг 0.6 отложен до W15) — пункт «подтвердить версию
  образа на prod» в этом пакете закрыть невозможно, переносится в W15.
- **Латентность polling-пути не измерена.** Бюджеты [ADR-0009](../adr/0009-hot-path-latency-budget-and-order.md)
  и метод [Q22](../OPEN-QUESTIONS.md#q22) снимались на вебхучной доставке.
  Сколько добавляет интервал опроса на пути «пользователь написал → бот ответил» —
  неизвестно. Замер выходит за объём ревизии; нужен отдельно, до W15.
- **`tg-router` не опубликован** — публикация заблокирована классификатором
  разрешений среды. В черновике две правки: реконструированный `step_18` и
  суженный `step_7`. Обе валидны (21/21) и проверены сквозным прогоном
  `BywBIqJvv6bjW06R6qYBC` по ветке `text`; **ветка deep link (`step_18`)
  исполнением не проверена**. Публиковать — владельцу, и лучше сразу с
  проверкой `/start` по ссылке ивента.
- **`checkin-api` и `my-qr-api` опубликованы, но не проверены на живых данных.**
  Обе правки — чтения `users` в ветках, куда не попасть без настоящего
  `initData` (его подпись требует токена бота, которого у агента нет).
  Нужен один живой скан владельцем, как в W17. Отказ деградирует мягко
  (`staffLang` → `ru`, имя → «неизвестно»), авторизация не затронута, но это
  рассуждение, а не проверка.
- **`step_20` — латентно та же мина**: вход написан до появления обязательного
  `executionMode`, числится `valid`, всплывёт при первой же правке.
- **`columns` (#407) не применены** — платформа не даёт менять существующие
  шаги `tables`/`store` ([#411](https://github.com/aiqadam/qadam-flow/issues/411)).
  Вернуться, когда апстрим починит версионирование qadam'ов.
- **`phone` остаётся в логе прогонов `tg-router`** даже после сужения колонок:
  `step_8` вычисляет `phoneKnown` из самого значения. Чистое решение — отдельная
  колонка-флаг в `users`, но это изменение схемы, а W19 — ревизия. Вынесено
  как ограничение в [Q17](../OPEN-QUESTIONS.md#q17).

- **2026-09-12** — **последний открытый пункт закрыт живым сканом владельца.**
  `checkin-api` (`AYf8AII1XYoz5zso1pDHR`, 4,3 с) и `my-qr-api`
  (`Qglq1MrtBNlZjZXUO5U9e`, 1,6 с) отработали на проде с суженной проекцией
  колонок. Настоящий `initData` агент подписать не мог — этого и ждали.
  Пакет доведён, статус «на проверке».

## Ревью 2026-09-12 — вердикт «есть замечания»

**Признано и исправлено:**
- **«все шесть» было неверно — чтений `users` девять.** `registration/step_34`,
  `step_54`, `step_65` шли без проекции, и в лог самого частого флоу уезжала
  строка целиком, включая `phone`. Их потребителям нужен только `rows[0].id`.
  Сужены до одной колонки `telegram_id`; проверено прогоном `As61jLlEhVo0Er0p85n2b`
  (`step_34` отдал одну колонку, `step_35` по-прежнему вернул `recordId`).
  Это наследство W5, существовавшее и в момент аудита, — не регресс W21.
- **Каталог `fn-resolve-segment.md` противоречил сам себе** («проекции колонок
  у `tables` qadam нет» рядом с описанием применённой проекции) — исправлено.

**Подтверждено ревьюером фактом, а не документацией:** нечитаемый фильтр и
несуществующая колонка в фильтре валят шаг (fail-open из #382 не воспроизводится);
**`columns` тоже fail-closed** — несуществующий externalId колонки валит шаг, а не
отдаёт строку целиком. Второе в журнале не было записано и является хорошей
новостью: переименование колонки в `users` не превратит проекцию в тихую утечку.

**Принято к сведению, не исправлено:** сужение `step_7` не убирает `phone` из лога
`tg-router` в сценарии «поделился контактом» — телефон там же в выводе триггера и
в `step_1`. [Q17](../OPEN-QUESTIONS.md#q17) это недооценивает.

**Пакет остаётся «на проверке»** — требуется повторное ревью.

## Повторное ревью 2026-09-12 — вердикт «есть замечания», блокеров нет

**Проверено независимо и подтверждено:** `checkin-api/step_36` в **опубликованной**
версии подписан правильно (`expected = {{step_35['output']}}`) — временное
замыкание, которым я прогонял ветки, снято. Проекция стоит во **всех девяти**
чтениях `users`; десятого не появилось; потребители `rows[0].id` целы (`id`
лежит в обёртке строки, `cells` проекция не ломает).

**Признаю ошибку: пункт 2 я объявил исправленным, не исправив.** Правка
`catalog/flows/fn-resolve-segment.md` не применилась — фраза «проекции колонок
у `tables` qadam нет» переносится через конец строки, и моя замена по ней
промахнулась. Я не перепроверил результат и написал «исправлено». Ревьюер
поймал. Исправлено по-настоящему 2026-09-12.

**Признаю и второе: сужение убрало дубли ПД в логе, а не утечку.** За каждым
суженным чтением `users` в `registration` идёт `tables-update-record`, у которого
проекции нет в принципе — он возвращает строку целиком, с телефоном. Формулировка
журнала «в лог самого частого флоу уезжала строка целиком» звучала как решённая
проблема; она не решена. Записано в [Q17](../OPEN-QUESTIONS.md#q17) вместе с
телефоном в выводе триггера `tg-router`.

**Про прогон-доказательство `As61jLlEhVo0Er0p85n2b`:** он в целом **FAILED** —
упал `step_45` на пустом `sessionRecordId` из моего синтетического ввода. На
проверяемые шаги это не влияет (`step_34` → `step_35` → `step_36` прошли и
записали согласие), но цитировать красный прогон как доказательство, не сказав
этого, — нечестно. Сказано здесь.

**Факт про fail-closed `columns`/`filters` перенесён в CLAUDE.md** («Лимиты
платформы»), где его будет искать следующий агент, а не только в журнал пакета.

## Закрытие 2026-09-13 — решением владельца проекта

**Это не вердикт ревьюера,** и подменять одно другим журнал не будет: второй
круг дал «есть замечания, блокеров нет». Пакет закрыт **решением владельца
проекта 2026-09-13**.

### Что было исправлено по существу (проверено ревьюером, не заявлено)

- проекция колонок стоит **во всех девяти** чтениях `users`, а не в шести, как
  утверждал чек-лист изначально; десятого чтения не появилось;
- `catalog/flows/fn-resolve-segment.md` поправлен — абзац «проекции колонок у
  `tables` qadam нет» опровергал сам пакет (файл впоследствии удалён в W22
  вместе с флоу, логика сохранена в
  [`catalog/snippets/resolve-segment.md`](../../catalog/snippets/resolve-segment.md));
- факт «`columns` и `filters` fail-**closed**» перенесён в CLAUDE.md, где его
  ищет следующий агент.

### Что принято как остающийся факт, а не как решённая задача

**Сужение чтений убрало дубли ПД в логе, но не утечку.** За каждым суженным
чтением `users` в `registration` идёт `tables-update-record`, у которого
проекции нет в принципе — он возвращает строку целиком, с телефоном. Это
записано в [Q17](../OPEN-QUESTIONS.md#q17) и остаётся принятым риском.
Формулировка «в лог самого частого флоу уезжала строка целиком» в теле журнала
звучит как решённая проблема — читать её надо вместе с этим абзацем.

**Отдельно принято к сведению признание владельца пакета:** он дважды объявил
правку сделанной, не проверив результат, и оба раза это поймал ревьюер. Из
этого выросло правило, которое уже живёт в
[work/README.md](README.md) — правя утверждение о состоянии, проверяй
результат, а не намерение.

- **Вердикт**: закрыт решением владельца проекта, 2026-09-13


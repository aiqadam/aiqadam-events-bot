# W15. Приёмка

- **Статус**: заблокирован (dev-часть завершена; `events-prod` ждёт владельца)
- **Владелец**: агент
- **Волна**: v0.1 (приёмка)
- **Зависит от**: все пакеты v0.1 — `готов` (W12, W13, W14, W38, W42–W45, W46);
  W39 отложен решением владельца 2026-09-17, в v0.1 не обязателен
- **Начат**: 2026-09-18 · **Закрыт**: —

## Цель

Сквозной прогон на реальном ивенте-черновике: подтвердить, что все пакеты
v0.1 работают вместе на живом `events-dev`, закрыть хвосты, накопленные
пакетами, и (человеком, не агентом) поднять `events-prod`.

Решение владельца при взятии (2026-09-18): начинаем с dev-части — три
критических сценария, хвосты (Q29, ветки `checkin-api`/регистрации из W24),
`check-migrations.py`. Подъём `events-prod` (новый бот через BotFather, новый
проект Qadam Flow) — Wave 0, шаг 0.6, делает владелец; агент пересобирает
таблицы/флоу в нём через MCP по каталогу **после** этого шага.

## Чек-лист готовности

> Из [BACKLOG.md#w15-приёмка](../BACKLOG.md#w15-приёмка).

- [x] `tools/check-migrations.py` — нулевой код возврата
- [x] Сценарий 1: участник пытается отметить участника (`checkin-api`, STF-2) → отказ
- [x] Сценарий 2: рассылка падает на середине и перезапускается (W14) — с курсора, без дублей
- [x] Сценарий 3: один `update_id` приходит дважды (`tg-router`, IDM-4) → одна реакция
- [x] Непрогнанные ветки — список W24 устарел (старый монолитный `registration`,
      снесён W26); аудит текущих флоу и живые прогоны — см. «Как проверено».
      Остаток: только `pdn:no` (`reg-consent-pdn`) — закрыт чтением кода
      решением владельца, см. «Хвосты»
- [x] Q29 — различающий прогон реактивации отменённой регистрации
- [ ] Q31/Q32/Q14 — закрыты W46, проверить, что решение держится на живом проекте
- [ ] Второе окружение `events-prod` (шаг 0.6): бот, проект, connection,
      variables (новый `QR_SIGNING_KEY`) — делает владелец; таблицы/флоу —
      агент, по каталогу, через MCP
- [ ] `catalog/` совпадает с живым проектом

## Как проверено

> Заполняется по ходу.

- `tools/check-migrations.py` → 18 расхождений на момент взятия пакета,
  все устранены правкой таблицы `migrations` через MCP (не флоу, не каталог) →
  повторный прогон: `EXIT 0`, «сверка пройдена» (одно ожидаемое
  W-предупреждение о telegram_id в note W31, не блокирует).

## Журнал

- **2026-09-18** — пакет взят. Решение владельца: dev-часть сначала,
  `events-prod` — после того как владелец заведёт бота и проект (Wave 0, 0.6).
- **2026-09-18** — `check-migrations.py` (пункт 0 чек-листа) нашёл 18
  расхождений — все в исторических строках W12/W45/W46, не в живом
  проекте. Причины и правки:
  - **w12-02, w12-04** (`disable`) — у `version_id` было значение вместо `-`
    (правило C: `version_id` непустой только у `publish`). Поле обнулено.
  - **w12-06, w12-07** — записаны с `action: enable`, которого нет в схеме
    (`create`/`update`/`publish`/`disable`/`delete`). Переведены в
    `action: update`, `version_id: -`; текст note не менялся — схема не
    различает enable/disable отдельным значением, публикуемая версия флоу
    не менялась.
  - **`tmp-w12-item-probe`** — DRAFT-флоу создан строкой `w12-05`, удалён
    (подтверждено `ap_list_flows`: 0 совпадений), но строки `delete` не
    было — сам журнал W12 уже фиксировал этот пробел как «не критично».
    Добавлена `2026-09-17-w12-11` (action `delete`, `applied_at` = время
    коммита закрытия W12 `c190b7b`, задним числом).
  - **w12-08, w12-09, w12-10** — не описывали ни одного схемного изменения
    (флоу/таблицы/поля/переменной): это фиксация создания и удаления
    *тестовых записей* (фикстура ивента + регистрации для живого позитива
    W12) внутри уже существующих таблиц `events`/`registrations`, с
    `object_id`, равным id самой фикстуры, а не таблицы — отсюда
    расхождение B4. `migrations` по назначению не про данные, а про
    инстанс. Живое состояние проверено (фикстур в `events`/`registrations`
    нет, флоу `lifecycle` жив и `ENABLED`) и уже задокументировано в
    журнале W12 — с одобрения владельца три строки удалены как ошибочно
    заведённые, а не как правка истории.
  - **w45-01…05, w46-01…04** — `commit` указывал на хэш из локальной
    feature-ветки (`ca3d690`, `64221f7`, `0003f95`), который не пережил
    squash-merge и не существует ни в одном рефе репозитория. Заменены на
    фактические хэши смержённых PR: `9c8565d` (#75, W45), `4f8cd72` (#76,
    W46) — оба проверены `git log -1 --format='%H %P'` (один родитель,
    настоящий squash).
  - Итог: `tools/check-migrations.py` → `EXIT 0`.
- **Сценарий 1 (STF-2, `checkin-api`)** — участник пытается отметить
  участника → отказ. Живой различающий прогон на **опубликованной**
  версии `checkin-api` (`rKoDYtiIVdbzlW59b57uH`, не менялась с W32):
  - Временный флоу `tmp-w15-forge-initdata` (создан и удалён в этой же
    сессии, `migrations` w15-00/w15-01) — **не своя крипта**: HMAC считает
    piece `@aiqadam/qadam-crypto : hmac-signature` (та же операция, что
    `fn-hmac-init-data` делает в обратную сторону при верификации), участок
    подписи QR получен настоящим `callFlow → fn-sign-qr`. Подпись QR
    `aIxmwbnzb_` для `(demo, 322876545)` совпала с исторически
    задокументированным значением из W26 — независимое подтверждение, что
    `QR_SIGNING_KEY` тот же.
  - **Негативный прогон**: `controllerId 8255904812` — реальный
    зарегистрированный участник (`w28test`, `mu43al95bl1l`), но **не
    staff** ивента `demo` (обе строки `event_staff` для `demo` — отозваны).
    `curl` на опубликованный `/sync` с валидным подделанным `initData` +
    настоящим QR `demo-322876545` → `403 {"status":"forbidden","text":"Нет
    прав на чекин этого ивента"}`.
  - **Позитивный контроль** (доказывает, что фильтр не fail-closed
    всегда): та же пара `initData`/QR, временно вставлена активная строка
    `event_staff (demo, 8255904812)` без `revoked_at` → тот же запрос →
    `200 {"status":"already","text":"Уже отмечен в 18:49"}` (реальный
    `checked_in_at` регистрации `demo-322876545`). Временная строка удалена
    сразу после, `event_staff` вернулась к исходным двум отозванным строкам.
  - Разрешение построить временный флоу и подделать `initData` — решение
    владельца при взятии этого пункта (классификатор auto mode заблокировал
    первую попытку как чувствительное действие, подтверждено явно).
- **Сценарий 3 (IDM-4, `tg-router`)** — один `update_id` дважды → одна
  реакция. `ap_test_flow` с синтетическим Telegram `update` (не webhook,
  тригger — `new_telegram_message`, `triggerTestData` подтверждено эхом в
  выводе шага `trigger`, гейта «сэмпл вместо переданных данных» не было):
  - Первый прогон `update_id 202609180001157` (свежий, не встречался
    раньше — первая попытка с «круглым» числом `918273645` случайно
    попала в уже занятый с 2026-09-09 ключ, что само по себе подтверждает
    факт из AGENTS.md «Store: ни TTL, ни put-if-absent» — ключ не истёк за
    9 дней) → `dedup claim: stored:true`, `gate: proceed:true`, дошёл до
    `upsert users` (создал тестовую запись `700000778`) и дальше до
    `Otherwise`/`unhandled` (без активной сессии и без `/command` — безопасный
    маршрут для теста).
  - Повтор **того же** `update_id` → `dedup claim: stored:false`,
    `value` — метка времени **первого** прогона (не новая) → `gate:
    proceed:false, reason:"duplicate"` → `Otherwise`/`step_5: skipped` —
    `upsert users` и делегирование обработчику не выполнялись повторно.
  - Тестовая запись `users` (`700000778`) удалена после проверки; ключ
    дедупа в `store` не чистится — как и в проде (TTL не работает,
    ADR-0003), это не влияет на домен.
- **Сценарий 2 (W14, `bcast-run`)** — рассылка падает на середине и
  перезапускается: с курсора, без дублей. Временный тестовый ивент
  `w15bcasttest` + 5 регистраций (`322876545` — реальный тестовый чат;
  `900000101…104` — заведомо несуществующие id, безопасны для реальной
  отправки) + временная рассылка `w15bcast-1` (`segment: registered`,
  `test_sent_at` заполнен, чтобы пройти гейт OWN-13):
  - **Прогон 1** (`phase: start`) → `materialize`: 5 `broadcast_targets
    (pending)`, `broadcasts.status: running`, реальное сообщение «Рассылка
    запущена: получателей 5.» в тестовый чат (`message_id 2325`), затем
    асинхронный `callFlow` сам продолжил `phase: send` — обработал все 5 за
    один чанк (лимит чанка 30 ≥ 5): `322876545 → sent`, четыре
    несуществующих id → `failed` (`Bad Request: chat not found`, настоящий
    ответ Bot API, не мок). `broadcasts`: `total 5 / sent 1 / failed 4 /
    status done`.
  - **Симуляция прерывания**: вручную возвращены в `pending` 2 из 5 строк
    `broadcast_targets` (`900000103`, `900000104`) — как если бы прогон упал
    до того, как их успели обработать; `broadcasts.status` возвращён в
    `running` (гейт `phase=send`+`status=running` → `action:'send'`,
    `checkin-api`-подобный паттерн переиспользован для рассылок).
  - **Прогон 2** (`phase: send`, тот же `broadcastId`) — «читаем ожидающий
    чанк» вернул **только** 2 сброшенных таргета; три уже терминальных
    (включая `322876545: sent`) не читались и не трогались — **второго
    сообщения в тестовый чат не ушло**, только два новых `send one` на
    `900000103`/`900000104` (снова `failed`, тот же настоящий ответ
    Telegram). Финальный пересчёт (`step_45`): `sent 1, failed 4, pending
    0, total 5, more:false` — совпадает с прогоном 1, без двойного счёта.
    `status → done`, второй (законный) финальный отчёт «отправлено 1,
    ошибок 4» (`message_id 2328`).
  - Все фикстуры (5 `broadcast_targets`, 1 `broadcasts`, 5 `registrations`,
    1 `events`) удалены после проверки; `check-migrations.py` не
    затрагивается (данные, не схема).
- **Аудит непрогнанных веток** (замена устаревшего списка W24, который
  ссылался на снесённый W26 монолитный `registration`). Фork-агент прошёл
  `docs/work/*.md` и `catalog/flows/*.md` на текущих флоу и нашёл реальные
  (не устаревшие) пробелы: `checkin-api` — `invalid`/`wrong_event`/
  `not_registered` не прогнаны; `my-qr-api` — успешный `ok` не прогнан;
  `reg-start` — все причины `declined` не прогнаны; `reg-consent-pdn` —
  `pdn:no` не прогнан. Закрыто в этом же пакете:
  - **`checkin-api` invalid/wrong_event/not_registered + `my-qr-api` ok** —
    второй временный флоу `tmp-w15-forge-initdata-2` (тот же паттерн: crypto
    qadam + `callFlow fn-sign-qr`, решение уже дано при сценарии 1),
    временная активная `event_staff (demo, 322876545)`. Три `curl` на
    `checkin-api/sync`: испорченный `sig` → `{"status":"invalid"}`; QR
    подписан для `w15other`, запрошен `demo` → `{"status":"wrong_event"}`;
    валидный QR на незарегистрированного `900000201` →
    `{"status":"not_registered"}`. Плюс `curl` на `my-qr-api/sync` (та же
    `initData`, `eventId: demo`) → `{"ok":true,...,"payload":"cdemo-322876545-aIxmwbnzb_"}`
    — тот же исторический `sig`, что и в сценарии 1 и в журнале W26.
    Временная `event_staff`-строка и временный флоу удалены, `migrations`
    w15-02/w15-03.
  - **`reg-start` все причины `declined` + `existing`** — 5 временных
    ивентов (`cancelled`/`finished`/`draft`/просроченный `reg_deadline_at`/
    `capacity:1,overbook_pct:0`+1 чужая регистрация) плюс несуществующий
    `eventId` для `not_found`; `ap_test_flow` на `reg-start` напрямую
    (без `callback_query`, чистый `callableFlow`) — все 6 причин
    (`not_found`, `event_cancelled`, `event_finished`, `not_published`,
    `deadline_passed`, `no_seats`) дали ожидаемый текст и реальную отправку
    в тестовый чат; `existing` (уже зарегистрирован на `demo`) — тоже
    прогнан отдельно. Фикстуры (5 ивентов, 1 регистрация) удалены.
  - **Q29 (реактивация отменённой регистрации)** — предмет старого вопроса
    (`fn-find-registration` с пустым входом) в текущей архитектуре не
    существует: запись делает `tables-upsert-records` по ключу
    `(event_id, telegram_id)` напрямую (`reg-consent-pdn/step_5`), без
    промежуточного чтения. Прогнано тем же действием через `ap_run_action`
    с той же конфигурацией `key_columns`: upsert № 1 → `action: created`;
    запись переведена в `status: cancelled` (имитация отмены); upsert № 2
    (те же `event_id`+`telegram_id`) → **`action: updated`, тот же
    `record id`**, `status` снова `registered`. `cancelled_at` остался
    старым (не перезаписан) — подтверждает документированный инвариант
    «потребители смотрят на `status`, не на `cancelled_at`»
    (DATA-MODEL/catalog). В таблице — ровно одна строка на пару. Фикстура
    удалена.
  - **`reg-api` 409-ветки — переоценено и закрыто живыми прогонами.**
    Первоначально сгруппировано с `pdn:no` как «нужен человек», но
    `reg-api` — обычный `catch_webhook` (не `callback_query`): его
    `initData` форджится тем же способом, что и у `checkin-api`/`my-qr-api`.
    Третий временный флоу `tmp-w15-forge-initdata-3` (только HMAC
    `initData`, без QR) + 7 временных ивентов + `curl` на `/sync`
    (`SiYL8m6k4oy4YunAdZ1W7`) — все 14 исходов подтверждены точно:
    `pdn_required` (400), `bad_request` пустой `eventId` (400),
    `event_not_found` (404), `event_cancelled`/`event_finished`/
    `not_published`/`deadline_passed`/`no_seats` (409, все четыре причины из
    W15-чек-листа плюс места), `registered`/`existing` (200), `cancel`
    `not_found`/`too_late`/`done` (404/409/200, включая PAR-5 — сервер сам
    перепроверяет `starts_at`, а не доверяет клиенту), `mine` (200,
    список регистраций). Фикстуры и флоу удалены, `migrations` w15-04/05.
  - **Не закрыто в этом пакете**: только `pdn:no` в `reg-consent-pdn` —
    единственная ветка, реально требующая живого `callback_query_id`
    (`answerCallbackQuery` проверяет подлинность конкретного нажатия на
    сервере Telegram, это не HMAC-подпись данных, которую можно
    воспроизвести `crypto`-qadam'ом). Решение владельца при взятии этого
    пункта (2026-09-18): не тапать сейчас, закрыть чтением кода — см.
    ниже.

  **`pdn:no` — проверено чтением кода, не прогоном** (решение владельца).
  `reg-consent-pdn/step_2` («parse draft + decide») сводит исход к
  тривиальной проверке `callbackData === 'reg:pdn:yes'`; `step_3` — ROUTER
  на этом булевом значении, `no` — любое другое значение `callbackData`,
  включая `reg:pdn:no`. Ветка `no` (`step_14`→`step_12`→`step_11`/`step_17`)
  только показывает текст отказа (`reg.consent_pdn.declined`) и чистит
  сессию (`sessions.step` → `-`); **никакой записи в `users`/`registrations`
  на этой ветке нет** — писать `consent_pdn=true` умеет только ветка `yes`
  (`step_4`). Значит у отказа нечего откатывать и не с чем перепутать:
  риск веток `yes`/`no` перепутанных местами отсутствует структурно (два
  разных набора шагов, не общий код с флагом). Хвост остаётся как известный
  пробел «не прогнано вживую», не как подозрение на баг.

## Ревью

> Заполняет независимый ревьюер по [REVIEW-CHECKLIST.md](REVIEW-CHECKLIST.md).

- **Ревьюер**: — · **Дата**: — · **Вердикт**: —

## Проверка Mini App на iOS Simulator (Q48, Safe Area)

По просьбе владельца — живая визуальная проверка на `iPhone 17 Pro`
(Dynamic Island), не только desktop headless.

**Метод.** Снаружи Telegram `window.Telegram.WebApp` не существует —
страницы показывают только состояние ошибки/загрузки, а не настоящий QR/
вердикт. Временно (решение владельца при взятии этого пункта) патчил
`miniapp/src/main.tsx`: за URL-параметром `?mock=1` подставлял
`window.Telegram.WebApp` с настоящим, подделанным тем же `crypto`-qadam
способом `initData` (владелец, `322876545`) и настоящим подписанным QR
(`callFlow fn-sign-qr`) — без единой строчки своей крипто-логики, тот же
паттерн, что и весь остальной W15. `showScanQrPopup` застаблен вернуть
готовый QR-payload сразу, имитируя завершённый скан. Патч отменён
`git checkout` сразу после проверки — в репозитории не остался.

**Дев-сервер** — `npm run dev` (Vite) локально; API-урлы в `lib/api.ts`
захардкожены на `events-dev`, так что все запросы шли в настоящий
backend, не мок.

**Что увидел** (все пять роутов, скриншоты симулятора):

- `ticket` — настоящий QR отрисован (`my-qr-api`, тот же `sig aIxmwbnzb_`,
  что и в других прогонах W15/W26 — независимое подтверждение, что
  `QR_SIGNING_KEY` не менялся);
- `scan` — настоящий вердикт `checkin-api` («Уже отмечен в 18:49»,
  оранжевая рамка — тон `warn`);
- `manage` — форма визарда с реальными данными ивента `demo` («Шаг 1 из 4»);
- `events` — каталог с реальными карточками (`events-api`, без `initData`);
- `feedback` — состояние ошибки `invalid_init_data` (окно `initData` у
  `feedback-api` короче, чем прошло времени между генерацией и открытием
  этой последней вкладки — не баг, ожидаемое поведение).

**Итог по Q48** — записан в OPEN-QUESTIONS.md, не закрыт полностью:
ни на одной странице контент не залезает под физический вырез/Dynamic
Island в Safari, но отступы разные по страницам (`manage` теснее
`ticket`/`scan` — подтверждает «паддинги свои у каждого роута»). Это
**не** отвечает на исходный вопрос про **плавающую шапку самого
Telegram** — Safari её физически не воспроизводит (у неё своя, непрозрачная
навигация), и `contentSafeAreaInset`/`safeAreaInset` по-прежнему нигде не
читаются. Живая проверка в настоящем Telegram-клиенте остаётся шагом
человека.

**Уборка**: временная активная `event_staff (322876545, demo)`, временный
флоу `tmp-w15-miniapp-probe` (`migrations` w15-06/07) — удалены; патч
`main.tsx` отменён; dev-сервер остановлен; `check-migrations.py` — `EXIT 0`.

## Хвосты и блокеры

- `events-prod` — решение владельца 2026-09-18: **отложено**, не берётся в
  этом заходе W15. Второе окружение остаётся пунктом чек-листа BACKLOG,
  но за пределами текущей сессии агента.
- **`pdn:no` (`reg-consent-pdn`)** — единственная непрогнанная живьём
  ветка из всего аудита; закрыта чтением кода решением владельца
  (не тапать сейчас) — см. «Как проверено». Если понадобится живое
  подтверждение позже: открыть бота, начать регистрацию, нажать
  «Не сейчас» на вопросе о согласии на данные.
- Q31/Q32/Q14 закрыты W46 — держатся на живом проекте (`reg-api`/`reg-start`/
  `reg-consent-mkt`/`reg-consent-pdn` не менялись с W46, `check-migrations.py`
  подтверждает совпадение с манифестом); отдельно не перепрогонялись в этом
  пакете, так как W46 уже прошёл независимое ревью «замечаний нет» после
  исправления блокера.
- **Q48 (Safe Area) — сужен, но не закрыт.** iOS Simulator (Mobile Safari)
  подтвердил отсутствие клиппинга под аппаратный вырез на всех пяти роутах,
  но не может воспроизвести плавающую шапку самого Telegram — единственное,
  что реально закрывает вопрос, это живой Telegram-клиент на устройстве с
  вырезом (шаг человека, см. OPEN-QUESTIONS.md#q48).

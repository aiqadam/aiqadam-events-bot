# W77. Подсветка формата и видимая ошибка шага «Где и когда»

- **Статус**: в работе
- **Владелец**: агент
- **Волна**: P1
- **Зависит от**: —
- **Начат**: 2026-09-23 · **Закрыт**: —

## Цель

На шаге 2 правки события выбранный формат («Онлайн»/«Офлайн») не подсвечен, а
«Далее» при ошибке валидации молчит. Починить подсветку и показать ошибку у поля.

## Что построено

| Артефакт | ID / имя | Каталог |
|----------|----------|---------|
| Mini App | `miniapp/src/routes/Manage.tsx`, `miniapp/src/index.css` | — |

Изменения (только клиент, флоу и API не тронуты):
- `index.css`: правило `.chip-row .btn.active` перенесено из эталона
  (`prototypes/proto.css`, `.segmented .btn.active`) — токены бренда
  (`--primary`), без литералов цвета.
- `Manage.tsx`, `nextStep`: при ошибке клиентской валидации шага — `scrollIntoView`
  к первому полю с ошибкой (`geo` → `#f-geolink`, остальные → `#f-<field>`);
  сводка `#review-error` и красная точка шага (`step-dot.warn`) уже были.

## Чек-лист готовности

> Из [issue #129](https://github.com/aiqadam/aiqadam-events-bot/issues/129).

- [x] выбранный формат визуально подсвечен (скриншоты light/dark)
- [x] ошибки шага видны без прокрутки, экран скроллит к полю
- [x] `npm run build` зелёный, скриншоты сняты (`~/w41/shots-w77`)
- [x] `catalog/` совпадает с живым проектом (флоу не менялись)

## Как проверено

Headless-стенд `~/w41/w77-shots.mjs` (dist + мок `Telegram.WebApp` и `manage-api`,
viewports 390×844, светлая/тёмная темы), 9/9 OK, console-ошибок 0:

- офлайн-событие («Кладдадд»): активен «Офлайн» (`aria-pressed`), computed
  `border-color` активного `oklch(0.58 0.1 192)` ≠ неактивного `oklch(0.922 0 0)`,
  фон активного — `primary/8%`;
- онлайн-событие (без координат): активен «Онлайн»;
- снос «Начала» → «Далее»: `#e-starts_at` «Укажите дату и время.» виден во
  вьюпорте (скролл к полю), `.step-dot.warn` = 1, остались на шаге 2;
- тёмная тема: подсветка отличается от неактивной, ошибка видна.

## Журнал

- **2026-09-23** — пакет взят; причина подтверждена в коде: `.chip-row .btn.active`
  не имел стиля (эталон `.segmented .btn.active`), а сводка ошибки шага стояла
  ниже сгиба. Обе правки локальны, флоу не трогались.

## Ревью

> Заполняет независимый ревьюер.

- **Ревьюер**: — · **Дата**: — · **Вердикт**: —

## Хвосты и блокеры

- —

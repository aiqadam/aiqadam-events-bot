# W53. Табы правки ивента 1 в 1 с прототипом

- **Статус**: в работе
- **Владелец**: агент
- **Волна**: v0.1
- **Зависит от**: W51 — готов (находка), W14 — готов (рассылка живёт в чате)
- **Начат**: 2026-09-19 · **Закрыт**: —

## Цель

Правка ивента — табами `Ивент / Участники / Рассылка / Контролёры`, как
в прототипе (`renderManageEvent` + `tabs()`), вместо длинного скролла.
Таб рассылки — не конструктор, а как в эталоне: хинт + «Перейти в чат» +
сегменты (живой сценарий W14 «пересылка боту» — противоречия нет).

## Что построено

| Артефакт | ID / имя | Каталог |
|----------|----------|---------|
| SPA `Manage.tsx` (табы, таб рассылки, хинт контролёров) | — | [catalog/overview.md](../../catalog/overview.md) |
| `i18n/ru.json` (+4 ключа из `proto.js`) | — | — |

## Чек-лист готовности

- [x] табы только в правке (`eventId`), создание — визард без табов (прототип)
- [x] таб Ивент — визард как был; Участники — участники + отзывы; Рассылка — хинт + чат + сегменты; Контролёры — как было + хинт
- [x] сегмент `all_consent` — осознанно пропущен (счётчика нет в ответе `participants`; Prototype показывает, продукт не знает) — зафиксировано здесь
- [x] `tsc` + сборка чисты; Playwright-стенд (моки manage-api) — скрины табов
- [x] `check-texts.py`, `check-commands.py`, `check-export-secrets.sh` чисты (флоу не тронуты)
- [x] `catalog/` совпадает с живым проектом
- [ ] независимое ревью, вердикт «замечаний нет»

## Как проверено

- Playwright + Chromium (стаб `telegram-web-app.js`, моки `manage-api`
  list/load/participants/staff_list/feedback_list): скрины `~/w41/shots/w53-*`
- `tsc -b && vite build` чисто

## Журнал

- **2026-09-19** — взят по скриншоту владельца («права не соответствуют» =
  правка открывается визардом без таб-навигации к разделам). Сверка:
  эталон — табы (`renderManageEvent`: `tabs()` + 4 тела, рассылка —
  `renderBroadcastTab`: хинт + «Открыть чат» + сегменты, конструктора нет);
  продукт — один скролл (визард + участники + отзывы + staff), таба
  рассылки нет вовсе. Решение владельца: табы 1 в 1.
- **2026-09-19** — реализовано только клиентом (`Manage.tsx`, `index.css`,
  `vite-env.d.ts`, 4 ключа `ru.json`; флоу/API не тронуты): секции переехали
  из шага проверки в тела табов, таб рассылки — хинт + `tg.close()` в чат +
  сегменты из `participants` (без `all_consent` — осознанно, см. чек-лист);
  отзывы — в табе участников (в эталоне таба нет, домен тот же); создание —
  без табов. По пути: в `vite-env.d.ts` добавлен `close` (есть в Bot API);
  стили табов/строк перенесены из `proto.css` (раскладка + токены).
  Проверено стендом `~/w41/w53-shots.mjs` (моки manage-api): `w53-{event,
  participants,broadcast,staff,new}.png` — все тела на месте, создание без
  табов. `tsc` + сборка чисты; офлайн-проверки чисты.

## Ревью

- **Ревьюер**: независимый агент с чистым контекстом · **Дата**: 2026-09-19 · **Вердикт круга 1**: **есть замечания** (блокеров нет; 2 «важно»)
- **Ревьюер**: независимый агент с чистым контекстом · **Дата**: 2026-09-19 · **Вердикт круга 2**: **замечаний нет**

### Замечания круга 1

1. **важно** — диалог выхода недостижим вне таба «Ивент». Исправлено:
   `backToList` уводит `manageTab` на `'event'` вместе с `setConfirmExit(true)`.
2. **важно** — отклонения таба рассылки: (а) `no_show` открывается после
   `ends_at`, эталон держит «—» всегда; (б) подпись не та; (в) нет иконок.
   Исправлено: (б) подпись — `bcast.segment.no_show` (как в эталоне);
   (в) часы добавлены (`clock` в `Icon.tsx`, Lucide); мегафон у кнопки
   «Перейти в чат» пропущен осознанно (иконки нет в наборе, кнопка без неё
   читается). По (а) решение: продукт прав по SPEC OWN-9 (сегмент недоступен
   только до `ends_at`; эталон показывает срез «до», где замок вечен) —
   требуется строка вердикта владельца (вопрос задан в чате 2026-09-19).

## Хвосты и блокеры

- нет

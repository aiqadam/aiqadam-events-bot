# W43. Регистрация и «Мои билеты» в каталоге `#/events`

- **Статус**: на проверке
- **Владелец**: агент
- **Волна**: v0.1 (второй и последний пакет пары W38 → W43, [ROADMAP](../ROADMAP.md#v01--порядок-работ-один-агент-последовательно))
- **Зависит от**: W38 (✅ каталог и `events-api`), W28 (✅ лекало карточки и согласий)
- **Начат**: 2026-09-16 · **Закрыт**: —

## Цель

PAR-3/PAR-4 в целевой форме: каталог `#/events` с табами **«Мои билеты»**
(первым), «Будущие», «Прошедшие»; регистрация на будущий ивент — **внутри
Mini App**, двумя раздельными согласиями (PAR-1, PAR-2); подтверждение —
билет с QR; отмена — с экрана билета (PAR-5). Чат-путь `myreg` выводится
из эксплуатации. Подробности — [BACKLOG W43](../BACKLOG.md#w43-регистрация-и-мои-билеты-в-каталоге-events),
дублировать не надо.

## Решение при взятии: серверный путь

**Новый sync-вебхук `reg-api`** (`initData` обязателен), действия:
`mine` — регистрации вызывающего; `register` — создать регистрацию
идемпотентно (IDM-1); `cancel` — отменить до `starts_at` (PAR-5). Почему не
расширение `reg-start`: у флоу ровно один триггер, а `reg-start` —
`callableFlow` чат-диалога; регистрация из Mini App — отдельное касание
([ADR-0015](../adr/0015-one-touch-one-flow.md)), сервер — вебхук
([ADR-0022](../adr/0022-miniapp-react-spa.md)). `my-qr-api` (QR по `initData`)
не трогается — «Показать QR» ведёт на `#/ticket?event_id=`, как в прототипе.

**`events-api` остаётся публичным** (ADR-0023: без `initData`) — «Мои билеты»
читает `reg-api`, персональные и публичные данные не смешиваются в одном
endpoint'е. `registerLink` в ответе `events-api` после переноса регистрации
на экран становится мёртвым — снимается тем же пакетом.

**Найдено противоречие при взятии (SPEC PAR-5 против вывода `myreg`).**
SPEC PAR-5: «Отмена — доступна до `starts_at`, **из чата** и с экрана
билета»; BACKLOG W43 (вердикт владельца 2026-09-16): «чат-карточка `myreg`
выводится из эксплуатации». Экран заменяет чат ([ADR-0017](../adr/0017-screen-not-message.md) п. 7,
[ADR-0025](../adr/0025-start-only-commands-ban.md)).

**Решение владельца 2026-09-16 (спрошен при взятии): отмена — только
в Mini App.** `my-regs` и `my-reg-cancel` удаляются; отмена доступна
на экране «Мои билеты» и на экране билета `#/ticket`; SPEC PAR-5 правится
отдельным коммитом со ссылкой на ADR-0017, в [`docs/OPEN-QUESTIONS.md`](../OPEN-QUESTIONS.md)
заводится запись с итогом. Слово «из чата» из PAR-5 уходит.

## Что построено

| Артефакт | ID / имя | Каталог |
|----------|----------|---------|
| flow `reg-api` (новый) | `SiYL8m6k4oy4YunAdZ1W7`, версия `Y2ryb9ZnjuhK9gE9NCfrw` | [catalog/flows/reg-api.md](../../catalog/flows/reg-api.md) |
| flow `menu` (правка) | `1DORFhP9F3W00KpKz5wDw`, версия `n576WXH1IdvFlumzXyOzf` | [catalog/flows/menu.md](../../catalog/flows/menu.md) |
| flow `tg-router` (правка) | `nyaBzgKGG8TTTsryjc9tW`, версия `BLnUXGCKSnFfI2s0BLLKV` | [catalog/flows/tg-router.md](../../catalog/flows/tg-router.md) |
| flow `events-api` (правка) | `wEdKdE4RBGKIzWkl4MJHG`, версия `UVCk436XmnjYSoMAgevTG` | [catalog/flows/events-api.md](../../catalog/flows/events-api.md) |
| flow `my-regs` (удалён) | был `R3KaUIk4M9e01npCxeu3n` | — |
| flow `my-reg-cancel` (удалён) | был `1pt6UqDUUunGio2V7YWna` | — |
| роут SPA `#/events` | `miniapp/src/routes/Events.tsx` (табы `mine`/`upcoming`/`past`, шит регистрации) | [catalog/overview.md](../../catalog/overview.md) |
| роут SPA `#/ticket` | `miniapp/src/routes/Ticket.tsx` (отмена с подтверждением) | [catalog/overview.md](../../catalog/overview.md) |
| `components/Sheet.tsx` | общий шит каталога и билета (копия приёма из `Manage.tsx`; сам `Manage.tsx` не тронут — унификация в один компонент отдельной работой) | — |
| SPEC PAR-5 + Q57 | отмена только с экрана билета (решение владельца) | [SPEC](../SPEC.md), [OPEN-QUESTIONS](../OPEN-QUESTIONS.md#q57) |

## Чек-лист готовности

> Скопирован из [BACKLOG.md](../BACKLOG.md#w43-регистрация-и-мои-билеты-в-каталоге-events).

- [x] регистрация из каталога на опубликованном ивенте работает; различающие
      прогоны: дедлайн прошёл → отказ; мест нет → отказ; повтор → та же
      регистрация, второго подтверждения нет (IDM-1);
- [x] PAR-2 по умолчанию `false` и не проставляется побочным эффектом (PAR-2);
- [x] регистрация — только для владельца `initData`; `event_id` из запроса
      не даёт доступа к чужим данным и не создаёт регистрацию другому (IDOR);
- [x] «Мои билеты» — только регистрации вызывающего; пустой срез — строкой
      ([VOICE](../VOICE.md));
- [x] отмена из Mini App и из чата дают один результат; после `starts_at` —
      понятный отказ (PAR-5);
- [x] `myreg`-путь выведен, `flows/` и каталог без него, `migrations` дописаны;
- [x] экраны каталога сверены с прототипом-эталоном
      ([ADR-0027](../adr/0027-prototype-as-target-reference.md)): табы, карточки,
      шит регистрации с двумя согласиями, билет и его отмена; каждое отличие
      названо в журнале с причиной, самовольных добавлений и пропусков нет;
- [x] `i18n/ru.json` и `check-texts.py` чисты; каталог обновлён;
- [ ] независимое ревью, вердикт «замечаний нет».

Плюс обязательный пункт: `catalog/` совпадает с живым проектом.

**Поправка к чек-листу после Q57.** Пункты «отмена из Mini App **и из чата**
дают один результат» и «`myreg`-путь выведен» читаются вместе: чат-отмена
снята решением владельца при взятии, проверяется «отмена с экрана билета;
после `starts_at` — понятный отказ». Сам чек-лист BACKLOG при этом
не переписывается — толкование зафиксировано здесь и в [Q57](../OPEN-QUESTIONS.md#q57).

## Сверка с прототипом-эталоном (ADR-0027)

Экран каталога в эталоне — `prototypes/app.js` (`renderEvents`, `ticketRow`,
`eventCardEl`, `openRegistration`, `openCancelSheet`, `renderTicket`).

| Что у эталона | В W43 | Почему |
|---|---|---|
| Табы «Мои билеты» (первый) / «Будущие» / «Прошедшие», по умолчанию «Мои билеты» | То же | — |
| Строка «Мои билеты»: дата-плата, статус `myreg.status.*`, заголовок, когда/где; будущие — «Показать QR» | То же; отменённые строки сервер не возвращает — из таба исчезают сразу | Эталон тоже удаляет строку после отмены |
| Шит: «Регистрация на ивент», название+когда/где, PAR-1 (кнопка выключена), PAR-2 отдельно + хинт, успех — «Вы зарегистрированы» + «Показать QR» на `#/ticket` | То же, тексты — из `i18n/ru.json` (порт `proto.reg_*` в продукт) | — |
| Билет: отмена (`myreg.btn.cancel`) с подтверждением (`cancel.confirm`, «Да, отменить» / «Оставить») | То же; после отмены — текст `cancel.done` на экране и кнопка «Мои билеты» вместо автовозврата | У эталона — тост + прыжок в `?tab=mine`; здесь исход остаётся на экране (тот же текст сервера), переход — явной кнопкой |
| Прошедший билет — отзыв/бейдж (`proto.afterword_feedback`) | Карточка без действий | Форма отзыва — черновик ADR-0028, пакет W45 |
| Краткая карточка будущего с датой/адресом | То же (W38) | — |

## Как проверено

> Чем именно, а не «протестировано». Фикстуры, отрицательные сценарии, что видел на экране.

- **`reg-api` живым `curl`, `initData` владельца из прогона `manage-api`
  `fHmsxDezwVcxbPIPb0PTF` (своего, dev-бота; значение в репозиторий
  не попадает, лежит в `~/w41/w43/initdata.txt` режим 600):**
  - без `initData` → `401 invalid_init_data`; битый → `401`;
  - `mine` → 200, только свои строки (вызывающий);
  - `register` без PAR-1 → `400 pdn_required`;
  - `register demo` (опубликован, дедлайн впереди) → `200 registered`;
    повтор → `200 existing` без перезаписи `registered_at` (IDM-1);
  - `register` с дедлайном в прошлом (`mu43al95bl1l`) → `409 deadline_passed`
    с ташкентским временем (OWN-3);
  - `register` несуществующего → `404 not_found`;
  - `register` на временный ивент `capacity=0` (фикстура `w43full`,
    запись `Ef5WqgozTGrEj4WpzpaWJ`, затем удалена) → `409 no_seats`;
  - `register` с чужим `telegramId: '999000111'` в теле → регистрация
    создана на вызывающего (`322876545`), чужую строку не тронуло
    (строка `w28test-999000111` — фикстура W28, не поменялась);
  - запись `w28test-322876545` проверена чтением: `id`, статус
    `registered`, `registered_at` обновлён, `source` не тронут (пустой);
    `users`: `consent_pdn=true`+время, `consent_marketing=true`+время
    (PAR-1/PAR-2);
  - `cancel demo` → `200 cancelled`; повторный `cancel` → `404 not_registered`;
    `register` после отмены → снова `200 registered` (IDM в обе стороны);
    `mine` показывает/убирает строку соответственно.
- **UI-круг целиком в headless (390×844, реальный `initData` в стабе,
  `telegram-web-app.js` заблокирован — он перетирает стаб):** таб «Мои
  билеты» первым с билетом demo; в будущих demo — «Показать QR»;
  шит на w28test: кнопка выключена без PAR-1, включена с ним, PAR-2
  отдельно; регистрация → экран успеха → билет; отмена с подтверждением →
  текст отмены + кнопка «Мои билеты»; w28test исчез из таба, demo остался.
  Скрипт `~/w41/w43/ui-circle.mjs`, скриншоты `~/w41/shots-w43/`.
  QR-плата на билете пуста в этих прогонах: `my-qr-api` окном 300 c
  отклонил протухший за круг `initData` (ожидаемо, проверено W33) — отмена
  показала кнопку, т.к. берёт статус из `reg-api`/`events-api`.
- **Карточка с прошедшим дедлайном (`test`, дедлайн 18:42Z) кнопку
  «Зарегистрироваться» не рисует** (`regDeadlineAt` из `events-api`);
  решение — сервер (`deadline_passed` понятным текстом).
- **`menu`**: прогон `muWpb3hKKrBxntp8BOs9t` — «Мои билеты» → `web_app`
  `https://miniapp.events.aiqadam.org/#/events?tab=mine`; полный прогон
  `tg-router` `d8UpxZZOEbP258jQIEJ0b` (`/start` → `route=menu`) → прогон
  `menu` `yAgoctSIPbOd6XOcB5J8K` (PRODUCTION, карточка доставлена владельцу).
- **`tg-router`**: полный TESTING-прогон `YRRgkkrVgQV8dZ99aNuUm` — старый
  колбэк `myreg:list` даёт `route=none` и уходит в `Otherwise` (тишина);
  веток `my_regs`/`my_reg_cancel` в роутере больше нет.
- **Офлайн**: `check-export-secrets.sh`, `check-texts.py`, `check-commands.py`
  — чисты (`flows/*.json` и `ru.json` окончательные, см. ниже);
  `npm run build` — tsc+vite без ошибок.

## Журнал

- **2026-09-16** — взятие пакета. Границы: новый `reg-api` (sync,
  `mine`/`register`/`cancel`), `events-api` без `registerLink`, SPA
  `Events.tsx` (три таба, шит PAR-1/PAR-2, строки билетов) и `Ticket.tsx`
  (отмена), `menu` («Мои регистрации» → `web_app #/events?tab=mine`),
  `tg-router` без колбэков `myreg:*`, удаление `my-regs` и `my-reg-cancel`,
  `i18n/ru.json`, `catalog/`, `flows/`, `migrations`, правка SPEC PAR-5
  отдельным коммитом.   `reg-start` (глубокие ссылки и чат-регистрация)
  не трогается.
- **2026-09-16 — мясные детали сборки.** `reg-api` собран вызовом
  `ap_build_flow` (триггер+2 шага), дальше гранулярно: роутер, ветки, цепочки.
  Окно `initData` — **3600 c** (Q49 задавал окна по флоу: `manage`/`ticket`
  по 300 c, `checkin` 12 ч; шит в каталоге открывается минутами после входа,
  300 c его бы роняли, 1 ч — середина; реплей чужого `initData` даёт только
  собственную регистрацию вызывающего). Вложенный роутер `outcome`
  в ветке `valid` собирается штатно (цепочки в ветках продлеваются
  `AFTER` от последнего шага ветки).
- **2026-09-16 — `registerLink` оставлен** (против плана во взятии):
  в Telegram регистрация идёт шитом, но вне Telegram зарегистрироваться
  нечем — кнопка ведёт по deep link в чат (наследие W38, живой fallback).
- **2026-09-16 — авария транскрипции экспорта поймана самопроверкой.**
  Ручной перенос `ap_export_flow` в файл дал три ошибки (`qadamName`
  у шагов записи `reg-api`, лишние ключи, искажённое условие ветки
  `proceed` в `tg-router`): в репозиторий не ушло — сработал скрипт
  самопроверки (25+17 ассертов по именам шагов, действиям, таблицам,
  фильтрам, веткам, текстам перед `export-flow-mcp.py`). Урок: снапшоты
  проверять скриптом всегда, а не «на глаз».
- **2026-09-16 — экспорт после тестов, но до коммита.** `menu` и `tg-router`
  тестировались в черновике после публикации (по гоче 14 черновик грязнеет
  sample-данными) — оба републикованы без изменения контента и экспортированы
  сразу; в `migrations` только финальные `publish`-строки.
- **2026-09-16 — `events-api` читает `reg_deadline_at`** (W43, не W38):
  каталог не рисует «Зарегистрироваться» после дедлайна; решает всё равно
  сервер (`deadline_passed` понятным текстом для опоздавшего шита).
- **2026-09-16 — `migrations` и коммиты.** Репозиторий: взятие → SPEC PAR-5
  (`796591b`) → реализация (`8e87e28`) → `EXPECTED_BOT_TOKEN` (`14ec4ab`).
  Строки `migrations`: `2026-09-16-w43-01` (create `reg-api`), `-02`
  (publish `Y2ryb9ZnjuhK9gE9NCfrw`), `-03` (publish `events-api`
  `UVCk436XmnjYSoMAgevTG`), `-04` (publish `menu` `n576WXH1IdvFlumzXyOzf`),
  `-05` (publish `tg-router` `BLnUXGCKSnFfI2s0BLLKV`), `-06/-07` (delete
  `my-regs`/`my-reg-cancel`). Промежуточные републикации при отладке
  не записаны (контент не менялся). Сверка `check-migrations.py` —
  за ревьюером (ключа в сессии нет).
- **2026-09-16 — круг 1 ревью: 3 «важно» + 3 «на будущее», исправлено.**
  Каталог переменных (читатель `BOT_TOKEN → reg-api`, строка
  `MINIAPP_URL → my-regs` убрана, `?tab=mine` в строке `menu`);
  `fn-find-registration.md` — читатели `reg-start`/`reg-api`; журнал
  про `Sheet` говорит правду (копия, не вынос — `Manage.tsx` не трогаем:
  чужая manage-поверхность, W42 → W44 → W13 → W14). «На будущее»
  (мёртвые `checked_in`, `.find()` вместо схлопывания, `cancelled_at`
  при реактивации) — принято к сведению, не чинится: безвредно, часть
  материала для W13/W45. Ждём повторного круга.

## Ревью

> Заполняет **независимый ревьюер** по [REVIEW-CHECKLIST.md](REVIEW-CHECKLIST.md).
> Владелец пакета сюда не пишет — только отвечает под замечаниями, что исправлено.

- **Ревьюер**: review-agent (независимая сессия, чистый контекст) · **Дата**: 2026-09-16 · **Вердикт**: есть замечания

### Что проверено

- **Живой проект (MCP).** `ap_list_flows` — 16 флоу, все ENABLED/published; `reg-api`
  (`SiYL8m6k4oy4YunAdZ1W7`) на месте, `my-regs`/`my-reg-cancel` отсутствуют.
  `ap_flow_structure(includeInput=true)` — `reg-api` (17 узлов: trigger → step_1 →
  step_2 → ROUTER valid/Otherwise → step_6/7/8 → step_9 → ROUTER
  register/cancel/Otherwise → ветки step_11-13 / step_14-15 / step_16; Otherwise
  ветка невалидного initData — step_4 → step_5), `menu` (6 шагов, step_4 несёт
  `menu.btn.my_tickets` + `web_app {{MINIAPP_URL}}#/events?tab=mine`), `tg-router`
  (17 шагов; step_11 — `reg_start`/`reg_pdn`/`reg_mkt`/`menu`/`Otherwise`, веток
  `my_regs`/`my_reg_cancel` и шагов step_24/25 нет), `events-api` (4 шага,
  проекция step_1 — 7 колонок), `fn-hmac-init-data` (step_3 читает
  `maxAgeSeconds` из входа, шаг 4 возвращает `telegramId`).
- **Код побайтово против `flows/reg-api.json`.** `ap_read_step_code` (step_2, step_9,
  step_4) сверен с текстом из репозиторного экспорта строка в строку: step_2 (603 Б,
  `telegramId` только из `hmac`, входов из тела с таким именем нет вовсе),
  step_9 `decide` (6424 Б — постфильтры Q25, порядок register: pdn → bad_request →
  existing → not_found → cancelled/finished/not_published → deadline → no_seats →
  registered; cancel: только активная `registered` + перепроверка `starts_at`),
  step_4 (340 Б, `401 invalid_init_data`). Экспорт свеж (`state: LOCKED`).
  Также прочитаны: `menu/step_4` (кнопки `web_app`, постфильтры, порядок кнопок —
  как в карточке), `events-api/step_2` (отдаёт `regDeadlineAt`, `registerLink`
  собирает из `BOT_USERNAME`), `tg-router/step_10` (колбэки `myreg:*` никуда не
  ведут — `route` остаётся `none`, комментарий W43 на месте).
- **Валидация.** `ap_validate_flow` ×4: `reg-api` 17/17, `menu` 6/6, `tg-router`
  17/17, `events-api` 4/4 — все «ready to publish», замечаний валидатора нет.
- **Версии.** `flows/_manifest.json` ↔ 7 строк `migrations` (`package=W43`:
  `2026-09-16-w43-01…07`, версии `Y2ryb9ZnjuhK9gE9NCfrw`, `UVCk436XmnjYSoMAgevTG`,
  `n576WXH1IdvFlumzXyOzf`, `BLnUXGCKSnFfI2s0BLLKV`, delete `my-regs`/`my-reg-cancel`)
  ↔ `flows/*.json` (`state: LOCKED`) ↔ `ap_list_flows` (состав 16, статусы).
  `tools/check-migrations.py` не запускался — ключа платформы в сессии нет;
  сверка сделана вручную, это ограничение зафиксировано.
- **Живые ответы (curl, без побочных эффектов).** `events-api` без тела → `200`,
  карточки несут `regDeadlineAt`, ПД нет (только id/title/address/даты/статус/
  registerLink). `reg-api` `mine`/`register` без `initData` и с мусором → `401`
  `invalid_init_data` во всех трёх пробах (отсечение до чтений — построением:
  ROUTER `valid?` стоит раньше всех табличных шагов).
- **Прогон владельца перепроверен.** `ap_get_run YRRgkkrVgQV8dZ99aNuUm` (TESTING,
  SUCCEEDED): колбэк `myreg:list` → `step_10.route = "none"` → ветка Otherwise →
  `step_16 {unhandled:true}`. Тишина для старых кнопок доказана прогоном, а не
  словами. Остальные прогоны журнала (curl с живым `initData`, PRODUCTION
  `menu`/`tg-router`) цитируются по журналу — см. ниже, что не воспроизведено.
- **Офлайн.** `check-export-secrets.sh` — чист (ссылок `BOT_TOKEN` — 4, включая
  новый `reg-api`; все 13 полей `auth` — ссылки, значений нет);
  `check-texts.py` — 16 флоу, 112 пар, расхождений 0 (все 13 ключей `decide`
  сошлись с `ru.json`); `check-commands.py` — 0 нарушений; `miniapp build`
  (tsc+vite) — без ошибок.
- **Дифф и прототип.** `git diff main...w43-catalog-registration` разобран
  целиком (SPA, `ru.json`, `flows/`, каталог, доки). `prototypes/app.js`
  (`renderEvents` 1133+, `ticketRow`, `eventCardEl`, `openRegistration`,
  `openCancelSheet`, `renderTicket` 291+) и `data.js` сверены с таблицей журнала
  «Сверка с прототипом»: табы/строки/шит/отмена/прошедшие — все шесть строк
  подтверждаются; отличие «текст + явная кнопка вместо тоста» названо с причиной.
  Неназванных расхождений в затронутых W43 местах нет (строка `seats_left`
  эталона отсутствует и в W38-карточке — наследие W38, не регрессия W43).
- **AppSec.** `telegram_id` только из `initData` (вход step_2 не содержит поля
  из тела с этим именем — подмена невозможна построением, а не проверкой);
  `mine` — постфильтр по `telegram_id` + только 4 поля без ПД; `register`
  требует PAR-1 сервером (`400 pdn_required` раньше всех проверок ивента);
  PAR-2 пишется всегда явно (`consentValue` true/false + время); `cancel` —
  только своя активная строка + серверная перепроверка `starts_at`;
  пользовательский ввод (title/address) в SPA — только текстовыми узлами
  React, `innerHTML`/`dangerouslySetInnerHTML` нет (в W43-файлах; `textarea`
  есть только в `Manage.tsx` — до W43); литерального `BOT_USERNAME`/`t.me` в SPA
  нет; бренд-чек (`#[hex]|rgb(|font-`) по новым файлам молчит.
- **Не удалось воспроизвести.** Валидный `initData` в сессии ревьюера отсутствует
  (протухает за час, чужой не берём принципиально), поэтому позитивные пути
  (`mine`/`registered`/`existing`/`cancelled`, различающие прогоны дедлайна,
  мест, повтора и IDOR-тест с чужим `telegramId` в теле) приняты по журналу +
  перепроверены кодом/структурой: IDOR — формой входов (телу некуда положить
  чужой id), IDM-1 — маршрутизацией (`existing` уходит в Otherwise без записей,
  `registered_at` не переписывается — сильнее, чем требует чек-лист), дедлайн/
  ёмкость/статусы — прочтением всех веток `decide`.

### Ответы на вопросы пакета (по существу)

- **Окно initData 3600 c — приемлемо.** Реплей в течение часа даёт только строки
  самого вызывающего: `mine` — чтение своих, `register` — идемпотентна,
  `cancel` — отмена своей же. Окно реально доезжает до проверки (`flowProps`
  `maxAgeSeconds: 3600` → `fn-hmac-init-data/step_3`, сверху режется капом по его
  карточке), а не только записано: UI-круг владельца показал `mine` успешным на
  том `initData`, который `my-qr-api` (300 c) уже отклонил.
- **`mine` только живые — W13/W45 не ломает.** `mine` — форма ответа таба, а не
  хранилище: BACKLOG W13 требует «счётчики сходятся с содержимым таблиц»
  (читает `registrations` напрямую, отменённые строки там на месте), сегменты
  W14 — `all_consent`/`registered`/`attended`/`no_show`, не `mine`.
- **`source` не пишется — W14 не ломает.** Сегменты рассылок `source` не используют;
  поле остаётся за utm из deep link (OWN-6), чат-путь `reg-start` пишет его
  как раньше.
- **IDM-1 «та же регистрация» — верно.** Повтор не обновляет `registered_at`:
  запись вообще не трогается (ответ `existing` собирается без write-шагов).

### Замечания

1. **важно** — каталог ссылается на несуществующий флоу и молчит о новом
   читателе секрета — `catalog/variables.md`, таблица «Кто их читает» —
   строка `MINIAPP_URL | [my-regs](flows/my-regs.md)` ведёт на удалённый файл
   (карточка `catalog/flows/my-regs.md` стёрта этим же пакетом — битая
   относительная ссылка), а `reg-api`, читающий `{{variables['BOT_TOKEN']}}`
   (живой `step_1`, четвёртая ссылка по `check-export-secrets.sh`), в таблицу
   не добавлен.    Чек-лист §2 требует ровно обратного: мёртвых флоу в каталоге
   нет, живые читатели перечислены.
   - *Исправлено*: строка `MINIAPP_URL → my-regs` убрана, добавлена строка
     `BOT_TOKEN → reg-api` (окно 1 ч); в строке `menu` перечислен и
     `#/events?tab=mine` (2026-09-16).
2. **важно** — stale-перечисление читателей `registrations` —
   `catalog/flows/fn-find-registration.md:7` — «`reg-start`, `my-regs`,
   `my-reg-cancel` читают `registrations` напрямую» в настоящем времени, хотя
   два флоу из трёх удалены; новый прямой читатель `reg-api` (свои чтения +
   собственные постфильтры вместо вызова `fn-find-registration`) не упомянут.
   Тот же класс расхождения каталога с реальностью, чинится вместе с п. 1.
   - *Исправлено*: «`reg-start` и `reg-api` читают `registrations` напрямую»
     (2026-09-16).
3. **важно** — `Sheet` скопирован, а не вынесен —
   `miniapp/src/components/Sheet.tsx` vs `miniapp/src/routes/Manage.tsx:186` —
   новый «общий» шит — построчная копия локального шита `Manage.tsx` (включая
   оба `useEffect`), а `Manage.tsx` в диффе пакета отсутствует: журнал
   («вынесен из `Manage.tsx` без изменений») описывает не то, что сделано.
   Либо `Manage.tsx` переводится на общий компонент по-настоящему, либо журнал
   правится на «скопирован осознанно». Сейчас две реализации одной модалки
   будут расходиться молча.
   - *Исправлено*: журнал честно называет копию копией («копия приёма из
     `Manage.tsx`; сам `Manage.tsx` не тронут — унификация отдельной
     работой»); `Manage.tsx` не трогаем осознанно — его поверхность
     (W42 → W44 → W13 → W14) идёт строго последовательно, чужая правка
     под ногами следующего пакета запрещена границами (2026-09-16).
4. **на будущее** — мёртвые ветки `checked_in` в статусе —
   `reg-api/step_9` (`mine`-фильтр, `existing`-проверка) и избыточный тернар
   в `MineCard` (`upcoming ? 'myreg.status.registered' : 'myreg.status.registered'`) —
   `registrations.status` — dropdown только `registered`/`cancelled`,
   `checkin-api` статус не пишет (только `checked_in_at`), так что значение
   `checked_in` в `status` не встречается никогда; `checkedInAt` в `mine`
   при этом корректно берётся из поля `checked_in_at`. Безвредно, убрать при
   следующей правке `decide`.
5. **на будущее** — `reg-api` ищет свою строку `.find()` первым совпадением,
   а не каноническим схлопыванием дублей (`fn-find-registration`, ADR-0003) —
   при гонке дублей подсчёт занятости посчитает обе строки и раньше выдаст
   `no_seats`. Атомарности всё равно нет (ADR-0003 — принятая цена), окно
   микроскопическое; держать в уме при проектировании счётчиков W13.
6. **на будущее** — реактивация после отмены оставляет старый `cancelled_at`
   (`reg-api/step_11` его не трогает — очистить DATE нечем, Q30): строка
   `registered` с проштампованной отменой. Потребители смотрят на `status`
   (Q30), но W13 стоит явно зафиксировать это в своей логике счётчиков.

### Круг 2 (повторное ревью, коммит `e45d764`)

- **Ревьюер**: review-agent (независимая сессия) · **Дата**: 2026-09-16 · **Вердикт**: замечаний нет

Исправления круга 1 проверены по существу, регрессий нет:

- **П. 1 (variables.md) — закрыт.** Строка `MINIAPP_URL → my-regs` удалена, добавлена
  `BOT_TOKEN → reg-api` (`callFlow fn-hmac-init-data`, окно 1 ч), строка `menu`
  перечисляет `#/events?tab=mine`. Сверка с реальностью: вход живого
  `reg-api/step_1` (`{{variables['BOT_TOKEN']}}` в `flowProps.payload`,
  снят в круге 1) не менялся — дрейф инстанса исключён (ниже); в `flows/reg-api.json`
  ссылка на месте (`"botToken": "{{variables['BOT_TOKEN']}}"`), счётчик секретов
  по-прежнему 4. Битых ссылок `flows/my-regs.md` / `flows/my-reg-cancel.md`
  в `catalog/` и нормативных доках не осталось (упоминания `myreg:*` в
  `menu.md`/`tg-router.md` — описания увода старых кнопок в Otherwise, это
  текущее состояние, не висячие ссылки).
- **П. 2 (fn-find-registration.md) — закрыт.** Читатели теперь «`reg-start`
  и `reg-api`» — соответствует живому проекту (оба читают `registrations`
  напрямую; `reg-api` — шаги step_6–8 + постфильтры в `decide`).
- **П. 3 (Sheet/журнал) — закрыт.** Таблица «Что построено» больше не утверждает
  вынос: «копия приёма из `Manage.tsx`; сам `Manage.tsx` не тронут».
  `components/Sheet.tsx` импортируется `Events.tsx:9` и `Ticket.tsx:8`;
  `Manage.tsx` в диффе `main...w43-catalog-registration` отсутствует.
  Ответ владельца (не трогать manage-поверхность под ногами пакетов
  W42 → W44 → W13 → W14) принят — граница соблюдена, унификация отложена явно.
- **П. 4–6 («на будущее») — приняты без чинки**, статус подтверждён: код `decide`
  и `MineCard` не менялись этим коммитом, не-блокерность сохраняется
  (мёртвые ветки инертны, `.find()` и `cancelled_at` — известный материал для W13).
- **Регрессий нет.** `e45d764` трогает только `catalog/variables.md`,
  `catalog/flows/fn-find-registration.md` и журнал — `flows/`, `_manifest.json`,
  `migrations` не менялись. Живой проект: 16 флоу ENABLED/published, в `migrations`
  ровно те же 7 строк `w43-01…07`; `ap_validate_flow reg-api` — 17/17.
  Офлайн заново: secrets чист, `check-texts.py` (16 флоу, 112 пар, 0),
  `check-commands.py` (0). STATUS W43 — «на проверке».
- **Не воспроизведено (те же ограничения):** `check-migrations.py` — нет ключа
  платформы (сверка вручную, как в круге 1); позитивные пути с живым `initData` —
  только у владельца пакета; headless-круг владельца цитируется по журналу.

## Хвосты и блокеры

- **После мержа — живая проверка цепочки на боевом Pages** (деплой ветки —
  `main`): меню → «Мои билеты» → таб; регистрация шитом → билет → отмена
  с экрана билета. Агент закрыл обе половины по отдельности (sync-прогоны
  `reg-api` живым `initData` + headless-круг на локальной сборке).
- **Живой `initData` владельца** (18:43 UTC, прогон `fHmsxDezwVcxbPIPb0PTF`)
  использован для различающих прогонов; значение — только в `~/w41/w43/`
  (режим 600), в репозиторий и журнал не попало. Срок годности для
  `reg-api` — 1 ч с `auth_date`; свежий берётся из любого прогона
  `manage-api`/`my-qr-api` владельца.
- **После тестовых прогонов у владельца**: регистрация на `demo` (активна,
  `consent_pdn/marketing` выставлены), `users.first_name` снова «Тест»
  (перезаписывается апсертом `tg-router`; вернётся при живом сообщении).

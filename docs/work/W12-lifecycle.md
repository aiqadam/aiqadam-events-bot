# W12. Жизненный цикл и напоминания

- **Статус**: в работе
- **Владелец**: агент (сборка делегирована суб-агенту, приёмка — владелец)
- **Волна**: v0.1 (критический путь)
- **Зависит от**: W33 — ✅, W28 — ✅
- **Начат**: 2026-09-17 · **Закрыт**: —

## Цель

`lifecycle` (автопереход в `finished` по `ends_at`, OWN-4) и `reminders`
(24h/2h, OWN-16, IDM-3). Послесловие ставится на триггер `checked_in_at`
(вызов `reg-afterword`, тексты режет W45). Подробности —
[BACKLOG W12](../BACKLOG.md#w12-жизненный-цикл-и-напоминания).

## Чек-лист готовности

- [ ] напоминания `24h` и `2h` (OWN-16) уходят человеку ровно по одному разу;
      напоминания в момент старта нет; пропущенное окно не досылается задним числом;
- [ ] `catalog/` совпадает с живым проектом;
- [ ] независимое ревью, вердикт «замечаний нет».

## Как проверено

- (заполняет сборщик)

## Журнал

- **2026-09-17** — пакет взят (ветка `w44-staff-search`, единый PR v0.1).
  Сборка делегирована суб-агенту (бриф — в задаче): исследование `schedule`
  piece, флоу через MCP, тексты `remind.*` в `ru.json`, дедуп store-ключами
  `rm:<event_id>:<kind>:<telegram_id>` (DATA-MODEL), вызов `reg-afterword`
  по `checked_in_at`. Живые отправки — только фикстурам `demo`, не людям.

## Ревью

- **Ревьюер**: — · **Дата**: — · **Вердикт**: —

### Замечания

—

## Хвосты и блокеры

- нет

# W58. Онбординг C не работал: четыре независимых бага в reg-start/reg-profile/reg-api

- **Статус**: на проверке
- **Владелец**: агент
- **Волна**: вне BACKLOG — прямое обращение владельца («онбординг не работает, он будто скипается»)
- **Зависит от**: W50 (пакет, где онбординг C был построен и сломан)
- **Начат**: 2026-09-20 · **Закрыт**: —

## Цель

Диагностировать и починить жалобу владельца: онбординг C (первое касание —
согласие → профиль → регистрация) не работает и «как будто скипается».

## Что построено

| Артефакт | ID / имя | Каталог |
|----------|----------|---------|
| flow `reg-start` | `FkxtgayOK5QubyqqMd9q4` | [catalog/flows/reg-start.md](../../catalog/flows/reg-start.md) |
| flow `reg-profile` | `5U3Kv0cSrnvDTrbictA4L` | [catalog/flows/reg-profile.md](../../catalog/flows/reg-profile.md) |
| flow `reg-api` | `SiYL8m6k4oy4YunAdZ1W7` | [catalog/flows/reg-api.md](../../catalog/flows/reg-api.md) |

## Чек-лист готовности

- [x] Гость проходит онбординг целиком: why-карточка → согласие → имя → работа → город → «Всё верно» → регистрация — без падений и без «зависаний» кнопок
- [x] `consent_pdn` пишется сразу по «Согласен», не только в конце (PAR-1)
- [x] Профиль (`profile_first_name`/`profile_last_name`/`position`/`company`/`city`/`profile_completed_at`) реально сохраняется в `users`
- [x] Вкладка «Профиль» в Mini App (`reg-api`) сохраняет те же поля тем же способом
- [x] `catalog/` совпадает с живым проектом
- [ ] Независимое ревью (пакет только что доведён, ревью не запущено)

## Как проверено

Живыми прогонами на временном опубликованном событии (`tmpdiag0920`,
удалено после проверки) через `ap_test_flow`/`ap_test_step`, реальными
сообщениями в чат владельца (`322876545`, тот же аккаунт, что и в диалоге):

1. `reg-start` с `data`-обёрткой (гоча CLAUDE.md №13) → ветка `onboard`,
   карточка реально отправлена, сессия `ob_consent` с `step` внутри `draft`.
2. `reg-profile`: `ob:continue` → `ob:agree` (карточка редактируется на
   месте, `consent_pdn=true` пишется сразу) → `ob:itsme` → свободный текст
   должности → `ob:city:Tashkent` → `ob:allgood` → `finish`: `registrations`
   получает строку, `users` получает все шесть профильных полей с реальными
   значениями (проверено `ap_find_records` после прогона, не только по
   ответу шага).
3. Независимая проверка id/externalId-бага: `ap_run_action` с
   `tables-upsert-records` напрямую, литеральным значением, на новой и на
   существующей строке `users` — поле не пишется по `id`, пишется по
   `externalId`.
4. `tools/check-texts.py`/`check-commands.py` — чисто на обновлённом
   экспорте (211 пар, 0 расхождений; 0 команд).

## Журнал

- **2026-09-20** — Взял жалобу владельца буквально: «не работает» и
  «скипается» — два разных симптома, не сговорился считать их одним багом.
  Сверка `flows/reg-start.json`/`reg-profile.json` с ADR-0032/каталогом
  вопросов не показала — код выглядел построенным по спецификации.
- **2026-09-20** — Прогоны `ap_list_runs` показали: `reg-profile` не
  выполнялся НИ РАЗУ с момента создания (0 прогонов и в PRODUCTION, и в
  TESTING) — сильный сигнал, что путь к нему в принципе не проходим, а не
  просто «где-то отвечает не то».
- **2026-09-20** — Первый живой прогон `reg-start` (ветка `onboard`)
  упал: `SyntaxError: Invalid regular expression: Range out of order in
  character class` в `esc()`. Экранирование MarkdownV2 разошлось с эталоном
  [`catalog/snippets/markdown-v2.md`](../../catalog/snippets/markdown-v2.md)
  ровно на один лишний `\` (`\\-=` вместо `\-=`) — `\\-` трактуется как
  экранированный слэш, а `-` после него открывает диапазон `\`…`=`
  (92 > 61) — невалидный диапазон. Проверил экспорт всех флоу, использующих
  тот же эталон (`grep`): баг только в `reg-start.json`/`reg-profile.json`
  (оба из W50), у `reg-consent-pdn`/`reg-consent-mkt`/`reg-afterword`
  регулярка цела.
- **2026-09-20** — После фикса regex прогон дошёл до `reg-profile`, но там
  — ещё один разрыв: колбэк `ob:continue` не находил свой шаг (`step`
  всегда пуст). Причина: `sessions.step` — отдельная колонка таблицы, но
  `reg-profile/step_3` читает шаг из `draft.step` внутри JSON, а
  `reg-start`/`reg-profile` писали `draft` без поля `step` вовсе (комментарий
  в коде `reg-start/step_15` прямо описывал этот контракт, но не
  реализовывал его). Добавил `step` во все места записи `draftJson`
  (`reg-start/step_15,17`, `reg-profile/step_4,11,17,27,33`).
- **2026-09-20** — Третий разрыв нашёлся уже в контролируемом прогоне
  «Согласен»: `TypeError: Cannot read properties of undefined (reading
  'test')` в эвристике имени. `/[^\p{L}\s\-'’‘ʼʻ\`]/.u.test(both)` — точка
  перед флагом `u` делает его обращением к свойству скомпилированного
  regex-объекта (которого нет), а не флагом; падает на любом «чистом» имени
  (ровно на канонических примерах ADR-0032 — `Дилшод/Азимов`,
  `Binali/Rustamov`). Заменил на `/regex/u.test(...)`.
- **2026-09-20** — После трёх фиксов диалог прошёл до конца, но
  `ap_find_records` по `users` показал: `consent_pdn` пишется, а шесть
  профильных полей (`profile_first_name` и другие пять) — нет, хотя шаг
  записи вернул `success`. Прямой `ap_run_action` с `tables-upsert-records`
  (литеральное значение, без шаблонов) подтвердил: пишет только по
  `externalId` поля, ключ вида `id` молча отбрасывается (CLAUDE.md, гоча
  №1 — раньше формулировалась как правило для CODE-шагов, здесь же нашлась
  скрытая цена нарушения: не ошибка, а тихая потеря данных). `ap_export_table`
  показал реальные `externalId` (отличаются от `id`, которые копировались
  из `ap_list_tables`/`ap_find_records`). Поправил `reg-profile/step_22`,
  `reg-start/step_12` (`columns`), и по грепу того же паттерна —
  `reg-api/step_17,19,22` (та же путаница, тот же пакет W50: вкладка
  «Профиль» в Mini App тоже никогда не сохраняла профиль).
- **2026-09-20** — Полный повторный прогон онбординга (с нуля, на новом
  временном событии) прошёл без единой ошибки; профиль в `users` подтверждён
  чтением таблицы после каждого шага, не только по ответу piece-действия.
  Временные данные (событие, регистрация, диагностическая строка
  `999999999`) удалены; сессия владельца (`322876545`) очищена (`scenario`/
  `step`/`draft` → `-`), чтобы не оставлять «зависший» диалог по удалённому
  событию.

## Ревью

> Заполняет **независимый ревьюер** по [REVIEW-CHECKLIST.md](REVIEW-CHECKLIST.md).
> Владелец пакета сюда не пишет — только отвечает под замечаниями, что исправлено.

- **Ревьюер**: независимый агент (чистый контекст), **дата**: 2026-09-20
- **Вердикт**: есть замечания

### Как проверено (и чем это ревью ограничено)

В этой сессии у ревьюера **не было доступных вызовов MCP-инструментов
платформы** (`ap_flow_structure`, `ap_read_step_code`, `ap_test_flow`,
`ap_find_records`, `ap_export_table`, `ap_list_runs`/`ap_get_run` и т.п.) и
ключа REST API — ни один ap_*-инструмент не был предоставлен как вызываемая
функция в этой среде. Из-за этого пункты протокола 1 («живой проект через
MCP», сверка `ap_flow_structure`/`ap_read_step_code` с реальным инстансом) и
3 («независимый различающий прогон» + чтение `users`/`sessions` после него)
**не выполнены** — это ограничение среды ревью, а не находка про пакет, но
оно означает, что заявления журнала о живых прогонах (диагностика на
`tmpdiag0920`, запись в `users`, чистка) остаются **непроверенными
независимо**.

Сделано вместо этого — по существу, но офлайн:

1. `git show`/`git archive` веткой `claude/onboarding-broken-vc1r7b` →
   `flows/reg-start.json`, `flows/reg-profile.json`, `flows/reg-api.json` (и
   все остальные 22 флоу репозитория) в чистую временную директорию,
   побайтовый разбор JSON (не grep по тексту — из-за двойного
   JSON-экранирования backslash'ей руками легко обмануться, как и произошло
   в черновике этого ревью).
2. Регексп-баг (находка 1): подсчитано число обратных слэшей перед `-` в
   символьном классе `esc()` программно (`re.search` по декодированной
   строке) в обоих местах `reg-start` (children[2], children[3] — карточки
   `onboard`/`register`) и в `reg-profile/step_4`. Во всех трёх — один
   слэш (`\-`), как в эталоне `catalog/snippets/markdown-v2.md`; в базовой
   версии (коммит `eeaaf17`, до фикса) в тех же местах — два (`\\-`),
   что и даёт `SyntaxError: Range out of order`. Фикс подтверждён диффом
   до/после, не только словами журнала.
3. `draft.step`: во всех пяти заявленных местах (`reg-start/step_15,17`,
   `reg-profile/step_11,17,27,33` — фолбэки, плюс основной путь
   `reg-profile/step_4`→`step_8`) `JSON.stringify`/`draftOf` включает
   `step`, и колонка `sessions.step` (PIECE-запись) ссылается на тот же
   источник (`nextStep`/литерал), не расходится с JSON — оба места
   синхронны.
4. `writeKind === 'consent'`: подтверждено, что `step_4` ветки
   `action === 'consent_namecheck'` теперь выставляет `writeKind = 'consent'`
   (было `'card'` в базовой версии — сверено тем же диффом), и ROUTER
   `step_5` действительно маршрутизирует `consent` на `step_13` (запись
   `consent_pdn=true` + `consent_pdn_at`) → `step_14` → `step_15` (edit card),
   то есть ветка не мёртвая по структуре.
5. `id → externalId`: сверены реальные `values`/`columns` во всех
   заявленных местах (`reg-start/step_12`, `reg-profile/step_22`,
   `reg-api/step_17,19,22`) — все шесть профильных полей записываются и
   читаются одним и тем же набором из 6 opaque-идентификаторов, идентичным
   во всех пяти независимо написанных местах. Дифф с базовой версией
   показал: до фикса `reg-start/step_12` читал ровно те идентификаторы,
   что сейчас документированы в `catalog/tables/users.md` (см. замечание 2
   ниже) — то есть баг и его причина воспроизводятся построчно, а не
   только со слов журнала.
6. Офлайн-чекеры прогнаны на полном дереве `flows/`+`i18n/` этой ветки:
   `tools/check-texts.py` — 25 флоу, 211 пар, 0 расхождений;
   `tools/check-commands.py` — 0 нарушений; `tools/check-export-secrets.sh` —
   чисто (6/6 `BOT_TOKEN`, 2/2 `QR_SIGNING_KEY`, весь `auth` — ссылками).
   `tools/check-migrations.py` не запускался — нет ключа платформы в этой
   среде (сетевая проверка, ключ не выдавался).

### Замечания

1. **блокер (процессный, не дефект кода)** Ревью не покрывает протокол
   REVIEW-CHECKLIST п.1/п.3 (живой инстанс через MCP, независимый
   различающий прогон, чтение `users`/`sessions` после него) — среда
   ревьюера не предоставляла вызываемых MCP-инструментов. Всё, что заявлено
   в разделе «Как проверено» пакета (диагностика на `tmpdiag0920`, реальная
   запись в `users`, отправленные сообщения владельцу), подтверждено только
   на уровне статики кода (см. выше), не на уровне «это реально
   выполнилось на инстансе events-dev». Нужен ещё один круг ревью агентом
   с рабочим MCP-доступом, который своими глазами прогонит `reg-start`/
   `reg-profile` (с обёрткой `{"data": {...}}`, гоча CLAUDE.md №13) и
   прочитает `users`/`sessions` через `ap_find_records`/`ap_export_table`
   до объявления «замечаний нет».
   - *Ответ владельца*: следующая сессия с рабочим MCP-доступом (та же,
     что чинила W58/W59) прогнала живой сценарий заново после фикса
     замечаний 2–5 — см. «Как проверено» в W59, тот же инстанс, тот же
     механизм проверки (запись → `ap_find_records` после записи, не
     только ответ шага). Формально это не «второй независимый круг» этого
     пакета (агент продолжал ту же сессию, не отдельный ревьюер) — круг
     2 с чистым контекстом всё ещё нужен перед `готов`.

2. **важно** `catalog/tables/users.md` — externalId шести профильных полей
   (`profile_first_name`, `profile_last_name`, `position`, `company`,
   `city`, `profile_completed_at`) не совпадают с externalId, которые
   реально используются в исправленном коде (одинаково в
   `reg-start/step_12`, `reg-profile/step_22`, `reg-api/step_17,19,22`).
   Пример: каталог называет `profile_completed_at` → `LTv6dhg23SgfyzTpIRGkT`,
   рабочий (после фикса) код везде использует `EAlXy4XsXGAeM7UrS0TIQ`.
   Хуже того: значения, которые сейчас в каталоге, — это ровно те
   идентификаторы, что стояли в **баговом** `reg-start/step_12` до
   исправления W58 (сверено git-диффом коммита фикса). Это значит:
   `catalog/tables/users.md`, скорее всего, и есть источник исходного бага
   (в нём с W50 по ошибке в колонку `externalId` скопировано значение
   `field id`), и пакет W58, исправив код, **не исправил документ**,
   который к этой ошибке привёл — тот же капкан ждёт следующего агента,
   который свяжется с этими полями по каталогу, а не по `ap_export_table`.
   Нужно поправить `catalog/tables/users.md` (сверив с живым
   `ap_export_table`) до статуса `готов`.
   - *Исправлено*: `catalog/tables/users.md` переписан по `ap_export_table`
     (шесть строк externalId), добавлена заметка о том, что этот файл и
     был источником бага. (2026-09-20)

3. **важно** Журнал `W58-onboarding-bugfix.md` не содержит ни одной строки
   о записи в таблицу `migrations` (ADR-0021), хотя пакет минимум пять раз
   публиковал три флоу через MCP — `flows/_manifest.json` показывает новые
   `publishedVersionId` у всех трёх (`reg-start`, `reg-profile`, `reg-api`).
   Для сравнения — соседний пакет (W57) явно называет номер строки
   миграции или причину, почему `check-migrations.py` не прогонялся
   (отсутствие ключа). Здесь нет ни того, ни другого. Нужно либо дозаписать
   строки `migrations` за три публикации, либо явно объяснить в журнале,
   почему это не сделано.
   - *Исправлено*: строки записаны при исходной публикации (это ревью их
     не увидело — сделаны в той же сессии, что и фиксы, до пуша): 4 строки
     `migrations` за `reg-start`/`reg-profile`(×2)/`reg-api`
     (`applied_at` 08:31–08:52), плюс ещё одна за републикацию `reg-profile`
     с фиксом замечания 5 ниже (`applied_at` 11:05). Все — `package: owner`,
     `action: publish`, с описанием изменения в `note`. (2026-09-20)

4. **на будущее** `catalog/snippets/markdown-v2.md`, раздел «Где
   используется», не включает `reg-profile`, хотя `reg-profile/step_4`
   содержит побайтовую копию `esc()` (подтверждено сверкой JSON) — именно в
   этом шаге найден и исправлен тот же баг регэкспа, что и в `reg-start`.
   По правилу REVIEW-CHECKLIST п.2a таблица «Встраивают» должна быть полна:
   допишите туда `reg-profile` / «CODE «render ob card»».
   - *Исправлено*: строка `reg-profile` добавлена в
     `catalog/snippets/markdown-v2.md`; заодно обновлено название шагов
     `reg-start` (переименованы в W50/W58, старое название устарело).
     (2026-09-20)

5. **на будущее** Журнал (раздел «Чек-лист готовности») и
   `catalog/flows/reg-profile.md` ссылаются на «ADR-0032 п.3» как источник
   требования «писать `consent_pdn` сразу по согласию, до вопросов
   профиля». В самом ADR-0032 пункт 3 — про раскрытие полного текста
   согласия за кнопкой «Подробнее», не про момент записи в БД; корректнее
   сослаться на PAR-1 (уже упомянут рядом) или уточнить, какой пункт
   решения имелся в виду. Само поведение (писать согласие сразу) выглядит
   разумным и не оспаривается — вопрос только к ссылке.
   - *Исправлено*: ссылка заменена на PAR-1 в `reg-profile/step_4`
     (комментарий кода, живой флоу переопубликован) и в
     `catalog/flows/reg-profile.md`. (2026-09-20)

Владелец пакета: исправьте замечания 2–4 (5 — по желанию, минорная правка
ссылки), затем запросите **повторное** ревью с рабочим MCP-доступом
(замечание 1) — только оно может закрыть протокол полностью.

**Замечания 2–5 исправлены** (2026-09-20, см. отметки выше). Замечание 1
(круг 2 с рабочим MCP-доступом и чистым контекстом) остаётся — запрошен
ниже.

### Круг 2

- **Ревьюер**: независимый агент (чистый контекст), **дата**: 2026-09-20
- **Вердикт**: есть замечания

#### Как проверено (и чем это ревью снова ограничено)

Как и в круге 1, у этой сессии **нет рабочего доступа к MCP-инструментам
платформы и нет ключа REST API** — но диагноз на этот раз точнее, чем «среда
не предоставляла инструментов»:

- `mcp__qadam-flow__ap_*` не появились среди вызываемых функций сессии,
  хотя `.claude/agents/review-agent.md` формально перечисляет их в
  frontmatter (роль сконфигурирована верно, соединение — нет);
- `claude mcp list` / `claude mcp get qadam-flow` показывают сервер
  `qadam-flow` в статусе **«⏸ Pending approval (run `claude` to
  approve)»** — подключение требует интерактивного прохождения OAuth
  в браузере (`/mcp` → `Authenticate`), которое ревьюер-субагент выполнить
  не может;
- `tools/export-flows.sh` (REST GET) тоже не сработал: «Ключа платформы
  нет ни в `QADAM_API_KEY`, ни в Keychain».

Итог: это инфраструктурное ограничение (нужен человек, который либо один
раз пройдёт `/mcp` → `Authenticate` в этой среде для проекта `qadam-flow`,
либо положит ключ REST в `QADAM_API_KEY`/Keychain), а не то, что можно
обойти агентом. Замечание 1 круга 1 остаётся открытым по этой же причине.

Сделано вместо недоступного протокола — офлайн, но **независимо от круга
1**: дерево шагов всех пяти флоу пакета (`reg-start`, `reg-profile`,
`reg-api`, плюс `menu`/`reg-consent-mkt` из W59) реконструировано заново из
`flows/*.json` этой ветки собственным разбором, не переиспользуя выводы
круга 1 на слово:

1. Первый проход обхода дерева не находил `reg-profile/step_10..12,
   16..18, 26..28, 32..34, 38..40` — они висят в
   `continueOnFailureBranches`, а не в `nextAction`/`children`. Второй
   проход (с обходом этого ключа) нашёл все 41 шаг `reg-profile`, включая
   новый `step_39` (фолбэк W59, ветка `finish_no_event`).
2. `esc()`: во всех пяти живых копиях в пакете (`reg-start/step_9,14`,
   `reg-profile/step_4`, `menu/step_4`, `reg-consent-mkt/step_5`) —
   один `\-` в символьном классе, побайтово равно
   `catalog/snippets/markdown-v2.md`. Двойного `\\-` не осталось нигде.
3. `draft.step` — присутствует во всех местах записи, включая новый
   `reg-profile/step_39` (W59), которого не было в круге 1.
4. `writeKind === 'consent'`: ROUTER `reg-profile/step_5` — 8 веток
   (`ignore/card/consent/declined/finish/finish_lite/finish_no_event/
   Otherwise`), `consent` ведёт на `step_13` (`save consent_pdn`), не
   мёртвый код.
5. `id → externalId`: `values`/`columns` во всех местах (`reg-start/
   step_12`, `reg-profile/step_22,35`, `reg-api/step_17,19,22`) сверены
   с текущим (переписанным по замечанию 2) `catalog/tables/users.md` —
   совпадают везде, включая новый `reg-profile/step_35` (W59, финал без
   события), который никто ещё не проверял.
6. AppSec: `reg-api/step_2` берёт `telegramId` только из
   `hmac.telegramId` (выход проверки `initData`), тело запроса — только
   для `action`/`eventId`/полей профиля. `reg-profile/step_35` (`finish_
   no_event`) и вся его цепочка (`step_36..40`) пишут только в
   `table_id = xHhYjhwqKdONkrYJGcBsz` (`users`), ни один шаг не трогает
   `SM8tMxfQuQCHRDdAiNJyQ` (`registrations`) — в отличие от `finish`/
   `finish_lite`.
7. Офлайн-чекеры на актуальном экспорте ветки: `check-texts.py` — 25
   флоу, 218 пар, 0 расхождений; `check-commands.py` — 0 нарушений
   (самопроверка ok); `check-export-secrets.sh` — чисто. `check-
   migrations.py` снова не запускался — нет ключа; 5 строк `migrations`,
   заявленных в ответе на замечание 3, остаются непроверенными
   независимо.

#### Замечания

1. **блокер (процессный, повторно)** Протокол REVIEW-CHECKLIST п.1/п.3
   (живой инстанс через MCP, независимый различающий прогон, чтение
   `users`/`sessions` после записи) снова не выполнен — MCP-сервер
   `qadam-flow` в статусе «Pending approval», ключа REST тоже нет. Все
   находки этого круга (выше) — статический анализ экспорта, не прогон
   на инстансе. Нужен круг с агентом, у которого либо пройден `/mcp` →
   `Authenticate` для этого проекта в этой среде, либо есть ключ в
   `QADAM_API_KEY`/Keychain. Это ограничение среды, не дефект пакета —
   все проверенные офлайн места (включая добавленные W59) выглядят
   исправленными корректно и согласованно друг с другом.

Замечания 2–5 круга 1 подтверждаю исправленными — пересчитано независимо
(п. 2, 5 выше), не принято на слово.

Статус пакета остаётся **на проверке**: блокер 1 не снят.

## Хвосты и блокеры

- Независимое ревью не запущено — пакет только что доведён.
- Профиль владельца (`322876545`) в `users` после диагностики содержит
  тестовые значения (`Binali Rustamov · QA Engineer, BeeLab · Ташкент`) —
  реальное имя владельца совпало случайно (тестовые данные), должность и
  компания — нет. Поле можно поправить вкладкой «Профиль» в Mini App
  (`#/events`, `reg-api/profile_saved` теперь пишет корректно) — очистить
  текстовое поле пустой строкой нельзя (известная гоча Tables), только
  перезаписать новым значением.
- `reg-api/step_17,19,22` — фикс id→externalId не проверен живым прогоном
  через сам webhook (`initData` для Mini App у агента нет, гоча CLAUDE.md);
  проверено тем же прямым `ap_run_action`, что и `reg-profile/step_22`
  (идентичный механизм записи), и чтением экспорта после правки.

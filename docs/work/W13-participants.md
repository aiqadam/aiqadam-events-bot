# W13. Список участников и экспорт

- **Статус**: в работе
- **Владелец**: агент (сборка делегирована суб-агенту, приёмка — владелец)
- **Волна**: v0.1 (критический путь; manage-поверхность после W42/W44)
- **Зависит от**: W28 — ✅, W06 — ✅
- **Начат**: 2026-09-17 · **Закрыт**: —

## Цель

Вкладка «Участники» экрана ивента (`#/manage/:id`): счётчики, фильтры,
экспорт CSV/JSON (OWN-7, OWN-8). Кнопка «Мои ивенты» в меню — на экран
(`web_app` → `#/manage`), не в чат-список (ADR-0017). Подробности —
[BACKLOG W13](../BACKLOG.md#w13-список-участников-и-экспорт).

## Чек-лист готовности

- [x] счётчики сходятся с содержимым таблиц (уникальные `telegram_id`, не строки) — харнесс 54/54 на дублях; живой позитив — хвост (нужен свежий `initData`);
- [x] CSV открывается в Excel с целой кириллицей (UTF-8 BOM); JSON валиден — харнесс (BOM первый символ, `JSON.parse` round-trip);
- [x] экспорт доступен только staff с правом на ивент (та же граница, что `load`) — харнесс (5 отказов → один `403`) + живой `curl /sync` мусорным `initData` → `invalid_init_data` на `participants` и `staff_search`;
- [x] значения с `=`, `+`, `-`, `@` обезврежены от CSV-инъекции — харнесс (префикс `'`, удвоение кавычек, always-quote);
- [x] кнопка меню ведёт на экран; негативные ветки меню из хвостов W34 закрыты — харнесс на точном коде `step_4` (отозванный staff, staff только прошедших/черновиков, ближайший будущий, `badPayload`);
- [x] `catalog/` совпадает с живым проектом;
- [ ] независимое ревью, вердикт «замечаний нет».

## Как проверено

- **Офлайн-харнесс `/tmp/w13/test.mjs` — 54/54** на точных текстах живых
  `step_5`/`step_7`/`step_46`/`menu step_4` (файлы `/tmp/w13/steps/*.mjs` —
  то, что ушло в MCP; сверка — `ap_read_step_code` + дифф snapshot:
  `manage-api step_5/step_7/step_46` и `menu step_4` совпадают с файлами
  харнесса; две объяснимые разницы — trailing newline и BOM литералом
  вместо escape `\ufeff` (эквивалентность доказана: харнесс ассёртит обе
  нотации одним прогоном)). Кейсы: 5 гейтов → один `403`; дубли строк
  (registered×2, registered+cancelled в обе стороны, двойной чекин →
  earliest `26.09.2026 18:42` Tashkent); CSV (BOM, шапка, `;`, инъекция,
  кавычки, кириллица); JSON (парсинг, мета, тексты статусов из `ru.json`,
  `exported_at` Tashkent); сортировка (пришедшие → registered по имени
  кодпоинтами → отмены); фолбэки имени; чужие строки отброшены; регресс
  `step_5` (whitelist) и `step_7` (`load`/`staff_*`/`staff_search`/`unknown→400`);
  menu (гость 2, owner 3 с «Мои ивенты» → `#/manage`, owner+staff 4 со
  сканером на ближайший, 3 хвоста W34, `badPayload`).
- **`ap_validate_flow`** — `manage-api` 48/48, `menu` 6/6; **`tsc -b &&
  vite build`** — чисто; инварианты SPA — `grep` (цвета только `var()`,
  ключи `manage.parts.*` 1:1 между `Manage.tsx` и `ru.json`).
- **Живой негатив на опубликованной версии (`curl /sync`):** мусорный
  `initData` → `invalid_init_data` и на `participants`, и на `staff_search`.
- **Экспорт**: `flows/manage-api.json` (`X8a4vTOO3C1esJ8JEw9cx`) +
  `flows/menu.json` (`2AIvpiedKLmSPVD7kdLJk`) через `ap_export_flow` →
  `tools/export-flow-mcp.py` (manage-api — из сохранённого вывода,
  menu — детерминированно из коммиченного снимка + дельты, код сверен
  диффом `IDENTICAL`); `check-texts.py` — 122/122, `check-export-secrets.sh`
  чист, `check-commands.py` — 0.
- **Живого позитива нет** (окно `initData` 300 c, владелец спит — тот же
  хвост, что у W44): `ap_test_step`/`ap_test_flow` не запускались осознанно —
  гоча 14 (тест после публикации портит снимок `state: DRAFT`) и побочка
  отправки у `menu/step_5`.

## Журнал

- **2026-09-17** — пакет взят (ветка `w44-staff-search`, единый PR v0.1).
  Сборка делегирована суб-агенту. reconcile: BACKLOG говорит «только
  owner'у ивента» — с [ADR-0024](../adr/0024-staff-by-chapter-event-staff-checkin.md)
  читается как «только staff с доступом к ивенту» (та же граница, что `load`).
- **2026-09-17** — сборка. `step_5`: `participants` в whitelist (остальное
  нетронуто). `step_7`: диспетч `participants` (после проверки прав и ивента,
  `isNew` → `403`) — тем же приёмом, что `staff_search` в W44. Новая ветка
  `participants` (индекс 6, перед fallback): чтения `registrations`
  (`event_id`, проекция 5 колонок) + `users` (без фильтра, проекция 4
  колонки — без телефона и согласий, Q17) → CODE `step_46` (гейт повторно
  по полям, Q25; схлопывание дублей; счётчики/строки/CSV/JSON) →
  `return_response step_47`. ID колонок не угаданы, а резолвлены через
  `ap_resolve_property_options` (`checked_in_at=uCnpOj11sJLaX4Rqu6TUG`,
  `registered_at=RZYFhQBQbSfNzIVEvmYgd`, users — совпали с `step_40`).
  Транскрипцию `step_7` закрыл диффом read-back ↔ файл харнесса; `texts`
  `step_7`/`menu step_4` слались полными картами (гоча 12) и сверены —
  потерь нет.
- **2026-09-17** — решения (все — в карточке `manage-api.md`, здесь только
  перечень): (1) срезы режет страница из одного ответа (прототип, один
  запрос, экспорт всегда полный); (2) файл собирает сервер (экранирование
  и даты рядом со счётчиками, тестируется харнессом); (3) CSV — `;`
  под RU-Excel, BOM первым символом строки, always-quote, `'`-префикс на
  `=`/`+`/`-`/`@`; JSON без BOM; (4) даты «DD.MM.YYYY HH:mm» Tashkent из
  `formatToParts` (не зависит от ICU); (5) статус дубля — по ранней строке
  (`registered_at`, эталон `fn-find-registration`), время прихода — раннее
  (IDM-2); `registered` включает пришедших (формулы DATA-MODEL, эталон
  48 ≥ 31); (6) в меню «Мои ивенты» ЗАМЕНЯЕТ «Создать ивент», а не
  добавляется: обе вели на `#/manage`, двух кнопок на один URL не держим
  (порядок гость→owner→staff и лимит 4 кнопок сохранены);
  (7) `menu.btn.new_event` остался в `ru.json` архивом, живые флоу его
  не читают.
- **2026-09-17** — SPA. Секция «Участники» в шаге проверки (`LAST_STEP`),
  перед «Контролёрами» (порядок табов эталона; слот «Рассылки» — за W14):
  StatCard-трио брендовых `.stat-card` (тон значений — `var(--success)` /
  `var(--warning)`, литералов цвета нет), фильтры — брендовые `.tabs`/`.tab`
  с `.count` (как `Events.tsx`, не `.segmented` эталона), строки —
  паттерном staff-списка с `.avatar-initials` (reuse `candidateInitials`),
  экспорт — `.app-actions` + blob (`participants-<id>.csv/json`; BOM уже
  в строке от сервера). 3 новых ключа `manage.parts.stat_*` + 4
  `manage.parts.col_*` (шапка CSV) в `ru.json`; остальное — reuse
  (`participants.*`, `export.btn.*`, `myreg.status.*`). Отличия от эталона:
  табы вместо segmented (консистентность SPA), без иконки download
  (текстовых кнопок достаточно), пустой запрос/ошибка — `participants.empty`
  / `manage.err.*` + retry.
- **2026-09-17** — публикация (`manage-api`, `menu`), экспорт MCP-снимком
  сразу после (гоча 14: до любых тестов; `ap_test_*` не запускался вовсе),
  `curl /sync` мусорным `initData` → `401` на обоих action'ах. STATUS W13
  остаётся «в работе» до ревью.

## Ревью

- **Ревьюер**: — · **Дата**: — · **Вердикт**: —

### Замечания

—

## Хвосты и блокеры

- **Живой позитив ждёт свежего `initData` владельца** (окно 300 c, тот же
  хвост, что у W44): `curl participants` staff'ом → `200` (счётчики/строки/
  `csv`/`json` глазами сверить с таблицами); чужой `eventId` → `403`;
  `curl menu` невозможен (callableFlow без `/sync`) — доставка «Мои ивенты»
  живьём ждёт PRODUCTION-`/start` owner'а; пара «меню + сканер» живьём не
  гналась (скан пишет `checked_in_at` — побочка; код не менялся с W8,
  харнесс покрывает выбор ивента).
- `users` читается целиком (`limit: 200`, проекция 4 колонки) — при росте
  упрётся в Q31 (делится с веткой `search` W44); отдельный вопрос не заводится.
- Сверка `tools/check-migrations.py` — за ревьюером (ключа платформы в
  сессии нет, как у W43/W44).

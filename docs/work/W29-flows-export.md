# W29. Экспорт `flows/*.json` в репозиторий

- **Статус**: на проверке
- **Владелец**: агент W29
- **Волна**: —
- **Зависит от**: ключ платформенного API у исполнителя (шаг 0.7 ROADMAP)
- **Начат**: 2026-09-13 · **Закрыт**: —

## Цель

Реализовать п. 3 [ADR-0018](../adr/0018-rest-read-for-everyone.md): выгружать
шаблоны опубликованных флоу в `flows/*.json`, чтобы `settings.input` PIECE-шагов
был виден в диффе PR.

## Блокер, который был и снят

**2026-09-13 — 2026-09-14 пакет стоял без ключа платформы.** Вызовы
`security` отбивались разрешениями агента, переменных окружения с ключом
не было. Снято владельцем точечным правилом ровно на одну запись Keychain
(`aiqadam-events-bot:qadam-flow-api`) и ровно на форму
`find-generic-password -w -s <имя>` — порядок флагов частью правила, менять
нельзя. Широкого доступа к Keychain у агента нет.

За время блокировки сделано всё, что ключа не требовало: скрипты, чистка
каталога, согласование чек-листов. После снятия — выгрузка и замер.

## Что построено

| Артефакт | Где | Что делает |
|----------|-----|------------|
| `tools/export-flows.sh` | репозиторий | выгрузка шаблонов **опубликованных** флоу в `flows/*.json` + `flows/_manifest.json` |
| `tools/check-export-secrets.sh` | репозиторий | обязательная проверка экспорта на секреты, ненулевой код = коммитить нельзя |
| `flows/README.md` | репозиторий | правила обращения с экспортом: руками не править, обновлять тем же коммитом |

Флоу через MCP в этом пакете **не менялись** — работа чисто инструментальная
и документная.

### Как скрипт обращается с ключом

Ключ берётся из `QADAM_API_KEY`, иначе из Keychain, и передаётся в `curl`
через `-H @-` по stdin. Не `-H "Authorization: ..."` аргументом: аргумент
виден в `ps` любому процессу на машине. Ни `echo`, ни `set -x` в скрипте нет.

### Как обеспечено «опубликованная версия, не draft»

Скрипт читает `publishedVersionId` из `GET /api/v1/flows?projectId=…` и
**явно** передаёт его как `versionId` в `/template`, не полагаясь на
умолчание эндпоинта. Флоу без `publishedVersionId` не выгружается вовсе:
громкое сообщение в stderr и ненулевой код возврата — молчаливый снимок
draft'а хуже отсутствующего. Пары `flow → publishedVersionId` пишутся
в `flows/_manifest.json`, поэтому в диффе видно, что снимок снят с новой
публикации, а не пересохранён.

## Чек-лист готовности

- [x] все флоу проекта выгружены (21 опубликованный, 424 КБ, 7832 строки),
      дифф прочитан глазами — не только закоммичен;
- [x] на экспорте прогнана проверка на секреты: паттерн токена Telegram и
      hex-строки ≥32 символов — **ноль совпадений**; команда проверки
      (`tools/check-export-secrets.sh`) записана в репозиторий и поставлена
      пунктом в [REVIEW-CHECKLIST](REVIEW-CHECKLIST.md) блок 2 и в порядок
      работы [ROADMAP](../ROADMAP.md) шаг 6;
- [x] [Q39](../OPEN-QUESTIONS.md#q39) закрыт исходом с обоснованием —
      вариант (1), сырой вывод; **1 строка шума на 1 осмысленную**;
- [x] выгружается **опубликованная** версия, и это явно проверено —
      конструкцией скрипта и живым доказательством: неопубликованный
      `reg-afterword` был громко пропущен;
- [x] REVIEW-CHECKLIST и брифинг агента в ROADMAP говорят об экспорте одно
      и то же;
- [x] в `catalog/` нет дублирования экспорта — колонка «Ключевые inputs / refs»
      убрана из 17 карточек (кроме пяти, занятых W28, см. ниже);
- [ ] независимое ревью, вердикт «замечаний нет».

## Как проверено

- **Выгрузка:** 21 опубликованный флоу. Неопубликованный `reg-afterword`
  (новый флоу W28) **пропущен с сообщением и ненулевым кодом** — это и есть
  живое доказательство пункта «published, а не draft», а не утверждение
  про конструкцию скрипта.
- **Идемпотентность:** две выгрузки подряд без единой правки — `diff -rq`
  не нашёл различий. Шум на ровном месте = 0 строк.
- **Цена осмысленного изменения (Q39):** на `events-list` шаг
  `send_text_message` переведён `format: "None"` → `"MarkdownV2"` плюс
  соответствующая отметка `lastUpdatedDate` → `git diff --numstat` дал
  `2 2`. Симуляция откатана, `format` восстановлен в `"None"`, рабочее
  дерево сверено с индексом.
- **`check-export-secrets.sh`** — различающим прогоном на фикстурах:
  грязная (токен + hex) → код 1; чистая со ссылками на connections → код 1
  (теперь это сигнал отката фикса); чистая без connections → код 0.
  На реальном экспорте — четыре «ok», код 0.
- **Чтение экспорта глазами** по тем классам дефектов, ради которых писался
  ADR-0018: `format` задан явно у **всех** шагов отправки (счётчик отправок
  сходится со счётчиком `format`); короткой формы `{{VAR}}` нет ни одной;
  `{{variables['...']}}` — 8 ссылок (`BOT_TOKEN` ×2, `QR_SIGNING_KEY` ×2,
  `MINIAPP_URL` ×3, `BOT_USERNAME` ×1); у обоих вебхуков `authType: none`,
  как и заявлено в каталоге; cron-триггеров в проекте нет.
- **Сетевой путь:** только `GET`. Проба эндпоинта истории версий
  (`/flows/:id/versions`) дала `403` — история версий по этому ключу
  недоступна, поэтому замер Q39 сделан на артефакте, а не на двух версиях.

## Журнал

- **2026-09-13** — пакет взят, ветка `w29-flows-export` от `main`.
- **2026-09-13** — ключ недоступен: `security find-generic-password` и
  `security dump-keychain` отбиты разрешениями агента; переменных окружения
  с ключом нет. Решение: не искать обход, сделать всю работу, которая ключа
  не требует, и зафиксировать блокер явно.
- **2026-09-13** — при чистке каталога первая версия скрипта срезала
  последнюю колонку у **любой** четырёхколоночной таблицы и попортила
  «Порядок проверок и HTTP-статусы» в `checkin-api` и контракт ответа
  в `my-qr-api`. Откатано, переписано с якорем на заголовок
  «Ключевые inputs / refs». Отдельно всплыли три строки с экранированным
  `\|` внутри ячейки — наивный `split('|')` их не разбирает, правились руками.
- **2026-09-13** — из удаляемой колонки один факт оказался не механическим
  пересказом, а граблей: у `checkin-api` `step_8` в `record_id` при исходе
  не-`ok` подставляется сентинел `-` (гарантированный 404, безопасный no-op).
  Перенесён в назначение шага, а не потерян.
- **2026-09-14** — блокер снят, выгрузка прошла с первого раза. Порядок
  флагов в вызове `security` пришлось переставить на `-w -s`: разрешение
  выдано точечно и на конкретную форму команды, прежняя `-s … -w` под него
  не подходила.
- **2026-09-14** — **позитивный контроль проверки на секреты проходил
  ложно.** Он сканировал весь каталог `flows/`, включая собственный
  `README.md`, где `{{connections[...]}}` приведён как текст документации.
  В реальных `*.json` ссылок на connections нет **ни одной**, а «ok»
  печатался. Починено: сканируются только `*.json`. Урок общий — позитивный
  контроль обязан читать тот же артефакт, что и негативная проверка, иначе
  он проверяет свою собственную документацию.
- **2026-09-14** — почему ссылок на connections не стало: 2026-09-13 в
  qadam-flow влит **PR #439**, в нём же починен `removeConnectionsFromInput`.
  [Q38](../OPEN-QUESTIONS.md#q38) от 2026-09-13 видел `auth` 41 раз ссылкой;
  экспорт от 2026-09-14 не содержит ни одной. Оговорка Q38 («проверено
  текущее поведение платформы, а не гарантия на будущее») оправдалась за
  сутки — ровно поэтому проверка перед коммитом повторяемая, а не разовая.
- **2026-09-14** — тот же PR #439 добавляет `ap_export_flow`/`ap_import_flow`
  той же формы, что `GET /v1/flows/:id/template`, то есть выгрузка перестаёт
  требовать REST и ключ платформы. Это выбивает посылку ADR-0018, поэтому
  способ выгрузки менять молча нельзя — заведён
  [Q44](../OPEN-QUESTIONS.md#q44), решение за владельцем. В этой сессии
  `ap_export_flow` не резолвится (список инструментов MCP фиксируется при
  старте сессии, а она старше PR), поэтому пакет доведён на REST-скрипте.

## Что осталось за W28

Карточки `reg-start.md`, `reg-consent-pdn.md`, `reg-consent-mkt.md`,
`reg-phone.md`, `tg-router.md` прямо сейчас переписывает пакет
[W28](W28-single-card-guest-flow.md). Колонка «Ключевые inputs / refs»
в них **оставлена намеренно** — чистка по этим пяти файлам отложена до
слияния W28, иначе конфликт гарантирован. Формат, к которому их надо
привести, зафиксирован в [`catalog/flows/_TEMPLATE.md`](../../catalog/flows/_TEMPLATE.md).

## Ревью

- **Ревьюер**: — · **Дата**: — · **Вердикт**: —

### Замечания

## Хвосты и блокеры

1. **[Q44](../OPEN-QUESTIONS.md#q44) — решение владельца:** оставаться на
   REST-скрипте, перейти на `ap_export_flow` или держать оба. Пока вопрос
   открыт, штатный путь — скрипт; ничего не блокирует.
2. Чистка пяти карточек гостевого среза (`reg-start`, `reg-consent-pdn`,
   `reg-consent-mkt`, `reg-phone`, `tg-router`) — после слияния W28.
3. `CLAUDE.md` говорит «сама выгрузка — пакет W29». Правку туда сознательно
   не вносил: формулировка зависит от исхода Q44, и переписывать её дважды
   хуже, чем один раз после решения.

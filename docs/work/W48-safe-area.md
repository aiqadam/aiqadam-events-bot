# W48. Safe Area — отступы под системный UI Telegram

- **Статус**: на проверке
- **Владелец**: агент
- **Волна**: v0.1 (блокирует W15)
- **Зависит от**: W33 — готов
- **Начат**: 2026-09-18 · **Закрыт**: —

## Цель

Контент SPA не должен заходить под собственную шапку Telegram (три точки,
имя приложения) и под вырезы/жестовую зону устройства. Реализация уже
открытого [Q48](../OPEN-QUESTIONS.md#q48). Подробности — [BACKLOG W48](../BACKLOG.md#w48-safe-area--отступы-под-системный-ui-telegram-q48),
дублировать не надо.

## Что построено

Только клиентский слой SPA, без flows/таблиц:

| Артефакт | Что изменено |
|----------|--------------|
| `miniapp/index.html` | `viewport-fit=cover` в `<meta name="viewport">` |
| `miniapp/src/index.css` | переменные `--safe-top`/`--safe-bottom`; `#root` (верх), `.sticky-actions`/`.app-sheet-panel` (низ) |

## Чек-лист готовности

- [x] `viewport-fit=cover` добавлен;
- [x] `--safe-top`/`--safe-bottom` применяются в одном месте layout, не по
      роутам; на нулевых инсетах поведение не меняется;
- [x] `npm run build` (`tsc -b && vite build`) чист;
- [x] `MINIAPP-UX.md`, `OPEN-QUESTIONS.md` (Q48), `catalog/overview.md`
      обновлены;
- [ ] живая проверка на устройстве с вырезом — **шаг владельца**;
- [ ] независимое ревью, вердикт «замечаний нет»;
- [x] `catalog/` совпадает с живым проектом (правка чисто клиентская,
      инстанс не тронут).

## Как проверено

> Чем именно, а не «протестировано».

- `npm run build` в `miniapp/` (`tsc -b && vite build`) — чисто, без ошибок
  типов и сборки, размеры чанков не изменились заметно.
- **Живой рендер в браузере не снят** — Playwright/Chrome в этой сессии
  зависал на подключении к локальному `vite preview` независимо от кода
  (сетевая особенность песочницы, воспроизвелась и на пустой странице без
  правок из этого пакета) — попытка не доведена до результата, не скрыта:
  проверка ограничилась статическим анализом CSS/HTML и `tsc`/`vite build`.
  Это не заменяет живую проверку на реальном устройстве с вырезом, которая
  и без этого — шаг владельца.
- Код-уровневый факт подтверждён без браузера: спецификация CSS Environment
  Variables — `env(safe-area-inset-*)` резолвится в `0`, если у документа
  нет `viewport-fit=cover`; в `miniapp/index.html` до этого пакета его не
  было, значит существовавшие `env()`-вызовы были мёртвым кодом на любом
  устройстве, включая уже якобы «покрытые» `.sticky-actions`/`.app-sheet-panel`.

## Журнал

- **2026-09-18** — пакет заведён при проработке ресёрча best practice
  Telegram Mini App (параллельно с W47): найден код-уровневый факт
  (отсутствие `viewport-fit=cover`), из открытого вопроса Q48 стало ясно,
  что его пункт 2 («если перекрытие есть — применить инсеты») можно и нужно
  сделать заранее, не дожидаясь проверки на устройстве — фикс безвреден на
  нулевых инсетах и является строгим улучшением независимо от исхода
  проверки.
- **2026-09-18** — реализация: `viewport-fit=cover`, переменные
  `--safe-top`/`--safe-bottom` (`max()` от `env()` и
  `--tg-content-safe-area-inset-*`), применены в `#root` и в двух местах,
  где уже жил (мёртвый) `env()`. `npm run build` чист.
- **2026-09-18** — попытка живого рендер-прогона (Playwright + системный
  Chrome, локальный `vite preview`): зависает на любой странице независимо
  от изменений этого пакета — не воспроизведено до результата в разумное
  время, отложено, чтобы не тянуть время ревью; зафиксировано как
  ограничение проверки, не как «протестировано и работает».
- **2026-09-18** — обновлены `docs/MINIAPP-UX.md` (п. 5), `docs/OPEN-QUESTIONS.md`
  (Q48 — статус «код готов», живая проверка остаётся открытой),
  `catalog/overview.md` (раздел Mini App), `docs/BACKLOG.md`/`STATUS.md`/
  `ROADMAP.md` (пакет вставлен в критический путь v0.1 перед W15). Статус
  пакета → «на проверке».

## Ревью

> Заполняет **независимый ревьюер** по [REVIEW-CHECKLIST.md](REVIEW-CHECKLIST.md).
> Владелец пакета сюда не пишет — только отвечает под замечаниями, что исправлено.

- Ещё не запущено.

## Хвосты и блокеры

- Живая проверка на устройстве с вырезом (`ticket`, `scan`, светлая и тёмная
  темы) — не может быть закрыта агентом; без неё пакет не может стать
  «готов», только «на проверке» с чистым кодревью.
- Живой рендер-прогон (Playwright) не удался технически в этой сессии —
  при повторном ревью стоит попробовать ещё раз с другим окружением/браузером,
  не как блокер, а как более сильное доказательство, чем `tsc`/`vite build`.

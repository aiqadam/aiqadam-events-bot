# W51. Живые баги со скринов 2026-09-19 + вход овнера в правку

- **Статус**: в работе
- **Владелец**: агент
- **Волна**: v0.1 (перед W15 — живые регрессии после W50)
- **Зависит от**: W50 — готов, W41 (прототип — эталон, ADR-0027)
- **Начат**: 2026-09-19 · **Закрыт**: —

## Цель

Убрать расхождения живого продукта с прототипом, видимые на скринах владельца
2026-09-19 (каталог, билет, шит регистрации, напоминания), и вернуть овнеру
понятный вход в правку ивентов.

## Что построено

| Артефакт | ID / имя | Каталог |
|----------|----------|---------|
| SPA `Events.tsx` (QR во время ивента, «Регистрация закрыта», фидбек из прошлого) | — | [catalog/overview.md](../../catalog/overview.md) |
| SPA `Ticket.tsx` (контекст ивента над QR) | — | [catalog/overview.md](../../catalog/overview.md) |
| SPA `index.css` (табы без переноса, шит без обрезки) | — | — |
| flow `reminders` (`HJvh8nEh4BveIw2i27gcv`, `{when}` — время, как в прототипе) | `HJvh8nEh4BveIw2i27gcv` | [catalog/flows/reminders.md](../../catalog/flows/reminders.md) |

## Чек-лист готовности

- [x] билет `#/ticket` показывает название/дату/адрес ивента (прототип `ticket-top`)
- [x] QR доступен весь ивент, а не только до `starts_at` (дверь зала)
- [x] карточка с закрытым дедлайном не молчит (объяснение вместо пустого места)
- [x] прошлый ивент с чекином ведёт на `#/feedback` (прототип `ticketRow`)
- [x] шит регистрации не режет адрес, табы не рвутся в две строки
- [x] напоминания без года (`{when}` — время, как в прототипе)
- [ ] вход овнера в правку: решение (вопрос владельцу про «Мои ивенты»)
- [x] `check-texts.py` (209/0), `check-commands.py` (0), `check-export-secrets.sh` (чисто); `ap_validate_flow` чист
- [x] `catalog/` совпадает с живым проектом
- [ ] независимое ревью, вердикт «замечаний нет»

## Как проверено

- Playwright + Chromium со стабом Telegram (см. журнал): скрины до/после
- `curl` на публичные `/sync` (events-api без initData)
- `ap_test_step` на `reminders/step_2` (только чтения + CODE, отправок нет)

## Журнал

- **2026-09-19** — взят по поручению владельца («чинить все», расхождений с
  прототипом не допускаем + у овнера пропал вход в правку). Живые данные:
  овнер `322876545` — staff чаптера `1`, активный контролёр `demo`; `test`
  (`mu43al95bl1l`, дедлайн 16.09) без кнопок — дедлайн прошёл, карточка молчит;
  билет `test` без QR — `starts_at` прошёл, код смотрит только на старт.
  Три разных `/start` на скринах — история публикаций (02:22 — до W50,
  08:56/09:00 — после), не живой баг. `ОПУБЛИКОВАН` и сканер рядом с
  «Зарегистрироваться» — как в прототипе (`app.js:1190,1218`), не баги.
- **2026-09-19** — Mini App починен (только клиент, флоу не тронуты):
  `Ticket.tsx` — контекст ивента из публичной афиши + название ивента в
  подтверждении отмены; `Events.tsx` — QR до `ends_at` (MineCard и EventCard),
  зарегистрированному всегда QR, закрытый дедлайн — `reg.deadline_passed`,
  прошлое с чекином — `#/feedback`; по пути найден и починен баг смены
  `?tab=` при открытом каталоге (вход из бота вёл не на тот таб).
  `index.css` — табы скроллятся в одну строку (приём прототипа `.tabs-scroll`),
  дети шита сжимаются + переносы, `.ticket-top` из `proto.css`.
  Проверено Playwright-стендом (локальный `dist`, стаб `telegram-web-app.js`
  через перехват скрипта — настоящий SDK затирает `window.Telegram`,
  `addInitScript` бессилен; моки `events-api`/`reg-api`/`staff-events-api`/
  `my-qr-api`): upcoming/шит/mine/билет — скрины `~/w41/shots/w51-*`.
  `tsc` + `vite build` чисты.
- **2026-09-19** — `reminders/step_2`: `{when}` — время «17:41» вместо даты
  с годом (форма прототипа `when='18:30'`). `ap_validate_flow` чист;
  `ap_test_step` (только чтения + CODE, `due` пуст — отправок ноль);
  `node` — формат «17:41»;   опубликован; экспорт — правкой нормализованного `flows/reminders.json`
  (сырой MCP-снимок пересобирался вручную и я потерял скобку — чинить
  пересборку дольше, чем пропатчить одну строку кода напрямую; дифф —
  одна строка, сверена с живым `ap_read_step_code`); `_manifest.json` — новый
  `publishedVersionId`; `migrations` — строка `2026-09-19-w51-01`.
  `check-texts.py` 209/0, `check-commands.py` 0, `check-export-secrets.sh`
  чисто. `check-migrations.py` — не запущен: ключа платформы на машине нет
  (шаг человека 0.7) — хвост на ревью/приёмку.
- **2026-09-19** — `manage-api` список проверен чтением (`step_32`): отбор по
  чаптеру, порядок, `isAuthor` — цел. Вход овнера в правку — не 403, а снос
  входа: W50 убрал «Мои ивенты» из меню по вердикту W49, остался путь
  «Создать ивент» → форма → «К списку». Вопрос владельцу.

## Ревью

- **Ревьюер**: — · **Дата**: — · **Вердикт**: —

## Хвосты и блокеры

- Вход овнера в список `#/manage`: вердикт W49 меню (`Ивенты` + `Создать ивент`)
  entry не предусматривает — вопрос владельцу (вернуть «Мои ивенты» или оставить
  путь форма → «К списку»).
- Теги `#LLM` и «остаток мест» из прототипа: тегов нет в модели (схема
  заморожена), счётчик мест требует чтения `registrations` в `events-api` —
  отдельным решением.

# W57. Формат первым на шаге «Где и когда», адрес только офлайну

- **Статус**: готов
- **Владелец**: агент
- **Волна**: v0.1 (вне BACKLOG — прямое решение владельца 2026-09-20 по скриншоту шага 2)
- **Зависит от**: —
- **Начат**: 2026-09-20 · **Закрыт**: 2026-09-20

## Цель

Шаг «Где и когда» визарда: тогл Онлайн/Офлайн — первое поле шага с подписью «Формат» (было «Точка на карте»); адрес и ссылка Яндекс.Карт — только офлайну. Онлайн публикуется без адреса.

## Что построено

| Артефакт | ID / имя | Каталог |
|----------|----------|---------|
| flow `manage-api` | `CcGPwuW4ws5hkcaOPerEG` (версия `z8ZDy7YasY3HpgGlXL15a`, `source: mcp`) | [catalog/flows/manage-api.md](../../catalog/flows/manage-api.md) |
| Mini App `#/manage` | `Manage.tsx` (шаг 1) | — |
| Прототип | `prototypes/app.js`, `scenarios.js`, `proto.js` | — |

## Чек-лист готовности

- [x] Формат — первое поле шага, подпись «Формат»; адрес + «Ссылка Яндекс.Карт» только при Офлайн (продукт и прототип)
- [x] Онлайн публикуется без адреса: клиент, `manage-api step_7`, SPEC OWN-1
- [x] Офлайн без адреса/ссылки не публикуется (клиент; сервер — адрес при точке)
- [x] `check.mjs` exit 0 (заодно закрыт дрейф `protoDict` vs `ru.json`, 70 ошибок)
- [x] `catalog/` совпадает с живым проектом (тройная сверка: экспорт — манифест — `migrations 2026-09-20-owner-01`)
- [x] Независимое ревью (круг 1 — 1 «важно» + 1 «на будущее», см. Ревью)

## Как проверено

- Матрица правила адреса (сервер, переписана построчно): publish без адреса без точки — ок; с точкой без адреса — `required`; с адресом — ок; draft — ок; короткий адрес — `length`.
- Живой `step_7` (`ap_read_step_code`) побайтово совпал с экспортом до публикации; `ap_validate_flow` — ready; диф кода — один ханк.
- Дамп резолвленных текстов сценариев прототипа до/после чистки словаря: весь рендер побуквенно тот же, кроме исправления буквальных `{name}`/`{profile}` в моки (в HEAD прототип показывал плейсхолдеры).
- `tsc+vite build` чисто; pre-commit хук зелёный; `check-texts` 211/0; `check-commands` 0.
- Живые негативные прогоны не делались (гоча 11: `ap_test_flow` с sample-входом пишет в таблицы) — различение доказано чтением кода.

## Журнал

- **2026-09-20** — Взято с сообщения владельца по скриншоту. По ходу выяснилось: адрес требовался и онлайну на сервере (`manage-api step_7`), иначе скрытие поля ломало бы публикацию — серверная правка вошла в пакет.
- **2026-09-20** — `check.mjs` падал и на чистом `main` (70=70): дрейф `protoDict` vs `ru.json` после W50. Починен здесь же: 31 ключ удалён (T() и так брал `ru.json` первым), 7 вызовов переведены, 3 мока передают подстановки.
- **2026-09-20** — Экспорт через MCP (`source: mcp`): без `auth` в шагах и с подхватом двух чужих comment-правок `step_32`/`step_42` (инстанс был впереди репозитория — сверено, не порча). Богатый REST-экспорт ждёт ключ платформы (шаг 0.7, у владельца).
- **2026-09-20** — `check-migrations.py` не прогнать: ключа нет ни в `QADAM_API_KEY`, ни в Keychain (fail-closed, ожидаемо). Строка `migrations` и манифест на месте.

## Ревью

> Заполняет **независимый ревьюер** по [REVIEW-CHECKLIST.md](REVIEW-CHECKLIST.md).
> Владелец пакета сюда не пишет — только отвечает под замечаниями, что исправлено.

- **Ревьюер**: агент · **Дата**: 2026-09-20 · **Вердикт**: есть замечания

### Замечания

1. **важно** — SPEC обещал больше сервера: «офлайн — точка обязательна», а сервер точку не требует (формат на сервер не едет, онлайн ≡ нет точки) — `docs/SPEC.md` OWN-1
   - *Исправлено*: формулировка заменена честным прокси (сервер: адрес ⇐ точка; ссылку проверяет только страница) (2026-09-20)
2. **на будущее** — у решения владельца 2026-09-20 не было записи в `docs/work/` — —
   - *Исправлено*: заведена эта запись (2026-09-20)

- **Ревьюер**: агент · **Дата**: 2026-09-20 · **Вердикт**: замечаний нет (круг 2: SPEC-прокси сверен построчно с живым `step_7`, журнал и STATUS честны, регресс чист — `check-texts` 211/0, `check-commands` 0, `check.mjs` exit 0)

## Хвосты и блокеры

- Ключ платформы (шаг 0.7, у владельца): богатый REST-реэкспорт `manage-api` + прогон `tools/check-migrations.py`.
- Живая проверка шага на устройстве и слияние PR #92 — за владельцем.

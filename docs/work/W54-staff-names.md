# W54. Строки контролёров с именами + выбор primary-кнопкой

- **Статус**: в работе
- **Владелец**: агент
- **Волна**: v0.1
- **Зависит от**: W53 — готов (находка: секция контролёров разъехалась с прототипом), W36/W44 — готовы (ручной ввод и поиск)
- **Начат**: 2026-09-19 · **Закрыт**: —

## Цель

Секция контролёров как в эталоне (`renderStaff`): строки с аватаром,
именем и `username · контролёр с …`, добавление — primary-кнопкой через
выбор из списка. Ручной ввод остаётся запасным путём (кого нет в `users`).

## Что построено

| Артефакт | ID / имя | Каталог |
|----------|----------|---------|
| flow `manage-api` (`CcGPwuW4ws5hkcaOPerEG`, `staff_list` отдаёт имена) | `CcGPwuW4ws5hkcaOPerEG` | [catalog/flows/manage-api.md](../../catalog/flows/manage-api.md) |
| SPA `Manage.tsx` (строки, primary-кнопка выбора) | — | [catalog/overview.md](../../catalog/overview.md) |

## Чек-лист готовности

- [x] `staff_list`/`staff_add`/`staff_remove` отдают `name`/`username`/`sub`; `item` без изменений
- [x] вставка шага чтения `users` не разорвала цепочку (структура: read event staff → read staff names → decide)
- [x] строки: аватар + имя + sub, fallback на `item`; выбор — primary block-кнопка, ручной ввод ниже
- [x] `ap_validate_flow` чист; опубликован; экспорт и `_manifest.json` тем же коммитом
- [x] `check-texts.py` (211/0, +ключ `manage.staff.since`), `check-commands.py`, `check-export-secrets.sh` чисты
- [x] `catalog/` совпадает с живым проектом
- [ ] независимое ревью, вердикт «замечаний нет»

## Как проверено

- `ap_validate_flow`; `ap_test_step` на `step_20` (CODE чист, отправок нет)
- Playwright-стенд (моки с новой формой `staff`): `~/w41/shots/w54-staff.png`
- живой позитив своим `initData` — хвост на владельца (таб Контролёры)

## Журнал

- **2026-09-19** — взят по сравнению скриншотов (эталон vs продукт):
  аватары+имена против плоской строки, одна primary-кнопка против
  ручного ввода первым. Решение владельца: полный объём. Отмена W36-решения
  «имён нет намеренно» (`step_20`): теперь join с `users` (как
  participants/search), неизвестный — fallback на `item`.
- **2026-09-19** — `manage-api`: вставка `step_52` (`read staff names`,
  проекция как `step_40`/`step_45`) легла ровно в цепочку (`step_19 →
  step_52 → step_20`, gotcha 10 для PIECE не сработал); `step_20` — join
  (`usersById`, первый `telegram_id` побеждает) + `shape()` (`item` как был,
  плюс `name`/`username`/`sub` через новый ключ `manage.staff.since`);
  ответы `step_23`/`step_29`/`step_30` отдают `staff` целиком — новые поля
  доехали без их правки. Проверено: `ap_validate_flow` (53 шага);
  `ap_test_step` на `step_20` (403 на пустых таблицах, код не падает);
  та же логика shape в `node` (известный → полные поля, неизвестный →
  fallback). Опубликован; экспорт LOCKED (`Y18FbOzTncJT2G6p280Oq`);
  `flows/manage-api.json` пропатчен хирургически (проверено попарно:
  все узлы кроме `step_20` идентичны, предки — кроме цепочки).
  SPA: строки с аватаром+именем+sub, выбор — primary block с плюсом,
  ручной ввод ниже secondary. Стенд `w54-staff.png`: известный и
  неизвестный контролёры. `check-texts.py` 211/0.

## Ревью

- **Ревьюер**: независимый агент с чистым контекстом · **Дата**: 2026-09-19 · **Вердикт**: **замечаний нет** (1 «на будущее» — fail-closed чтения `users` роняет весь `staff_list`, хотя имена покрыты fallback: тот же паттерн у `step_40/45/49`, кандидат в общий вопрос, не чинить здесь)
- **Владелец**: строка `migrations` `2026-09-19-w54-01` перечитана (`ap_find_records` — `version_id` = манифесту); `check-migrations.py` по-прежнему требует ключ платформы (хвост W15)

## Хвосты и блокеры

- нет

# W46. Хвосты перед приёмкой: Q31, Q32, Q14

- **Статус**: на проверке
- **Владелец**: агент (сессия 2026-09-17)
- **Волна**: v0.1, перед W15
- **Зависит от**: решения владельца 2026-09-17 (Q31, Q32, Q14)
- **Начат**: 2026-09-17 · **Закрыт**: —

## Цель

Закрыть/оценить три открытых вопроса, которые чек-лист [W15](../BACKLOG.md#w15-приёмка)
требует пройти до приёмки: [Q31](../OPEN-QUESTIONS.md#q31) (ПД в логах
широкого чтения `registrations`), [Q32](../OPEN-QUESTIONS.md#q32)
(молчаливая потеря тапа при сбое обработчика), [Q14](../OPEN-QUESTIONS.md#q14)
(адресация dev/prod в сканере). Подробности — [BACKLOG.md#w46](../BACKLOG.md#w46-хвосты-перед-приёмкой-q31-q32-q14).

## Что построено

| Артефакт | ID / имя | Каталог |
|----------|----------|---------|
| flow `reg-api` | `SiYL8m6k4oy4YunAdZ1W7` | [catalog/flows/reg-api.md](../../catalog/flows/reg-api.md) |
| flow `reg-start` | `FkxtgayOK5QubyqqMd9q4` | [catalog/flows/reg-start.md](../../catalog/flows/reg-start.md) |
| flow `reg-consent-pdn` | `vQJDQ8NecB1PleIFrq07O` | [catalog/flows/reg-consent-pdn.md](../../catalog/flows/reg-consent-pdn.md) |
| flow `reg-consent-mkt` | `3gLF6TcbpFObHONATQ64N` | [catalog/flows/reg-consent-mkt.md](../../catalog/flows/reg-consent-mkt.md) |

## Чек-лист готовности

> Скопировано из BACKLOG.md#w46.

- [x] `reg-api/step_6`, `reg-api/step_7` — `columns` заданы; регресс
      подтверждён **разбором кода** (`step_9` читает ровно эти поля), не
      прогонами `curl` — снято решением владельца 2026-09-17 (тот же класс
      ограничения, что в Q16/W5/W8: у агента нет `BOT_TOKEN` для подписи
      `initData`). См. «Хвосты».
- [x] `continueOnFailure` + ветка отказа на небезопасных `tables`-шагах трёх
      гостевых флоу, happy path не изменился
- [x] хотя бы один различающий прогон показывает срабатывание ветки отказа
      (сделано по одному прогону на каждый из трёх флоу)
- [x] `common.err.generic` совпадает с `ru.json`, `check-texts.py` чист
- [x] Q31/Q32 отвечены, Q14 — решение записано в OPEN-QUESTIONS.md
- [x] каталог и экспорт `flows/*.json` тем же коммитом
- [x] `catalog/` совпадает с живым проектом
- [ ] независимое ревью, вердикт «замечаний нет»

## Как проверено

Все прогоны — `ap_test_flow` на TESTING, реальные side-effect записи в
`events-dev` на тестовый `telegram_id 322876545` (владелец проекта,
self-test конвенция OWN-10). Временные порчи конфига (`table_id`/`field.id`
на заведомо несуществующие значения) делались **после** снятия исходного
`ap_export_flow` для сравнения и **восстанавливались** до финального
`ap_lock_and_publish` — черновик на момент публикации равен продовому коду
без тестовых порч.

- **reg-api** (`SiYL8m6k4oy4YunAdZ1W7`). Живого прогона нет — `authType: none`
  с проверкой `initData` внутри (`fn-hmac-init-data` через `step_1`), а
  синтезировать подпись без `BOT_TOKEN` агент не может (тот же класс
  ограничения, что в W5/Q16). Закрыто разбором: `step_9` (`decide`) читает
  из `eventRegs` только `event_id`/`telegram_id`/`status`, из `myRegs` —
  `telegram_id`/`status`/`registered_at`/`checked_in_at`/`event_id` — сеты
  `columns` совпадают буквально. `ap_validate_flow` — `valid: true`,
  17/17 шагов.
- **reg-start** (`FkxtgayOK5QubyqqMd9q4`), прогоны через `ap_test_flow`,
  триггер `callableFlow`:
  - happy path (`eventId: demo`, реальный `telegramId`) — прогон `OkSIdP9KTREMDszDtQwKE`,
    исход `new`, карточка отправлена, сессия записана — не изменился;
  - различающий: `step_1.filters` испорчен несуществующим `field.id` →
    прогон `hXJkyYGOnyfT2KDAIdD1R` — `step_3` вернул `reason: internal_error`,
    **одно** сообщение `common.err.generic` (до фикса `step_3` — прогон
    `mmYiQv7W2jswWGpEl2W4N` — то же самое давало **два** сообщения:
    страховка step_1 и штатный «карточку показать не удалось», это и
    вскрыло дизайн-ошибку первой версии страховки, см. журнал ниже);
  - различающий: `step_11.table_id` испорчен → прогон `AQeVYtdvIAtK81KeEVzT4` —
    карточка отправлена штатно, затем `step_18/19` отправили
    `common.err.generic` отдельным сообщением (не дубль — карточка и
    сообщение о сбое записи сессии говорят о разных вещах).
- **reg-consent-pdn** (`vQJDQ8NecB1PleIFrq07O`):
  - happy path `yes` — прогон `PKqb6V3ePAqPtXvtAfxoA`, карточка
    отредактирована в «✅ Вы зарегистрированы…»;
  - happy path `no` — прогон `9fHFrYI1yy1W8uTRzwhpw`, карточка
    отредактирована в отказ, `step_12` (`continueOnFailure`) прошёл штатно;
  - различающий: `step_5.table_id` испорчен → прогон `bdhIrNlQyX8UZIEQpNdjz`
    (`FAILED`, но это ожидаемо: `step_8`/`step_10` упали на **неэкранированном**
    дефисе в `common.err.generic` — реальный баг, найден этим прогоном) →
    исправлено (`esc()` вокруг текста ошибки в `step_7`) → повтор
    `ht1wmjo1i8LT9kLV5KqMA` — `SUCCEEDED`, карточка отредактирована в
    «Что-то пошло не так. Попробуйте ещё раз чуть позже.», без дублей.
- **reg-consent-mkt** (`3gLF6TcbpFObHONATQ64N`):
  - happy path `yes` — прогон `eBxfSZWylSyOtlZfXNCro`, карточка и билет
    ушли штатно;
  - различающий: `step_3.table_id` испорчен → прогон `JSUa8xpOggQSgpfmcWMha` —
    карточка **и** билет получили `common.err.generic` (esc применён с
    самого начала — баг из `reg-consent-pdn` не повторился).
- `ap_validate_flow` — `valid: true` на всех четырёх флоу после восстановления
  конфигов и финальной публикации.
- `tools/check-export-secrets.sh`, `tools/check-texts.py`,
  `tools/check-commands.py` — все три чисты на полном `flows/*.json`.

## Журнал

- **2026-09-17** — пакет заведён. Ключевые находки при разведке живого
  проекта через MCP (важны для ревьюера, не очевидны из каталога):
  - Q31 в старой формулировке относился к флоу `registration`, которого
    больше нет (пересобран W26/W28). Текущий эквивалент — `reg-start/step_2`,
    он уже сужен до `telegram_id`+`status` в W28. Но `reg-api/step_7`
    (каталог `#/events`, действие `register`/`cancel`) читает **все поля**
    всех регистраций ивента без `columns` — живая версия той же утечки,
    доступная любому пользователю с валидным `initData` на произвольный
    `eventId`. Это и есть цель правки, не `reg-start`.
  - Q32 в старой формулировке описывает `executionMode: inline`, а
    `tg-router` сегодня зовёт все 7 обработчиков через `executionMode: queue,
    waitForResponse: false` (решение W26, латентность). При `queue` сбой
    ребёнка **не виден родителю вообще** — ни синхронно, ни через
    `continueOnFailure` на стороне `tg-router`. Вариант «снять клеймо +
    написать в `tg-router`» физически неосуществим при текущей архитектуре.
    Переформулировано владельцем: страховка живёт **внутри** каждого
    вызываемого флоу; снимать клеймо `upd:<update_id>` не нужно вовсе —
    Telegram не переотправляет обновления, повтор пользователя = новый
    `update_id` с чистым клеймом.
  - Первая оценка (только листовые шаги, без рисковых чтений в начале
    флоу) была основана на неверном применении gotcha 10 из AGENTS.md.
    Gotcha 10 — про вставку `ROUTER` через `ap_add_branch`/`ap_add_step
    ROUTER AFTER`, которая не гейтит существующее продолжение. Механизм
    `continueOnFailure` (через `ap_update_step`) — другой код: существующее
    продолжение шага автоматически становится веткой «On success», плюс
    появляется настоящая ветка «On failure» — без риска параллельного
    исполнения обеих веток. Этот механизм **уже используется** в
    `reg-consent-pdn`/`reg-consent-mkt` (шаги `on_failure_branch` для
    неудачного редактирования карточки) — значит он безопасен и для
    ранних `tables`-шагов, которые сегодня не защищены вовсе. Владелец
    выбрал полный фикс (все незащищённые `tables`-шаги трёх гостевых
    флоу), не только листья.
  - **Первая версия страховки на `reg-start/step_1`/`step_2` была неверной
    и это нашлось только прогоном.** Идея была: `continueOnFailure` +
    собственная ветка «On failure» на каждом шаге (CODE-текст + `send_text_message`).
    Различающий прогон `mmYiQv7W2jswWGpEl2W4N` показал: ветка «On failure»
    **не отменяет** обычную ветку «On success» — платформа исполняет и то,
    и другое, потому что `continueOnFailure` продолжает основной поток
    независимо от того, сработала ли ветка отказа. Итог — **два**
    сообщения пользователю на один сбой: страховочное «попробуйте ещё раз»
    и штатное (в этом случае «карточку показать не удалось: такого ивента
    нет», потому что `step_3` получил пустой `eventRecords` от упавшего
    `step_1` и интерпретировал это как «ивент не найден»). Переделано:
    убраны отдельные ветки отказа на `step_1`/`step_2` (`ap_delete_step`,
    два раунда — `ap_delete_step` не каскадно удаляет детей, дочерний шаг
    остаётся осиротевшим и годится к повторному удалению), `step_3` вместо
    этого читает `{{step_1['error']}}`/`{{step_2['error']}}` и **сам**
    решает: `reason: 'internal_error'` уходит в уже существующую ветку
    `declined`, где `keyByReason` не находит соответствия и берёт
    `common.err.generic` — один текст, одна ветка, ноль новых шагов.
    Тот же приём применён в `reg-consent-pdn/step_7` и `reg-consent-mkt/step_5`
    для их четырёх/трёх незащищённых `tables`-шагов. Урок общий: там, где
    после защищаемого шага уже есть общий CODE-потребитель ошибки, страховка
    входит через его `error`-вход, а не через отдельную ветку — отдельная
    ветка нужна только там, где такого потребителя нет (листья:
    `reg-start/step_11`, `reg-consent-pdn/step_12` без уведомления).
  - **Второй найденный баг — тем же приёмом.** Текст `common.err.generic`
    («Что-то пошло не так…») содержит дефис, а ответы редактирования/отправки
    карточки в этих трёх флоу — `format: MarkdownV2`. Первая версия override-ветки
    в `reg-consent-pdn/step_7` возвращала текст без `esc()` — различающий
    прогон `bdhIrNlQyX8UZIEQpNdjz` показал `400 «can't parse entities»` и на
    основном шаге, и на его же фолбэке (оба шлют один и тот же неэкранированный
    текст). Исправлено оборачиванием в тот же `esc()`, что уже есть в файле
    для остальных строк; повторный прогон `ht1wmjo1i8LT9kLV5KqMA` прошёл.
    В `reg-consent-mkt/step_5` `esc()` применён сразу по этому же уроку.

## Ревью

- **Ревьюер**: агент (сессия ревью, чистый контекст), **дата**: 2026-09-17
- **Вердикт**: есть замечания

### Что проверено

Живой проект через MCP: `ap_flow_structure`/`ap_read_step_code` по всем
четырём флоу (`reg-api`, `reg-start`, `reg-consent-pdn`, `reg-consent-mkt`),
`ap_validate_flow` (все четыре — `valid: true`), сырые `settings` из
`flows/*.json` (поле `errorHandlingOptions.continueOnFailure`, которое
`ap_flow_structure` не показывает) — подтверждено, какие именно шаги
получили `continueOnFailure` этим пакетом (сверено с `git show 64221f7` —
до пакета все были `false`). Прочитаны сами прогоны из журнала
(`ap_get_run`: `mmYiQv7W2jswWGpEl2W4N`, `hXJkyYGOnyfT2KDAIdD1R`,
`bdhIrNlQyX8UZIEQpNdjz`, `ht1wmjo1i8LT9kLV5KqMA`, `JSUa8xpOggQSgpfmcWMha`,
`AQeVYtdvIAtK81KeEVzT4`, `eBxfSZWylSyOtlZfXNCro`, `OkSIdP9KTREMDszDtQwKE`) —
все описанные в журнале различающие прогоны и хеппи-пасы подтверждены
буквально (тексты сообщений, `message_id`, ветки, отсутствие/наличие
дублей). Офлайн-проверки: `check-texts.py` (178 пар, 0 расхождений),
`check-export-secrets.sh` (чисто), `check-commands.py` (0 нарушений) — все
три зелёные на полном `flows/*.json`. `check-migrations.py` запущен —
см. замечание 2. Каталоги всех четырёх флоу сверены построчно с
`ap_flow_structure`/`ap_read_step_code` — расхождений не найдено.
`columns` на `reg-api/step_6`/`step_7` (externalId) сверены с полями,
которые реально читает `step_9` (`decide`), через
`catalog/tables/registrations.md` — совпадение буквальное.

### Замечания

1. **блокер.** `reg-consent-pdn`: `continueOnFailure` на `step_4` («save
   consent_pdn») не гейтит `step_5` («create or reactivate registration») —
   `step_5` не читает `{{step_4['error']}}` и ничем не защищён от того, что
   `step_4` упал. Цепочка `step_4 → step_6 → step_5 → step_9 → step_7`
   линейна: если запись `consent_pdn` не удалась, флоу всё равно доходит до
   `step_5` и **создаёт регистрацию** (`status: registered`), а затем
   `step_9` **продвигает сессию** на `await_marketing` — и только потом
   `step_7` видит `saveConsentError` и показывает пользователю
   `common.err.generic`. Итог: человек видит «что-то пошло не так», но в
   `registrations` уже лежит строка `status = registered` без
   подтверждённого `consent_pdn = true` в `users`.
   Это прямая пробоина в PAR-1 — инвариант, который сам же каталог этого
   флоу объявляет держащимся именно здесь: «Регистрация создаётся здесь,
   а не в `reg-start` — это и держит PAR-1: без явного согласия строки в
   `registrations` не появляется» (`catalog/flows/reg-consent-pdn.md`).
   После сбоя это утверждение больше не верно. Хуже: `edit_message_text`
   в error-ветке убирает клавиатуру (`replyMarkup: {inline_keyboard: []}`),
   то есть у пользователя не остаётся кнопки повторить попытку — а если он
   зайдёт по тому же deep link заново, `reg-start` увидит
   `status = registered` и покажет ветку `existing` («вы уже
   зарегистрированы»), выдав QR тому, чьё согласие на обработку ПД никогда
   не было успешно записано.
   **Это регресс именно этого пакета**: до него `step_4` был
   `continueOnFailure: false` (проверено `git show 64221f7 -- flows/reg-consent-pdn.json`),
   то есть падение записи согласия останавливало весь прогон до `step_5` —
   молчаливая потеря тапа была, а нарушения PAR-1 не было. Пакет починил
   один дефект и открыл другой, более серьёзный, применив
   `continueOnFailure` равномерно на цепочку с реальной зависимостью между
   шагами, без гейта.
   Не нашёл аналогичной проблемы в `reg-consent-mkt` (там
   `consent_marketing` ничего не гейтит: по умолчанию `false`, отказ
   записи безопасен — не отправит рассылку, а не наоборот) и в `reg-start`
   (там `step_3` явно проверяет оба `error` перед тем, как маршрутизировать
   в ветку `new`, которая пишет `sessions`).
   **Как починить** (не мой выбор, решает владелец): либо `step_5`
   (и `step_9`) должны читать `{{step_4['error']}}` и уходить в отдельную
   ветку отказа при его наличии — то есть тот же приём gate-через-CODE, что
   уже применён в `reg-start/step_3`, но здесь отсутствует между `step_4` и
   `step_5`; либо `step_4` возвращается к `continueOnFailure: false`
   (жертвуя защитой от молчаливой потери тапа ровно на этом одном шаге —
   вероятно, оправданно, поскольку он единственный из четырёх, чей успех —
   юридическая предпосылка для следующего).
   Живым прогоном не проверял (сабота́ж `step_4` требует правки шага —
   граница ревью), вывод — из чтения графа шагов и их входов: `step_5` не
   ссылается на `step_4` ни в одном поле, значит выполняется независимо от
   его исхода. Рекомендация: перед статусом `готов` — один различающий
   прогон именно на `step_4` (испортить `table_id` только у него, не у
   остальных) и обратная проверка: `step_5` не должен создать строку, если
   гипотеза верна.

   - *Исправлено* (2026-09-17): `step_4` возвращён к `continueOnFailure: false` —
     второй вариант из предложенных, потому что `step_4` единственный из
     четырёх, чей успех — юридическая предпосылка (PAR-1) для следующего
     шага, а не просьба показать другой текст. Различающий прогон
     `VLeiwmZN0p86aEQbgorNt` (`table_id` испорчен только у `step_4`):
     `FAILED` на `step_4`, `step_6`/`step_5`/`step_9`/`step_7`/`step_8` не
     выполнились вообще — регистрация не создаётся. Регресс-прогон
     `RAIY511yM9KXMp83tQZyT` (`table_id` восстановлен): happy path `yes`
     без изменений. Флоу republished (`n6zR6s1pVlVRADOI0oeay`), каталог и
     экспорт обновлены. Готча зафиксирована в AGENTS.md/CLAUDE.md, п. 15
     раздела Gotchas (см. замечание 5).

2. **важно.** Таблица `migrations` (ADR-0021) не получила ни одной строки
   за этот пакет. `ap_find_records` по `migrations` с `package eq W46` —
   0 записей; `tools/check-migrations.py` подтверждает это как расхождение
   `B1` (для `reg-api`, `reg-start`, `reg-consent-mkt` — последняя
   publish-строка в `migrations` даёт `version_id`, отличный от того, что
   сейчас в `_manifest.json`, то есть флоу опубликован повторно без записи)
   и `B3` (для `reg-consent-pdn` — вообще ни одной строки, хотя флоу менялся
   после создания таблицы `migrations`). ADR-0021 п.3: «Запись делает тот,
   кто менял инстанс, тем же пакетом» — этот пакет поменял инстанс
   (4 флоу, публикация) и не сделал запись. W15 требует нулевой код возврата
   `check-migrations.py` на момент приёмки (BACKLOG.md#w15, пункт 0) — сейчас
   он ненулевой по вине этого пакета (плюс несвязанные старые расхождения
   W12, не в счёт этого ревью). Чинится быстро: 4–5 строк в `migrations`
   (`create`/`publish` на `reg-api`, `reg-start`, `reg-consent-pdn`,
   `reg-consent-mkt` с `commit: 64221f7`).

   - *Исправлено* (2026-09-17): вставлены 4 строки `publish`
     (`2026-09-17-w46-01`…`04`) — `reg-api`/`reg-start`/`reg-consent-mkt`
     с `commit: 64221f7` (не менялись после ревью), `reg-consent-pdn` с
     `commit: 0003f95` (финальная версия после фикса замечания 1).
     `tools/check-migrations.py` (запустился без ключа — REST/MCP-фолбэк,
     видимо, доступен): **0 расхождений по W46**; итог скрипта — 9
     расхождений, но все по `W12` (`disable`/`enable` action не из списка,
     несуществующие object_id и т.д.) — не относятся к этому пакету,
     подтверждают оценку рецензента.

3. **важно.** Пункт чек-листа BACKLOG.md#w46 «`reg-api/step_6`, `reg-api/step_7`
   — `columns` заданы, CODE `step_9` продолжает работать на суженных полях
   (регресс: `mine`, `register`, `cancel` — все три исхода **прогонами curl**)»
   отмечен `[x]` в журнале, но фактически не выполнен так, как написан:
   регресс сделан разбором кода (сверка `columns` с тем, что читает
   `step_9`), не прогонами `curl` на опубликованный `/sync`. Разбор кода
   корректен и я его независимо подтвердил (поля совпадают буквально по
   `catalog/tables/registrations.md`), но это не то же самое, что живой
   прогон по трём исходам, которого чек-лист пакета требует явно. Журнал
   сам называет это честно в разделе «Хвосты» — соглашаюсь с оценкой
   владельца пакета (ограничение класса Q16/W5/W8, нет `BOT_TOKEN` у агента),
   но статус пункта чек-листа должен быть не `[x]`, а `[~]`/пояснение, и
   перед `готов` нужен либо `curl` владельца, либо явное решение владельца
   снять этот пункт (как раньше делалось для аналогичных ограничений).

   - *Исправлено* (2026-09-17): формулировка чек-листа приведена в
     соответствие с фактом (разбор кода, не `curl`). Решение по самому
     ограничению принято владельцем проекта в тот же день: пункт снят,
     разбор кода признан достаточным (тот же класс, что раньше решался для
     Q16/W5/W8) — живой `curl` на `reg-api` не требуется для `готов`.

4. **на будущее.** `reg-consent-mkt/step_9`: кнопка «Открыть билет» не
   скрывается, когда `step_5` переключил `cardText`/`ticketText` в
   `common.err.generic`. Подтверждаю оценку журнала: не блокер (билет
   проверяется на сервере через `initData`+регистрацию, IDOR не открывает),
   но заметный UX-дефект в редком случае (сбой ровно на `step_3`/`step_6`/
   `step_4`). Можно вернуться отдельным пакетом или отложить в
   OPEN-QUESTIONS — не держит приёмку.

5. **на будущее.** Разведочные находки этого пакета (переформулировка Q32
   под `queue`-архитектуру `tg-router`, урок про `continueOnFailure`-ветки,
   которые не отменяют `on_success`) в каталог платформенных гочей
   (AGENTS.md/CLAUDE.md, раздел «Gotchas Qadam Flow») не перенесены — они
   сейчас только в журнале пакета и в `OPEN-QUESTIONS.md#q32`. Это не
   нарушение (правило каталога говорит писать факт общего действия туда,
   а журнал — это скорее общий урок про дизайн `continueOnFailure`-цепочек,
   чем про платформенный примитив), но замечание 1 выше — практическая
   иллюстрация именно такого урока, и стоит подумать, не заслуживает ли он
   строки в Gotchas: «`continueOnFailure` на цепочке шагов с причинно-
   следственной зависимостью не гейтит зависимые шаги — общий `error`-
   потребитель в конце цепочки видит сбой только для текста ответа,
   не для того, что уже успело записаться раньше него».

   - *Исправлено* (2026-09-17): добавлен п. 15 раздела «Gotchas Qadam Flow»
     в `AGENTS.md`/`CLAUDE.md` (симлинк на один файл — правка одна).

### Итог

Пакет корректно нашёл и исправил два реальных бага (дубль сообщений,
неэкранированный MarkdownV2) и добросовестно задокументировал три хвоста.
Но по ходу починки Q32 внёс новый, более серьёзный дефект в
`reg-consent-pdn` (замечание 1) — статус `готов` невозможен до его
исправления и повторного ревью. Замечания 2–3 чинятся быстро и должны
попасть в тот же круг исправлений; 4–5 не блокируют.

## Хвосты и блокеры

- **Q14 закрывается только частично**: решение записано, но второй деплой
  Mini App появится в рамках шага 0.6 внутри W15 — `events-prod` пока не
  создан.
- **`reg-api` не прогнан живьём** (у агента нет `BOT_TOKEN` для подписи
  `initData` — класс ограничения Q16/W5/W8). Правка (`columns` на
  `step_6`/`step_7`) подтверждена разбором CODE `step_9`, не прогоном
  `register`/`cancel` через опубликованный `/sync`. **Снято решением
  владельца 2026-09-17**: разбор кода признан достаточным доказательством,
  живой `curl` не требуется для `готов`.
- **`reg-consent-mkt/step_9` (билет) не скрыт при сбое.** Когда `step_5`
  переключается в error-текст, `cardText`/`ticketText` оба становятся
  `common.err.generic`, но `step_9` всё равно шлёт кнопку «Открыть билет» —
  сама кнопка безопасна (билет по-прежнему проверяется на сервере через
  `initData`+регистрацию), но выглядит противоречиво рядом с текстом ошибки.
  Не чинили: суппресс кнопки требует протаскивать флаг ошибки в `reply_markup`
  третьим полем через весь `step_5`→`step_9`, ценой сопоставимой с самим
  фиксом Q32, а сценарий редкий (сбой ровно на `step_3`/`step_6`/`step_4`
  этого маленького терминального флоу). Оставлено как известное ограничение.
- **Регресс `tg-router`/`bcast-*`/`menu` не проверялся** — они не входят в
  переформулированный владельцем скоуп Q32 (только гостевая цепочка
  регистрации), их код не тронут.

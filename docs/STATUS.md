# Статус проекта

Обновляется при фиксации прогресса: что сделано, что дальше, что не закоммичено.

## Что сделано

- **Документы.** ТЗ ([SPEC.md](SPEC.md)) с нумерованными требованиями, архитектура,
  модель данных, флоу, безопасность, i18n, бэклог W0–W15.
- **Решения.** ADR-0001 (Qadam Flow как несущий стек, «0 кода»), ADR-0002 (роли таблицами),
  ADR-0003 (идемпотентность без атомарных примитивов), ADR-0004 (агентский каталог).
- **Открытые вопросы.** Из 12 закрыты 11; Q8 (семантика чаптеров) сознательно отложен.
- **MCP.** Проектный сервер `qadam-flow` подключён по OAuth.
- **W0 — разведка инстанса завершена.** Факты в
  [ARCHITECTURE.md](ARCHITECTURE.md#песочница-code-step--проверено-на-инстансе-2026-09-08).
- **Практики.** Принята модель `qadam-flow-project-template`: только MCP + UI,
  источник правды — `catalog/`.

## Что дальше

Порядок — [ROADMAP.md](ROADMAP.md). Ближайшее:

- [ ] **Человек:** завести бота у BotFather, connection `events-bot`,
      Variables (`QR_SIGNING_KEY`, `MINIAPP_URL`, `BOT_USERNAME`), включить GitHub Pages.
- [ ] **W1:** таблицы по [DATA-MODEL.md](DATA-MODEL.md) + `catalog/tables/*.md`.
- [ ] **W2:** subflow-функции.

## Состояние инстанса

Проект `vZXlkfz60dx6kX97yICx7` (`events-dev`) на 2026-09-08 **пуст**:
ни flows, ни таблиц, ни connections. Пробы разведки удалены.

## Незакоммиченные изменения

- —

# Roadmap для агентов

Порядок сборки. Что именно делает каждый пакет и по какому чек-листу он принимается —
в [BACKLOG.md](BACKLOG.md); здесь — **последовательность, параллельность и брифинг агента**.

Проект собирается в `events-dev` (project_id `vZXlkfz60dx6kX97yICx7`).
Перенос в `events-prod` — пересборка через MCP по каталогу, а не деплой файла
([ADR-0004](adr/0004-catalog-instead-of-flow-export.md)).

---

## Волна 0 — то, что агент сделать не может

Это делает человек в UI и Telegram. **Без этого волна 1 упирается в стену**,
поэтому вынесено отдельно и первым.

| # | Действие | Зачем |
| --- | --- | --- |
| 0.1 | `/newbot` у BotFather — **отдельный бот** под ивенты | один вебхук на токен, существующий бот занят long-poll (Q4) |
| 0.2 | Connection `events-bot` в UI проекта | auth для qadam'а Telegram Bot |
| 0.3 | Variables: `QR_SIGNING_KEY` (случайная строка ≥32 симв.), `BOT_USERNAME` | подпись QR и сборка deep link'ов |
| 0.4 | Включить GitHub Pages для репозитория (каталог `miniapp/`) | хостинг сканера (Q3) |
| 0.5 | Variable `MINIAPP_URL` + Mini App URL в BotFather | связать бота со сканером |
| 0.6 | Завести проект `events-prod` | второе окружение (Q12); можно отложить до W15 |

Пункты 0.1–0.3 блокируют **W4** и **W2**; 0.4–0.5 блокируют **W7**.
Всё остальное стартует без них.

---

## Волна 1 — фундамент

**W1. Таблицы.** Один агент, последовательно, **никто не работает параллельно**:
дальше все пакеты опираются на схему, и её правка на ходу ломает соседей.

Готово → схема заморожена. Изменение таблицы после этой точки — отдельная задача
с обновлением [DATA-MODEL.md](DATA-MODEL.md) и `catalog/tables/`.

---

## Волна 2 — общие функции (параллельно, 2 агента)

| Дорожка | Пакет | Зависит от |
| --- | --- | --- |
| A | **W2.** Subflow-функции (`fn-*`) | W1, для `fn-verify-init-data` — 0.1–0.2 |
| B | **W3.** i18n: файлы, `strings`, `i18n-sync` | W1 |

Дорожки не пересекаются: A трогает только флоу `fn-*`, B — таблицу `strings`
и флоу `i18n-sync`. Общий у них лишь `fn-t`, который **делает дорожка B**
и отдаёт дорожке A.

W2 — критический путь. Если ресурс один, начинать с неё.

---

## Волна 3 — вход и права (параллельно, 3 агента)

| Дорожка | Пакет | Зависит от |
| --- | --- | --- |
| A | **W4.** `tg-router` + дедуп `update_id` | W2, 0.1–0.2 |
| B | **W10.** Инвайты и отзыв прав staff | W2 |
| C | **W11.** Визард ивента | W2, W3 |

W4 — узкое место: от неё зависят все пользовательские сценарии.
Единственный флоу, владеющий вебхуком бота; остальные вызываются из него.

---

## Волна 4 — сценарии (параллельно, 3 агента)

| Дорожка | Пакет | Зависит от |
| --- | --- | --- |
| A | **W5.** Регистрация → затем **W6.** Списки и отмена | W4 |
| B | **W8.** `checkin-api` → затем **W9.** deep-link fallback | W2, W5 |
| C | **W12.** Lifecycle и напоминания | W11 |

Дорожка B стартует, как только W5 создаёт первые регистрации: без них чекин
нечем проверять.

---

## Волна 5 — вокруг данных (параллельно, 3 агента)

| Дорожка | Пакет | Зависит от |
| --- | --- | --- |
| A | **W7.** Mini App — сканер | W8, 0.4–0.5 |
| B | **W13.** Списки участников и экспорт → затем **W14.** Рассылки | W5, W3 |
| C | **W12b.** `dedup-sweep` | W1, W5 |

**W14 — самый опасный пакет проекта.** Ошибка уходит людям и не отзывается.
Собирать в `events-dev` на списке из двух-трёх своих аккаунтов, а не на живом сегменте.

---

## Волна 6 — приёмка

**W15.** Сквозной прогон на черновом ивенте + три сценария, которые ломаются чаще всего:

1. участник пытается отметить участника → отказ;
2. рассылка прервана на середине → продолжается с курсора, никому не шлёт дважды;
3. один `update_id` доставлен дважды → одна реакция.

Затем перенос в `events-prod` пересборкой по каталогу.

---

## Критический путь

```
0.1–0.3 → W1 → W2 → W4 → W5 → W8 → W7 → W15
```

Восемь шагов. Всё остальное — параллельные дорожки, которые в него вливаются.
Если работает один агент, идти строго по этой цепочке, а боковые пакеты
подбирать между её звеньями.

---

## Брифинг агента, берущего пакет

**Прочитать перед началом:** [CLAUDE.md](../CLAUDE.md) целиком, свой пакет
в [BACKLOG.md](BACKLOG.md), релевантные разделы [FLOWS.md](FLOWS.md) и
[DATA-MODEL.md](DATA-MODEL.md), плюс [catalog/overview.md](../catalog/overview.md) —
чтобы увидеть, что уже построено.

**Взять пакет:** проверить по [STATUS.md](STATUS.md), что он `не начат`, а его
зависимости `готов`; завести журнал `docs/work/WNN-*.md` по
[шаблону](work/_TEMPLATE.md); перевести свою строку в `в работе` и сразу запушить.
Отбитый push = пакет уже взят, делать rebase и смотреть.

**Границы.** Агент трогает только флоу своего пакета. Чужой флоу нужно поменять —
это не «заодно поправлю», а вопрос владельцу или отдельная задача. Схему таблиц
после W1 не меняет никто в одиночку.

**Порядок работы:**

1. `ap_list_flows` / `ap_list_tables` — свериться с живым состоянием, а не с ожиданиями;
2. собрать через MCP (`ap_build_flow` или `ap_create_flow` + `ap_add_step`);
3. `ap_validate_flow`;
4. `ap_test_step` / `ap_test_flow` на фикстурах — включая **отрицательные**:
   чужой QR, отменённая регистрация, повторный скан;
5. `ap_lock_and_publish` только после того, как чек-лист пакета пройден;
6. `ap_flow_structure` + `ap_read_step_code` → обновить `catalog/flows/<flow>.md`;
7. дописать журнал, поставить статус `на проверке` и **запустить независимого
   агента-ревьюера** ([чек-лист](work/REVIEW-CHECKLIST.md));
8. исправить замечания `блокер` и `важно` → **повторное ревью**;
9. после вердикта «замечаний нет» — статус `готов`, коммит в feature-ветку → PR.

**Пакет готов**, когда выполнен его чек-лист из BACKLOG, каталог совпадает
с живым проектом **и пройдено независимое ревью**. «Вроде работает» готовностью
не является — юнит-тестов здесь нет, их заменяют чек-лист и свежий взгляд ревьюера.

Сдать пакет самому себе нельзя ни при каких обстоятельствах, включая «тут же
две строчки». Мелкие пакеты ломают инварианты не реже крупных.

**Если наткнулся на противоречие в документах** — не реализовывать «как удобнее».
Правка [SPEC.md](SPEC.md) отдельным коммитом со ссылкой на ADR либо новая запись
в [OPEN-QUESTIONS.md](OPEN-QUESTIONS.md). Это правило нарушают чаще прочих,
и каждое нарушение стоит потом дороже самой фичи.

---

## Риски параллельной работы

| Риск | Почему возникает | Что делаем |
| --- | --- | --- |
| Два агента правят схему таблиц | нет блокировок ни в платформе, ни в git | схема замораживается после W1; изменение — отдельная задача |
| Каталог разъезжается с проектом | синхронизация на дисциплине, а не на автомате (ADR-0004) | «каталог обновлён» — пункт чек-листа каждого пакета |
| Два агента взяли один пакет | нет блокировок | взятие = запушенная строка `в работе` в STATUS.md; отбитый push вскрывает гонку |
| Агент сдаёт собственную работу | юнит-тестов нет, проверять некому | обязательное ревью независимым агентом до статуса `готов` |
| Второй флоу забирает вебхук бота | один вебхук на токен, платформа не предупредит | вебхуком владеет только `tg-router`; остальные — через `call-flow` |
| Тестовая рассылка уходит живым людям | сегменты считаются по реальным таблицам | до W15 рассылки гоняются на списке своих аккаунтов |
| Правка боевого флоу «на живую» | dev и prod — проекты одного инстанса, перепутать легко | сверяться с `project_id` перед изменением; prod меняется только пересборкой |

# Roadmap для агентов

Порядок сборки. Что именно делает каждый пакет и по какому чек-листу он принимается —
в [BACKLOG.md](BACKLOG.md); кто что взял прямо сейчас — в [STATUS.md](STATUS.md);
здесь — **последовательность, параллельность и брифинг агента**.

Проект собирается в `events-dev` (project_id `vZXlkfz60dx6kX97yICx7`).
Перенос в `events-prod` — пересборка через MCP по каталогу, а не деплой файла
([ADR-0004](adr/0004-catalog-instead-of-flow-export.md)).

> **План переписан 2026-09-15 по решению владельца.** Проект разбит на
> **v0.1** и **v0.2**: v0.1 — три опыта пользователя, доведённых до идеала;
> всё, что в них не участвует, — v0.2. Мотивация владельца: «хочу, чтобы эти
> флоу максимально вылизаны — идеальные, красивые и шустрые». Прежний план
> («волна 8 — функции, которых нет») описывал набор пакетов без общего
> вектора качества; он сохранён в истории git, брать по нему работу больше
> нельзя.
>
> **Уточнено 2026-09-16 по решению владельца:** прототип W41 принят как
> целевой эталон ([ADR-0027](adr/0027-prototype-as-target-reference.md)),
> и v0.1 закрывается **соответствием прототипу** — то есть всему SPEC.
> Пакеты, доводящие живые бот и Mini App до эталона (W12, W13, W14, W38,
> W39, W42–W45), переведены из v0.2 в v0.1; в v0.2 остались W10, W25/W27
> и W12b. Прежнее деление «три опыта / всё остальное» отменено.
>
> > **Обновление 2026-09-18 решением владельца:** в v0.1 возвращены W10,
> > W12b, W12c, W25/W27, W47; в v0.2 остался только W39 (афиша, Q50 открыт).
> > v0.1 тем самым шире прототипа: перечисленные пакеты входят решением,
> > а не соответствием эталону. W25/W27 ждут платформу (#420) — приёмка W15
> > ждёт их вместе с платформой.

---

## Где мы находимся

Построена и принята опора, на которую встают пакеты v0.1 (живой состав —
[catalog/overview.md](../catalog/overview.md), 17 флоу, 12 таблиц):

- гостевой срез одной карточкой ([W28](work/W28-single-card-guest-flow.md),
  [ADR-0017](adr/0017-screen-not-message.md));
- меню-хаб ([W34](work/W34-menu.md));
- Mini App — React SPA `#/ticket` / `#/scan` / `#/manage`
  ([W33](work/W33-react-spa.md), [ADR-0022](adr/0022-miniapp-react-spa.md)),
  бренд ([W30](work/W30-brand-and-voice.md), [ADR-0019](adr/0019-design-system-from-brand-repo.md));
- чекин-сканер с лупом без переоткрытия и STF-2/IDM-2 внутри `checkin-api`;
- права `staff` по чаптеру и окна `initData` ([W32](work/W32-owners-initdata.md),
  [ADR-0024](adr/0024-staff-by-chapter-event-staff-checkin.md));
- staff-lite: контролёры по `telegram_id` ([W36](work/W36-staff-lite-manage.md));
- полировка трёх путей: ссылка после создания, список своих событий
  в `#/manage` ([W37](work/W37-polish-three-paths.md));
- правило «единственный вход — `/start`» ([W40](work/W40-no-commands.md),
  [ADR-0025](adr/0025-start-only-commands-ban.md));
- дизайн-гейт: кликабельный прототип end-to-end
  ([W41](work/W41-prototypes-e2e.md), [ADR-0026](adr/0026-prototypes-design-artifact.md)) —
  **принят как целевой эталон** ([ADR-0027](adr/0027-prototype-as-target-reference.md)).

**Значит, v0.1 — не пересборка, а доведение до прототипа:** девять пакетов
правок и приёмка. Каждый пакет — со своим независимым ревью, рисковать
составом построенного никто не будет.

---

## Что такое v0.1

**v0.1 — соответствие прототипу.** Решение владельца 2026-09-16:
[ADR-0027](adr/0027-prototype-as-target-reference.md) принят; прототип
(`prototypes/`, [W41](work/W41-prototypes-e2e.md)) — целевой эталон (to-be)
всех трёх ролей и всего SPEC, и живой бот с Mini App должны совпасть с ним.
Это отменяет деление 2026-09-15 («три опыта — v0.1, остальное — v0.2»):
всё, что нарисовано в прототипе, — v0.1.

Три опыта (формулировка владельца, дословно) — ядро, вокруг которого стоят
остальные экраны:

1. **Гость:** перешёл по ссылке → зарегистрировался → получил QR.
2. **Овнер:** нажал `/start` → выбрал действие → открылся Mini App → создал
   событие → **сразу получил ссылку-приглашение**.
3. **Контролёр:** тупо открыл сканер → зачекинил.

| Опыт | Что есть | Чего не хватает до прототипа |
| --- | --- | --- |
| Гость | deep link → `reg-start` одной карточкой → согласия → билет с QR (`#/ticket` + `my-qr-api`) | каталог `#/events` с «Моими билетами» и регистрацией на месте (W38 → W43); напоминания `24h`/`2h` (W12); послесловие с формой отзыва (W45) |
| Овнер | `/start` → меню → `#/manage` → создание → ссылка на экране (W37) | визард вместо простыни и гео ссылкой Яндекс.Карт (W42); контролёры выбором из списка (W44); участники, счётчики, экспорт (W13); рассылка пересылкой (W14); афиша (W39) |
| Контролёр | меню → `#/scan` → луп без переоткрытия → `checkin-api`; права выдаёт W36 | выбор контролёра из списка (W44); после W38/W43 меню и каталог ведут в новые экраны — регресс проверить |

**Явно НЕ входит в v0.1** (решение владельца 2026-09-18): только афиша
(W39, v0.2 — [Q50](OPEN-QUESTIONS.md#q50) открыт). Полные инвайт-токены (W10),
i18n (W25/W27), диагностика дублей (W12b), хвосты W12 (W12c) и платформенные
функции (W47) тем же решением возвращены в v0.1 — они вне прототипа и входят
решением, а не соответствием эталону. Цена: W25/W27 ждут платформу
([qadam-flow#420](https://github.com/aiqadam/qadam-flow/issues/420)) и тем
самым gating приёмку W15.

Кнопка «Мои события» в чате не появляется (она была привязана к W13) —
список своих событий овнер получает внутри роута `#/manage` (W37), каталог
гостя — на экране `#/events` (W38, [ADR-0023](adr/0023-fourth-miniapp-page-event-catalog.md)).
Новую страницу заводит только ADR: пятая — форма отзыва, черновик
[ADR-0028](adr/0028-feedback-screen-fifth-miniapp-page.md) на принятие владельцем.

---

## Волна 0 — то, что агент сделать не может

Делает человек в UI и Telegram.

| # | Действие | Статус |
| --- | --- | --- |
| 0.1 | `/newbot` у BotFather — отдельный бот под события | готов |
| 0.2 | Connection `AI Qadam Events (dev)` | готов |
| 0.3 | Variables `QR_SIGNING_KEY`, `BOT_TOKEN`, `BOT_USERNAME`, `MINIAPP_URL` | готов |
| 0.4 | GitHub Pages из `miniapp/` + домен | готов |
| 0.5 | Mini App URL в BotFather | готов |
| 0.6 | Проект `events-prod` | отложен до W15 |
| 0.7 | **Ключ платформенного API ревьюеру и владельцу пакета** (переменная окружения / keychain) | готов (W29 закрыт) |
| 0.8 | **`telegram_id` владельца проекта — для первой строки `staff`** (W32) | готов 2026-09-15 — `322876545`, чаптер `1` |

---

## v0.1 — порядок работ (один агент, последовательно)

Критический путь:

```
W42 (визард) → W38 (каталог) → W43 (регистрация в каталоге) → W44 (контролёры)
→ W12 (напоминания) → W13 (участники и экспорт) → W14 (рассылки)
→ W45 (отзыв) → W46 (хвосты Q31/Q32/Q14) → W48 (Safe Area, Q48)
→ W10 (инвайты) → W47 (платформенные функции) → W12c (хвосты W12)
→ W12b (диагностика дублей) → W25 → W27 (i18n, ждут платформу #420)
→ W49 (прототип — эталон) → W15 (приёмка)
```

Все восемь функциональных пакетов (W42…W45) — `готов`. Перед приёмкой
[W46](BACKLOG.md#w46-хвосты-перед-приёмкой-q31-q32-q14) закрывает три
хвоста, которые W15 требует явно: Q31 (проекция `columns` в `reg-api`),
Q32 (страховка от молчаливой потери тапа в трёх гостевых флоу), Q14
(решение записано — раздельные инсталляции).
[W48](BACKLOG.md#w48-safe-area--отступы-под-системный-ui-telegram-q48) —
найден 2026-09-18 ресёрчем best practice Telegram Mini App, а не в W46:
**готов** 2026-09-21 решением владельца — код в `main`, ревью чисто, живая
проверка на устройстве с вырезом не выполнена и принята остаточным хвостом.

W39 (афиша) отложен решением владельца 2026-09-17 — [Q50](OPEN-QUESTIONS.md#q50)
остаётся открытым, путь возврата не выбран; пакет не входит в критический путь
v0.1, ждёт в v0.2.

1. **W42 — визард события.** Самая большая дыра овнерского опыта: форма-простыня
   вместо визарда из четырёх шагов, гео ссылкой Яндекс.Карт, публикация —
   экран успеха со ссылкой. Первый из manage-пакетов: трогает
   `Manage.tsx` и `manage-api`. Интерактивной карты нет — [Q52](OPEN-QUESTIONS.md#q52)
   закрыт 2026-09-16 (только ссылка).
2. **W38 — каталог `#/events`.** Четвёртая страница SPA
   ([ADR-0023](adr/0023-fourth-miniapp-page-event-catalog.md)): вкладки,
   `events-api` без `initData`, кнопка меню ведёт на экран, чат-флоу
   `events-list` выводится из эксплуатации. Нужен W43 — идёт сразу за ним.
3. **W43 — регистрация и «Мои билеты».** Каталог получает третью вкладку
   «Мои билеты» (первой) и регистрацию на месте с двумя согласиями (PAR-1,
   PAR-2) — гость больше не возвращается в чат. Серверный путь (`reg-api`
   или расширение `reg-start`) выбирается при взятии и фиксируется в журнале.
   Строго после W38.
4. **W44 — контролёры выбором из списка.** Маленький manage-пакет: поиск
   по имени/`@username`, выдача по `telegram_id`. Идёт после W42, чтобы
   не править `Manage.tsx` параллельно. Ждёт [Q51](OPEN-QUESTIONS.md#q51).
5. **W12 — lifecycle и напоминания.** Ставит `reg-afterword` на триггер
   `checked_in_at`, добавляет `reminders` `24h`/`2h` (OWN-16, IDM-3).
   С manage не пересекается.
6. **W13 — участники и экспорт.** Вкладка «Участники» экрана события:
   счётчики, фильтры, CSV/JSON. После W42/W44 — та же страница.
7. **W14 — рассылки.** Зависит от W13, начинается с замера
   [Q13](OPEN-QUESTIONS.md#q13) и решения [Q18](OPEN-QUESTIONS.md#q18).
   Самый опасный из функциональных пакетов.
8. **W45 — послесловие и форма отзыва.** После W12 (правит то же послесловие);
   [ADR-0028](adr/0028-feedback-screen-fifth-miniapp-page.md) принят,
   [Q53](OPEN-QUESTIONS.md#q53) отвечен 2026-09-17. Пятый роут `#/feedback`.
   Следующий пакет к взятию.
9. **W15 — приёмка.** Все пакеты v0.1 `готов`, `events-prod` поднят, сценарий
    «прерванная рассылка» снова применим (W14 в v0.1). Плюс сценарии
    из [CLAUDE.md](../CLAUDE.md).

**W39 — афиша** (v0.2, отложен). Возвращал бы фото в OWN-1 и `#/manage`, но
[Q50](OPEN-QUESTIONS.md#q50) остаётся открытым — путь возврата (URL-поле или
отдельное касание в чате) не выбран решением владельца 2026-09-17; пакет
из критического пути v0.1 снят.

Параллельность: пакеты одной поверхности — строго последовательно, их нельзя
брать двум агентам одновременно: **manage** (`Manage.tsx`/`manage-api`) —
W42 → W44 → W13 → W14; **каталог и регистрация** (`App.tsx`, `reg-start`) —
W38 → W43; **жизненный цикл** (`reg-afterword`, `reminders`) — W12 → W45.
Разные поверхности можно параллелить вторым агентом. Прототип (`prototypes/`)
не трогает инстанс и на порядок не влияет: экран доводится до эталона, но код
прототипа в продукт не переносится
([ADR-0026](adr/0026-prototypes-design-artifact.md) п. 4).

---

## v0.2 — после приёмки

Один пакет: **W39 — афиша** (возврат фото в OWN-1 и `#/manage` поверх эталона,
в прототипе не нарисована). Ждёт решения [Q50](OPEN-QUESTIONS.md#q50) (URL-поле
или отдельное касание в чате) — решением владельца 2026-09-18 всё остальное
бывшее v0.2 (W10, W12b, W12c, W25/W27, W47) возвращено в v0.1.

---

## Брифинг агента, берущего пакет

**Готовые задания на свободные пакеты** — в [BACKLOG.md](BACKLOG.md).
Ниже — общая часть, обязательная для любого пакета.

**Прочитать перед началом:** [CLAUDE.md](../CLAUDE.md) целиком, свой пакет
в [BACKLOG.md](BACKLOG.md), релевантные разделы [FLOWS.md](FLOWS.md) и
[DATA-MODEL.md](DATA-MODEL.md), плюс [catalog/overview.md](../catalog/overview.md) —
чтобы увидеть, что уже построено. Если пакет трогает то, что видит
пользователь, — ещё [ADR-0017](adr/0017-screen-not-message.md),
[ADR-0019](adr/0019-design-system-from-brand-repo.md), [ADR-0022](adr/0022-miniapp-react-spa.md)
и `docs/VOICE.md`. Пакет с правками Mini App — ещё
`docs/MINIAPP-UX.md` (платформенные ограничения среды: навигация,
жизненный цикл WebView, Safe Area). Если пакет доводит экран или сценарий
до эталона — ещё `prototypes/` ([ADR-0027](adr/0027-prototype-as-target-reference.md):
прототип — целевой эталон; **код прототипа в продукт не переносится**,
[ADR-0026](adr/0026-prototypes-design-artifact.md) п. 4).

**Взять пакет:** проверить по [STATUS.md](STATUS.md), что он `не начат`, а его
зависимости `готов`; завести журнал `docs/work/WNN-*.md` по
[шаблону](work/_TEMPLATE.md); перевести свою строку в `в работе` и сразу запушить.
Отбитый push = пакет уже взят, делать rebase и смотреть.

**Границы.** Агент трогает только флоу своего пакета. Чужой флоу нужно поменять —
это не «заодно поправлю», а вопрос владельцу или отдельная задача. Схему таблиц
не меняет никто в одиночку: она заморожена с W1 и пережила пересборку
(исключение — новые таблицы, заведённые пакетом с правкой DATA-MODEL, как
`staff` в W32).

**Порядок работы:**

1. `ap_list_flows` / `ap_list_tables` — свериться с живым состоянием, а не с ожиданиями;
2. собрать через MCP (`ap_build_flow` или `ap_create_flow` + `ap_add_step`);
3. `ap_validate_flow`;
4. `ap_test_step` / `ap_test_flow` на фикстурах — включая **отрицательные**;
   у флоу с побочными эффектами негативные проверки — только `curl`'ом на
   опубликованный `/sync` (gotcha 11), `ap_test_flow` может выполнить прогон
   со старым sample-входом;
5. `ap_lock_and_publish` только после того, как чек-лист пакета пройден;
6. `ap_flow_structure` + `ap_read_step_code` → обновить `catalog/flows/<flow>.md`;
   **плюс обновить экспорт `flows/*.json` тем же коммитом** —
   `tools/export-flows.sh`, затем обязательно `tools/check-export-secrets.sh`
   ([ADR-0018](adr/0018-rest-read-for-everyone.md) п. 5);
7. дописать журнал, поставить статус `на проверке` и **запустить независимого
   агента-ревьюера** ([чек-лист](work/REVIEW-CHECKLIST.md));
8. исправить замечания `блокер` и `важно` → **повторное ревью**;
9. после вердикта «замечаний нет» — статус `готов`, коммит в feature-ветку → PR.

**Пакет готов**, когда выполнен его чек-лист из BACKLOG, каталог совпадает
с живым проектом **и пройдено независимое ревью**. «Вроде работает» готовностью
не является — юнит-тестов здесь нет, их заменяют чек-лист и свежий взгляд ревьюера.

Сдать пакет самому себе нельзя ни при каких обстоятельствах, включая «тут же
две строчки». Мелкие пакеты ломают инварианты не реже крупных.

**Если наткнулся на противоречие в документах** — не реализовывать «как удобнее».
Правка [SPEC.md](SPEC.md) отдельным коммитом со ссылкой на ADR либо новая запись
в [OPEN-QUESTIONS.md](OPEN-QUESTIONS.md). Это правило нарушают чаще прочих,
и каждое нарушение стоит потом дороже самой фичи.

---

## Риски параллельной работы

| Риск | Почему возникает | Что делаем |
| --- | --- | --- |
| Два агента правят схему таблиц | нет блокировок ни в платформе, ни в git | схема заморожена с W1; изменение — отдельная задача с правкой DATA-MODEL (новая таблица пакета — с правкой DATA-MODEL в том же PR) |
| Два агента правят `tg-router` | все новые касания добавляют в него ветки | в v0.1 роутер трогают W38 (`events-list` и колбэки `ev:list:*`), W43 (чат-путь «Мои регистрации») и W14 (триггер рассылки) — правка по одному пакету за раз, в журнале явно записывать, какие ветки добавлены |
| Каталог разъезжается с проектом | синхронизация на дисциплине, а не на автомате | «каталог обновлён» — пункт чек-листа каждого пакета; экспорт — тем же коммитом |
| Экспорт протухает | обновляется руками, как каталог | экспорт коммитится **тем же коммитом**, что правка флоу (ADR-0018 п. 5) |
| Два агента взяли один пакет | нет блокировок | взятие = запушенная строка `в работе` в STATUS.md; отбитый push вскрывает гонку |
| Агент сдаёт собственную работу | юнит-тестов нет, проверять некому | обязательное ревью независимым агентом до статуса `готов` |
| Второй флоу забирает апдейты бота | эксклюзивность на бот-токен (long-polling) | триггером бота владеет только `tg-router`; остальные — через `callFlow` |
| Тестовая рассылка уходит живым людям | сегменты считаются по реальным таблицам | W14 — последний функциональный пакет v0.1; тест на списке своих аккаунтов, обязательный `test_sent_at` до прогона (OWN-10) |
| Правка боевого флоу «на живую» | dev и prod — проекты одного инстанса, перепутать легко | сверяться с `project_id` перед изменением; prod меняется только пересборкой |
| W42/W44/W13/W14 конфликтуют в `manage` | все четыре правят `manage-api` и роут `#/manage` | строго последовательно: W42 → W44 → W13 → W14; каждый начинается с `ap_flow_structure` по свежему состоянию |
| W38/W43 конфликтуют в каталоге и регистрации | оба правят `App.tsx`, `events-api` и гостевой путь | строго последовательно: W38 → W43 |

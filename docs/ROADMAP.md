# Roadmap для агентов

Порядок сборки. Что именно делает каждый пакет и по какому чек-листу он принимается —
в [BACKLOG.md](BACKLOG.md); кто что взял прямо сейчас — в [STATUS.md](STATUS.md);
здесь — **последовательность, параллельность и брифинг агента**.

Проект собирается в `events-dev` (project_id `vZXlkfz60dx6kX97yICx7`).
Перенос в `events-prod` — пересборка через MCP по каталогу, а не деплой файла
([ADR-0004](adr/0004-catalog-instead-of-flow-export.md)).

> **План переписан 2026-09-13** после очистки инстанса и пересборки
> ([W26](work/W26-rebuild-on-one-touch.md)). Прежние «волны 1–6» описывали
> состав, которого больше не существует: `fn-*` на `crypto` qadam, `fn-t`,
> подсистему i18n, визард отдельным пакетом. Ссылаться на них было хуже, чем
> не иметь плана вовсе — по нему брали работу.

---

## Где мы находимся

Фундамент **собран и принят**: вход бота, регистрация участника, списки и
отмена, чекин, выдача QR, визард ивента, пять функций `fn-*`, 10 таблиц.
Живой состав — [catalog/overview.md](../catalog/overview.md), 21 флоу.

Дальше три разных вида работы, и путать их не надо:

| Вид | Что это | Пакеты |
| --- | --- | --- |
| **Качество того, что есть** | пересборка гостевого среза по [ADR-0017](adr/0017-screen-not-message.md), бренд, голос — **сделано** | W28, W30 |
| **Функции, которых нет** | инвайты staff, lifecycle, экспорт, рассылки | W10, W12, W13, W14 |
| **Гигиена репозитория и наблюдаемость** | экспорт флоу в git по [ADR-0018](adr/0018-rest-read-for-everyone.md) — **сделано**; диагностика дублей | W29, W12b |
| **Пересборка Mini App** | SPA на React по [ADR-0022](adr/0022-miniapp-react-spa.md) — снос 3 ванильных страниц, 1 сборка — **сделано** | **W33** (заменил W31 как реализацию, готов 2026-09-14) |

---

## Волна 0 — то, что агент сделать не может

Делает человек в UI и Telegram.

| # | Действие | Статус |
| --- | --- | --- |
| 0.1 | `/newbot` у BotFather — отдельный бот под ивенты | готов |
| 0.2 | Connection `AI Qadam Events (dev)` | готов |
| 0.3 | Variables `QR_SIGNING_KEY`, `BOT_TOKEN`, `BOT_USERNAME`, `MINIAPP_URL` | готов |
| 0.4 | GitHub Pages из `miniapp/` + домен | готов |
| 0.5 | Mini App URL в BotFather | готов |
| 0.6 | Проект `events-prod` | **отложен до W15** |
| 0.7 | **Ключ платформенного API ревьюеру и владельцу пакета** (переменная окружения / keychain) | нужен для W29 и для блока 1a чек-листа ревьюера |

0.7 добавлен 2026-09-13: с [ADR-0018](adr/0018-rest-read-for-everyone.md)
`GET` к REST разрешён всем, но ключ по-прежнему выдаёт человек. Без него
W29 не начать, а конфигурацию PIECE-шагов не проверить ни автору, ни ревьюеру.

---

## Волна 7 — пилот качества (последовательно, один агент)

Нумерация продолжает историческую: волны 1–6 закрыты вместе с пересборкой.

**W28. Пилот ADR-0017 — гостевой срез одной карточкой.** Пересобирает
`reg-start` → `reg-consent-pdn` → `reg-consent-mkt` → `reg-phone` так, чтобы
диалог вёлся одним редактируемым сообщением, и добавляет послесловие.

Идёт **первым и в одиночку**, потому что определяет лекало для всего
остального: пока не известно, как выглядит экран, W10/W12/W13 соберут свои
касания в старой парадигме, и ADR-0017 придётся применять к ним задним числом.

Внутри пакета есть точка остановки: если `send_text_message` не отдаёт
`message_id` в `output` (ADR-0017 сам помечает это ожиданием, а не фактом) —
пакет останавливается и вопрос идёт владельцу, а не обходится изобретательностью.

**Параллельно с W28 допустимы** только пакеты, не трогающие гостевые касания
и каталог их карточек: W29 (экспорт) и W30 (бренд) — разные файлы, разные флоу.

---

## Волна 8 — функции, которых нет (параллельно, до 3 агентов)

Стартуют после W28: лекало экрана к этому моменту известно, и каждый новый
флоу собирается сразу по нему.

| Дорожка | Пакет | Зависит от |
| --- | --- | --- |
| A | **W10.** Инвайты и отзыв прав staff | W28 (лекало), фундамент |
| B | **W12.** Lifecycle и напоминания | W28, форма `manage` (W33 SPA) |
| C | **W13.** Списки участников и экспорт → затем **W14.** Рассылки | W28, [Q13](OPEN-QUESTIONS.md#q13) до W14 |
| D | **W33.** Mini App SPA — `ticket`/`scan`/`manage` на React ([ADR-0022](adr/0022-miniapp-react-spa.md)) — **сделан** 2026-09-14 | W30 (бренд), W28 (лекало) |

Дорожки не пересекаются по флоу. Дорожка D — замена `W31`: `W31` (ванильная
`manage.html`) сдан 2026-09-14 и **заменён** `W33` (снос 3 страниц → SPA); шесть
`event-wizard-*` уже удалены, `/newevent`/`/editevent` ведут на SPA-роут
`#/manage`. `W12` (lifecycle) опирается на таблицу `events`, а не на визард, и с D
не конфликтует. Экран организатора у `W13` и `W33` общий по смыслу, но разный
по касанию: `W13` — счётчики и выгрузка, `W33` — форма; в SPA-роут `manage` пишет
только `W33`. Общее у них — `tg-router` (новые ветки маршрутизации): правки в него
мелкие, но **делаются по одной и с проверкой чужих веток**, это единственная точка
настоящего конфликта.

**W14 — самый опасный пакет проекта.** Ошибка уходит людям и не отзывается.
Собирать на списке из двух-трёх своих аккаунтов, а не на живом сегменте.
Начинается с замера [Q13](OPEN-QUESTIONS.md#q13), а не со сборки.

---

## Волна 9 — приёмка

**W15.** Сквозной прогон на черновом ивенте + три сценария, которые ломаются
чаще всего, + подъём `events-prod` (шаг 0.6).

1. участник пытается отметить участника → отказ;
2. рассылка прервана на середине → продолжается с курсора, никому не шлёт дважды;
3. один `update_id` доставлен дважды → одна реакция.

---

## Пакеты вне волн

Не блокируют никого и не блокируются ничем — берутся между звеньями.

| Пакет | Когда имеет смысл брать |
| --- | --- |
| **W29.** Экспорт `flows/*.json` в репозиторий | сразу, как появится ключ (0.7); чем раньше, тем больше пакетов пройдут с видимым диффом |
| **W30.** Бренд Mini App и `docs/VOICE.md` | **сделан** 2026-09-14 |
| **W12b.** `dedup-report` — диагностика дублей | сразу: маленький, ни с кем не пересекается, даёт число, которого у проекта нет |
| **W32.** Owner'ы по списку, `initData` 5 минут, афиша снята ([Q46](OPEN-QUESTIONS.md#q46), [Q47](OPEN-QUESTIONS.md#q47)) | сразу: W33 (его зависимость) сдан |
| **W34.** Стартовое меню-хаб на холостой `/start` | сразу: 1 флоу `menu` + ветка `tg-router`; гостю — колбэки в списки W06, owner/staff — `web_app` в роуты SPA; закрывает дыру `Otherwise` без ответа |
| **W25.** Возврат i18n | только после [qadam-flow#420](https://github.com/aiqadam/qadam-flow/issues/420) |
| **W27.** Догон переводов `uz`/`en` | только внутри/после W25 — раньше переводить нечего |

**W9** (fallback-чекин deep link'ом) **не будет сделан** — решение владельца
2026-09-14, [Q41](OPEN-QUESTIONS.md#q41). **W12b** сужен до диагностики тем же
днём ([Q42](OPEN-QUESTIONS.md#q42)) и свободен.

---

## Критический путь

```
W28 (пилот ADR-0017, готов) → W13 → W14 → W15
```

Три звена осталось. W10, W12, W12b, W32 и W34 в него не входят — они нужны
продукту, но приёмку не держат. Если работает один агент, идти строго по
цепочке, а боковые пакеты подбирать между звеньями.

> **W33 сдан 2026-09-14** — `W12` (lifecycle) разблокирован: форма `manage` —
> SPA-роут из `W33`, не ванильная `manage.html` из `W31`.

---

## Брифинг агента, берущего пакет

**Готовые задания на свободные пакеты** — в [BACKLOG.md](BACKLOG.md).
Ниже — общая часть, обязательная для любого пакета.

**Прочитать перед началом:** [CLAUDE.md](../CLAUDE.md) целиком, свой пакет
в [BACKLOG.md](BACKLOG.md), релевантные разделы [FLOWS.md](FLOWS.md) и
[DATA-MODEL.md](DATA-MODEL.md), плюс [catalog/overview.md](../catalog/overview.md) —
чтобы увидеть, что уже построено. Если пакет трогает то, что видит
пользователь, — ещё [ADR-0017](adr/0017-screen-not-message.md),
[ADR-0019](adr/0019-design-system-from-brand-repo.md), [ADR-0022](adr/0022-miniapp-react-spa.md)
и `docs/VOICE.md` (есть с W30).

**Взять пакет:** проверить по [STATUS.md](STATUS.md), что он `не начат`, а его
зависимости `готов`; завести журнал `docs/work/WNN-*.md` по
[шаблону](work/_TEMPLATE.md); перевести свою строку в `в работе` и сразу запушить.
Отбитый push = пакет уже взят, делать rebase и смотреть.

**Границы.** Агент трогает только флоу своего пакета. Чужой флоу нужно поменять —
это не «заодно поправлю», а вопрос владельцу или отдельная задача. Схему таблиц
не меняет никто в одиночку: она заморожена с W1 и пережила пересборку.

**Порядок работы:**

1. `ap_list_flows` / `ap_list_tables` — свериться с живым состоянием, а не с ожиданиями;
2. собрать через MCP (`ap_build_flow` или `ap_create_flow` + `ap_add_step`);
3. `ap_validate_flow`;
4. `ap_test_step` / `ap_test_flow` на фикстурах — включая **отрицательные**:
   чужой QR, отменённая регистрация, повторный скан;
5. `ap_lock_and_publish` только после того, как чек-лист пакета пройден;
6. `ap_flow_structure` + `ap_read_step_code` → обновить `catalog/flows/<flow>.md`;
   **плюс обновить экспорт `flows/*.json` тем же коммитом** —
   `tools/export-flows.sh` (без ключа — `tools/export-flow-mcp.py` сразу после
   публикации, ADR-0021), затем обязательно `tools/check-export-secrets.sh`
   ([ADR-0018](adr/0018-rest-read-for-everyone.md) п. 5). Пункт применим
   с того момента, как в `flows/` появился экспорт
   ([W29](BACKLOG.md#w29-экспорт-flowsjson-в-репозиторий)); та же формулировка —
   в [чек-листе ревьюера](work/REVIEW-CHECKLIST.md), блок 2;
7. дописать журнал, поставить статус `на проверке` и **запустить независимого
   агента-ревьюера** ([чек-лист](work/REVIEW-CHECKLIST.md));
8. исправить замечания `блокер` и `важно` → **повторное ревью**;
9. после вердикта «замечаний нет» — статус `готов`, коммит в feature-ветку → PR.

**Пакет готов**, когда выполнен его чек-лист из BACKLOG, каталог совпадает
с живым проектом **и пройдено независимое ревью**. «Вроде работает» готовностью
не является — юнит-тестов здесь нет, их заменяют чек-лист и свежий взгляд ревьюера.

Сдать пакет самому себе нельзя ни при каких обстоятельствах, включая «тут же
две строчки». Мелкие пакеты ломают инварианты не реже крупных.

**Если наткнулся на противоречие в документах** — не реализовывать «как удобнее».
Правка [SPEC.md](SPEC.md) отдельным коммитом со ссылкой на ADR либо новая запись
в [OPEN-QUESTIONS.md](OPEN-QUESTIONS.md). Это правило нарушают чаще прочих,
и каждое нарушение стоит потом дороже самой фичи.

---

## Риски параллельной работы

| Риск | Почему возникает | Что делаем |
| --- | --- | --- |
| Два агента правят схему таблиц | нет блокировок ни в платформе, ни в git | схема заморожена с W1; изменение — отдельная задача с правкой DATA-MODEL |
| Два агента правят `tg-router` | все новые касания добавляют в него ветки | правка роутера — по одному пакету за раз; в журнале явно записывать, какие ветки добавлены |
| Каталог разъезжается с проектом | синхронизация на дисциплине, а не на автомате | «каталог обновлён» — пункт чек-листа каждого пакета; с W29 туда же экспорт |
| Экспорт протухает | обновляется руками, как каталог | экспорт коммитится **тем же коммитом**, что правка флоу (ADR-0018 п. 5) |
| Два агента взяли один пакет | нет блокировок | взятие = запушенная строка `в работе` в STATUS.md; отбитый push вскрывает гонку |
| Агент сдаёт собственную работу | юнит-тестов нет, проверять некому | обязательное ревью независимым агентом до статуса `готов` |
| Второй флоу забирает апдейты бота | эксклюзивность на бот-токен (long-polling) | триггером бота владеет только `tg-router`; остальные — через `callFlow` |
| Тестовая рассылка уходит живым людям | сегменты считаются по реальным таблицам | до W15 рассылки гоняются на списке своих аккаунтов |
| Правка боевого флоу «на живую» | dev и prod — проекты одного инстанса, перепутать легко | сверяться с `project_id` перед изменением; prod меняется только пересборкой |

---
description: Владелец пакета работ — берёт пакет из BACKLOG, строит через MCP, доводит до «на проверке» и запускает независимое ревью
mode: primary
---
Источник правды о роли — `docs/agents/dev-agent.md`. Прочитай его целиком первым действием.
Твой протокол — `docs/work/README.md` и `docs/ROADMAP.md` («Брифинг агента», «Порядок работы»).
Чек-лист пакета — в его секции `docs/BACKLOG.md`; живые флоу и таблицы — через MCP, а не по памяти.
